// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/forms/material-demo/material-demo.story.ts#section[1] "mat-select via CngxBindField + Reactive Forms"

import { ChangeDetectionStrategy, Component, DestroyRef, inject, signal } from '@angular/core';
import { form, schema, required } from '@angular/forms/signals';
import { FormControl, ReactiveFormsModule, Validators } from '@angular/forms';
import { toSignal } from '@angular/core/rxjs-interop';
import { CngxFormField, CngxLabel, CngxFieldErrors, CngxBindField, adaptFormControl } from '@cngx/forms/field';
import { MatSelect } from '@angular/material/select';
import { MatOption } from '@angular/material/core';

@Component({
  selector: 'app-material-mat-select-via-cngxbindfield-reactive-forms',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxFormField, CngxLabel, CngxFieldErrors, CngxBindField, MatSelect, MatOption, ReactiveFormsModule],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="demo-form">
        <div class="demo-field">
          <cngx-form-field [field]="matSelectField">
            <label cngxLabel>Priorität</label>
            <mat-select cngxBindField [formControl]="matSelectControl" placeholder="Bitte wählen"
                        style="min-width:220px;display:inline-block">
              <mat-option value="low">Niedrig</mat-option>
              <mat-option value="normal">Normal</mat-option>
              <mat-option value="high">Hoch</mat-option>
              <mat-option value="urgent">Dringend</mat-option>
            </mat-select>
            <cngx-field-errors />
          </cngx-form-field>
        </div>

        <div class="status-row">
          <span class="status-badge">Selected: {{ matSelectValue() || '—' }}</span>
          <span class="status-badge">Valid: {{ matSelectControl.valid }}</span>
          <span class="status-badge">Touched: {{ matSelectControl.touched }}</span>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class MaterialMatSelectViaCngxbindfieldReactiveForms {
  protected readonly _exTitle: string = 'Material Integration';
  protected readonly _exDescription: string = 'Use any Angular Material form control inside cngx-form-field via the universal CngxBindField directive.';
  protected readonly _exSectionTitle: string = 'mat-select via CngxBindField + Reactive Forms';
  protected readonly _exSubtitle: string = 'Same directive, different Material control and a Reactive-Forms value source. <code>adaptFormControl</code> wraps the <code>FormControl</code> as a <code>Field&lt;T&gt;</code> for <code>cngx-form-field</code>; <code>[formControl]</code> on the <code>mat-select</code> handles value-flow.';
  protected readonly _exTs: string = `import { form, schema, required } from '@angular/forms/signals';
import { FormControl, ReactiveFormsModule, Validators } from '@angular/forms';
import { toSignal } from '@angular/core/rxjs-interop';
import { CngxFormField, CngxLabel, CngxFieldErrors, CngxBindField, adaptFormControl } from '@cngx/forms/field';
import { MatSelect } from '@angular/material/select';
import { MatOption } from '@angular/material/core';

private readonly model = signal({ name: '', website: '' });
private readonly matSchema = schema<{ name: string; website: string }>(root => {
  required(root.name);
  required(root.website);
});
protected readonly matForm = form(this.model, this.matSchema);
protected readonly nameField = this.matForm.name;
protected readonly websiteField = this.matForm.website;

protected readonly matSelectControl = new FormControl<string>('', { validators: [Validators.required], nonNullable: true });
protected readonly matSelectField = adaptFormControl(this.matSelectControl, 'priority', inject(DestroyRef));
protected readonly matSelectValue = toSignal(this.matSelectControl.valueChanges, { initialValue: this.matSelectControl.value });

protected touchAll(): void {
  this.nameField().markAsTouched();
  this.websiteField().markAsTouched();
  this.matSelectControl.markAsTouched();
}`;
  protected readonly _exHtml: string = `<div class="demo-form">
  <div class="demo-field">
    <cngx-form-field [field]="matSelectField">
      <label cngxLabel>Priorität</label>
      <mat-select cngxBindField [formControl]="matSelectControl" placeholder="Bitte wählen"
                  style="min-width:220px;display:inline-block">
        <mat-option value="low">Niedrig</mat-option>
        <mat-option value="normal">Normal</mat-option>
        <mat-option value="high">Hoch</mat-option>
        <mat-option value="urgent">Dringend</mat-option>
      </mat-select>
      <cngx-field-errors />
    </cngx-form-field>
  </div>

  <div class="status-row">
    <span class="status-badge">Selected: {{ matSelectValue() || '—' }}</span>
    <span class="status-badge">Valid: {{ matSelectControl.valid }}</span>
    <span class="status-badge">Touched: {{ matSelectControl.touched }}</span>
  </div>
</div>`;
  private readonly model = signal({ name: '', website: '' });
    private readonly matSchema = schema<{ name: string; website: string }>(root => {
      required(root.name);
      required(root.website);
    });
    protected readonly matForm = form(this.model, this.matSchema);
    protected readonly nameField = this.matForm.name;
    protected readonly websiteField = this.matForm.website;

    protected readonly matSelectControl = new FormControl<string>('', { validators: [Validators.required], nonNullable: true });
    protected readonly matSelectField = adaptFormControl(this.matSelectControl, 'priority', inject(DestroyRef));
    protected readonly matSelectValue = toSignal(this.matSelectControl.valueChanges, { initialValue: this.matSelectControl.value });

    protected touchAll(): void {
      this.nameField().markAsTouched();
      this.websiteField().markAsTouched();
      this.matSelectControl.markAsTouched();
    }
}
