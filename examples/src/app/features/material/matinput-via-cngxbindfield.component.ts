// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/forms/material-demo/material-demo.story.ts#section[0] "matInput via CngxBindField"

import { ChangeDetectionStrategy, Component, DestroyRef, inject, signal } from '@angular/core';
import { form, schema, required, FormField } from '@angular/forms/signals';
import { FormControl, Validators } from '@angular/forms';
import { toSignal } from '@angular/core/rxjs-interop';
import { CngxFormField, CngxLabel, CngxFieldErrors, CngxBindField, adaptFormControl } from '@cngx/forms/field';
import { MatFormField } from '@angular/material/form-field';
import { MatInput } from '@angular/material/input';

@Component({
  selector: 'app-material-matinput-via-cngxbindfield',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxFormField, CngxLabel, CngxFieldErrors, FormField, CngxBindField, MatFormField, MatInput],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="demo-form">
        <div class="demo-field">
          <cngx-form-field [field]="nameField">
            <label cngxLabel>Name</label>
            <mat-form-field appearance="outline" style="width:100%">
              <input matInput cngxBindField [formField]="nameField" placeholder="Jane Doe" />
            </mat-form-field>
            <cngx-field-errors />
          </cngx-form-field>
        </div>

        <div class="demo-field">
          <cngx-form-field [field]="websiteField">
            <label cngxLabel>Website</label>
            <mat-form-field appearance="outline" style="width:100%">
              <input matInput cngxBindField [formField]="websiteField" placeholder="https://example.com" />
            </mat-form-field>
            <cngx-field-errors />
          </cngx-form-field>
        </div>

        <button class="chip" (click)="touchAll()">Touch all fields</button>

        <div class="status-row">
          <span class="status-badge">Name valid: {{ nameField().valid() }}</span>
          <span class="status-badge">Website valid: {{ websiteField().valid() }}</span>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class MaterialMatinputViaCngxbindfield {
  protected readonly _exTitle: string = 'Material Integration';
  protected readonly _exDescription: string = 'Use any Angular Material form control inside cngx-form-field via the universal CngxBindField directive.';
  protected readonly _exSectionTitle: string = 'matInput via CngxBindField';
  protected readonly _exSubtitle: string = '<code>mat-form-field</code> provides the Material input chrome. <code>cngx-form-field</code> provides the A11y coordination. Add <code>cngxBindField</code> on the same element as <code>matInput</code>.';
  protected readonly _exTs: string = `import { form, schema, required, FormField } from '@angular/forms/signals';
import { FormControl, Validators } from '@angular/forms';
import { toSignal } from '@angular/core/rxjs-interop';
import { CngxFormField, CngxLabel, CngxFieldErrors, CngxBindField, adaptFormControl } from '@cngx/forms/field';
import { MatFormField } from '@angular/material/form-field';
import { MatInput } from '@angular/material/input';

private readonly model = signal({ name: '', website: '' });
private readonly matSchema = schema<{ name: string; website: string }>(root => {
  required(root.name);
  required(root.website);
});
protected readonly matForm = form(this.model, this.matSchema);
protected readonly nameField = this.matForm.name;
protected readonly websiteField = this.matForm.website;

protected readonly matSelectControl = new FormControl<string>('', { validators: [Validators.required], nonNullable: true });
protected readonly matSelectField = adaptFormControl(this.matSelectControl, 'priority', inject(DestroyRef));
protected readonly matSelectValue = toSignal(this.matSelectControl.valueChanges, { initialValue: this.matSelectControl.value });

protected touchAll(): void {
  this.nameField().markAsTouched();
  this.websiteField().markAsTouched();
  this.matSelectControl.markAsTouched();
}`;
  protected readonly _exHtml: string = `<div class="demo-form">
  <div class="demo-field">
    <cngx-form-field [field]="nameField">
      <label cngxLabel>Name</label>
      <mat-form-field appearance="outline" style="width:100%">
        <input matInput cngxBindField [formField]="nameField" placeholder="Jane Doe" />
      </mat-form-field>
      <cngx-field-errors />
    </cngx-form-field>
  </div>

  <div class="demo-field">
    <cngx-form-field [field]="websiteField">
      <label cngxLabel>Website</label>
      <mat-form-field appearance="outline" style="width:100%">
        <input matInput cngxBindField [formField]="websiteField" placeholder="https://example.com" />
      </mat-form-field>
      <cngx-field-errors />
    </cngx-form-field>
  </div>

  <button class="chip" (click)="touchAll()">Touch all fields</button>

  <div class="status-row">
    <span class="status-badge">Name valid: {{ nameField().valid() }}</span>
    <span class="status-badge">Website valid: {{ websiteField().valid() }}</span>
  </div>
</div>`;
  private readonly model = signal({ name: '', website: '' });
    private readonly matSchema = schema<{ name: string; website: string }>(root => {
      required(root.name);
      required(root.website);
    });
    protected readonly matForm = form(this.model, this.matSchema);
    protected readonly nameField = this.matForm.name;
    protected readonly websiteField = this.matForm.website;

    protected readonly matSelectControl = new FormControl<string>('', { validators: [Validators.required], nonNullable: true });
    protected readonly matSelectField = adaptFormControl(this.matSelectControl, 'priority', inject(DestroyRef));
    protected readonly matSelectValue = toSignal(this.matSelectControl.valueChanges, { initialValue: this.matSelectControl.value });

    protected touchAll(): void {
      this.nameField().markAsTouched();
      this.websiteField().markAsTouched();
      this.matSelectControl.markAsTouched();
    }
}
