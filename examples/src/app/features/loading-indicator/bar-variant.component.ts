// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/ui/feedback/loading-indicator-demo/loading-indicator-demo.story.ts#section[1] "Bar Variant"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxLoadingIndicator } from '@cngx/ui/feedback';

@Component({
  selector: 'app-loading-indicator-bar-variant',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxLoadingIndicator],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div style="position:relative;border:1px solid var(--cngx-border,#ddd);border-radius:8px;padding:24px;min-height:80px">
        <cngx-loading-indicator [loading]="isLoading()" variant="bar" label="Refreshing"
          style="position:absolute;top:0;left:0;right:0" />
        <p>Container content. Click "Start Loading" above to see the bar.</p>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class LoadingIndicatorBarVariant {
  protected readonly _exTitle: string = 'Loading Indicator';
  protected readonly _exDescription: string = 'Purely visual loading indicator — spinner or bar variant. Delay + minDuration prevent flash.';
  protected readonly _exSectionTitle: string = 'Bar Variant';
  protected readonly _exSubtitle: string = 'YouTube-style thin line. Place at the top of a container.';
  protected readonly _exTs: string = `import { CngxLoadingIndicator } from '@cngx/ui/feedback';

protected readonly isLoading = signal(false);

protected handleToggle(): void {
  this.isLoading.set(true);
  setTimeout(() => this.isLoading.set(false), 2000);
}`;
  protected readonly _exHtml: string = `<div style="position:relative;border:1px solid var(--cngx-border,#ddd);border-radius:8px;padding:24px;min-height:80px">
  <cngx-loading-indicator [loading]="isLoading()" variant="bar" label="Refreshing"
    style="position:absolute;top:0;left:0;right:0" />
  <p>Container content. Click "Start Loading" above to see the bar.</p>
</div>`;
  protected readonly isLoading = signal(false);

    protected handleToggle(): void {
      this.isLoading.set(true);
      setTimeout(() => this.isLoading.set(false), 2000);
    }
}
