// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/ui/mat-paginator-demo/mat-paginator-demo.story.ts#section[1] "Headless — Custom Pagination Controls"

import { ChangeDetectionStrategy, Component, computed, signal, viewChild } from '@angular/core';
import { PEOPLE } from '../../fixtures';
import { CngxPaginate } from '@cngx/common/data';

@Component({
  selector: 'app-mat-paginator-headless-custom-pagination-controls',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxPaginate],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div cngxPaginate #pg2="cngxPaginate" [total]="allItems().length" [cngxPageSize]="3">
        <ul style="list-style: none; padding: 0; margin: 0;">
          @for (item of allItems().slice(pg2.range()[0], pg2.range()[1]); track item.name) {
            <li style="
              padding: 8px 12px;
              border-bottom: 1px solid var(--cngx-border, #eee);
              font-size: 0.875rem;
            ">
              <strong>{{ item.name }}</strong> — {{ item.role }}, {{ item.location }}
            </li>
          }
        </ul>

        <div class="button-row" style="margin-top: 12px; align-items: center;">
          <button class="sort-btn" (click)="pg2.first()" [disabled]="pg2.isFirst()">First</button>
          <button class="sort-btn" (click)="pg2.previous()" [disabled]="pg2.isFirst()">Prev</button>
          <span style="font-size: 0.8125rem; padding: 0 8px;">
            {{ pg2.pageIndex() + 1 }} / {{ pg2.totalPages() }}
          </span>
          <button class="sort-btn" (click)="pg2.next()" [disabled]="pg2.isLast()">Next</button>
          <button class="sort-btn" (click)="pg2.last()" [disabled]="pg2.isLast()">Last</button>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class MatPaginatorHeadlessCustomPaginationControls {
  protected readonly _exTitle: string = 'MatPaginator';
  protected readonly _exDescription: string = 'Material paginator wrapper that connects to a headless CngxPaginate directive via explicit reference binding.';
  protected readonly _exSectionTitle: string = 'Headless — Custom Pagination Controls';
  protected readonly _exSubtitle: string = '<code>CngxPaginate</code> is headless — you can build any UI on top. This example uses plain buttons instead of <code>mat-paginator</code>.';
  protected readonly _exTs: string = `import { PEOPLE } from '../../fixtures';
import { CngxPaginate } from '@cngx/common/data';

protected readonly allItems = signal(PEOPLE);
protected readonly pg = viewChild.required<CngxPaginate>('pg');
protected readonly displayedItems = computed(() => {
  const [start, end] = this.pg().range();
  return this.allItems().slice(start, end);
});`;
  protected readonly _exHtml: string = `<div cngxPaginate #pg2="cngxPaginate" [total]="allItems().length" [cngxPageSize]="3">
  <ul style="list-style: none; padding: 0; margin: 0;">
    @for (item of allItems().slice(pg2.range()[0], pg2.range()[1]); track item.name) {
      <li style="
        padding: 8px 12px;
        border-bottom: 1px solid var(--cngx-border, #eee);
        font-size: 0.875rem;
      ">
        <strong>{{ item.name }}</strong> — {{ item.role }}, {{ item.location }}
      </li>
    }
  </ul>

  <div class="button-row" style="margin-top: 12px; align-items: center;">
    <button class="sort-btn" (click)="pg2.first()" [disabled]="pg2.isFirst()">First</button>
    <button class="sort-btn" (click)="pg2.previous()" [disabled]="pg2.isFirst()">Prev</button>
    <span style="font-size: 0.8125rem; padding: 0 8px;">
      {{ pg2.pageIndex() + 1 }} / {{ pg2.totalPages() }}
    </span>
    <button class="sort-btn" (click)="pg2.next()" [disabled]="pg2.isLast()">Next</button>
    <button class="sort-btn" (click)="pg2.last()" [disabled]="pg2.isLast()">Last</button>
  </div>
</div>`;
  protected readonly allItems = signal(PEOPLE);
    protected readonly pg = viewChild.required<CngxPaginate>('pg');
    protected readonly displayedItems = computed(() => {
      const [start, end] = this.pg().range();
      return this.allItems().slice(start, end);
    });
}
