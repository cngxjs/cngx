// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/ui/mat-paginator-demo/mat-paginator-demo.story.ts#section[0] "CngxMatPaginator — Material Paginator"

import { ChangeDetectionStrategy, Component, computed, signal, viewChild } from '@angular/core';
import { PEOPLE } from '../../fixtures';
import { CngxPaginate } from '@cngx/common/data';
import { CngxMatPaginator } from '@cngx/ui/material';

@Component({
  selector: 'app-mat-paginator-material-paginator',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxPaginate, CngxMatPaginator],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div cngxPaginate #pg="cngxPaginate" [total]="allItems().length">
        <table class="demo-table" style="width: 100%;">
          <thead>
            <tr>
              <th style="text-align: left;">Name</th>
              <th style="text-align: left;">Role</th>
              <th style="text-align: left;">Location</th>
            </tr>
          </thead>
          <tbody>
            @for (item of displayedItems(); track item.name) {
              <tr>
                <td>{{ item.name }}</td>
                <td>{{ item.role }}</td>
                <td>{{ item.location }}</td>
              </tr>
            } @empty {
              <tr><td colspan="3" class="empty-cell">No data</td></tr>
            }
          </tbody>
        </table>
      </div>

      <cngx-mat-paginator [cngxPaginateRef]="pg" [pageSizeOptions]="[3, 5, 10]" />

      <div class="event-grid" style="margin-top: 12px">
        <div class="event-row">
          <span class="event-label">Page</span>
          <span class="event-value">{{ pg.pageIndex() + 1 }} / {{ pg.totalPages() }}</span>
        </div>
        <div class="event-row">
          <span class="event-label">Page size</span>
          <span class="event-value">{{ pg.pageSize() }}</span>
        </div>
        <div class="event-row">
          <span class="event-label">Showing</span>
          <span class="event-value">{{ pg.range()[0] + 1 }}–{{ pg.range()[1] }} of {{ pg.total() }}</span>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class MatPaginatorMaterialPaginator {
  protected readonly _exTitle: string = 'MatPaginator';
  protected readonly _exDescription: string = 'Material paginator wrapper that connects to a headless CngxPaginate directive via explicit reference binding.';
  protected readonly _exSectionTitle: string = 'CngxMatPaginator — Material Paginator';
  protected readonly _exSubtitle: string = '<code>&lt;cngx-mat-paginator&gt;</code> wraps <code>MatPaginator</code> and connects to a <code>CngxPaginate</code> directive via <code>[cngxPaginateRef]</code>. The headless <code>CngxPaginate</code> manages page state; the Material wrapper renders the UI.';
  protected readonly _exTs: string = `import { PEOPLE } from '../../fixtures';
import { CngxPaginate } from '@cngx/common/data';
import { CngxMatPaginator } from '@cngx/ui/material';

protected readonly allItems = signal(PEOPLE);
protected readonly pg = viewChild.required<CngxPaginate>('pg');
protected readonly displayedItems = computed(() => {
  const [start, end] = this.pg().range();
  return this.allItems().slice(start, end);
});`;
  protected readonly _exHtml: string = `<div cngxPaginate #pg="cngxPaginate" [total]="allItems().length">
  <table class="demo-table" style="width: 100%;">
    <thead>
      <tr>
        <th style="text-align: left;">Name</th>
        <th style="text-align: left;">Role</th>
        <th style="text-align: left;">Location</th>
      </tr>
    </thead>
    <tbody>
      @for (item of displayedItems(); track item.name) {
        <tr>
          <td>{{ item.name }}</td>
          <td>{{ item.role }}</td>
          <td>{{ item.location }}</td>
        </tr>
      } @empty {
        <tr><td colspan="3" class="empty-cell">No data</td></tr>
      }
    </tbody>
  </table>
</div>

<cngx-mat-paginator [cngxPaginateRef]="pg" [pageSizeOptions]="[3, 5, 10]" />`;
  protected readonly allItems = signal(PEOPLE);
    protected readonly pg = viewChild.required<CngxPaginate>('pg');
    protected readonly displayedItems = computed(() => {
      const [start, end] = this.pg().range();
      return this.allItems().slice(start, end);
    });
}
