// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/a11y/focus-restore-demo/focus-restore-demo.story.ts#section[0] "Panel with Automatic Restore"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxFocusRestore } from '@cngx/common/a11y';

@Component({
  selector: 'app-focus-restore-panel-with-automatic-restore',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxFocusRestore],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <button (click)="panelOpen.set(true)" class="chip">Open Panel</button>

      @if (panelOpen()) {
        <div cngxFocusRestore
             style="margin-top:12px;padding:16px;border:1px solid var(--cngx-border,#ddd);border-radius:8px">
          <p style="margin:0 0 12px">Panel content. Focus will restore on close.</p>
          <button (click)="panelOpen.set(false)" class="chip">Close Panel</button>
        </div>
      }

      <div class="event-grid" style="margin-top:12px">
        <div class="event-row">
          <span class="event-label">Panel open</span>
          <span class="event-value">{{ panelOpen() }}</span>
        </div>
      </div>
  `,
})
export class FocusRestorePanelWithAutomaticRestore {
  protected readonly _exTitle: string = 'Focus Restore';
  protected readonly _exDescription: string = 'Captures the previously focused element and restores focus when the host is destroyed. Prevents focus loss to body.';
  protected readonly _exSectionTitle: string = 'Panel with Automatic Restore';
  protected readonly _exSubtitle: string = 'Click "Open Panel" — focus moves into the panel. Close it — focus returns to the button that opened it.';
  protected readonly panelOpen = signal(false);
    protected readonly detailsOpen = signal(false);
}
