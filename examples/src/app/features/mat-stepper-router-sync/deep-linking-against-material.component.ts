// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/ui/mat-stepper/mat-stepper-router-sync-demo/mat-stepper-router-sync-demo.story.ts#section[0] "Deep-linking against Material"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxStep, CngxStepContent, CngxStepperRouterSync } from '@cngx/common/stepper';
import { CngxMatStepper } from '@cngx/ui/mat-stepper';

@Component({
  selector: 'app-mat-stepper-router-sync-deep-linking-against-material',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxMatStepper, CngxStep, CngxStepContent, CngxStepperRouterSync],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <cngx-mat-stepper
        [(activeStepIndex)]="active"
        cngxStepperRouterSync
        paramName="step"
        aria-label="Material onboarding"
      >
        <div cngxStep id="profile" label="Profile">
          <ng-template cngxStepContent><p>Set your display name and avatar.</p></ng-template>
        </div>
        <div cngxStep id="notifications" label="Notifications">
          <ng-template cngxStepContent><p>Choose which events should email you.</p></ng-template>
        </div>
        <div cngxStep id="security" label="Security">
          <ng-template cngxStepContent><p>Enable two-factor authentication.</p></ng-template>
        </div>
        <div cngxStep id="confirm" label="Confirm">
          <ng-template cngxStepContent><p>Review your choices and finish.</p></ng-template>
        </div>
      </cngx-mat-stepper>
      <div class="event-grid" style="margin-top:var(--demo-grid-gap, 12px)">
        <div class="event-row"><span class="event-label">Active step</span><span class="event-value">{{ active() }}</span></div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class MatStepperRouterSyncDeepLinkingAgainstMaterial {
  protected readonly _exTitle: string = 'Mat-Stepper — router sync';
  protected readonly _exDescription: string = 'Cross-cutting compose: <code>&lt;cngx-mat-stepper&gt;</code> + <code>cngxStepperRouterSync</code>. Clicking a Material step updates the URL fragment; reloading lands on that step. Same brain, Material skin — proves the layer split.';
  protected readonly _exSectionTitle: string = 'Deep-linking against Material';
  protected readonly _exSubtitle: string = 'The router-sync directive composes against the shared presenter, so Material consumers get URL deep-linking with zero extra wiring.';
  protected readonly _exTs: string = `import { CngxStep, CngxStepContent, CngxStepperRouterSync } from '@cngx/common/stepper';
import { CngxMatStepper } from '@cngx/ui/mat-stepper';

protected readonly active = signal(0);`;
  protected readonly _exHtml: string = `<cngx-mat-stepper
  [(activeStepIndex)]="active"
  cngxStepperRouterSync
  paramName="step"
  aria-label="Material onboarding"
>
  <div cngxStep id="profile" label="Profile">
    <ng-template cngxStepContent><p>Set your display name and avatar.</p></ng-template>
  </div>
  <div cngxStep id="notifications" label="Notifications">
    <ng-template cngxStepContent><p>Choose which events should email you.</p></ng-template>
  </div>
  <div cngxStep id="security" label="Security">
    <ng-template cngxStepContent><p>Enable two-factor authentication.</p></ng-template>
  </div>
  <div cngxStep id="confirm" label="Confirm">
    <ng-template cngxStepContent><p>Review your choices and finish.</p></ng-template>
  </div>
</cngx-mat-stepper>`;
  protected readonly active = signal(0);
}
