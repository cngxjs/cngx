// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/forms/filter-builder-demo/filter-builder-demo.story.ts#section[0] "Basic — two-way binding + JSON inspection"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxFilterBuilder, createEmptyFilterRoot, type FilterGroup } from '@cngx/forms/filter-builder';
import { FILTER_BUILDER_FIELDS, FILTER_BUILDER_SEED } from '../../../fixtures';

@Component({
  selector: 'app-filter-builder-basic-two-way-binding-json-inspection',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxFilterBuilder],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="demo-form">
        <cngx-filter-builder [fields]="fields" [(value)]="tree" />
        <div class="status-row">
          <button class="chip" (click)="resetTree()">Reset to empty</button>
        </div>
        <pre class="code-block"><code>{{ formatTree(tree()) }}</code></pre>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class FilterBuilderBasicTwoWayBindingJsonInspection {
  protected readonly _exTitle: string = 'Filter Builder';
  protected readonly _exDescription: string = 'Recursive query-builder for cngx — composable FilterGroup / FilterExpression tree, pluggable value editors, three logic operators (and/or/xor) plus orthogonal negated modifier.';
  protected readonly _exSectionTitle: string = 'Basic — two-way binding + JSON inspection';
  protected readonly _exSubtitle: string = 'Default editors (string/number/boolean/date). Type or click to add filters; the <code>[(value)]</code> tree is mirrored in the JSON panel below.';
  protected readonly _exTs: string = `import { CngxFilterBuilder, createEmptyFilterRoot, type FilterGroup } from '@cngx/forms/filter-builder';
import { FILTER_BUILDER_FIELDS, FILTER_BUILDER_SEED } from '../../../fixtures';

protected readonly fields = FILTER_BUILDER_FIELDS;
protected readonly tree = signal<FilterGroup>(createEmptyFilterRoot());
protected readonly seedTree = signal<FilterGroup>(FILTER_BUILDER_SEED);

protected formatTree(t: FilterGroup): string {
  return JSON.stringify(t, null, 2);
}

protected resetTree(): void {
  this.tree.set(createEmptyFilterRoot());
}`;
  protected readonly _exHtml: string = `<div class="demo-form">
  <cngx-filter-builder [fields]="fields" [(value)]="tree" />
  <div class="status-row">
    <button class="chip" (click)="resetTree()">Reset to empty</button>
  </div>
  <pre class="code-block"><code>{{ formatTree(tree()) }}</code></pre>
</div>`;
  protected readonly fields = FILTER_BUILDER_FIELDS;
    protected readonly tree = signal<FilterGroup>(createEmptyFilterRoot());
    protected readonly seedTree = signal<FilterGroup>(FILTER_BUILDER_SEED);

    protected formatTree(t: FilterGroup): string {
      return JSON.stringify(t, null, 2);
    }

    protected resetTree(): void {
      this.tree.set(createEmptyFilterRoot());
    }
}
