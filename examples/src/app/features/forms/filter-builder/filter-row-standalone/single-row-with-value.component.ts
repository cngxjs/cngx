// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/forms/filter-row-standalone-demo/filter-row-standalone-demo.story.ts#section[0] "Single row with [(value)]"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxFilterRow, type FilterExpression, type FilterFieldDef } from '@cngx/forms/filter-builder';
import { JsonPipe } from '@angular/common';

@Component({
  selector: 'app-filter-row-standalone-single-row-with-value',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxFilterRow, JsonPipe],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <cngx-filter-row [fields]="fields" [(value)]="value" />

      <div class="row-actions">
        <button type="button" (click)="reset()">Reset to null</button>
      </div>

      <pre class="code-block">{{ value() | json }}</pre>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class FilterRowStandaloneSingleRowWithValue {
  protected readonly _exTitle: string = 'Filter Row — minimal';
  protected readonly _exDescription: string = 'One standalone &lt;cngx-filter-row&gt; with [(value)]. No table, no builder wrapper. Shows the raw two-way binding contract so consumers can copy-paste the pattern.';
  protected readonly _exSectionTitle: string = 'Single row with [(value)]';
  protected readonly _exSubtitle: string = 'Edit field, operator, and value below. The bound signal updates on every change; clicking Remove writes <code>null</code>.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'molecule' }, { dim: 'audience', value: 'dev' }, { dim: 'artifact', value: 'building-block' }, { dim: 'focus', value: 'composition' }, { dim: 'framework', value: 'signal-forms' }];
  protected readonly _exTs: string = `import { CngxFilterRow, type FilterExpression, type FilterFieldDef } from '@cngx/forms/filter-builder';
import { JsonPipe } from '@angular/common';

protected readonly fields: readonly FilterFieldDef[] = [
  { key: 'name', label: 'Name', editorType: 'string' },
  { key: 'age', label: 'Age', editorType: 'number' },
  { key: 'active', label: 'Active', editorType: 'boolean' },
];
protected readonly value = signal<FilterExpression | null>(null);
protected reset(): void {
  this.value.set(null);
}`;
  protected readonly _exHtml: string = `<cngx-filter-row [fields]="fields" [(value)]="value" />

<div class="row-actions">
  <button type="button" (click)="reset()">Reset to null</button>
</div>

<pre class="code-block">{{ value() | json }}</pre>`;
  protected readonly fields: readonly FilterFieldDef[] = [
      { key: 'name', label: 'Name', editorType: 'string' },
      { key: 'age', label: 'Age', editorType: 'number' },
      { key: 'active', label: 'Active', editorType: 'boolean' },
    ];
    protected readonly value = signal<FilterExpression | null>(null);
    protected reset(): void {
      this.value.set(null);
    }
}
