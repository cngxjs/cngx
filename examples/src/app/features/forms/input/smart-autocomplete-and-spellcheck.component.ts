// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/forms/input-demo/input-demo.story.ts#section[0] "Smart Autocomplete and Spellcheck"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { form, schema, required, email, minLength, maxLength, FormField } from '@angular/forms/signals';
import { CngxFormField, CngxLabel, CngxHint, CngxFieldErrors } from '@cngx/forms/field';
import { CngxInput } from '@cngx/forms/input';

@Component({
  selector: 'app-input-smart-autocomplete-and-spellcheck',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxFormField, CngxLabel, CngxInput, CngxHint, CngxFieldErrors, FormField],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="demo-form">
        <div class="demo-field">
          <cngx-form-field [field]="emailField">
            <label cngxLabel>E-Mail</label>
            <input cngxInput [formField]="emailField" placeholder="max@example.com" />
            <span cngxHint>autocomplete="email", spellcheck="false" (inferred from field name "email")</span>
            <cngx-field-errors />
          </cngx-form-field>
        </div>
        <div class="demo-field">
          <cngx-form-field [field]="passwordField">
            <label cngxLabel>Password</label>
            <input cngxInput [formField]="passwordField" type="password" placeholder="At least 8 characters" />
            <span cngxHint>autocomplete="current-password" (inferred from field name "password")</span>
            <cngx-field-errors />
          </cngx-form-field>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class InputSmartAutocompleteAndSpellcheck {
  protected readonly _exTitle: string = 'Input Directives';
  protected readonly _exDescription: string = 'Smart input directives with ARIA projection, autocomplete inference, password toggle, and character counter.';
  protected readonly _exSectionTitle: string = 'Smart Autocomplete and Spellcheck';
  protected readonly _exSubtitle: string = '<code>CngxInput</code> auto-infers <code>autocomplete</code> and <code>spellcheck</code> from the field name. A field named <code>email</code> gets <code>autocomplete="email"</code> and <code>spellcheck="false"</code>. A field named <code>password</code> gets <code>autocomplete="current-password"</code>. Override via <code>provideFormField(withAutocompleteMappings({...}), withNoSpellcheck(...))</code>. The inferred values are shown below each input.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'molecule' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'design' }, { dim: 'audience', value: 'a11y' }, { dim: 'artifact', value: 'standalone' }, { dim: 'focus', value: 'behavior' }, { dim: 'focus', value: 'composition' }, { dim: 'focus', value: 'a11y-pattern' }];
  protected readonly _exTs: string = `import { form, schema, required, email, minLength, maxLength, FormField } from '@angular/forms/signals';
import { CngxFormField, CngxLabel, CngxHint, CngxFieldErrors } from '@cngx/forms/field';
import { CngxInput } from '@cngx/forms/input';

private readonly loginModel = signal({ email: '', password: '' });
private readonly loginSchema = schema<{ email: string; password: string }>(root => {
  required(root.email);
  email(root.email);
  required(root.password);
  minLength(root.password, 8);
  maxLength(root.password, 64);
});
protected readonly loginForm = form(this.loginModel, this.loginSchema);
protected readonly emailField = this.loginForm.email;
protected readonly passwordField = this.loginForm.password;`;
  protected readonly _exHtml: string = `<div class="demo-form">
  <div class="demo-field">
    <cngx-form-field [field]="emailField">
      <label cngxLabel>E-Mail</label>
      <input cngxInput [formField]="emailField" placeholder="max@example.com" />
      <span cngxHint>autocomplete="email", spellcheck="false" (inferred from field name "email")</span>
      <cngx-field-errors />
    </cngx-form-field>
  </div>
  <div class="demo-field">
    <cngx-form-field [field]="passwordField">
      <label cngxLabel>Password</label>
      <input cngxInput [formField]="passwordField" type="password" placeholder="At least 8 characters" />
      <span cngxHint>autocomplete="current-password" (inferred from field name "password")</span>
      <cngx-field-errors />
    </cngx-form-field>
  </div>
</div>`;
  // ── Login form ─────────────────────────────────────────────
    private readonly loginModel = signal({ email: '', password: '' });
    private readonly loginSchema = schema<{ email: string; password: string }>(root => {
      required(root.email);
      email(root.email);
      required(root.password);
      minLength(root.password, 8);
      maxLength(root.password, 64);
    });
    protected readonly loginForm = form(this.loginModel, this.loginSchema);
    protected readonly emailField = this.loginForm.email;
    protected readonly passwordField = this.loginForm.password;

    // ── Bio form ───────────────────────────────────────────────
    private readonly bioModel = signal({ bio: '' });
    private readonly bioSchema = schema<{ bio: string }>(root => {
      required(root.bio);
      minLength(root.bio, 10);
      maxLength(root.bio, 140);
    });
    protected readonly bioForm = form(this.bioModel, this.bioSchema);
    protected readonly bioField = this.bioForm.bio;

    // ── Second bio for custom template ─────────────────────────
    private readonly bio2Model = signal({ bio: '' });
    private readonly bio2Schema = schema<{ bio: string }>(root => {
      maxLength(root.bio, 140);
    });
    protected readonly bio2Form = form(this.bio2Model, this.bio2Schema);
    protected readonly bio2Field = this.bio2Form.bio;
}
