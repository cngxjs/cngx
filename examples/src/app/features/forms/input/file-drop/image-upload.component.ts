// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/forms/file-drop-demo/file-drop-demo.story.ts#section[0] "Image Upload"

import { ChangeDetectionStrategy, Component } from '@angular/core';
import { CngxFileDrop } from '@cngx/forms/input';

@Component({
  selector: 'app-file-drop-image-upload',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxFileDrop],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0 || _exUses.length > 0) {
        <div class="cngx-ex-meta">
          @if (_exTags.length > 0) {
            <ul class="cngx-ex-tags" aria-label="Tags">
              @for (t of _exTags; track t.dim + ':' + t.value) {
                <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
              }
            </ul>
          }
          @if (_exUses.length > 0) {
            <p class="cngx-ex-uses"><span class="cngx-ex-uses__label">uses</span>
              @for (u of _exUses; track u; let last = $last) {
                <code>{{ u }}</code>@if (!last) {<span class="cngx-ex-uses__sep">, </span>}
              }
            </p>
          }
        </div>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="demo-form">
        <div cngxFileDrop [accept]="['image/*']" [maxSize]="5_000_000" [multiple]="true"
          #drop="cngxFileDrop"
        >
          @if (drop.dragging()) {
            <p style="margin:0;font-weight:500">Drop images here</p>
          } @else if (drop.files().length) {
            <p style="margin:0">{{ drop.files().length }} file(s) selected</p>
          } @else {
            <p style="margin:0">Drag images here or
              <button class="chip" (click)="drop.browse()">Browse</button>
            </p>
            <p style="margin:4px 0 0;font-size:0.75rem;color:var(--text-muted,#666)">Max 5 MB per file</p>
          }
        </div>

        @if (drop.files().length) {
          <div class="status-row" style="margin-top:8px">
            @for (file of drop.files(); track file.name) {
              <span class="status-badge">{{ file.name }} ({{ (file.size / 1024).toFixed(1) }} KB)</span>
            }
          </div>
          <button class="chip" (click)="drop.clear()" style="margin-top:4px">Clear</button>
        }

        @if (drop.rejected().length) {
          <div style="margin-top:8px;color:var(--cngx-field-error-color,#d32f2f);font-size:0.75rem">
            @for (r of drop.rejected(); track r.file.name) {
              <div>Rejected: {{ r.file.name }} ({{ r.reason }})</div>
            }
          </div>
        }
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class FileDropImageUpload {
  protected readonly _exTitle: string = 'File Drop';
  protected readonly _exDescription: string = 'Headless drag-and-drop file behavior with MIME/size validation, programmatic file picker, and rejection feedback.';
  protected readonly _exSectionTitle: string = 'Image Upload';
  protected readonly _exSubtitle: string = 'Accepts images up to 5 MB. Drag a file over the zone or click Browse. Rejected files show in the rejection list.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [];
  protected readonly _exUses: readonly string[] = ['CngxFileDrop'];
  protected readonly _exTs: string = `import { CngxFileDrop } from '@cngx/forms/input';`;
  protected readonly _exHtml: string = `<div class="demo-form">
  <div cngxFileDrop [accept]="['image/*']" [maxSize]="5_000_000" [multiple]="true"
    #drop="cngxFileDrop"
  >
    @if (drop.dragging()) {
      <p style="margin:0;font-weight:500">Drop images here</p>
    } @else if (drop.files().length) {
      <p style="margin:0">{{ drop.files().length }} file(s) selected</p>
    } @else {
      <p style="margin:0">Drag images here or
        <button class="chip" (click)="drop.browse()">Browse</button>
      </p>
      <p style="margin:4px 0 0;font-size:0.75rem;color:var(--text-muted,#666)">Max 5 MB per file</p>
    }
  </div>

  @if (drop.files().length) {
    <div class="status-row" style="margin-top:8px">
      @for (file of drop.files(); track file.name) {
        <span class="status-badge">{{ file.name }} ({{ (file.size / 1024).toFixed(1) }} KB)</span>
      }
    </div>
    <button class="chip" (click)="drop.clear()" style="margin-top:4px">Clear</button>
  }

  @if (drop.rejected().length) {
    <div style="margin-top:8px;color:var(--cngx-field-error-color,#d32f2f);font-size:0.75rem">
      @for (r of drop.rejected(); track r.file.name) {
        <div>Rejected: {{ r.file.name }} ({{ r.reason }})</div>
      }
    </div>
  }
</div>`;
}
