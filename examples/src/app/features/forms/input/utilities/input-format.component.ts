// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/forms/utilities-demo/utilities-demo.story.ts#section[2] "Input Format"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxInputFormat } from '@cngx/forms/input';

@Component({
  selector: 'app-utilities-input-format',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxInputFormat],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="demo-form">
        <div class="demo-field">
          <label class="demo-label">Amount (formatted as USD on blur)</label>
          <input [cngxInputFormat]="formatCurrency" [parse]="parseCurrency"
            #fmt="cngxInputFormat" placeholder="Enter amount..." class="demo-input" />
          <div class="status-row">
            <span class="status-badge">Raw: {{ fmt.rawValue() }}</span>
            <span class="status-badge">Display: {{ fmt.displayValue() }}</span>
          </div>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class UtilitiesInputFormat {
  protected readonly _exTitle: string = 'Input Utilities';
  protected readonly _exDescription: string = 'Small headless behaviors: clear button, clipboard copy, and display formatting.';
  protected readonly _exSectionTitle: string = 'Input Format';
  protected readonly _exSubtitle: string = '<code>[cngxInputFormat]</code> formats on blur (display) and parses on focus (edit). Reactive forms receive the raw value.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'atom' }, { dim: 'audience', value: 'dev' }, { dim: 'artifact', value: 'building-block' }, { dim: 'focus', value: 'behavior' }];
  protected readonly _exTs: string = `import { CngxInputFormat } from '@cngx/forms/input';

protected readonly formatCurrency = (v: string) => {
  const n = parseFloat(v);
  return isNaN(n) ? v : new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }).format(n);
};
protected readonly parseCurrency = (v: string) => v.replace(/[^0-9.\\-]/g, '');
protected readonly token = signal('sk-proj-abc123def456ghi789');`;
  protected readonly _exHtml: string = `<div class="demo-form">
  <div class="demo-field">
    <label class="demo-label">Amount (formatted as USD on blur)</label>
    <input [cngxInputFormat]="formatCurrency" [parse]="parseCurrency"
      #fmt="cngxInputFormat" placeholder="Enter amount..." class="demo-input" />
    <div class="status-row">
      <span class="status-badge">Raw: {{ fmt.rawValue() }}</span>
      <span class="status-badge">Display: {{ fmt.displayValue() }}</span>
    </div>
  </div>
</div>`;
  protected readonly formatCurrency = (v: string) => {
      const n = parseFloat(v);
      return isNaN(n) ? v : new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }).format(n);
    };
    protected readonly parseCurrency = (v: string) => v.replace(/[^0-9.\-]/g, '');
    protected readonly token = signal('sk-proj-abc123def456ghi789');
}
