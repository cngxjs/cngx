// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/layout/infinite-scroll-demo/infinite-scroll-demo.story.ts#section[0] "Scrollable List"

import { ChangeDetectionStrategy, Component, computed, signal } from '@angular/core';
import { CngxInfiniteScroll } from '@cngx/common/layout';

@Component({
  selector: 'app-infinite-scroll-scrollable-list',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxInfiniteScroll],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="status-row" style="margin-bottom:12px">
        <span class="status-badge">Items: {{ items().length }}</span>
        <span class="status-badge">Has more: {{ hasMore() }}</span>
        @if (loading()) {
          <span class="status-badge">Loading...</span>
        }
      </div>
      <div class="scroll-root" style="max-height:300px;overflow-y:auto;border:1px solid var(--cngx-border,#e0e0e0);border-radius:8px">
        @for (item of items(); track item) {
          <div style="padding:12px 16px;border-bottom:1px solid var(--cngx-border,#e0e0e0)">
            {{ item }}
          </div>
        }
        <div cngxInfiniteScroll [enabled]="hasMore()" [loading]="loading()" [root]="'.scroll-root'" (loadMore)="loadMore()"
          style="padding:16px;text-align:center;color:var(--text-muted,#999)">
          @if (loading()) {
            Loading more...
          } @else if (!hasMore()) {
            All items loaded.
          }
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class InfiniteScrollScrollableList {
  protected readonly _exTitle: string = 'Infinite Scroll';
  protected readonly _exDescription: string = 'Intersection-based infinite scroll directive that triggers a callback when a sentinel element enters the viewport.';
  protected readonly _exSectionTitle: string = 'Scrollable List';
  protected readonly _exSubtitle: string = 'Scroll to the bottom to load more items. The sentinel element triggers <code>(scrolled)</code> when it enters the viewport. Loading stops at 100 items.';
  protected readonly _exTs: string = `import { CngxInfiniteScroll } from '@cngx/common/layout';
protected readonly items = signal(Array.from({ length: 5 }, (_, i) => \`Item \${i + 1}\`));
  protected readonly loading = signal(false);
  protected readonly hasMore = computed(() => this.items().length < 100);

  protected loadMore(): void {
    if (this.loading() || !this.hasMore()) return;
    this.loading.set(true);
    setTimeout(() => {
      const current = this.items();
      const next = Array.from({ length: 10 }, (_, i) => \`Item \${current.length + i + 1}\`);
      this.items.set([...current, ...next]);
      this.loading.set(false);
    }, 800);
  }`;
  protected readonly _exHtml: string = `<div class="status-row" style="margin-bottom:12px">
    <span class="status-badge">Items: {{ items().length }}</span>
    <span class="status-badge">Has more: {{ hasMore() }}</span>
    @if (loading()) {
      <span class="status-badge">Loading...</span>
    }
  </div>
  <div class="scroll-root" style="max-height:300px;overflow-y:auto;border:1px solid var(--cngx-border,#e0e0e0);border-radius:8px">
    @for (item of items(); track item) {
      <div style="padding:12px 16px;border-bottom:1px solid var(--cngx-border,#e0e0e0)">
        {{ item }}
      </div>
    }
    <div cngxInfiniteScroll [enabled]="hasMore()" [loading]="loading()" [root]="'.scroll-root'" (loadMore)="loadMore()"
      style="padding:16px;text-align:center;color:var(--text-muted,#999)">
      @if (loading()) {
        Loading more...
      } @else if (!hasMore()) {
        All items loaded.
      }
    </div>
  </div>`;
  protected readonly items = signal(Array.from({ length: 5 }, (_, i) => `Item ${i + 1}`));
    protected readonly loading = signal(false);
    protected readonly hasMore = computed(() => this.items().length < 100);

    protected loadMore(): void {
      if (this.loading() || !this.hasMore()) return;
      this.loading.set(true);
      setTimeout(() => {
        const current = this.items();
        const next = Array.from({ length: 10 }, (_, i) => `Item ${current.length + i + 1}`);
        this.items.set([...current, ...next]);
        this.loading.set(false);
      }, 800);
    }
}
