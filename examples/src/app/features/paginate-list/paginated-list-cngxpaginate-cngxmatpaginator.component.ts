// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/data/paginate-list-demo/paginate-list.story.ts#section[0] "Paginated List — CngxPaginate + CngxMatPaginator"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { PEOPLE, type Person } from '../../fixtures';
import { CngxPaginate } from '@cngx/common/data';
import { CngxMatPaginator } from '@cngx/ui/material';

@Component({
  selector: 'app-paginate-list-paginated-list-cngxpaginate-cngxmatpaginator',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxPaginate, CngxMatPaginator],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div cngxPaginate #pg="cngxPaginate" [total]="items().length" [cngxPageSize]="5" style="display:contents">
        <ul style="list-style:none;padding:0;margin:0">
          @for (p of items().slice(pg.range()[0], pg.range()[1]); track p.name) {
            <li style="padding:8px 0;border-bottom:1px solid var(--cngx-border-color,#e0e0e0)">
              <strong>{{ p.name }}</strong> &mdash; {{ p.role }}, {{ p.location }}
            </li>
          }
        </ul>
        <cngx-mat-paginator [cngxPaginateRef]="pg" [pageSizeOptions]="[5, 10, 16]" />
        <div class="status-row">
          <span class="status-badge">
            {{ pg.range()[0] + 1 }}–{{ pg.range()[1] > pg.total() ? pg.total() : pg.range()[1] }} of {{ pg.total() }} people
          </span>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class PaginateListPaginatedListCngxpaginateCngxmatpaginator {
  protected readonly _exTitle: string = 'Paginate — List';
  protected readonly _exDescription: string = '';
  protected readonly _exSectionTitle: string = 'Paginated List — CngxPaginate + CngxMatPaginator';
  protected readonly _exSubtitle: string = '<code>CngxPaginate</code> works with any list, not just tables. Put <code>[cngxPaginate]</code> on any wrapper element, bind <code>[total]</code> to the full item count, and slice the array with <code>pg.range()</code> in the <code>@for</code> loop. <code>CngxMatPaginator</code> provides the Material paginator UI via <code>[cngxPaginateRef]</code>.';
  protected readonly _exTs: string = `import { PEOPLE, type Person } from '../../fixtures';
import { CngxPaginate } from '@cngx/common/data';
import { CngxMatPaginator } from '@cngx/ui/material';

// Expand the dataset so pagination is clearly visible (3 pages of 5)
protected readonly items = signal<Person[]>([
  ...PEOPLE,
  ...PEOPLE.map((p: Person) => ({ ...p, name: p.name + ' Jr.' })),
]);`;
  protected readonly _exHtml: string = `<div cngxPaginate #pg="cngxPaginate" [total]="items().length" [cngxPageSize]="5" style="display:contents">
  <ul style="list-style:none;padding:0;margin:0">
    @for (p of items().slice(pg.range()[0], pg.range()[1]); track p.name) {
      <li style="padding:8px 0;border-bottom:1px solid var(--cngx-border-color,#e0e0e0)">
        <strong>{{ p.name }}</strong> &mdash; {{ p.role }}, {{ p.location }}
      </li>
    }
  </ul>
  <cngx-mat-paginator [cngxPaginateRef]="pg" [pageSizeOptions]="[5, 10, 16]" />
  <div class="status-row">
    <span class="status-badge">
      {{ pg.range()[0] + 1 }}–{{ pg.range()[1] > pg.total() ? pg.total() : pg.range()[1] }} of {{ pg.total() }} people
    </span>
  </div>
</div>`;
  // Expand the dataset so pagination is clearly visible (3 pages of 5)
    protected readonly items = signal<Person[]>([
      ...PEOPLE,
      ...PEOPLE.map((p: Person) => ({ ...p, name: p.name + ' Jr.' })),
    ]);
}
