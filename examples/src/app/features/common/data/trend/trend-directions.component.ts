// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/data/trend-demo/trend-demo.story.ts#section[0] "Trend Directions"

import { ChangeDetectionStrategy, Component } from '@angular/core';
import { CngxTrend } from '@cngx/common/data';

@Component({
  selector: 'app-trend-trend-directions',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxTrend],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div style="display:flex;gap:32px;align-items:center">
        <div style="text-align:center">
          <div style="font-size:0.75rem;color:var(--text-muted);margin-bottom:8px">Positive</div>
          <cngx-trend [value]="5.3" />
        </div>
        <div style="text-align:center">
          <div style="font-size:0.75rem;color:var(--text-muted);margin-bottom:8px">Negative</div>
          <cngx-trend [value]="-2.1" />
        </div>
        <div style="text-align:center">
          <div style="font-size:0.75rem;color:var(--text-muted);margin-bottom:8px">Flat</div>
          <cngx-trend [value]="0" />
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class TrendTrendDirections {
  protected readonly _exTitle: string = 'Trend';
  protected readonly _exDescription: string = 'Displays a directional trend indicator with arrow icon and formatted percentage. Consumer can override the SR label for full context.';
  protected readonly _exSectionTitle: string = 'Trend Directions';
  protected readonly _exSubtitle: string = 'Positive (green, up arrow), negative (red, down arrow), and zero (neutral, right arrow).';
  protected readonly _exTs: string = `import { CngxTrend } from '@cngx/common/data';`;
  protected readonly _exHtml: string = `<div style="display:flex;gap:32px;align-items:center">
  <div style="text-align:center">
    <div style="font-size:0.75rem;color:var(--text-muted);margin-bottom:8px">Positive</div>
    <cngx-trend [value]="5.3" />
  </div>
  <div style="text-align:center">
    <div style="font-size:0.75rem;color:var(--text-muted);margin-bottom:8px">Negative</div>
    <cngx-trend [value]="-2.1" />
  </div>
  <div style="text-align:center">
    <div style="font-size:0.75rem;color:var(--text-muted);margin-bottom:8px">Flat</div>
    <cngx-trend [value]="0" />
  </div>
</div>`;
}
