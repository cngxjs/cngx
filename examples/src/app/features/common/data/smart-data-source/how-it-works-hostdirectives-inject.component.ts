// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/data/smart-data-source-demo/smart-data-source-demo.story.ts#section[1] "How It Works — hostDirectives + inject()"

import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core';
import { toSignal } from '@angular/core/rxjs-interop';
import { CngxFilter, CngxSort, injectSmartDataSource } from '@cngx/common';
import { PEOPLE, type Person } from '../../../../fixtures';

@Component({
  selector: 'app-smart-data-source-how-it-works-hostdirectives-inject',
  changeDetection: ChangeDetectionStrategy.OnPush,
  hostDirectives: [CngxSort, CngxFilter],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <pre class="code-block"><code>@Component(&#123;
      selector: 'my-table',
      hostDirectives: [
        &#123; directive: CngxSort &#125;,
        &#123; directive: CngxFilter &#125;,
      ],
    &#125;)
    class MyTableComponent &#123;
      // inject() finds the hostDirective instances automatically
      protected readonly sort   = inject(CngxSort,   &#123; host: true &#125;);
      protected readonly filter = inject(CngxFilter, &#123; host: true &#125;);

      private readonly items = signal(data);

      // SmartDataSource discovers CngxSort + CngxFilter via inject()
      // because we&apos;re inside the component&apos;s injection context
      private readonly ds = injectSmartDataSource(this.items);

      protected readonly rows = toSignal(this.ds.connect(), &#123; initialValue: [] &#125;);
    &#125;</code></pre>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class SmartDataSourceHowItWorksHostdirectivesInject {
  protected readonly _exTitle: string = 'SmartDataSource';
  protected readonly _exDescription: string = '';
  protected readonly _exSectionTitle: string = 'How It Works — hostDirectives + inject()';
  protected readonly _exSubtitle: string = 'The key is calling <code>injectSmartDataSource()</code> inside a component whose host element has <code>[cngxSort]</code> / <code>[cngxFilter]</code> as <code>hostDirectives</code>. The factory uses optional <code>inject()</code> to auto-discover them.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'molecule' }, { dim: 'audience', value: 'dev' }, { dim: 'artifact', value: 'building-block' }, { dim: 'focus', value: 'composition' }, { dim: 'focus', value: 'integration' }];
  protected readonly _exTs: string = `import { toSignal } from '@angular/core/rxjs-interop';
import { CngxFilter, CngxSort, injectSmartDataSource } from '@cngx/common';
import { PEOPLE, type Person } from '../../../../fixtures';

protected readonly sort = inject(CngxSort, { host: true });
protected readonly filter = inject(CngxFilter<Person>, { host: true });
private readonly items = signal(PEOPLE);
private readonly ds = injectSmartDataSource(this.items);
protected readonly rows = toSignal(this.ds.connect(), { initialValue: [] as Person[] });`;
  protected readonly _exHtml: string = `  <pre class="code-block"><code>@Component(&#123;
  selector: 'my-table',
  hostDirectives: [
    &#123; directive: CngxSort &#125;,
    &#123; directive: CngxFilter &#125;,
  ],
&#125;)
class MyTableComponent &#123;
  // inject() finds the hostDirective instances automatically
  protected readonly sort   = inject(CngxSort,   &#123; host: true &#125;);
  protected readonly filter = inject(CngxFilter, &#123; host: true &#125;);

  private readonly items = signal(data);

  // SmartDataSource discovers CngxSort + CngxFilter via inject()
  // because we&apos;re inside the component&apos;s injection context
  private readonly ds = injectSmartDataSource(this.items);

  protected readonly rows = toSignal(this.ds.connect(), &#123; initialValue: [] &#125;);
&#125;</code></pre>`;
  protected readonly sort = inject(CngxSort, { host: true });
    protected readonly filter = inject(CngxFilter<Person>, { host: true });

    protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort(
      (a: string, b: string) => a.localeCompare(b),
    );
    protected readonly total = PEOPLE.length;

    private readonly items = signal(PEOPLE);
    private readonly ds = injectSmartDataSource(this.items);
    protected readonly rows = toSignal(this.ds.connect(), { initialValue: [] as Person[] });

    protected filterBy(location: string | null): void {
      if (location === null) {
        this.filter.clear();
      } else {
        this.filter.setPredicate((p) => p.location === location);
      }
    }
}
