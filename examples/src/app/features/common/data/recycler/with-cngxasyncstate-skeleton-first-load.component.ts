// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/data/recycler-demo/recycler-demo.story.ts#section[4] "With CngxAsyncState — Skeleton First Load"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { injectRecycler } from '@cngx/common/data';

@Component({
  selector: 'app-recycler-with-cngxasyncstate-skeleton-first-load',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <pre class="code-block"><code>// With CngxAsyncState integration
    readonly state = injectAsyncState(() => this.api.getAll());

    readonly recycler = injectRecycler({{ '{' }}
      scrollElement: '.scroll',
      totalCount: () => (this.state.data() ?? []).length,
      estimateSize: 64,
      state: this.state,         // drives isLoading, isRefreshing, isEmpty
      skeletonDelay: 300,        // fast loads never show skeleton
    {{ '}' }});

    readonly visible = this.recycler.sliced(
      computed(() => this.state.data() ?? [])
    );

    // Template:
    // &#64;if (recycler.showSkeleton()) {{ '{' }}
    //   &#64;for (_ of skeletonRange(recycler.skeletonSlots()); track $index) {{ '{' }}
    //     &lt;div class="skeleton-item" aria-hidden="true"&gt;&lt;/div&gt;
    //   {{ '}' }}
    // {{ '}' }} &#64;else {{ '{' }}
    //   &#64;for (item of visible(); track item.id) {{ '{' }} ... {{ '}' }}
    // {{ '}' }}</code></pre>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class RecyclerWithCngxasyncstateSkeletonFirstLoad {
  protected readonly _exTitle: string = 'Recycler — DOM Recycling';
  protected readonly _exDescription: string = 'Signal-based virtualizer for long lists. Items outside the viewport are removed from the DOM. Consumer renders with @for and two spacer containers.';
  protected readonly _exSectionTitle: string = 'With CngxAsyncState — Skeleton First Load';
  protected readonly _exSubtitle: string = 'The recycler derives <code>isLoading</code>, <code>showSkeleton</code>, and <code>isEmpty</code> from a <code>CngxAsyncState</code> source — same pattern as <code>CngxCardGrid</code> and <code>CngxTreetable</code>. Skeleton slots fill the viewport height automatically.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'molecule' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'a11y' }, { dim: 'artifact', value: 'building-block' }, { dim: 'focus', value: 'behavior' }, { dim: 'focus', value: 'a11y-pattern' }, { dim: 'focus', value: 'async-state' }];
  protected readonly _exTs: string = `import { injectRecycler } from '@cngx/common/data';

protected readonly allItems = signal(
  Array.from({ length: 5000 }, (_, i) => ({
    id: i,
    name: 'Item ' + (i + 1),
    description: 'Description for item ' + (i + 1),
  })),
);

protected readonly recycler = injectRecycler({
  scrollElement: '.recycler-scroll',
  totalCount: () => this.allItems().length,
  estimateSize: 48,
  overscan: 10,
});

protected readonly visibleItems = this.recycler.sliced(this.allItems);

protected readonly targetIndex = signal(0);

protected handleScrollTo(): void {
  this.recycler.scrollToIndex(this.targetIndex());
}`;
  protected readonly _exHtml: string = `  <pre class="code-block"><code>// With CngxAsyncState integration
readonly state = injectAsyncState(() => this.api.getAll());

readonly recycler = injectRecycler({{ '{' }}
  scrollElement: '.scroll',
  totalCount: () => (this.state.data() ?? []).length,
  estimateSize: 64,
  state: this.state,         // drives isLoading, isRefreshing, isEmpty
  skeletonDelay: 300,        // fast loads never show skeleton
{{ '}' }});

readonly visible = this.recycler.sliced(
  computed(() => this.state.data() ?? [])
);

// Template:
// &#64;if (recycler.showSkeleton()) {{ '{' }}
//   &#64;for (_ of skeletonRange(recycler.skeletonSlots()); track $index) {{ '{' }}
//     &lt;div class="skeleton-item" aria-hidden="true"&gt;&lt;/div&gt;
//   {{ '}' }}
// {{ '}' }} &#64;else {{ '{' }}
//   &#64;for (item of visible(); track item.id) {{ '{' }} ... {{ '}' }}
// {{ '}' }}</code></pre>`;
  protected readonly allItems = signal(
      Array.from({ length: 5000 }, (_, i) => ({
        id: i,
        name: 'Item ' + (i + 1),
        description: 'Description for item ' + (i + 1),
      })),
    );

    protected readonly recycler = injectRecycler({
      scrollElement: '.recycler-scroll',
      totalCount: () => this.allItems().length,
      estimateSize: 48,
      overscan: 10,
    });

    protected readonly visibleItems = this.recycler.sliced(this.allItems);

    protected readonly targetIndex = signal(0);

    protected handleScrollTo(): void {
      this.recycler.scrollToIndex(this.targetIndex());
    }
}
