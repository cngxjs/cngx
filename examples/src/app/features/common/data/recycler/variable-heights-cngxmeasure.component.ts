// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/data/recycler-demo/recycler-demo.story.ts#section[1] "Variable Heights — CngxMeasure"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { injectRecycler, CngxMeasure } from '@cngx/common/data';

@Component({
  selector: 'app-recycler-variable-heights-cngxmeasure',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxMeasure],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="recycler-var-scroll"
           style="height:400px;overflow-y:auto;border:1px solid var(--cngx-color-border,#e0e0e0);border-radius:8px">
        <div [style.paddingTop.px]="varRecycler.offsetBefore()"
             [style.paddingBottom.px]="varRecycler.offsetAfter()">
          @for (item of varVisible(); track item.id; let i = $index) {
            <div [cngxMeasure]="varRecycler" [cngxMeasureIndex]="varRecycler.start() + i"
                 style="padding:12px 16px;border-bottom:1px solid var(--cngx-color-border,#e0e0e0)">
              <strong>{{ item.name }}</strong>
              <p style="margin:4px 0;color:var(--cngx-text-muted,#666)">{{ item.content }}</p>
            </div>
          }
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class RecyclerVariableHeightsCngxmeasure {
  protected readonly _exTitle: string = 'Recycler — DOM Recycling';
  protected readonly _exDescription: string = 'Signal-based virtualizer for long lists. Items outside the viewport are removed from the DOM. Consumer renders with @for and two spacer containers.';
  protected readonly _exSectionTitle: string = 'Variable Heights — CngxMeasure';
  protected readonly _exSubtitle: string = 'For items with varying heights, add <code>[cngxMeasure]</code> to each item. The recycler accumulates measured heights for accurate scroll position calculation.';
  protected readonly _exTs: string = `import { injectRecycler, CngxMeasure } from '@cngx/common/data';

protected readonly allItems = signal(
  Array.from({ length: 5000 }, (_, i) => ({
    id: i,
    name: 'Item ' + (i + 1),
    description: 'Description for item ' + (i + 1),
  })),
);

protected readonly recycler = injectRecycler({
  scrollElement: '.recycler-scroll',
  totalCount: () => this.allItems().length,
  estimateSize: 48,
  overscan: 10,
});

protected readonly visibleItems = this.recycler.sliced(this.allItems);

protected readonly targetIndex = signal(0);

protected handleScrollTo(): void {
  this.recycler.scrollToIndex(this.targetIndex());
}



protected readonly variableItems = signal(
  Array.from({ length: 2000 }, (_, i) => ({
    id: i,
    name: 'Item ' + (i + 1),
    content: Array.from({ length: 1 + (i % 5) }, (__, j) => 'Line ' + (j + 1)).join(' | '),
  })),
);

protected readonly varRecycler = injectRecycler({
  scrollElement: '.recycler-var-scroll',
  totalCount: () => this.variableItems().length,
  estimateSize: 48,
  overscan: 5,
});

protected readonly varVisible = this.varRecycler.sliced(this.variableItems);`;
  protected readonly _exHtml: string = `<div class="recycler-var-scroll"
     style="height:400px;overflow-y:auto;border:1px solid var(--cngx-color-border,#e0e0e0);border-radius:8px">
  <div [style.paddingTop.px]="varRecycler.offsetBefore()"
       [style.paddingBottom.px]="varRecycler.offsetAfter()">
    @for (item of varVisible(); track item.id; let i = $index) {
      <div [cngxMeasure]="varRecycler" [cngxMeasureIndex]="varRecycler.start() + i"
           style="padding:12px 16px;border-bottom:1px solid var(--cngx-color-border,#e0e0e0)">
        <strong>{{ item.name }}</strong>
        <p style="margin:4px 0;color:var(--cngx-text-muted,#666)">{{ item.content }}</p>
      </div>
    }
  </div>
</div>`;
  protected readonly allItems = signal(
      Array.from({ length: 5000 }, (_, i) => ({
        id: i,
        name: 'Item ' + (i + 1),
        description: 'Description for item ' + (i + 1),
      })),
    );

    protected readonly recycler = injectRecycler({
      scrollElement: '.recycler-scroll',
      totalCount: () => this.allItems().length,
      estimateSize: 48,
      overscan: 10,
    });

    protected readonly visibleItems = this.recycler.sliced(this.allItems);

    protected readonly targetIndex = signal(0);

    protected handleScrollTo(): void {
      this.recycler.scrollToIndex(this.targetIndex());
    }

  protected readonly variableItems = signal(
      Array.from({ length: 2000 }, (_, i) => ({
        id: i,
        name: 'Item ' + (i + 1),
        content: Array.from({ length: 1 + (i % 5) }, (__, j) => 'Line ' + (j + 1)).join(' | '),
      })),
    );

    protected readonly varRecycler = injectRecycler({
      scrollElement: '.recycler-var-scroll',
      totalCount: () => this.variableItems().length,
      estimateSize: 48,
      overscan: 5,
    });

    protected readonly varVisible = this.varRecycler.sliced(this.variableItems);
}
