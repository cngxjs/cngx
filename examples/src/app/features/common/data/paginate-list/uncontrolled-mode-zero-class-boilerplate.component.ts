// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/data/paginate-list-demo/paginate-list.story.ts#section[1] "Uncontrolled Mode — Zero Class Boilerplate"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { PEOPLE, type Person } from '../../../../fixtures';

@Component({
  selector: 'app-paginate-list-uncontrolled-mode-zero-class-boilerplate',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0 || _exUses.length > 0) {
        <div class="cngx-ex-meta">
          @if (_exTags.length > 0) {
            <ul class="cngx-ex-tags" aria-label="Tags">
              @for (t of _exTags; track t.dim + ':' + t.value) {
                <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
              }
            </ul>
          }
          @if (_exUses.length > 0) {
            <p class="cngx-ex-uses"><span class="cngx-ex-uses__label">uses</span>
              @for (u of _exUses; track u; let last = $last) {
                <code>{{ u }}</code>@if (!last) {<span class="cngx-ex-uses__sep">, </span>}
              }
            </p>
          }
        </div>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <pre class="code-block"><code>// Uncontrolled — zero class boilerplate
    &lt;div cngxPaginate #pg="cngxPaginate" [total]="items.length"&gt;
      &#64;for (item of items.slice(pg.range()[0], pg.range()[1]); track item.id) &#123;
        &lt;div&gt;&#123;<!-- -->&#123; item.name &#125;<!-- -->&#125;&lt;/div&gt;
      &#125;
      &lt;cngx-mat-paginator [cngxPaginateRef]="pg" /&gt;
    &lt;/div&gt;

    // Controlled — page state in component class
    protected readonly pageIndex = signal(0);
    protected readonly pageSize  = signal(10);

    // In template:
    // [cngxPageIndex]="pageIndex()"
    // [cngxPageSize]="pageSize()"
    // (pageChange)="pageIndex.set($event)"
    // (pageSizeChange)="pageSize.set($event)"</code></pre>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class PaginateListUncontrolledModeZeroClassBoilerplate {
  protected readonly _exTitle: string = 'Paginate — List';
  protected readonly _exDescription: string = '';
  protected readonly _exSectionTitle: string = 'Uncontrolled Mode — Zero Class Boilerplate';
  protected readonly _exSubtitle: string = 'In uncontrolled mode, <code>CngxPaginate</code> manages its own <code>pageIndex</code> and <code>pageSize</code> internally. No component signals needed — just bind <code>[total]</code> and read <code>pg.range()</code> in the template. <code>CngxMatPaginator</code> writes directly to the directive\'s internal state via <code>setPage()</code> / <code>setPageSize()</code>. Use controlled mode (<code>[cngxPageIndex]</code>, <code>(pageChange)</code>) when you need the page state in your component class.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [];
  protected readonly _exUses: readonly string[] = ['CngxPaginate', 'CngxMatPaginator'];
  protected readonly _exTs: string = `import { PEOPLE, type Person } from '../../../../fixtures';

// Expand the dataset so pagination is clearly visible (3 pages of 5)
protected readonly items = signal<Person[]>([
  ...PEOPLE,
  ...PEOPLE.map((p: Person) => ({ ...p, name: p.name + ' Jr.' })),
]);`;
  protected readonly _exHtml: string = `  <pre class="code-block"><code>// Uncontrolled — zero class boilerplate
&lt;div cngxPaginate #pg="cngxPaginate" [total]="items.length"&gt;
  &#64;for (item of items.slice(pg.range()[0], pg.range()[1]); track item.id) &#123;
    &lt;div&gt;&#123;<!-- -->&#123; item.name &#125;<!-- -->&#125;&lt;/div&gt;
  &#125;
  &lt;cngx-mat-paginator [cngxPaginateRef]="pg" /&gt;
&lt;/div&gt;

// Controlled — page state in component class
protected readonly pageIndex = signal(0);
protected readonly pageSize  = signal(10);

// In template:
// [cngxPageIndex]="pageIndex()"
// [cngxPageSize]="pageSize()"
// (pageChange)="pageIndex.set($event)"
// (pageSizeChange)="pageSize.set($event)"</code></pre>`;
  // Expand the dataset so pagination is clearly visible (3 pages of 5)
    protected readonly items = signal<Person[]>([
      ...PEOPLE,
      ...PEOPLE.map((p: Person) => ({ ...p, name: p.name + ' Jr.' })),
    ]);
}
