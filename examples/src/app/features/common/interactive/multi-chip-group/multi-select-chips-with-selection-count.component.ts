// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: examples/stories/common/interactive/multi-chip-group-demo/multi-chip-group-demo.story.ts#section[0] "Multi-select chips with selection count"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxMultiChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';

@Component({
  selector: 'app-multi-chip-group-multi-select-chips-with-selection-count',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxMultiChipGroup, CngxChipInGroup, CngxChip],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <cngx-multi-chip-group #group="cngxMultiChipGroup" label="Tags" [(selectedValues)]="picked">
        @for (tag of tags; track tag) {
          <cngx-chip cngxChipInGroup [value]="tag">{{ tag }}</cngx-chip>
        }
      </cngx-multi-chip-group>
      <p class="caption">
        selected (<code>{{ group.selectedCount() }}</code>):
        <code>{{ picked().join(', ') || '(none)' }}</code>
      </p>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class MultiChipGroupMultiSelectChipsWithSelectionCount {
  protected readonly _exTitle: string = 'Multi chip group (multi-select)';
  protected readonly _exDescription: string = 'Multi-select chip group molecule. Owns <code>selectedValues = model&lt;T[]&gt;</code>, provides <code>CNGX_CHIP_GROUP_HOST</code> with multi-select semantics, and uses <code>createSelectionController</code> internally for membership tracking. Renders <code>role="listbox" aria-multiselectable="true"</code>; chips toggle independently. Use <code>[keyFn]</code> for object-valued options where reference equality is unstable.';
  protected readonly _exSectionTitle: string = 'Multi-select chips with selection count';
  protected readonly _exSubtitle: string = 'Independent toggle per chip. <code>selectedCount</code> is exposed on the group for label hints. Multi-select uses <code>createSelectionController</code> with structural-equality membership tracking.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'molecule' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'design' }, { dim: 'artifact', value: 'building-block' }, { dim: 'focus', value: 'composition' }, { dim: 'focus', value: 'a11y-pattern' }];
  protected readonly _exTs: string = `import { CngxMultiChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';

protected readonly tags = ['urgent', 'review', 'blocker', 'follow-up', 'on-hold'] as const;
protected readonly picked = signal<string[]>(['urgent', 'review']);`;
  protected readonly _exHtml: string = `<cngx-multi-chip-group #group="cngxMultiChipGroup" label="Tags" [(selectedValues)]="picked">
  @for (tag of tags; track tag) {
    <cngx-chip cngxChipInGroup [value]="tag">{{ tag }}</cngx-chip>
  }
</cngx-multi-chip-group>
<p class="caption">
  selected (<code>{{ group.selectedCount() }}</code>):
  <code>{{ picked().join(', ') || '(none)' }}</code>
</p>`;
  protected readonly tags = ['urgent', 'review', 'blocker', 'follow-up', 'on-hold'] as const;
    protected readonly picked = signal<string[]>(['urgent', 'review']);
}
