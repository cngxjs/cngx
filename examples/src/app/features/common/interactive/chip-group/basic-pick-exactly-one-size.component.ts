// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/chip-group-demo/chip-group-demo.story.ts#section[0] "Basic — pick exactly one size"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';

@Component({
  selector: 'app-chip-group-basic-pick-exactly-one-size',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxChipGroup, CngxChipInGroup, CngxChip],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <cngx-chip-group label="T-shirt size" [(selected)]="size" [disabled]="groupDisabled()">
        @for (s of sizes; track s) {
          <cngx-chip cngxChipInGroup [value]="s">{{ s.toUpperCase() }}</cngx-chip>
        }
      </cngx-chip-group>
      <p class="caption">picked: <code>{{ size() ?? '(none)' }}</code></p>
      <button type="button" (click)="groupDisabled.set(!groupDisabled())">toggle group disabled</button>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class ChipGroupBasicPickExactlyOneSize {
  protected readonly _exTitle: string = 'Chip group (single-select)';
  protected readonly _exDescription: string = 'Single-select chip group molecule. Owns <code>selected = model&lt;T | undefined&gt;</code>, provides <code>CNGX_CHIP_GROUP_HOST</code> so projected <code>&lt;cngx-chip cngxChipInGroup&gt;</code> leaves derive their <code>aria-selected</code> from the parent\'s selection. Renders <code>role="listbox"</code>; composes <code>CngxRovingTabindex</code> for arrow-key navigation. Single-mode behaviour: re-clicking the active chip clears the selection (toggle off).';
  protected readonly _exSectionTitle: string = 'Basic — pick exactly one size';
  protected readonly _exSubtitle: string = 'Click any chip to select it; click again to deselect. Only one chip is <code>aria-selected</code> at a time.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'molecule' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'design' }, { dim: 'artifact', value: 'building-block' }, { dim: 'focus', value: 'visual-variants' }, { dim: 'focus', value: 'composition' }];
  protected readonly _exTs: string = `import { CngxChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';

protected readonly sizes = ['sm', 'md', 'lg'] as const;
protected readonly size = signal<'sm' | 'md' | 'lg' | undefined>('md');
protected readonly groupDisabled = signal(false);`;
  protected readonly _exHtml: string = `<cngx-chip-group label="T-shirt size" [(selected)]="size" [disabled]="groupDisabled()">
  @for (s of sizes; track s) {
    <cngx-chip cngxChipInGroup [value]="s">{{ s.toUpperCase() }}</cngx-chip>
  }
</cngx-chip-group>
<p class="caption">picked: <code>{{ size() ?? '(none)' }}</code></p>
<button type="button" (click)="groupDisabled.set(!groupDisabled())">toggle group disabled</button>`;
  protected readonly sizes = ['sm', 'md', 'lg'] as const;
    protected readonly size = signal<'sm' | 'md' | 'lg' | undefined>('md');
    protected readonly groupDisabled = signal(false);
}
