// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/listbox-search-demo/listbox-search-demo.story.ts#section[0] "Command palette"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxListbox, CngxListboxSearch, CngxOption } from '@cngx/common/interactive';

@Component({
  selector: 'app-listbox-search-command-palette',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxListbox, CngxListboxSearch, CngxOption],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <input
        cngxListboxSearch
        class="palette-search"
        type="search"
        placeholder="Search commands…"
        aria-label="Search commands"
        #search="cngxListboxSearch"
      />
      <div
        cngxListbox
        [label]="'Palette'"
        [cngxSearchRef]="search"
        class="ad-listbox"
        tabindex="0"
        (valueChange)="lastSelected.set($any($event))"
        #palette="cngxListbox"
      >
        @let term = palette.searchTerm().toLowerCase();
        @for (cmd of commands(); track cmd.value) {
          @if (!term || cmd.label.toLowerCase().includes(term)) {
            <div cngxOption [value]="cmd.value" [label]="cmd.label">{{ cmd.label }}</div>
          }
        }
      </div>
      @if (palette.options().length === 0) {
        <p class="empty">No matching commands.</p>
      }
      <div class="event-grid" style="margin-top:8px">
        <div class="event-row">
          <span class="event-label">Last selected</span>
          <span class="event-value">{{ lastSelected() ?? '—' }}</span>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class ListboxSearchCommandPalette {
  protected readonly _exTitle: string = 'Listbox Search';
  protected readonly _exDescription: string = 'Search input that drives a sibling CngxListbox. Term is debounced via the underlying CngxSearch hostDirective; listboxes read term + matchFn reactively.';
  protected readonly _exSectionTitle: string = 'Command palette';
  protected readonly _exSubtitle: string = 'Type to filter; the listbox shows only matching options. <code>hasSearchResults</code> drives the empty state.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'molecule' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'a11y' }, { dim: 'artifact', value: 'building-block' }, { dim: 'focus', value: 'composition' }, { dim: 'focus', value: 'a11y-pattern' }, { dim: 'focus', value: 'behavior' }];
  protected readonly _exTs: string = `import { CngxListbox, CngxListboxSearch, CngxOption } from '@cngx/common/interactive';

protected readonly commands = signal<Array<{ value: string; label: string }>>([
  { value: 'open-file', label: 'Open file…' },
  { value: 'open-folder', label: 'Open folder…' },
  { value: 'save', label: 'Save' },
  { value: 'save-as', label: 'Save as…' },
  { value: 'close', label: 'Close' },
  { value: 'reload', label: 'Reload' },
]);
protected readonly lastSelected = signal<string | null>(null);`;
  protected readonly _exHtml: string = `<input
  cngxListboxSearch
  class="palette-search"
  type="search"
  placeholder="Search commands…"
  aria-label="Search commands"
  #search="cngxListboxSearch"
/>
<div
  cngxListbox
  [label]="'Palette'"
  [cngxSearchRef]="search"
  class="ad-listbox"
  tabindex="0"
  (valueChange)="lastSelected.set($any($event))"
  #palette="cngxListbox"
>
  @let term = palette.searchTerm().toLowerCase();
  @for (cmd of commands(); track cmd.value) {
    @if (!term || cmd.label.toLowerCase().includes(term)) {
      <div cngxOption [value]="cmd.value" [label]="cmd.label">{{ cmd.label }}</div>
    }
  }
</div>
@if (palette.options().length === 0) {
  <p class="empty">No matching commands.</p>
}`;
  protected readonly commands = signal<Array<{ value: string; label: string }>>([
      { value: 'open-file', label: 'Open file…' },
      { value: 'open-folder', label: 'Open folder…' },
      { value: 'save', label: 'Save' },
      { value: 'save-as', label: 'Save as…' },
      { value: 'close', label: 'Close' },
      { value: 'reload', label: 'Reload' },
    ]);
    protected readonly lastSelected = signal<string | null>(null);
}
