// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/disclosure-demo/disclosure-demo.story.ts#section[2] "Controlled Mode"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxDisclosure } from '@cngx/common';

@Component({
  selector: 'app-disclosure-controlled-mode',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxDisclosure],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0 || _exUses.length > 0) {
        <div class="cngx-ex-meta">
          @if (_exTags.length > 0) {
            <ul class="cngx-ex-tags" aria-label="Tags">
              @for (t of _exTags; track t.dim + ':' + t.value) {
                <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
              }
            </ul>
          }
          @if (_exUses.length > 0) {
            <p class="cngx-ex-uses"><span class="cngx-ex-uses__label">uses</span>
              @for (u of _exUses; track u; let last = $last) {
                <code>{{ u }}</code>@if (!last) {<span class="cngx-ex-uses__sep">, </span>}
              }
            </p>
          }
        </div>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="button-row">
        <button class="sort-btn" (click)="controlledOpen.set(!controlledOpen())">
          External: {{ controlledOpen() ? 'open' : 'closed' }}
        </button>
      </div>
      <button cngxDisclosure #cd="cngxDisclosure"
              [cngxDisclosureOpened]="controlledOpen()"
              (openedChange)="controlledOpen.set($event)"
              [controls]="'ctrl-content'"
              class="sort-btn" style="margin-top: 0.5rem;">
        Trigger: {{ cd.opened() ? 'expanded' : 'collapsed' }}
      </button>
      @if (cd.opened()) {
        <div id="ctrl-content" style="padding: 0.75rem; margin-top: 0.5rem; background: var(--cngx-surface-alt, #f9fafb); border-radius: 4px; font-size: 0.875rem;">
          Controlled content — state owned by parent signal.
        </div>
      }
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class DisclosureControlledMode {
  protected readonly _exTitle: string = 'Disclosure';
  protected readonly _exDescription: string = 'Generic expand/collapse atom. Manages aria-expanded, keyboard interaction (Enter, Space, click), and controlled+uncontrolled state. Usable for accordions, FAQs, nav groups, collapsible panels.';
  protected readonly _exSectionTitle: string = 'Controlled Mode';
  protected readonly _exSubtitle: string = 'Bind <code>[cngxDisclosureOpened]</code> to a signal for external state control.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [];
  protected readonly _exUses: readonly string[] = ['CngxDisclosure'];
  protected readonly _exTs: string = `import { CngxDisclosure } from '@cngx/common';

protected readonly controlledOpen = signal(false);`;
  protected readonly _exHtml: string = `<div class="button-row">
  <button class="sort-btn" (click)="controlledOpen.set(!controlledOpen())">
    External: {{ controlledOpen() ? 'open' : 'closed' }}
  </button>
</div>
<button cngxDisclosure #cd="cngxDisclosure"
        [cngxDisclosureOpened]="controlledOpen()"
        (openedChange)="controlledOpen.set($event)"
        [controls]="'ctrl-content'"
        class="sort-btn" style="margin-top: 0.5rem;">
  Trigger: {{ cd.opened() ? 'expanded' : 'collapsed' }}
</button>
@if (cd.opened()) {
  <div id="ctrl-content" style="padding: 0.75rem; margin-top: 0.5rem; background: var(--cngx-surface-alt, #f9fafb); border-radius: 4px; font-size: 0.875rem;">
    Controlled content — state owned by parent signal.
  </div>
}`;
  protected readonly controlledOpen = signal(false);
}
