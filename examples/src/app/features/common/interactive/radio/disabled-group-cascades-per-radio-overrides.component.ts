// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/radio-demo/radio-demo.story.ts#section[2] "Disabled — group cascades, per-radio overrides"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxRadioGroup, CngxRadio } from '@cngx/common/interactive';

@Component({
  selector: 'app-radio-disabled-group-cascades-per-radio-overrides',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxRadioGroup, CngxRadio],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <button type="button" (click)="groupDisabled.set(!groupDisabled())" class="sort-btn">
        {{ groupDisabled() ? 'Enable group' : 'Disable group' }}
      </button>
      <cngx-radio-group [(value)]="payment" [disabled]="groupDisabled()" name="payment-method-d">
        <cngx-radio value="card">Card</cngx-radio>
        <cngx-radio value="cash">Cash (per-radio disabled)</cngx-radio>
        <cngx-radio value="invoice" [disabled]="true">Invoice (per-radio disabled)</cngx-radio>
      </cngx-radio-group>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class RadioDisabledGroupCascadesPerRadioOverrides {
  protected readonly _exTitle: string = 'Radio group';
  protected readonly _exDescription: string = 'Single-select radio-group molecule and its CngxRadio leaves. Group provides CNGX_RADIO_GROUP for parent-child contract (never injects the concrete class) and CngxRovingTabindex as host directive for arrow-key focus movement. Auto-select-on-arrow is wired via a transient pendingArrowSelect flag — Tab-into-group does NOT auto-select; only an arrow keydown followed by focus moves the value.';
  protected readonly _exSectionTitle: string = 'Disabled — group cascades, per-radio overrides';
  protected readonly _exSubtitle: string = 'Group <code>[disabled]</code> cascades to every leaf via <code>radioDisabled = computed(() => group.disabled() || disabled())</code>. Per-radio <code>[disabled]</code> blocks only that leaf and is skipped by roving navigation.';
  protected readonly _exTs: string = `import { CngxRadioGroup, CngxRadio } from '@cngx/common/interactive';

protected readonly payment = signal<'card' | 'cash' | 'invoice' | undefined>(undefined);
protected readonly orientation = signal<'horizontal' | 'vertical'>('vertical');
protected readonly groupDisabled = signal(false);`;
  protected readonly _exHtml: string = `<button type="button" (click)="groupDisabled.set(!groupDisabled())" class="sort-btn">
  {{ groupDisabled() ? 'Enable group' : 'Disable group' }}
</button>
<cngx-radio-group [(value)]="payment" [disabled]="groupDisabled()" name="payment-method-d">
  <cngx-radio value="card">Card</cngx-radio>
  <cngx-radio value="cash">Cash (per-radio disabled)</cngx-radio>
  <cngx-radio value="invoice" [disabled]="true">Invoice (per-radio disabled)</cngx-radio>
</cngx-radio-group>`;
  protected readonly payment = signal<'card' | 'cash' | 'invoice' | undefined>(undefined);
    protected readonly orientation = signal<'horizontal' | 'vertical'>('vertical');
    protected readonly groupDisabled = signal(false);
}
