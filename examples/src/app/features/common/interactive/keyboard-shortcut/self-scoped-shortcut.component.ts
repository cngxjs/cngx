// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/keyboard-shortcut-demo/keyboard-shortcut-demo.story.ts#section[1] "Self-Scoped Shortcut"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxKeyboardShortcut } from '@cngx/common/interactive';

@Component({
  selector: 'app-keyboard-shortcut-self-scoped-shortcut',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxKeyboardShortcut],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div [cngxKeyboardShortcut]="'escape'" [shortcutScope]="'self'"
           (shortcutTriggered)="handleEscape()"
           tabindex="0"
           style="padding:16px;border:2px dashed var(--cngx-color-border,#ddd);border-radius:8px;
                  max-width:300px;cursor:pointer;outline:none"
           [style.borderColor]="escapeCount() > 0 ? 'var(--interactive,#f5a623)' : ''">
        <p style="margin:0;font-size:0.875rem">Click me, then press <kbd style="padding:2px 6px;background:var(--code-bg,#f5f5f5);border:1px solid var(--code-border,#ddd);border-radius:3px;font-size:0.8rem">Escape</kbd></p>
      </div>
      <div class="event-grid" style="margin-top:12px">
        <div class="event-row">
          <span class="event-label">Escape triggers</span>
          <span class="event-value">{{ escapeCount() }}</span>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class KeyboardShortcutSelfScopedShortcut {
  protected readonly _exTitle: string = 'Keyboard Shortcut';
  protected readonly _exDescription: string = 'Declarative keyboard shortcut handler. Supports mod (Meta/Ctrl), global/self scope, and input element filtering.';
  protected readonly _exSectionTitle: string = 'Self-Scoped Shortcut';
  protected readonly _exSubtitle: string = 'Escape only fires when this box has focus. Click it first, then press Escape.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [];
  protected readonly _exTs: string = `import { CngxKeyboardShortcut } from '@cngx/common/interactive';

protected globalCount = signal(0);
protected escapeCount = signal(0);
protected lastShortcut = signal('none');

protected handleGlobal(): void {
  this.globalCount.update(n => n + 1);
  this.lastShortcut.set('Ctrl+K / Cmd+K');
}

protected handleEscape(): void {
  this.escapeCount.update(n => n + 1);
  this.lastShortcut.set('Escape (scoped)');
}`;
  protected readonly _exHtml: string = `<div [cngxKeyboardShortcut]="'escape'" [shortcutScope]="'self'"
     (shortcutTriggered)="handleEscape()"
     tabindex="0"
     style="padding:16px;border:2px dashed var(--cngx-color-border,#ddd);border-radius:8px;
            max-width:300px;cursor:pointer;outline:none"
     [style.borderColor]="escapeCount() > 0 ? 'var(--interactive,#f5a623)' : ''">
  <p style="margin:0;font-size:0.875rem">Click me, then press <kbd style="padding:2px 6px;background:var(--code-bg,#f5f5f5);border:1px solid var(--code-border,#ddd);border-radius:3px;font-size:0.8rem">Escape</kbd></p>
</div>`;
  protected globalCount = signal(0);
    protected escapeCount = signal(0);
    protected lastShortcut = signal('none');

    protected handleGlobal(): void {
      this.globalCount.update(n => n + 1);
      this.lastShortcut.set('Ctrl+K / Cmd+K');
    }

    protected handleEscape(): void {
      this.escapeCount.update(n => n + 1);
      this.lastShortcut.set('Escape (scoped)');
    }
}
