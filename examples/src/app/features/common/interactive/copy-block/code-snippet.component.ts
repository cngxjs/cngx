// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/copy-block-demo/copy-block-demo.story.ts#section[0] "Code Snippet"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxCopyBlock } from '@cngx/common/interactive';

@Component({
  selector: 'app-copy-block-code-snippet',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxCopyBlock],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <cngx-copy-block [value]="installCmd">
        {{ installCmd }}
      </cngx-copy-block>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class CopyBlockCodeSnippet {
  protected readonly _exTitle: string = 'Copy Block';
  protected readonly _exDescription: string = 'Molecule: code/text block with built-in copy button and SR announcement.';
  protected readonly _exSectionTitle: string = 'Code Snippet';
  protected readonly _exSubtitle: string = 'Click "Copy" to copy the install command. The button shows "Copied!" for 2 seconds.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'molecule' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'design' }, { dim: 'artifact', value: 'standalone' }, { dim: 'focus', value: 'visual-variants' }, { dim: 'focus', value: 'behavior' }];
  protected readonly _exTs: string = `import { CngxCopyBlock } from '@cngx/common/interactive';

protected readonly installCmd = 'npm install @cngx/common';
protected readonly apiKey = signal('sk_test_EXAMPLE_KEY_1234567890');`;
  protected readonly _exHtml: string = `<cngx-copy-block [value]="installCmd">
  {{ installCmd }}
</cngx-copy-block>`;
  protected readonly installCmd = 'npm install @cngx/common';
    protected readonly apiKey = signal('sk_test_EXAMPLE_KEY_1234567890');
}
