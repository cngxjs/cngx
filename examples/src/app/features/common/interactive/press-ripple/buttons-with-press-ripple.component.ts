// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/press-ripple-demo/press-ripple-demo.story.ts#section[0] "Buttons with Press + Ripple"

import { ChangeDetectionStrategy, Component } from '@angular/core';
import { CngxPressRipple } from '@cngx/common/interactive';

@Component({
  selector: 'app-press-ripple-buttons-with-press-ripple',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxPressRipple],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="button-row" style="gap:12px">
        <button cngxPressRipple class="chip"
                style="transition:transform 80ms ease"
                [class.cngx-pressed]="">
          Default
        </button>

        <button cngxPressRipple [rippleColor]="'#f5a623'" class="chip"
                style="transition:transform 80ms ease">
          Amber Ripple
        </button>

        <button cngxPressRipple [rippleCentered]="true" class="chip"
                style="width:48px;height:48px;border-radius:50%;padding:0;transition:transform 80ms ease">
          C
        </button>
      </div>

      <p style="margin-top:12px;font-size:0.8125rem;color:var(--cngx-text-secondary,#666)">
        One directive, two behaviors. The molecule composes <code>CngxPressable</code> + <code>CngxRipple</code> via hostDirectives.
      </p>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class PressRippleButtonsWithPressRipple {
  protected readonly _exTitle: string = 'Press + Ripple';
  protected readonly _exDescription: string = 'Molecule combining CngxPressable + CngxRipple in one directive. Press feedback + ripple wave with a single attribute.';
  protected readonly _exSectionTitle: string = 'Buttons with Press + Ripple';
  protected readonly _exSubtitle: string = 'Click or tap each button. You get both instant press feedback (scale via <code>.cngx-pressed</code>) and a ripple wave expanding from the pointer position.';
  protected readonly _exTs: string = `import { CngxPressRipple } from '@cngx/common/interactive';`;
  protected readonly _exHtml: string = `<div class="button-row" style="gap:12px">
  <button cngxPressRipple class="chip"
          style="transition:transform 80ms ease"
          [class.cngx-pressed]="">
    Default
  </button>

  <button cngxPressRipple [rippleColor]="'#f5a623'" class="chip"
          style="transition:transform 80ms ease">
    Amber Ripple
  </button>

  <button cngxPressRipple [rippleCentered]="true" class="chip"
          style="width:48px;height:48px;border-radius:50%;padding:0;transition:transform 80ms ease">
    C
  </button>
</div>

<p style="margin-top:12px;font-size:0.8125rem;color:var(--cngx-text-secondary,#666)">
  One directive, two behaviors. The molecule composes <code>CngxPressable</code> + <code>CngxRipple</code> via hostDirectives.
</p>`;
}
