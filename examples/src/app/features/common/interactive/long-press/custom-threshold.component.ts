// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: examples/stories/common/interactive/long-press-demo/long-press-demo.story.ts#section[1] "Custom Threshold"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxLongPress } from '@cngx/common/interactive';

@Component({
  selector: 'app-long-press-custom-threshold',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxLongPress],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <button cngxLongPress [threshold]="1000" #lp2="cngxLongPress"
              (longPressed)="pressCount.update(n => n + 1)"
              class="chip"
              [style.background]="lp2.longPressing() ? '#ffebee' : ''"
              [style.borderColor]="lp2.longPressing() ? '#c62828' : ''">
        {{ lp2.longPressing() ? 'Hold 1s to delete...' : 'Long press to delete' }}
      </button>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class LongPressCustomThreshold {
  protected readonly _exTitle: string = 'Long Press';
  protected readonly _exDescription: string = 'Detects long-press gestures via Pointer Events. Cancels on move to prevent accidental triggers during scrolling.';
  protected readonly _exSectionTitle: string = 'Custom Threshold';
  protected readonly _exSubtitle: string = 'Set <code>[threshold]="1000"</code> for a 1-second hold. Useful for destructive actions.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'atom' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'a11y' }, { dim: 'artifact', value: 'building-block' }, { dim: 'focus', value: 'behavior' }];
  protected readonly _exTs: string = `import { CngxLongPress } from '@cngx/common/interactive';

protected pressCount = signal(0);`;
  protected readonly _exHtml: string = `<button cngxLongPress [threshold]="1000" #lp2="cngxLongPress"
        (longPressed)="pressCount.update(n => n + 1)"
        class="chip"
        [style.background]="lp2.longPressing() ? '#ffebee' : ''"
        [style.borderColor]="lp2.longPressing() ? '#c62828' : ''">
  {{ lp2.longPressing() ? 'Hold 1s to delete...' : 'Long press to delete' }}
</button>`;
  protected pressCount = signal(0);
    protected lastEvent = signal<string>('none');

    protected handleLongPress(event: PointerEvent): void {
      this.pressCount.update(n => n + 1);
      this.lastEvent.set(`(${Math.round(event.clientX)}, ${Math.round(event.clientY)})`);
    }
}
