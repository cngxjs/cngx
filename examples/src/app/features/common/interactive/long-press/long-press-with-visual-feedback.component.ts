// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/long-press-demo/long-press-demo.story.ts#section[0] "Long Press with Visual Feedback"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxLongPress } from '@cngx/common/interactive';

@Component({
  selector: 'app-long-press-long-press-with-visual-feedback',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxLongPress],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div cngxLongPress (longPressed)="handleLongPress($event)"
           #lp="cngxLongPress"
           style="display:inline-flex;align-items:center;justify-content:center;width:160px;height:80px;
                  border:2px solid var(--cngx-border,#ddd);border-radius:12px;cursor:pointer;user-select:none;
                  transition:border-color 150ms,background 150ms"
           [style.borderColor]="lp.longPressing() ? 'var(--interactive,#f5a623)' : ''"
           [style.background]="lp.longPressing() ? 'var(--interactive-subtle-bg,#fff8e1)' : ''">
        {{ lp.longPressing() ? 'Hold...' : 'Long press me' }}
      </div>

      <div class="event-grid" style="margin-top:12px">
        <div class="event-row">
          <span class="event-label">Long pressing</span>
          <span class="event-value">{{ lp.longPressing() }}</span>
        </div>
        <div class="event-row">
          <span class="event-label">Completed</span>
          <span class="event-value">{{ pressCount() }} times</span>
        </div>
        <div class="event-row">
          <span class="event-label">Last position</span>
          <span class="event-value">{{ lastEvent() }}</span>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class LongPressLongPressWithVisualFeedback {
  protected readonly _exTitle: string = 'Long Press';
  protected readonly _exDescription: string = 'Detects long-press gestures via Pointer Events. Cancels on move to prevent accidental triggers during scrolling.';
  protected readonly _exSectionTitle: string = 'Long Press with Visual Feedback';
  protected readonly _exSubtitle: string = 'Hold for 500ms (default). The holding state is shown via <code>longPressing()</code> signal. Moving >10px cancels.';
  protected readonly _exTs: string = `import { CngxLongPress } from '@cngx/common/interactive';

protected pressCount = signal(0);
protected lastEvent = signal<string>('none');

protected handleLongPress(event: PointerEvent): void {
  this.pressCount.update(n => n + 1);
  this.lastEvent.set(\`(\${Math.round(event.clientX)}, \${Math.round(event.clientY)})\`);
}`;
  protected readonly _exHtml: string = `<div cngxLongPress (longPressed)="handleLongPress($event)"
     #lp="cngxLongPress"
     style="display:inline-flex;align-items:center;justify-content:center;width:160px;height:80px;
            border:2px solid var(--cngx-border,#ddd);border-radius:12px;cursor:pointer;user-select:none;
            transition:border-color 150ms,background 150ms"
     [style.borderColor]="lp.longPressing() ? 'var(--interactive,#f5a623)' : ''"
     [style.background]="lp.longPressing() ? 'var(--interactive-subtle-bg,#fff8e1)' : ''">
  {{ lp.longPressing() ? 'Hold...' : 'Long press me' }}
</div>`;
  protected pressCount = signal(0);
    protected lastEvent = signal<string>('none');

    protected handleLongPress(event: PointerEvent): void {
      this.pressCount.update(n => n + 1);
      this.lastEvent.set(`(${Math.round(event.clientX)}, ${Math.round(event.clientY)})`);
    }
}
