// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: examples/stories/common/interactive/retry-demo/retry-demo.story.ts#section[1] "optimistic() — Instant Like Toggle"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { withRetry, optimistic } from '@cngx/common/interactive';
import { of, switchMap, throwError, timer } from 'rxjs';

@Component({
  selector: 'app-retry-optimistic-instant-like-toggle',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="button-row">
        <button (click)="toggleLike(!liked())" class="chip"
                [style.background]="liked() ? '#fce4ec' : ''"
                [style.borderColor]="liked() ? '#e91e63' : ''">
          {{ liked() ? 'Liked' : 'Like' }}
        </button>
      </div>
      <div class="event-grid" style="margin-top:12px">
        <div class="event-row">
          <span class="event-label">liked</span>
          <span class="event-value">{{ liked() }}</span>
        </div>
        <div class="event-row">
          <span class="event-label">Rolled back</span>
          <span class="event-value">{{ likeState.rolledBack() }}</span>
        </div>
        @if (likeState.error()) {
          <div class="event-row">
            <span class="event-label">Error</span>
            <span class="event-value" style="color:var(--cngx-color-danger)">{{ likeState.error() }}</span>
          </div>
        }
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class RetryOptimisticInstantLikeToggle {
  protected readonly _exTitle: string = 'Retry + Optimistic';
  protected readonly _exDescription: string = 'withRetry() wraps AsyncAction with automatic retry. optimistic() updates signals immediately with rollback on error.';
  protected readonly _exSectionTitle: string = 'optimistic() — Instant Like Toggle';
  protected readonly _exSubtitle: string = 'Click to toggle. The state updates instantly (optimistic). If the server rejects (30% chance), it rolls back.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'atom' }, { dim: 'audience', value: 'dev' }, { dim: 'artifact', value: 'building-block' }, { dim: 'focus', value: 'async-state' }, { dim: 'focus', value: 'error-handling' }, { dim: 'focus', value: 'behavior' }];
  protected readonly _exTs: string = `import { withRetry, optimistic } from '@cngx/common/interactive';
import { of, switchMap, throwError, timer } from 'rxjs';

protected readonly liked = signal(false);
private readonly likeResult = optimistic(this.liked, (value: boolean) =>
  timer(1000).pipe(
    switchMap(() => Math.random() > 0.3
      ? of(value)
      : throwError(() => new Error('Server rejected'))),
  ),
);
protected readonly toggleLike = this.likeResult[0];
protected readonly likeState = this.likeResult[1];`;
  protected readonly _exHtml: string = `<div class="button-row">
  <button (click)="toggleLike(!liked())" class="chip"
          [style.background]="liked() ? '#fce4ec' : ''"
          [style.borderColor]="liked() ? '#e91e63' : ''">
    {{ liked() ? 'Liked' : 'Like' }}
  </button>
</div>`;
  // Flaky action — fails 60% of the time
    private readonly flakyAction = () => new Promise<void>((resolve, reject) =>
      setTimeout(() => Math.random() > 0.6 ? resolve() : reject(new Error('Network error')), 500),
    );

    private readonly retryResult = withRetry(this.flakyAction, {
      maxAttempts: 3,
      delay: 800,
      backoff: 'exponential',
    });
    protected readonly retryAction = this.retryResult[0];
    protected readonly retryState = this.retryResult[1];

    // Optimistic toggle
    protected readonly liked = signal(false);
    private readonly likeResult = optimistic(this.liked, (value: boolean) =>
      timer(1000).pipe(
        switchMap(() => Math.random() > 0.3
          ? of(value)
          : throwError(() => new Error('Server rejected'))),
      ),
    );
    protected readonly toggleLike = this.likeResult[0];
    protected readonly likeState = this.likeResult[1];
}
