// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/menu-trigger-demo/menu-trigger-demo.story.ts#section[0] "Dropdown menu"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxMenu, CngxMenuItem, CngxMenuTrigger } from '@cngx/common/interactive';
import { CngxPopover, CngxPopoverTrigger } from '@cngx/common/popover';

@Component({
  selector: 'app-menu-trigger-dropdown-menu',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxMenu, CngxMenuItem, CngxMenuTrigger, CngxPopover, CngxPopoverTrigger],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0 || _exUses.length > 0) {
        <div class="cngx-ex-meta">
          @if (_exTags.length > 0) {
            <ul class="cngx-ex-tags" aria-label="Tags">
              @for (t of _exTags; track t.dim + ':' + t.value) {
                <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
              }
            </ul>
          }
          @if (_exUses.length > 0) {
            <p class="cngx-ex-uses"><span class="cngx-ex-uses__label">uses</span>
              @for (u of _exUses; track u; let last = $last) {
                <code>{{ u }}</code>@if (!last) {<span class="cngx-ex-uses__sep">, </span>}
              }
            </p>
          }
        </div>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <style>
        .trigger { min-width: 160px; padding: 8px 12px; border: 1px solid var(--cngx-color-border, #d0d5dd); border-radius: 6px; background: var(--cngx-color-surface, #fff); color: var(--cngx-color-text, inherit); cursor: pointer; font: inherit; }
        .trigger:focus-visible { outline: 2px solid var(--cngx-color-primary, #4a8cff); outline-offset: 2px; }
        .pop { margin-top: 4px; min-width: 180px; border: 1px solid var(--cngx-color-border, #d0d5dd); border-radius: 6px; background: var(--cngx-color-surface, #fff); color: var(--cngx-color-text, inherit); box-shadow: 0 4px 12px oklch(0 0 0 / 0.12); }
      </style>
      <button
        type="button"
        class="trigger"
        [cngxMenuTrigger]="menu"
        [cngxPopoverTrigger]="pop"
        [haspopup]="'menu'"
        [popover]="pop"
        (click)="pop.toggle()"
        #trigger="cngxMenuTrigger"
      >
        Actions
      </button>
      <div cngxPopover #pop="cngxPopover" class="pop">
        <ul
          cngxMenu
          class="menu"
          [label]="'File actions'"
          tabindex="0"
          (itemActivated)="lastAction.set($any($event))"
          #menu="cngxMenu"
        >
          <li cngxMenuItem value="new">New</li>
          <li cngxMenuItem value="open">Open…</li>
          <li cngxMenuItem value="save">Save</li>
          <li cngxMenuItem value="close">Close</li>
        </ul>
      </div>
      <div class="event-grid" style="margin-top:12px">
        <div class="event-row">
          <span class="event-label">Last action</span>
          <span class="event-value">{{ lastAction() ?? '—' }}</span>
        </div>
        <div class="event-row">
          <span class="event-label">Open</span>
          <span class="event-value">{{ trigger.isOpen() ? 'yes' : 'no' }}</span>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class MenuTriggerDropdownMenu {
  protected readonly _exTitle: string = 'Menu Trigger';
  protected readonly _exDescription: string = 'Button that opens a CngxMenu inside a CngxPopover. Full keyboard support; activation always closes the menu (menu semantics).';
  protected readonly _exSectionTitle: string = 'Dropdown menu';
  protected readonly _exSubtitle: string = 'Press ArrowDown/Enter/Space to open, Escape to close. Arrow keys navigate, typeahead jumps to matching labels.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [];
  protected readonly _exUses: readonly string[] = ['CngxMenuTrigger', 'CngxMenu', 'CngxMenuItem'];
  protected readonly _exTs: string = `import { CngxMenu, CngxMenuItem, CngxMenuTrigger } from '@cngx/common/interactive';
import { CngxPopover, CngxPopoverTrigger } from '@cngx/common/popover';

protected readonly lastAction = signal<string | null>(null);`;
  protected readonly _exHtml: string = `<style>
  .trigger { min-width: 160px; padding: 8px 12px; border: 1px solid var(--cngx-color-border, #d0d5dd); border-radius: 6px; background: var(--cngx-color-surface, #fff); color: var(--cngx-color-text, inherit); cursor: pointer; font: inherit; }
  .trigger:focus-visible { outline: 2px solid var(--cngx-color-primary, #4a8cff); outline-offset: 2px; }
  .pop { margin-top: 4px; min-width: 180px; border: 1px solid var(--cngx-color-border, #d0d5dd); border-radius: 6px; background: var(--cngx-color-surface, #fff); color: var(--cngx-color-text, inherit); box-shadow: 0 4px 12px oklch(0 0 0 / 0.12); }
</style>
<button
  type="button"
  class="trigger"
  [cngxMenuTrigger]="menu"
  [cngxPopoverTrigger]="pop"
  [haspopup]="'menu'"
  [popover]="pop"
  (click)="pop.toggle()"
  #trigger="cngxMenuTrigger"
>
  Actions
</button>
<div cngxPopover #pop="cngxPopover" class="pop">
  <ul
    cngxMenu
    class="menu"
    [label]="'File actions'"
    tabindex="0"
    (itemActivated)="lastAction.set($any($event))"
    #menu="cngxMenu"
  >
    <li cngxMenuItem value="new">New</li>
    <li cngxMenuItem value="open">Open…</li>
    <li cngxMenuItem value="save">Save</li>
    <li cngxMenuItem value="close">Close</li>
  </ul>
</div>`;
  protected readonly lastAction = signal<string | null>(null);
}
