// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/menu-checkable-demo/menu-checkable-demo.story.ts#section[0] "Text formatting menu"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxMenu, CngxMenuGroup, CngxMenuItemCheckbox, CngxMenuItemRadio } from '@cngx/common/interactive';

@Component({
  selector: 'app-menu-checkable-text-formatting-menu',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxMenu, CngxMenuGroup, CngxMenuItemCheckbox, CngxMenuItemRadio],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <ul cngxMenu [label]="'Text formatting'" class="menu" tabindex="0">
        <li cngxMenuItemCheckbox value="bold" [(checked)]="bold">
          Bold
        </li>
        <li cngxMenuItemCheckbox value="italic" [(checked)]="italic">
          Italic
        </li>
        <li cngxMenuItemCheckbox value="underline" [(checked)]="underline">
          Underline
        </li>
        <li role="separator" class="sep"></li>
        <div cngxMenuGroup [label]="'Alignment'" name="alignment">
          <li cngxMenuItemRadio value="left">Left</li>
          <li cngxMenuItemRadio value="center">Center</li>
          <li cngxMenuItemRadio value="right">Right</li>
        </div>
      </ul>
      <div class="event-grid" style="margin-top:12px">
        <div class="event-row">
          <span class="event-label">Bold</span>
          <span class="event-value">{{ bold() ? 'on' : 'off' }}</span>
        </div>
        <div class="event-row">
          <span class="event-label">Italic</span>
          <span class="event-value">{{ italic() ? 'on' : 'off' }}</span>
        </div>
        <div class="event-row">
          <span class="event-label">Underline</span>
          <span class="event-value">{{ underline() ? 'on' : 'off' }}</span>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class MenuCheckableTextFormattingMenu {
  protected readonly _exTitle: string = 'Checkable Menu Items';
  protected readonly _exDescription: string = 'Menu items with their own state: checkboxes for toggles, radios for mutually exclusive choices inside a CngxMenuGroup.';
  protected readonly _exSectionTitle: string = 'Text formatting menu';
  protected readonly _exSubtitle: string = 'Checkboxes for independent toggles, radios inside a group for exclusive selection.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'molecule' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'a11y' }, { dim: 'artifact', value: 'building-block' }, { dim: 'focus', value: 'composition' }, { dim: 'focus', value: 'a11y-pattern' }];
  protected readonly _exTs: string = `import { CngxMenu, CngxMenuGroup, CngxMenuItemCheckbox, CngxMenuItemRadio } from '@cngx/common/interactive';

protected readonly bold = signal(true);
protected readonly italic = signal(false);
protected readonly underline = signal(false);`;
  protected readonly _exHtml: string = `<ul cngxMenu [label]="'Text formatting'" class="menu" tabindex="0">
  <li cngxMenuItemCheckbox value="bold" [(checked)]="bold">
    Bold
  </li>
  <li cngxMenuItemCheckbox value="italic" [(checked)]="italic">
    Italic
  </li>
  <li cngxMenuItemCheckbox value="underline" [(checked)]="underline">
    Underline
  </li>
  <li role="separator" class="sep"></li>
  <div cngxMenuGroup [label]="'Alignment'" name="alignment">
    <li cngxMenuItemRadio value="left">Left</li>
    <li cngxMenuItemRadio value="center">Center</li>
    <li cngxMenuItemRadio value="right">Right</li>
  </div>
</ul>`;
  protected readonly bold = signal(true);
    protected readonly italic = signal(false);
    protected readonly underline = signal(false);
}
