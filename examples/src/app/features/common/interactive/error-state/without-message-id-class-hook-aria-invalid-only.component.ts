// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/error-state-demo/error-state-demo.story.ts#section[1] "Without message id — class hook + aria-invalid only"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxErrorState } from '@cngx/common/interactive';

@Component({
  selector: 'app-error-state-without-message-id-class-hook-aria-invalid-only',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxErrorState],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <label style="display: block; margin-bottom: 6px; font-weight: 600;">
        <span>Password</span>
        <input
          #passwordInput
          type="password"
          [cngxErrorState]="passwordInvalid()"
          [style.border]="passwordInvalid() ? '1px solid #b00020' : '1px solid #d1d5db'"
          [style.outline-color]="passwordInvalid() ? '#b00020' : 'transparent'"
          style="padding: 6px 8px; min-width: 240px; border-radius: 4px; display: block; margin-top: 4px;"
        />
      </label>

      <pre style="margin: 8px 0 12px; padding: 8px; background: #f3f4f6; border-radius: 4px; font-size: 0.85em;">aria-invalid      : "{{ passwordInput.getAttribute('aria-invalid') }}"
    aria-errormessage : {{ passwordInput.getAttribute('aria-errormessage') ?? 'null (no id bound)' }}
    class.cngx-error  : {{ passwordInput.classList.contains('cngx-error') }}</pre>

      <button type="button" (click)="passwordInvalid.set(!passwordInvalid())">
        Toggle invalid state
      </button>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class ErrorStateWithoutMessageIdClassHookAriaInvalidOnly {
  protected readonly _exTitle: string = 'Error State';
  protected readonly _exDescription: string = '<code>[cngxErrorState]</code> is a generic host-element marker. Toggles <code>.cngx-error</code> + reactive <code>aria-invalid</code> + <code>aria-errormessage</code> on any DOM element — cngx, Material, CDK, native, or third-party host. Works without an aggregator; pair with <code>cngxErrorScope</code> when you need reveal-on-submit semantics. Both ARIA attributes stay in the DOM with explicit values per cngx convention; visibility of the linked message is the consumer\'s <code>aria-hidden</code> to wire.';
  protected readonly _exSectionTitle: string = 'Without message id — class hook + aria-invalid only';
  protected readonly _exSubtitle: string = '<code>cngxErrorMessageId</code> is optional. Skip it when the error message is rendered elsewhere (e.g. a summary panel) or when only the visual cue is needed. <code>aria-invalid</code> still flips between <code>"false"</code> and <code>"true"</code>; <code>aria-errormessage</code> simply never appears.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'atom' }, { dim: 'audience', value: 'dev' }, { dim: 'artifact', value: 'building-block' }, { dim: 'focus', value: 'error-handling' }];
  protected readonly _exTs: string = `import { CngxErrorState } from '@cngx/common/interactive';

protected readonly passwordInvalid = signal(false);`;
  protected readonly _exHtml: string = `  <label style="display: block; margin-bottom: 6px; font-weight: 600;">
    <span>Password</span>
    <input
      #passwordInput
      type="password"
      [cngxErrorState]="passwordInvalid()"
      [style.border]="passwordInvalid() ? '1px solid #b00020' : '1px solid #d1d5db'"
      [style.outline-color]="passwordInvalid() ? '#b00020' : 'transparent'"
      style="padding: 6px 8px; min-width: 240px; border-radius: 4px; display: block; margin-top: 4px;"
    />
  </label>

  <pre style="margin: 8px 0 12px; padding: 8px; background: #f3f4f6; border-radius: 4px; font-size: 0.85em;">aria-invalid      : "{{ passwordInput.getAttribute('aria-invalid') }}"
aria-errormessage : {{ passwordInput.getAttribute('aria-errormessage') ?? 'null (no id bound)' }}
class.cngx-error  : {{ passwordInput.classList.contains('cngx-error') }}</pre>

  <button type="button" (click)="passwordInvalid.set(!passwordInvalid())">
    Toggle invalid state
  </button>`;
  protected readonly emailInvalid = signal(false);
    protected readonly passwordInvalid = signal(false);
}
