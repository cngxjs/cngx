// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/click-outside-demo/click-outside-demo.story.ts#section[0] "CngxClickOutside — Dropdown"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxClickOutside } from '@cngx/common/interactive';

@Component({
  selector: 'app-click-outside-dropdown',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxClickOutside],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="button-row">
        <button class="sort-btn" (click)="open.set(!open())">
          Toggle dropdown ({{ open() ? 'open' : 'closed' }})
        </button>
      </div>

      @if (open()) {
        <div
          cngxClickOutside
          (clickOutside)="open.set(false)"
          style="
            display: inline-block;
            padding: 12px 16px;
            border: 1px solid var(--cngx-color-border, #ddd);
            border-radius: 6px;
            background: var(--cngx-surface-alt, #f8f9fa);
            margin-top: 8px;
          "
        >
          <p style="margin: 0 0 8px">I close when you click outside me.</p>
          <button class="sort-btn" (click)="$event.stopPropagation()">Inner button (won't close)</button>
        </div>
      }

      <div class="output-badge" style="margin-top:12px">
        Dropdown: <strong>{{ open() ? 'open' : 'closed' }}</strong>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class ClickOutsideDropdown {
  protected readonly _exTitle: string = 'ClickOutside';
  protected readonly _exDescription: string = 'Emits an event when the user interacts outside the host element. Useful for closing dropdowns, tooltips, and overlays.';
  protected readonly _exSectionTitle: string = 'CngxClickOutside — Dropdown';
  protected readonly _exSubtitle: string = '<code>[cngxClickOutside]</code> listens for <code>pointerdown</code> on the document and emits <code>(clickOutside)</code> when the event target is outside the host. Works for both mouse and touch via the Pointer Events API.';
  protected readonly _exTs: string = `import { CngxClickOutside } from '@cngx/common/interactive';

protected open = signal(false);
protected clickCount = signal(0);
protected enabled = signal(true);`;
  protected readonly _exHtml: string = `<div class="button-row">
  <button class="sort-btn" (click)="open.set(!open())">
    Toggle dropdown ({{ open() ? 'open' : 'closed' }})
  </button>
</div>

@if (open()) {
  <div
    cngxClickOutside
    (clickOutside)="open.set(false)"
    style="
      display: inline-block;
      padding: 12px 16px;
      border: 1px solid var(--cngx-color-border, #ddd);
      border-radius: 6px;
      background: var(--cngx-surface-alt, #f8f9fa);
      margin-top: 8px;
    "
  >
    <p style="margin: 0 0 8px">I close when you click outside me.</p>
    <button class="sort-btn" (click)="$event.stopPropagation()">Inner button (won't close)</button>
  </div>
}

<div class="output-badge" style="margin-top:12px">
  Dropdown: <strong>{{ open() ? 'open' : 'closed' }}</strong>
</div>`;
  protected open = signal(false);
    protected clickCount = signal(0);
    protected enabled = signal(true);
}
