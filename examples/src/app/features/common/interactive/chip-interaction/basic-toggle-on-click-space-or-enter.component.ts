// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: examples/stories/common/interactive/chip-interaction-demo/chip-interaction-demo.story.ts#section[0] "Basic — toggle on click, Space, or Enter"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxChipInteraction } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';

@Component({
  selector: 'app-chip-interaction-basic-toggle-on-click-space-or-enter',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxChipInteraction, CngxChip],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <cngx-chip cngxChipInteraction [value]="'favourite'" [(selected)]="favourite">Favourite</cngx-chip>
      <cngx-chip cngxChipInteraction [value]="'featured'" [(selected)]="featured">Featured</cngx-chip>
      <p class="caption">favourite: <code>{{ favourite() }}</code> • featured: <code>{{ featured() }}</code></p>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class ChipInteractionBasicToggleOnClickSpaceOrEnter {
  protected readonly _exTitle: string = 'Chip interaction (standalone)';
  protected readonly _exDescription: string = 'Standalone interactive chip atom — applies <code>[cngxChipInteraction]</code> onto the existing <code>&lt;cngx-chip&gt;</code> display pill and adds <code>role="option"</code> selection semantics with a local-owned <code>selected</code> model. Provides <code>CNGX_CONTROL_VALUE</code> so <code>CngxFormBridge</code> can adapt it to Reactive Forms. Use this when a chip stands alone (filter tag, single suggestion); use <code>[cngxChipInGroup]</code> instead for chips inside a chip-group.';
  protected readonly _exSectionTitle: string = 'Basic — toggle on click, Space, or Enter';
  protected readonly _exSubtitle: string = 'The chip exposes <code>[(selected)]</code> as a two-way model. Click, Space, or Enter flips it; ARIA <code>aria-selected</code> reflects the state reactively.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'molecule' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'design' }, { dim: 'audience', value: 'a11y' }, { dim: 'artifact', value: 'standalone' }, { dim: 'focus', value: 'a11y-pattern' }, { dim: 'focus', value: 'behavior' }];
  protected readonly _exTs: string = `import { CngxChipInteraction } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';

protected readonly favourite = signal(false);
protected readonly featured = signal(true);`;
  protected readonly _exHtml: string = `<cngx-chip cngxChipInteraction [value]="'favourite'" [(selected)]="favourite">Favourite</cngx-chip>
<cngx-chip cngxChipInteraction [value]="'featured'" [(selected)]="featured">Featured</cngx-chip>
<p class="caption">favourite: <code>{{ favourite() }}</code> • featured: <code>{{ featured() }}</code></p>`;
  protected readonly favourite = signal(false);
    protected readonly featured = signal(true);
    protected readonly tagRemoved = signal(0);
    protected readonly locked = signal(false);
}
