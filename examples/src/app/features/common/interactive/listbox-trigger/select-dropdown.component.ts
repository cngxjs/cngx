// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: examples/stories/common/interactive/listbox-trigger-demo/listbox-trigger-demo.story.ts#section[0] "Select dropdown"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxListbox, CngxListboxTrigger, CngxOption } from '@cngx/common/interactive';
import { CngxPopover, CngxPopoverTrigger } from '@cngx/common/popover';

@Component({
  selector: 'app-listbox-trigger-select-dropdown',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxListbox, CngxListboxTrigger, CngxOption, CngxPopover, CngxPopoverTrigger],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <button
        type="button"
        class="trigger"
        [cngxListboxTrigger]="lb"
        [cngxPopoverTrigger]="pop"
        [haspopup]="'listbox'"
        [popover]="pop"
        (click)="pop.toggle()"
        #trigger="cngxListboxTrigger"
      >
        {{ lb.selectedLabel() ?? 'Choose a color' }}
      </button>
      <div cngxPopover #pop="cngxPopover" class="pop">
        <div
          cngxListbox
          class="ad-listbox"
          [label]="'Color'"
          tabindex="0"
          (valueChange)="selectedColor.set($any($event))"
          #lb="cngxListbox"
        >
          <div cngxOption value="red">Red</div>
          <div cngxOption value="green">Green</div>
          <div cngxOption value="blue">Blue</div>
          <div cngxOption value="orange">Orange</div>
          <div cngxOption value="purple">Purple</div>
        </div>
      </div>
      <div class="event-grid" style="margin-top:12px">
        <div class="event-row">
          <span class="event-label">Selected</span>
          <span class="event-value">{{ selectedColor() ?? '—' }}</span>
        </div>
        <div class="event-row">
          <span class="event-label">Open</span>
          <span class="event-value">{{ trigger.isOpen() ? 'yes' : 'no' }}</span>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class ListboxTriggerSelectDropdown {
  protected readonly _exTitle: string = 'Listbox Trigger';
  protected readonly _exDescription: string = 'Dropdown select composed from CngxListboxTrigger + CngxPopover + CngxListbox. Full keyboard model (open/close, navigate, activate, focus return).';
  protected readonly _exSectionTitle: string = 'Select dropdown';
  protected readonly _exSubtitle: string = 'Click or press ArrowDown to open. Enter / click on an option to select and close.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'molecule' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'a11y' }, { dim: 'artifact', value: 'building-block' }, { dim: 'focus', value: 'composition' }, { dim: 'focus', value: 'a11y-pattern' }, { dim: 'focus', value: 'behavior' }];
  protected readonly _exTs: string = `import { CngxListbox, CngxListboxTrigger, CngxOption } from '@cngx/common/interactive';
import { CngxPopover, CngxPopoverTrigger } from '@cngx/common/popover';

protected readonly selectedColor = signal<string | null>(null);`;
  protected readonly _exHtml: string = `<button
  type="button"
  class="trigger"
  [cngxListboxTrigger]="lb"
  [cngxPopoverTrigger]="pop"
  [haspopup]="'listbox'"
  [popover]="pop"
  (click)="pop.toggle()"
  #trigger="cngxListboxTrigger"
>
  {{ lb.selectedLabel() ?? 'Choose a color' }}
</button>
<div cngxPopover #pop="cngxPopover" class="pop">
  <div
    cngxListbox
    class="ad-listbox"
    [label]="'Color'"
    tabindex="0"
    (valueChange)="selectedColor.set($any($event))"
    #lb="cngxListbox"
  >
    <div cngxOption value="red">Red</div>
    <div cngxOption value="green">Green</div>
    <div cngxOption value="blue">Blue</div>
    <div cngxOption value="orange">Orange</div>
    <div cngxOption value="purple">Purple</div>
  </div>
</div>`;
  protected readonly selectedColor = signal<string | null>(null);
}
