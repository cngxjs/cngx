// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/chip-input-demo/chip-input-demo.story.ts#section[1] "Async validation with supersede semantics"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxChipInput } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';

@Component({
  selector: 'app-chip-input-async-validation-with-supersede-semantics',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxChipInput, CngxChip],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="chip-strip">
        @for (token of tokens(); track token) {
          <cngx-chip [removable]="true" (remove)="removeToken(token)">{{ token }}</cngx-chip>
        }
        <input
          cngxChipInput
          placeholder="Min 3 chars; lowercase enforced"
          [existingTokens]="tokens()"
          [validateToken]="validateMinLen"
          (tokenCreated)="addToken($event)"
          (tokenRemoved)="popToken()"
        />
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class ChipInputAsyncValidationWithSupersedeSemantics {
  protected readonly _exTitle: string = 'Chip input (tokenizing)';
  protected readonly _exDescription: string = 'Tokenizing input on a native <code>&lt;input&gt;</code>. Type a value and press Enter (or any configured separator) to emit <code>(tokenCreated)</code>. Backspace at empty input emits <code>(tokenRemoved)</code> — the consumer drops the last chip. Paste with embedded separators emits one token per non-empty fragment. Optional async <code>[validateToken]</code> drives an internal <code>CngxStateful</code> validation slot — bridge directives like <code>&lt;cngx-toast-on /&gt;</code> auto-discover it.';
  protected readonly _exSectionTitle: string = 'Async validation with supersede semantics';
  protected readonly _exSubtitle: string = '<code>[validateToken]</code> runs on every separator-key creation. Concurrent validations supersede via a monotonic id — the last one wins. <code>aria-busy</code> reflects pending state; <code>aria-invalid</code> reflects rejection.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'molecule' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'design' }, { dim: 'audience', value: 'a11y' }, { dim: 'artifact', value: 'standalone' }, { dim: 'focus', value: 'behavior' }, { dim: 'focus', value: 'a11y-pattern' }];
  protected readonly _exTs: string = `import { CngxChipInput } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';

protected readonly tokens = signal<string[]>(['typescript', 'angular']);
protected readonly addToken = (value: string): void => {
  this.tokens.update((curr) => [...curr, value]);
};
protected readonly popToken = (): void => {
  this.tokens.update((curr) => curr.slice(0, -1));
};
protected readonly removeToken = (token: string): void => {
  this.tokens.update((curr) => curr.filter((t) => t !== token));
};
protected readonly validateMinLen = async (value: string): Promise<string> => {
  await new Promise((resolve) => setTimeout(resolve, 250));
  if (value.length < 3) {
    throw new Error('Tokens must be at least 3 characters.');
  }
  return value.toLowerCase();
};`;
  protected readonly _exHtml: string = `<div class="chip-strip">
  @for (token of tokens(); track token) {
    <cngx-chip [removable]="true" (remove)="removeToken(token)">{{ token }}</cngx-chip>
  }
  <input
    cngxChipInput
    placeholder="Min 3 chars; lowercase enforced"
    [existingTokens]="tokens()"
    [validateToken]="validateMinLen"
    (tokenCreated)="addToken($event)"
    (tokenRemoved)="popToken()"
  />
</div>`;
  protected readonly tokens = signal<string[]>(['typescript', 'angular']);
    protected readonly addToken = (value: string): void => {
      this.tokens.update((curr) => [...curr, value]);
    };
    protected readonly popToken = (): void => {
      this.tokens.update((curr) => curr.slice(0, -1));
    };
    protected readonly removeToken = (token: string): void => {
      this.tokens.update((curr) => curr.filter((t) => t !== token));
    };
    protected readonly validateMinLen = async (value: string): Promise<string> => {
      await new Promise((resolve) => setTimeout(resolve, 250));
      if (value.length < 3) {
        throw new Error('Tokens must be at least 3 characters.');
      }
      return value.toLowerCase();
    };
}
