// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/toggle-demo/toggle-demo.story.ts#section[1] "Disabled with reason"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxToggle } from '@cngx/common/interactive';

@Component({
  selector: 'app-toggle-disabled-with-reason',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxToggle],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <button type="button" (click)="systemLocked.set(!systemLocked())" class="sort-btn">
        {{ systemLocked() ? 'Unlock OS preference' : 'Lock OS preference' }}
      </button>
      <cngx-toggle
        [(value)]="dark"
        [disabled]="systemLocked()"
        [disabledReason]="systemLocked() ? 'Locked by your OS preference' : ''"
      >Dark mode</cngx-toggle>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class ToggleDisabledWithReason {
  protected readonly _exTitle: string = 'Toggle';
  protected readonly _exDescription: string = 'Single-value boolean switch atom. role="switch" with reactive aria-checked, aria-disabled, aria-describedby for the consumer-supplied disabled reason. Click + Space + Enter all flip. Provides CNGX_CONTROL_VALUE so CngxFormBridge (Phase 7) can bind to it without per-atom CVA.';
  protected readonly _exSectionTitle: string = 'Disabled with reason';
  protected readonly _exSubtitle: string = 'When <code>[disabled]</code> is true and <code>disabledReason</code> is non-empty, the host emits <code>aria-describedby</code> pointing to a hidden span — screen-readers announce <em>why</em> the control is disabled.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [];
  protected readonly _exTs: string = `import { CngxToggle } from '@cngx/common/interactive';

protected readonly notifications = signal(false);
protected readonly dark = signal(true);
protected readonly systemLocked = signal(false);`;
  protected readonly _exHtml: string = `<button type="button" (click)="systemLocked.set(!systemLocked())" class="sort-btn">
  {{ systemLocked() ? 'Unlock OS preference' : 'Lock OS preference' }}
</button>
<cngx-toggle
  [(value)]="dark"
  [disabled]="systemLocked()"
  [disabledReason]="systemLocked() ? 'Locked by your OS preference' : ''"
>Dark mode</cngx-toggle>`;
  protected readonly notifications = signal(false);
    protected readonly dark = signal(true);
    protected readonly systemLocked = signal(false);
}
