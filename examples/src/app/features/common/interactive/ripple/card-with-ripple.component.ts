// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/ripple-demo/ripple-demo.story.ts#section[1] "Card with Ripple"

import { ChangeDetectionStrategy, Component } from '@angular/core';
import { CngxRipple } from '@cngx/common/interactive';

@Component({
  selector: 'app-ripple-card-with-ripple',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxRipple],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div cngxRipple #rp="cngxRipple"
           style="padding:20px;border:1px solid var(--cngx-color-border,#ddd);
                  border-radius:8px;cursor:pointer;max-width:280px;user-select:none">
        <strong>Tappable Card</strong>
        <p style="margin:8px 0 0;font-size:0.875rem;color:var(--cngx-text-secondary,#666)">
          Click anywhere on this card.
        </p>
      </div>
      <div class="event-grid" style="margin-top:12px">
        <div class="event-row">
          <span class="event-label">Ripple active</span>
          <span class="event-value">{{ rp.active() }}</span>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class RippleCardWithRipple {
  protected readonly _exTitle: string = 'Ripple';
  protected readonly _exDescription: string = 'Touch/click ripple feedback without Material dependency. CSS-var positioned, consumer-styled.';
  protected readonly _exSectionTitle: string = 'Card with Ripple';
  protected readonly _exSubtitle: string = 'Apply to any container element. The directive sets <code>position: relative</code> and <code>overflow: hidden</code> on the host automatically.';
  protected readonly _exTs: string = `import { CngxRipple } from '@cngx/common/interactive';`;
  protected readonly _exHtml: string = `<div cngxRipple #rp="cngxRipple"
     style="padding:20px;border:1px solid var(--cngx-color-border,#ddd);
            border-radius:8px;cursor:pointer;max-width:280px;user-select:none">
  <strong>Tappable Card</strong>
  <p style="margin:8px 0 0;font-size:0.875rem;color:var(--cngx-text-secondary,#666)">
    Click anywhere on this card.
  </p>
</div>`;
}
