// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/error-aggregator-demo/error-aggregator-demo.story.ts#section[2] "cngx-popover-panel host"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxErrorAggregator, CngxErrorSource } from '@cngx/common/interactive';
import { CngxPopoverPanel, CngxPopoverTrigger, CngxPopoverHeader, CngxPopoverBody } from '@cngx/common/popover';

@Component({
  selector: 'app-error-aggregator-cngx-popover-panel-host',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxPopoverPanel, CngxPopoverTrigger, CngxPopoverHeader, CngxPopoverBody, CngxErrorAggregator, CngxErrorSource],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <button
        [cngxPopoverTrigger]="billingPanel.popover"
        (click)="billingPanel.popover.toggle()"
        type="button"
      >
        Billing status — {{ billingDeclined() ? 'has issue' : 'all good' }}
      </button>
      <button type="button" (click)="billingDeclined.set(!billingDeclined())" style="margin-inline-start: 8px;">
        Toggle declined
      </button>
      <cngx-popover-panel #billingPanel>
        <span cngxPopoverHeader>Billing</span>
        <div cngxPopoverBody cngxErrorAggregator #billing="cngxErrorAggregator">
          <span cngxErrorSource="declined" [when]="billingDeclined()" label="Last charge declined"></span>
          <p>Recent activity for this account.</p>
          <pre style="margin: 8px 0; padding: 8px; background: #f3f4f6; border-radius: 4px; font-size: 0.85em;">hasError    : {{ billing.hasError() }}
    errorCount  : {{ billing.errorCount() }}
    shouldShow  : {{ billing.shouldShow() }}</pre>
          @if (billing.hasError()) {
            <ul style="color: #b00020; margin: 8px 0; padding-inline-start: 24px;">
              @for (label of billing.errorLabels(); track label) {
                <li>{{ label }}</li>
              }
            </ul>
          }
        </div>
      </cngx-popover-panel>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class ErrorAggregatorCngxPopoverPanelHost {
  protected readonly _exTitle: string = 'Error Aggregator';
  protected readonly _exDescription: string = '<code>cngxErrorAggregator</code> rolls up child <code>cngxErrorSource</code> directives into one live A11y surface. Derived signals (<code>hasError</code>, <code>errorCount</code>, <code>activeErrors</code>, <code>errorLabels</code>, <code>shouldShow</code>, <code>announcement</code>) all carry structural <code>equal</code> fns so unrelated re-emissions do not cascade. The directive is template-free — render the SR live region yourself. Each section below shows the reactive state at the top so the consumer sees every signal toggle live.';
  protected readonly _exSectionTitle: string = 'cngx-popover-panel host';
  protected readonly _exSubtitle: string = 'The aggregator binds to a popover-panel body. Click the trigger to open the popover; inside the panel, the aggregator rolls up its sources just like any other host.';
  protected readonly _exTs: string = `import { CngxErrorAggregator, CngxErrorSource } from '@cngx/common/interactive';
import { CngxPopoverPanel, CngxPopoverTrigger, CngxPopoverHeader, CngxPopoverBody } from '@cngx/common/popover';

protected readonly emailFormatBad = signal(true);
protected readonly emailTaken = signal(false);
protected readonly passwordWeak = signal(true);
protected readonly profileBioEmpty = signal(true);
protected readonly profileAvatarMissing = signal(false);
protected readonly billingDeclined = signal(true);
protected readonly tabAddressIncomplete = signal(true);
protected readonly tabPaymentInvalid = signal(true);`;
  protected readonly _exHtml: string = `  <button
    [cngxPopoverTrigger]="billingPanel.popover"
    (click)="billingPanel.popover.toggle()"
    type="button"
  >
    Billing status — {{ billingDeclined() ? 'has issue' : 'all good' }}
  </button>
  <button type="button" (click)="billingDeclined.set(!billingDeclined())" style="margin-inline-start: 8px;">
    Toggle declined
  </button>
  <cngx-popover-panel #billingPanel>
    <span cngxPopoverHeader>Billing</span>
    <div cngxPopoverBody cngxErrorAggregator #billing="cngxErrorAggregator">
      <span cngxErrorSource="declined" [when]="billingDeclined()" label="Last charge declined"></span>
      <p>Recent activity for this account.</p>
      <pre style="margin: 8px 0; padding: 8px; background: #f3f4f6; border-radius: 4px; font-size: 0.85em;">hasError    : {{ billing.hasError() }}
errorCount  : {{ billing.errorCount() }}
shouldShow  : {{ billing.shouldShow() }}</pre>
      @if (billing.hasError()) {
        <ul style="color: #b00020; margin: 8px 0; padding-inline-start: 24px;">
          @for (label of billing.errorLabels(); track label) {
            <li>{{ label }}</li>
          }
        </ul>
      }
    </div>
  </cngx-popover-panel>`;
  protected readonly emailFormatBad = signal(true);
    protected readonly emailTaken = signal(false);
    protected readonly passwordWeak = signal(true);
    protected readonly profileBioEmpty = signal(true);
    protected readonly profileAvatarMissing = signal(false);
    protected readonly billingDeclined = signal(true);
    protected readonly tabAddressIncomplete = signal(true);
    protected readonly tabPaymentInvalid = signal(true);
}
