// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/copy-text-demo/copy-text-demo.story.ts#section[0] "Copy Token"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxCopyText } from '@cngx/common/interactive';

@Component({
  selector: 'app-copy-text-copy-token',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxCopyText],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0 || _exUses.length > 0) {
        <div class="cngx-ex-meta">
          @if (_exTags.length > 0) {
            <ul class="cngx-ex-tags" aria-label="Tags">
              @for (t of _exTags; track t.dim + ':' + t.value) {
                <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
              }
            </ul>
          }
          @if (_exUses.length > 0) {
            <p class="cngx-ex-uses"><span class="cngx-ex-uses__label">uses</span>
              @for (u of _exUses; track u; let last = $last) {
                <code>{{ u }}</code>@if (!last) {<span class="cngx-ex-uses__sep">, </span>}
              }
            </p>
          }
        </div>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div style="display:flex;align-items:center;gap:8px">
        <code style="padding:6px 10px;background:var(--code-bg,#f5f5f5);border:1px solid var(--code-border,#ddd);
                     border-radius:4px;font-size:0.8125rem;overflow:hidden;text-overflow:ellipsis;max-width:280px">
          {{ token() }}
        </code>
        <button [cngxCopyText]="token()" #cp="cngxCopyText" class="chip"
                [style.background]="cp.copied() ? 'var(--success-bg, #e8f5e9)' : ''"
                [style.borderColor]="cp.copied() ? 'var(--success-fg, #2e7d32)' : ''">
          {{ cp.copied() ? 'Copied!' : 'Copy' }}
        </button>
      </div>
      <div class="event-grid" style="margin-top:12px">
        <div class="event-row">
          <span class="event-label">copied</span>
          <span class="event-value">{{ cp.copied() }}</span>
        </div>
        <div class="event-row">
          <span class="event-label">supported</span>
          <span class="event-value">{{ cp.supported }}</span>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class CopyTextCopyToken {
  protected readonly _exTitle: string = 'Copy Text';
  protected readonly _exDescription: string = 'Clipboard copy behavior without forms dependency. Clipboard API with execCommand fallback.';
  protected readonly _exSectionTitle: string = 'Copy Token';
  protected readonly _exSubtitle: string = 'Click the button to copy the API token. The <code>copied()</code> signal drives "Copied!" feedback.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [];
  protected readonly _exUses: readonly string[] = ['CngxCopyText'];
  protected readonly _exTs: string = `import { CngxCopyText } from '@cngx/common/interactive';

protected readonly token = signal('eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9');
protected readonly shareUrl = signal('https://cngx.dev/docs/copy-text');`;
  protected readonly _exHtml: string = `<div style="display:flex;align-items:center;gap:8px">
  <code style="padding:6px 10px;background:var(--code-bg,#f5f5f5);border:1px solid var(--code-border,#ddd);
               border-radius:4px;font-size:0.8125rem;overflow:hidden;text-overflow:ellipsis;max-width:280px">
    {{ token() }}
  </code>
  <button [cngxCopyText]="token()" #cp="cngxCopyText" class="chip"
          [style.background]="cp.copied() ? 'var(--success-bg, #e8f5e9)' : ''"
          [style.borderColor]="cp.copied() ? 'var(--success-fg, #2e7d32)' : ''">
    {{ cp.copied() ? 'Copied!' : 'Copy' }}
  </button>
</div>`;
  protected readonly token = signal('eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9');
    protected readonly shareUrl = signal('https://cngx.dev/docs/copy-text');
}
