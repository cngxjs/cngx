// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/display/radio-indicator-demo/radio-indicator-demo.story.ts#section[3] "Custom dotGlyph"

import { ChangeDetectionStrategy, Component } from '@angular/core';
import { CngxRadioIndicator } from '@cngx/common/display';

@Component({
  selector: 'app-radio-indicator-custom-dotglyph',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxRadioIndicator],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <ng-template #starGlyph>
        <span aria-hidden="true" style="font-size:0.6em">★</span>
      </ng-template>
      <div class="row">
        <div class="cell">
          <cngx-radio-indicator [checked]="true" [dotGlyph]="starGlyph" />
          <span class="caption">custom — star</span>
        </div>
        <div class="cell">
          <cngx-radio-indicator [dotGlyph]="starGlyph" />
          <span class="caption">unchecked — glyph hidden</span>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class RadioIndicatorCustomDotglyph {
  protected readonly _exTitle: string = 'Radio indicator';
  protected readonly _exDescription: string = 'Decorative dot-in-circle atom. Mirrors CngxCheckboxIndicator: aria-hidden, no outputs, full --cngx-radio-indicator-* theming. Used by the CngxSelect "radio" indicator variant; future single-value form atoms compose the same skin.';
  protected readonly _exSectionTitle: string = 'Custom dotGlyph';
  protected readonly _exSubtitle: string = 'Project a <code>TemplateRef&lt;void&gt;</code> via <code>[dotGlyph]</code> to replace the default dot with a brand glyph or SVG. Glyph still gates on <code>checked</code>.';
  protected readonly _exTs: string = `import { CngxRadioIndicator } from '@cngx/common/display';`;
  protected readonly _exHtml: string = `<ng-template #starGlyph>
  <span aria-hidden="true" style="font-size:0.6em">★</span>
</ng-template>
<div class="row">
  <div class="cell">
    <cngx-radio-indicator [checked]="true" [dotGlyph]="starGlyph" />
    <span class="caption">custom — star</span>
  </div>
  <div class="cell">
    <cngx-radio-indicator [dotGlyph]="starGlyph" />
    <span class="caption">unchecked — glyph hidden</span>
  </div>
</div>`;
}
