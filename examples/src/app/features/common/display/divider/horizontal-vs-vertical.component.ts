// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/display/divider-demo/divider-demo.story.ts#section[0] "Horizontal vs vertical"

import { ChangeDetectionStrategy, Component } from '@angular/core';
import { CngxDivider } from '@cngx/common/display';

@Component({
  selector: 'app-divider-horizontal-vs-vertical',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxDivider],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0 || _exUses.length > 0) {
        <div class="cngx-ex-meta">
          @if (_exTags.length > 0) {
            <ul class="cngx-ex-tags" aria-label="Tags">
              @for (t of _exTags; track t.dim + ':' + t.value) {
                <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
              }
            </ul>
          }
          @if (_exUses.length > 0) {
            <p class="cngx-ex-uses"><span class="cngx-ex-uses__label">uses</span>
              @for (u of _exUses; track u; let last = $last) {
                <code>{{ u }}</code>@if (!last) {<span class="cngx-ex-uses__sep">, </span>}
              }
            </p>
          }
        </div>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="section">First row</div>
      <cngx-divider></cngx-divider>
      <div class="section">Second row</div>
      <div class="inline-row">
        <span>left</span>
        <cngx-divider orientation="vertical"></cngx-divider>
        <span>right</span>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class DividerHorizontalVsVertical {
  protected readonly _exTitle: string = 'Divider';
  protected readonly _exDescription: string = 'Presentational separator with proper ARIA semantics. Horizontal or vertical, optionally inset.';
  protected readonly _exSectionTitle: string = 'Horizontal vs vertical';
  protected readonly _exSubtitle: string = 'Default orientation is horizontal. Use vertical in flex rows to split inline content.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [];
  protected readonly _exUses: readonly string[] = ['CngxDivider'];
  protected readonly _exTs: string = `import { CngxDivider } from '@cngx/common/display';`;
  protected readonly _exHtml: string = `<div class="section">First row</div>
<cngx-divider></cngx-divider>
<div class="section">Second row</div>
<div class="inline-row">
  <span>left</span>
  <cngx-divider orientation="vertical"></cngx-divider>
  <span>right</span>
</div>`;
}
