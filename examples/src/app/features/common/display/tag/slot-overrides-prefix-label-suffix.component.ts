// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/display/tag-demo/tag-demo.story.ts#section[6] "Slot overrides — prefix / label / suffix"

import { ChangeDetectionStrategy, Component } from '@angular/core';
import { CngxTag, CngxTagPrefix, CngxTagSuffix, CngxIcon } from '@cngx/common/display';

@Component({
  selector: 'app-tag-slot-overrides-prefix-label-suffix',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxTag, CngxTagPrefix, CngxTagSuffix, CngxIcon],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div style="display: flex; gap: 12px; align-items: center; flex-wrap: wrap;">
        <span cngxTag color="success">
          <ng-template cngxTagPrefix>
            <cngx-icon size="sm" aria-hidden="true">
              <svg viewBox="0 0 16 16" focusable="false"><path fill="currentColor" d="M6.5 11.5 3 8l1.4-1.4 2.1 2.1L11.6 4l1.4 1.4z" /></svg>
            </cngx-icon>
          </ng-template>
          Active
        </span>
        <span cngxTag color="info">
          Frontend
          <ng-template cngxTagSuffix>
            <cngx-icon size="sm" aria-hidden="true">
              <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" focusable="false"><path d="m4 6 4 4 4-4" /></svg>
            </cngx-icon>
          </ng-template>
        </span>
        <span cngxTag color="warning">
          <ng-template cngxTagPrefix>
            <cngx-icon size="sm" aria-hidden="true">
              <svg viewBox="0 0 16 16" focusable="false"><circle cx="8" cy="8" r="4" fill="currentColor" /></svg>
            </cngx-icon>
          </ng-template>
          Pending review
        </span>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class TagSlotOverridesPrefixLabelSuffix {
  protected readonly _exTitle: string = 'Tag';
  protected readonly _exDescription: string = 'Decorative label / badge / status indicator. Dual selector ([cngxTag] and &lt;cngx-tag&gt;) so it composes onto any host element including <a> for link-mode tags. Removable affordances live in CngxChip; clickable interactions live on native &lt;button cngxTag&gt; / <a cngxTag>.';
  protected readonly _exSectionTitle: string = 'Slot overrides — prefix / label / suffix';
  protected readonly _exSubtitle: string = 'Project <code>&lt;ng-template cngxTagPrefix&gt;</code>, <code>&lt;ng-template cngxTagLabel&gt;</code>, or <code>&lt;ng-template cngxTagSuffix&gt;</code> to control each region. Prefix and suffix slots render no DOM when omitted; the default label wraps content in <code>cngx-tag__label</code> for ellipsis support.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [];
  protected readonly _exTs: string = `import { CngxTag, CngxTagPrefix, CngxTagSuffix, CngxIcon } from '@cngx/common/display';`;
  protected readonly _exHtml: string = `<div style="display: flex; gap: 12px; align-items: center; flex-wrap: wrap;">
  <span cngxTag color="success">
    <ng-template cngxTagPrefix>
      <cngx-icon size="sm" aria-hidden="true">
        <svg viewBox="0 0 16 16" focusable="false"><path fill="currentColor" d="M6.5 11.5 3 8l1.4-1.4 2.1 2.1L11.6 4l1.4 1.4z" /></svg>
      </cngx-icon>
    </ng-template>
    Active
  </span>
  <span cngxTag color="info">
    Frontend
    <ng-template cngxTagSuffix>
      <cngx-icon size="sm" aria-hidden="true">
        <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" focusable="false"><path d="m4 6 4 4 4-4" /></svg>
      </cngx-icon>
    </ng-template>
  </span>
  <span cngxTag color="warning">
    <ng-template cngxTagPrefix>
      <cngx-icon size="sm" aria-hidden="true">
        <svg viewBox="0 0 16 16" focusable="false"><circle cx="8" cy="8" r="4" fill="currentColor" /></svg>
      </cngx-icon>
    </ng-template>
    Pending review
  </span>
</div>`;
}
