// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/display/badge-demo/badge-demo.story.ts#section[2] "Inline and hidden"

import { ChangeDetectionStrategy, Component } from '@angular/core';
import { CngxBadge } from '@cngx/common/display';

@Component({
  selector: 'app-badge-inline-and-hidden',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxBadge],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0 || _exUses.length > 0) {
        <div class="cngx-ex-meta">
          @if (_exTags.length > 0) {
            <ul class="cngx-ex-tags" aria-label="Tags">
              @for (t of _exTags; track t.dim + ':' + t.value) {
                <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
              }
            </ul>
          }
          @if (_exUses.length > 0) {
            <p class="cngx-ex-uses"><span class="cngx-ex-uses__label">uses</span>
              @for (u of _exUses; track u; let last = $last) {
                <code>{{ u }}</code>@if (!last) {<span class="cngx-ex-uses__sep">, </span>}
              }
            </p>
          }
        </div>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="row">
        <span [cngxBadge]="'NEW'" position="inline" color="warning">Feature</span>
        <button type="button" class="chip" [cngxBadge]="3" [hidden]="true">Hidden</button>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class BadgeInlineAndHidden {
  protected readonly _exTitle: string = 'Badge';
  protected readonly _exDescription: string = 'Floating counter / dot indicator attached to any host element. Purely visual — semantics live on the host via aria-label.';
  protected readonly _exSectionTitle: string = 'Inline and hidden';
  protected readonly _exSubtitle: string = 'Inline renders after the host content. Hidden tears the badge out of the DOM — useful for toggle states.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [];
  protected readonly _exUses: readonly string[] = ['CngxBadge'];
  protected readonly _exTs: string = `import { CngxBadge } from '@cngx/common/display';`;
  protected readonly _exHtml: string = `<div class="row">
  <span [cngxBadge]="'NEW'" position="inline" color="warning">Feature</span>
  <button type="button" class="chip" [cngxBadge]="3" [hidden]="true">Hidden</button>
</div>`;
}
