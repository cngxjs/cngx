// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/layout/drawer-demo/drawer-demo.story.ts#section[5] "Pattern — Consumer Wiring"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { type DrawerMode } from '@cngx/common';
import { CngxAriaExpanded } from '@cngx/common';

@Component({
  selector: 'app-drawer-pattern-consumer-wiring',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <pre class="code-block"><code>&lt;div cngxDrawer #drawer="cngxDrawer"&gt;
      &lt;!-- Consumer wires CngxAriaExpanded on the trigger --&gt;
      &lt;button [cngxAriaExpanded]="drawer.opened()"
              [controls]="'sidebar'" (click)="drawer.toggle()"&gt;
        Menu
      &lt;/button&gt;

      &lt;!-- CngxFocusTrap is a hostDirective — consumer binds [enabled] --&gt;
      &lt;nav [cngxDrawerPanel]="drawer" position="left"
           [enabled]="drawer.opened()" [autoFocus]="true"
           id="sidebar" role="navigation"&gt;
        ...
      &lt;/nav&gt;

      &lt;!-- Optional: content shifts via CSS --&gt;
      &lt;main [cngxDrawerContent]="drawer"&gt;...&lt;/main&gt;

      &lt;!-- Optional: backdrop is pure CSS --&gt;
      &lt;div class="backdrop" [class.visible]="drawer.opened()"
           (click)="drawer.close()"&gt;&lt;/div&gt;
    &lt;/div&gt;</code></pre>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class DrawerPatternConsumerWiring {
  protected readonly _exTitle: string = 'Drawer';
  protected readonly _exDescription: string = 'Headless drawer/sidebar system: CngxDrawer (state), CngxDrawerPanel (sliding panel), CngxDrawerContent (content offset). Supports left/right/top/bottom, focus trapping, click-outside close, Escape key, and controlled+uncontrolled modes.';
  protected readonly _exSectionTitle: string = 'Pattern — Consumer Wiring';
  protected readonly _exSubtitle: string = 'The drawer system is fully headless. Directives set CSS classes; the consumer styles them. <code>CngxAriaExpanded</code> and <code>CngxFocusTrap</code> are wired by the consumer — not auto-injected.';
  protected readonly _exTs: string = `import { type DrawerMode } from '@cngx/common';
import { CngxAriaExpanded } from '@cngx/common';

// Controlled drawer
protected readonly controlledOpen = signal(false);

// Direction playground
protected readonly direction = signal<'left' | 'right' | 'top' | 'bottom'>('left');
protected readonly directions: ('left' | 'right' | 'top' | 'bottom')[] = ['left', 'right', 'top', 'bottom'];

// Mode playground
protected readonly mode = signal<DrawerMode>('over');
protected readonly modes: DrawerMode[] = ['over', 'push', 'side'];

// Event log
protected readonly events = signal<string[]>([]);
protected logEvent(name: string): void {
  this.events.update(e => [name + ' @ ' + new Date().toLocaleTimeString(), ...e].slice(0, 5));
}`;
  protected readonly _exHtml: string = `  <pre class="code-block"><code>&lt;div cngxDrawer #drawer="cngxDrawer"&gt;
  &lt;!-- Consumer wires CngxAriaExpanded on the trigger --&gt;
  &lt;button [cngxAriaExpanded]="drawer.opened()"
          [controls]="'sidebar'" (click)="drawer.toggle()"&gt;
    Menu
  &lt;/button&gt;

  &lt;!-- CngxFocusTrap is a hostDirective — consumer binds [enabled] --&gt;
  &lt;nav [cngxDrawerPanel]="drawer" position="left"
       [enabled]="drawer.opened()" [autoFocus]="true"
       id="sidebar" role="navigation"&gt;
    ...
  &lt;/nav&gt;

  &lt;!-- Optional: content shifts via CSS --&gt;
  &lt;main [cngxDrawerContent]="drawer"&gt;...&lt;/main&gt;

  &lt;!-- Optional: backdrop is pure CSS --&gt;
  &lt;div class="backdrop" [class.visible]="drawer.opened()"
       (click)="drawer.close()"&gt;&lt;/div&gt;
&lt;/div&gt;</code></pre>`;
  // Controlled drawer
    protected readonly controlledOpen = signal(false);

    // Direction playground
    protected readonly direction = signal<'left' | 'right' | 'top' | 'bottom'>('left');
    protected readonly directions: ('left' | 'right' | 'top' | 'bottom')[] = ['left', 'right', 'top', 'bottom'];

    // Mode playground
    protected readonly mode = signal<DrawerMode>('over');
    protected readonly modes: DrawerMode[] = ['over', 'push', 'side'];

    // Event log
    protected readonly events = signal<string[]>([]);
    protected logEvent(name: string): void {
      this.events.update(e => [name + ' @ ' + new Date().toLocaleTimeString(), ...e].slice(0, 5));
    }
}
