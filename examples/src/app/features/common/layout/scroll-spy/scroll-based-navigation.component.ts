// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/layout/scroll-spy-demo/scroll-spy-demo.story.ts#section[0] "Scroll-Based Navigation"

import { ChangeDetectionStrategy, Component } from '@angular/core';
import { CngxScrollSpy } from '@cngx/common/layout';

@Component({
  selector: 'app-scroll-spy-scroll-based-navigation',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxScrollSpy],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div style="display:flex;gap:16px;max-width:600px">
        <nav [cngxScrollSpy]="sectionIds" [root]="'.spy-container'" [threshold]="0.1" #spy="cngxScrollSpy"
             style="position:sticky;top:0;display:flex;flex-direction:column;gap:4px;min-width:100px;padding-top:8px">
          @for (id of sectionIds; track id) {
            <a href="javascript:void(0)"
               (click)="scrollTo($event, id)"
               class="chip"
               [class.chip--active]="spy.activeId() === id"
               style="text-decoration:none;font-size:0.8125rem;text-transform:capitalize">
              {{ id.replace('spy-', '') }}
            </a>
          }
        </nav>
        <div class="spy-container" style="height:300px;overflow-y:auto;flex:1;border:1px solid var(--cngx-color-border,#ddd);border-radius:8px">
          @for (id of sectionIds; track id) {
            <section [id]="id" style="min-height:200px;padding:20px;border-bottom:1px solid var(--cngx-color-border,#eee)">
              <h3 style="margin:0 0 8px;text-transform:capitalize">{{ id.replace('spy-', '') }}</h3>
              <p style="color:var(--cngx-text-secondary,#666);font-size:0.875rem">
                Scroll through this section to see the nav update.
                This section has enough height to demonstrate the intersection ratio tracking.
              </p>
            </section>
          }
        </div>
      </div>
      <div class="event-grid" style="margin-top:12px">
        <div class="event-row">
          <span class="event-label">Active section</span>
          <span class="event-value">{{ spy.activeId() ?? 'none' }}</span>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class ScrollSpyScrollBasedNavigation {
  protected readonly _exTitle: string = 'Scroll Spy';
  protected readonly _exDescription: string = 'Tracks which section is most visible in the viewport. Ideal for scroll-based navigation highlighting.';
  protected readonly _exSectionTitle: string = 'Scroll-Based Navigation';
  protected readonly _exSubtitle: string = 'Scroll the container. The nav highlights the most visible section. Active section ID is exposed as a signal.';
  protected readonly _exTs: string = `import { CngxScrollSpy } from '@cngx/common/layout';

protected readonly sectionIds = ['spy-intro', 'spy-features', 'spy-pricing', 'spy-faq'];

protected scrollTo(event: Event, id: string): void {
  event.preventDefault();
  document.getElementById(id)?.scrollIntoView({ behavior: 'smooth', block: 'start' });
}`;
  protected readonly _exHtml: string = `<div style="display:flex;gap:16px;max-width:600px">
  <nav [cngxScrollSpy]="sectionIds" [root]="'.spy-container'" [threshold]="0.1" #spy="cngxScrollSpy"
       style="position:sticky;top:0;display:flex;flex-direction:column;gap:4px;min-width:100px;padding-top:8px">
    @for (id of sectionIds; track id) {
      <a href="javascript:void(0)"
         (click)="scrollTo($event, id)"
         class="chip"
         [class.chip--active]="spy.activeId() === id"
         style="text-decoration:none;font-size:0.8125rem;text-transform:capitalize">
        {{ id.replace('spy-', '') }}
      </a>
    }
  </nav>
  <div class="spy-container" style="height:300px;overflow-y:auto;flex:1;border:1px solid var(--cngx-color-border,#ddd);border-radius:8px">
    @for (id of sectionIds; track id) {
      <section [id]="id" style="min-height:200px;padding:20px;border-bottom:1px solid var(--cngx-color-border,#eee)">
        <h3 style="margin:0 0 8px;text-transform:capitalize">{{ id.replace('spy-', '') }}</h3>
        <p style="color:var(--cngx-text-secondary,#666);font-size:0.875rem">
          Scroll through this section to see the nav update.
          This section has enough height to demonstrate the intersection ratio tracking.
        </p>
      </section>
    }
  </div>
</div>`;
  protected readonly sectionIds = ['spy-intro', 'spy-features', 'spy-pricing', 'spy-faq'];

    protected scrollTo(event: Event, id: string): void {
      event.preventDefault();
      document.getElementById(id)?.scrollIntoView({ behavior: 'smooth', block: 'start' });
    }
}
