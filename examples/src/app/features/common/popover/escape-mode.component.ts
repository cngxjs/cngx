// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/popover/popover-demo/popover-demo.story.ts#section[3] "Escape & Mode"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxPopover, CngxPopoverTrigger } from '@cngx/common/popover';
import type { PopoverPlacement } from '@cngx/common/popover';

@Component({
  selector: 'app-popover-escape-mode',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxPopover, CngxPopoverTrigger],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0 || _exUses.length > 0) {
        <div class="cngx-ex-meta">
          @if (_exTags.length > 0) {
            <ul class="cngx-ex-tags" aria-label="Tags">
              @for (t of _exTags; track t.dim + ':' + t.value) {
                <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
              }
            </ul>
          }
          @if (_exUses.length > 0) {
            <p class="cngx-ex-uses"><span class="cngx-ex-uses__label">uses</span>
              @for (u of _exUses; track u; let last = $last) {
                <code>{{ u }}</code>@if (!last) {<span class="cngx-ex-uses__sep">, </span>}
              }
            </p>
          }
        </div>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div style="display:flex;gap:16px;flex-wrap:wrap;padding-top:20px">
        <div>
          <button [cngxPopoverTrigger]="noEsc" (click)="noEsc.toggle()" class="chip">
            No Escape
          </button>
          <div cngxPopover #noEsc="cngxPopover" [closeOnEscape]="false" placement="bottom"
               style="padding:8px 12px;font-size:0.8125rem">
            Escape won't close this. Click the button again.
          </div>
        </div>
        <div>
          <button [cngxPopoverTrigger]="autoPop" (click)="autoPop.toggle()" class="chip">
            Auto mode
          </button>
          <div cngxPopover #autoPop="cngxPopover" mode="auto" placement="bottom"
               style="padding:8px 12px;font-size:0.8125rem">
            Click outside to dismiss (browser light dismiss).
          </div>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class PopoverEscapeMode {
  protected readonly _exTitle: string = 'Popover';
  protected readonly _exDescription: string = 'Signal-driven state machine for the native Popover API. CSS Anchor Positioning, transition-aware lifecycle, no CDK Overlay dependency.';
  protected readonly _exSectionTitle: string = 'Escape & Mode';
  protected readonly _exSubtitle: string = 'Set <code>[closeOnEscape]="false"</code> to prevent Escape dismiss. Set <code>mode="auto"</code> for browser-native light dismiss.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [];
  protected readonly _exUses: readonly string[] = ['CngxPopover', 'CngxPopoverTrigger'];
  protected readonly _exTs: string = `import { CngxPopover, CngxPopoverTrigger } from '@cngx/common/popover';
import type { PopoverPlacement } from '@cngx/common/popover';

protected readonly menuItems = signal(['Edit', 'Duplicate', 'Archive', 'Delete']);
protected readonly placements: PopoverPlacement[] = ['bottom', 'bottom-start', 'bottom-end', 'top', 'left', 'right'];`;
  protected readonly _exHtml: string = `<div style="display:flex;gap:16px;flex-wrap:wrap;padding-top:20px">
  <div>
    <button [cngxPopoverTrigger]="noEsc" (click)="noEsc.toggle()" class="chip">
      No Escape
    </button>
    <div cngxPopover #noEsc="cngxPopover" [closeOnEscape]="false" placement="bottom"
         style="padding:8px 12px;font-size:0.8125rem">
      Escape won't close this. Click the button again.
    </div>
  </div>
  <div>
    <button [cngxPopoverTrigger]="autoPop" (click)="autoPop.toggle()" class="chip">
      Auto mode
    </button>
    <div cngxPopover #autoPop="cngxPopover" mode="auto" placement="bottom"
         style="padding:8px 12px;font-size:0.8125rem">
      Click outside to dismiss (browser light dismiss).
    </div>
  </div>
</div>`;
  protected readonly menuItems = signal(['Edit', 'Duplicate', 'Archive', 'Delete']);
    protected readonly placements: PopoverPlacement[] = ['bottom', 'bottom-start', 'bottom-end', 'top', 'left', 'right'];
}
