// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/chart/primitives-demo/primitives-demo.story.ts#section[1] "Multi-series line + axis labels + legend"

import { ChangeDetectionStrategy, Component } from '@angular/core';
import { CngxChart, CngxAxis, CngxLine, CngxChartLegend } from '@cngx/common/chart';
import { createManualState } from '@cngx/common/data';

@Component({
  selector: 'app-primitives-multi-series-line-axis-labels-legend',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxChart, CngxAxis, CngxLine, CngxChartLegend],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0 || _exUses.length > 0) {
        <div class="cngx-ex-meta">
          @if (_exTags.length > 0) {
            <ul class="cngx-ex-tags" aria-label="Tags">
              @for (t of _exTags; track t.dim + ':' + t.value) {
                <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
              }
            </ul>
          }
          @if (_exUses.length > 0) {
            <p class="cngx-ex-uses"><span class="cngx-ex-uses__label">uses</span>
              @for (u of _exUses; track u; let last = $last) {
                <code>{{ u }}</code>@if (!last) {<span class="cngx-ex-uses__sep">, </span>}
              }
            </p>
          }
        </div>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div style="border:1px solid var(--border, #e5e7eb); border-radius: 4px; padding: 8px 8px 32px 40px; display: inline-block; max-width: 100%; box-sizing: border-box">
      <cngx-chart
        [data]="[10, 12, 18, 22, 24, 28, 32, 30, 27, 26, 30, 35]"
        [width]="480"
        [height]="200"
        aria-label="Two-series traffic and error trend over twelve months."
      >
        <svg:g cngxAxis position="bottom" type="linear" [domain]="[0, 11]" [ticks]="6" [grid]="true" label="Months"></svg:g>
        <svg:g cngxAxis position="left" type="linear" [domain]="[0, 40]" [grid]="true" label="Requests / sec"></svg:g>
        <svg:g cngxLine
          [strokeWidth]="2"
          style="--cngx-line-color: var(--primary, #3b82f6)"
        ></svg:g>
        <svg:g cngxLine
          [data]="[2, 4, 3, 8, 6, 5, 9, 12, 8, 10, 7, 6]"
          [strokeWidth]="2"
          style="--cngx-line-color: var(--danger, #d2452f)"
        ></svg:g>
      </cngx-chart>
      </div>
      <cngx-chart-legend
        [items]="[
          { label: 'Traffic', color: 'var(--primary, #3b82f6)' },
          { label: 'Errors',  color: 'var(--danger, #d2452f)' }
        ]"
        style="margin-top:8px"
      />
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class PrimitivesMultiSeriesLineAxisLabelsLegend {
  protected readonly _exTitle: string = 'Chart Primitives';
  protected readonly _exDescription: string = 'Compose &lt;cngx-chart&gt; + [cngxAxis] + layer atoms ([cngxLine], [cngxArea], [cngxBar], [cngxScatter], [cngxThreshold], [cngxBand]) directly. The seven preset molecules wrap these primitives; this demo shows how to compose them.';
  protected readonly _exSectionTitle: string = 'Multi-series line + axis labels + legend';
  protected readonly _exSubtitle: string = 'Two metrics on shared scales. Adds [label]="..." on each axis for X/Y titles, and pairs the chart with a presentational &lt;cngx-chart-legend&gt; driven by an [items] array — decoupled from the layer atoms by design.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [];
  protected readonly _exUses: readonly string[] = ['CngxChart', 'CngxAxis', 'CngxLine', 'CngxArea', 'CngxBar', 'CngxScatter', 'CngxThreshold', 'CngxBand'];
  protected readonly _exTs: string = `import { CngxChart, CngxAxis, CngxLine, CngxChartLegend } from '@cngx/common/chart';
import { createManualState } from '@cngx/common/data';

protected readonly months: readonly string[] = [
  'Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'
];
protected readonly monthByIndex = (_: unknown, i: number): string => this.months[i];

protected readonly latencyData: readonly { t: Date; v: number }[] = [
  { t: new Date(2026, 0, 5), v: 145 },
  { t: new Date(2026, 0, 12), v: 168 },
  { t: new Date(2026, 0, 19), v: 192 },
  { t: new Date(2026, 0, 26), v: 220 },
  { t: new Date(2026, 1, 2), v: 285 },
  { t: new Date(2026, 1, 9), v: 240 },
  { t: new Date(2026, 1, 16), v: 195 },
];
protected readonly latencyDomain: readonly Date[] = [
  new Date(2026, 0, 5),
  new Date(2026, 1, 16),
];
protected readonly latencyTime = (d: { t: Date; v: number }): Date => d.t;
protected readonly latencyValue = (d: { t: Date; v: number }): number => d.v;
protected readonly dateFmt = (v: unknown): string => {
  const d = v instanceof Date ? v : new Date(Number(v));
  return d.toLocaleDateString('en', { month: 'short', day: '2-digit' });
};

protected readonly scatterData: readonly { x: number; y: number }[] = [
  { x: 12, y: 18 }, { x: 22, y: 24 }, { x: 18, y: 30 }, { x: 35, y: 42 },
  { x: 48, y: 55 }, { x: 56, y: 62 }, { x: 64, y: 78 }, { x: 72, y: 70 },
  { x: 80, y: 88 }, { x: 88, y: 82 }, { x: 30, y: 12 }, { x: 95, y: 60 },
];
protected readonly scatterX = (d: { x: number; y: number }): number => d.x;
protected readonly scatterY = (d: { x: number; y: number }): number => d.y;
protected readonly priceFmt = (v: unknown): string => '$' + Number(v);

protected readonly chartStateData: readonly number[] = [8, 12, 14, 9, 18, 22, 25, 19, 16, 24, 28, 32];
protected readonly chartState = createManualState<readonly number[]>();

protected showSkeleton(): void { this.chartState.reset(); this.chartState.set('loading'); }
protected showSuccess(): void { this.chartState.setSuccess(this.chartStateData); }
protected showEmpty(): void { this.chartState.reset(); this.chartState.setSuccess([]); }
protected showError(): void { this.chartState.reset(); this.chartState.setError(new Error('Telemetry feed offline')); }`;
  protected readonly _exHtml: string = `<div style="border:1px solid var(--border, #e5e7eb); border-radius: 4px; padding: 8px 8px 32px 40px; display: inline-block; max-width: 100%; box-sizing: border-box">
<cngx-chart
  [data]="[10, 12, 18, 22, 24, 28, 32, 30, 27, 26, 30, 35]"
  [width]="480"
  [height]="200"
  aria-label="Two-series traffic and error trend over twelve months."
>
  <svg:g cngxAxis position="bottom" type="linear" [domain]="[0, 11]" [ticks]="6" [grid]="true" label="Months"></svg:g>
  <svg:g cngxAxis position="left" type="linear" [domain]="[0, 40]" [grid]="true" label="Requests / sec"></svg:g>
  <svg:g cngxLine
    [strokeWidth]="2"
    style="--cngx-line-color: var(--primary, #3b82f6)"
  ></svg:g>
  <svg:g cngxLine
    [data]="[2, 4, 3, 8, 6, 5, 9, 12, 8, 10, 7, 6]"
    [strokeWidth]="2"
    style="--cngx-line-color: var(--danger, #d2452f)"
  ></svg:g>
</cngx-chart>
</div>
<cngx-chart-legend
  [items]="[
    { label: 'Traffic', color: 'var(--primary, #3b82f6)' },
    { label: 'Errors',  color: 'var(--danger, #d2452f)' }
  ]"
  style="margin-top:8px"
/>`;
  protected readonly months: readonly string[] = [
    'Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'
  ];
  protected readonly monthByIndex = (_: unknown, i: number): string => this.months[i];

  protected readonly latencyData: readonly { t: Date; v: number }[] = [
    { t: new Date(2026, 0, 5), v: 145 },
    { t: new Date(2026, 0, 12), v: 168 },
    { t: new Date(2026, 0, 19), v: 192 },
    { t: new Date(2026, 0, 26), v: 220 },
    { t: new Date(2026, 1, 2), v: 285 },
    { t: new Date(2026, 1, 9), v: 240 },
    { t: new Date(2026, 1, 16), v: 195 },
  ];
  protected readonly latencyDomain: readonly Date[] = [
    new Date(2026, 0, 5),
    new Date(2026, 1, 16),
  ];
  protected readonly latencyTime = (d: { t: Date; v: number }): Date => d.t;
  protected readonly latencyValue = (d: { t: Date; v: number }): number => d.v;
  protected readonly dateFmt = (v: unknown): string => {
    const d = v instanceof Date ? v : new Date(Number(v));
    return d.toLocaleDateString('en', { month: 'short', day: '2-digit' });
  };

  protected readonly scatterData: readonly { x: number; y: number }[] = [
    { x: 12, y: 18 }, { x: 22, y: 24 }, { x: 18, y: 30 }, { x: 35, y: 42 },
    { x: 48, y: 55 }, { x: 56, y: 62 }, { x: 64, y: 78 }, { x: 72, y: 70 },
    { x: 80, y: 88 }, { x: 88, y: 82 }, { x: 30, y: 12 }, { x: 95, y: 60 },
  ];
  protected readonly scatterX = (d: { x: number; y: number }): number => d.x;
  protected readonly scatterY = (d: { x: number; y: number }): number => d.y;
  protected readonly priceFmt = (v: unknown): string => '$' + Number(v);

  protected readonly chartStateData: readonly number[] = [8, 12, 14, 9, 18, 22, 25, 19, 16, 24, 28, 32];
  protected readonly chartState = createManualState<readonly number[]>();

  protected showSkeleton(): void { this.chartState.reset(); this.chartState.set('loading'); }
  protected showSuccess(): void { this.chartState.setSuccess(this.chartStateData); }
  protected showEmpty(): void { this.chartState.reset(); this.chartState.setSuccess([]); }
  protected showError(): void { this.chartState.reset(); this.chartState.setError(new Error('Telemetry feed offline')); }
}
