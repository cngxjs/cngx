// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/interactive/disclosure-demo/disclosure-demo.story.ts#section[0] "Basic — Uncontrolled"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxDisclosure } from '@cngx/common';

@Component({
  selector: 'app-disclosure-basic-uncontrolled',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxDisclosure],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <button cngxDisclosure #d="cngxDisclosure" [controls]="'faq-1'" class="sort-btn">
        {{ d.opened() ? 'Collapse' : 'Expand' }} answer
      </button>
      @if (d.opened()) {
        <div id="faq-1" style="padding: 0.75rem; margin-top: 0.5rem; border-left: 3px solid var(--interactive, #f5a623); background: var(--cngx-surface-alt, #f9fafb);">
          <p style="margin: 0; font-size: 0.875rem;">This content is revealed by the disclosure trigger above.</p>
        </div>
      }
      <div class="status-row" style="margin-top: 0.5rem;">
        <span class="status-badge" [class.active]="d.opened()">{{ d.opened() ? 'expanded' : 'collapsed' }}</span>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class DisclosureBasicUncontrolled {
  protected readonly _exTitle: string = 'Disclosure';
  protected readonly _exDescription: string = 'Generic expand/collapse atom. Manages aria-expanded, keyboard interaction (Enter, Space, click), and controlled+uncontrolled state. Usable for accordions, FAQs, nav groups, collapsible panels.';
  protected readonly _exSectionTitle: string = 'Basic — Uncontrolled';
  protected readonly _exSubtitle: string = 'Click the trigger or press <strong>Enter</strong>/<strong>Space</strong> to toggle. <code>aria-expanded</code> and <code>aria-controls</code> are set automatically.';
  protected readonly _exTs: string = `import { CngxDisclosure } from '@cngx/common';

protected readonly controlledOpen = signal(false);`;
  protected readonly _exHtml: string = `<button cngxDisclosure #d="cngxDisclosure" [controls]="'faq-1'" class="sort-btn">
  {{ d.opened() ? 'Collapse' : 'Expand' }} answer
</button>
@if (d.opened()) {
  <div id="faq-1" style="padding: 0.75rem; margin-top: 0.5rem; border-left: 3px solid var(--interactive, #f5a623); background: var(--cngx-surface-alt, #f9fafb);">
    <p style="margin: 0; font-size: 0.875rem;">This content is revealed by the disclosure trigger above.</p>
  </div>
}
<div class="status-row" style="margin-top: 0.5rem;">
  <span class="status-badge" [class.active]="d.opened()">{{ d.opened() ? 'expanded' : 'collapsed' }}</span>
</div>`;
  protected readonly controlledOpen = signal(false);
}
