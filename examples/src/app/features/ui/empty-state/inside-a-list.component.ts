// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: examples/stories/ui/empty-state-demo/empty-state-demo.story.ts#section[4] "Inside a List"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxEmptyState } from '@cngx/ui/empty-state';

@Component({
  selector: 'app-empty-state-inside-a-list',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxEmptyState],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div style="border:1px solid var(--cngx-color-border);border-radius:8px;min-height:120px;display:flex;align-items:center;justify-content:center">
        @if (listItems().length === 0) {
          <cngx-empty-state
            title="All tasks completed"
            description="Nothing left to do. Enjoy your day!">
            <svg cngxEmptyStateIcon width="48" height="48" viewBox="0 0 24 24" fill="none"
                 stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14" />
              <polyline points="22 4 12 14.01 9 11.01" />
            </svg>
          </cngx-empty-state>
        }
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class EmptyStateInsideAList {
  protected readonly _exTitle: string = 'Empty State';
  protected readonly _exDescription: string = 'Universal empty-state atom for grids, tables, lists, and dashboards. Communicates why a view is empty and what the user can do next. Shows a default icon when none is projected.';
  protected readonly _exSectionTitle: string = 'Inside a List';
  protected readonly _exSubtitle: string = 'Replaces list content when empty. Works with any container — no special wiring needed.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'molecule' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'design' }, { dim: 'artifact', value: 'standalone' }, { dim: 'focus', value: 'visual-variants' }, { dim: 'focus', value: 'composition' }];
  protected readonly _exTs: string = `import { CngxEmptyState } from '@cngx/ui/empty-state';

protected listItems = signal<string[]>([]);`;
  protected readonly _exHtml: string = `<div style="border:1px solid var(--cngx-color-border);border-radius:8px;min-height:120px;display:flex;align-items:center;justify-content:center">
  @if (listItems().length === 0) {
    <cngx-empty-state
      title="All tasks completed"
      description="Nothing left to do. Enjoy your day!">
      <svg cngxEmptyStateIcon width="48" height="48" viewBox="0 0 24 24" fill="none"
           stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
        <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14" />
        <polyline points="22 4 12 14.01 9 11.01" />
      </svg>
    </cngx-empty-state>
  }
</div>`;
  protected showTable = signal(true);
    protected tableItems = signal<string[]>([]);
    protected cardItems = signal<string[]>([]);
    protected listItems = signal<string[]>([]);
}
