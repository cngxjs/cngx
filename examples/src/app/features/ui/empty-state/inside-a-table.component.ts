// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/ui/empty-state-demo/empty-state-demo.story.ts#section[3] "Inside a Table"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxEmptyState } from '@cngx/ui/empty-state';

@Component({
  selector: 'app-empty-state-inside-a-table',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxEmptyState],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="table-wrap">
        <table class="demo-table">
          <thead>
            <tr>
              <th>Name</th>
              <th>Room</th>
              <th>Status</th>
            </tr>
          </thead>
          <tbody>
            @if (tableItems().length === 0) {
              <tr>
                <td colspan="3" style="padding:0">
                  <cngx-empty-state
                    title="No patients found"
                    description="Adjust filters or add a new patient.">
                    <svg cngxEmptyStateIcon width="40" height="40" viewBox="0 0 24 24" fill="none"
                         stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                      <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
                      <circle cx="9" cy="7" r="4" />
                      <line x1="17" y1="11" x2="23" y2="11" />
                    </svg>
                    <button cngxEmptyStateAction class="chip">Add patient</button>
                  </cngx-empty-state>
                </td>
              </tr>
            }
          </tbody>
        </table>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class EmptyStateInsideATable {
  protected readonly _exTitle: string = 'Empty State';
  protected readonly _exDescription: string = 'Universal empty-state atom for grids, tables, lists, and dashboards. Communicates why a view is empty and what the user can do next. Shows a default icon when none is projected.';
  protected readonly _exSectionTitle: string = 'Inside a Table';
  protected readonly _exSubtitle: string = 'Replaces table body when no rows match. The table header stays visible for context.';
  protected readonly _exTs: string = `import { CngxEmptyState } from '@cngx/ui/empty-state';

protected showTable = signal(true);
protected tableItems = signal<string[]>([]);
protected cardItems = signal<string[]>([]);
protected listItems = signal<string[]>([]);`;
  protected readonly _exHtml: string = `<div class="table-wrap">
  <table class="demo-table">
    <thead>
      <tr>
        <th>Name</th>
        <th>Room</th>
        <th>Status</th>
      </tr>
    </thead>
    <tbody>
      @if (tableItems().length === 0) {
        <tr>
          <td colspan="3" style="padding:0">
            <cngx-empty-state
              title="No patients found"
              description="Adjust filters or add a new patient.">
              <svg cngxEmptyStateIcon width="40" height="40" viewBox="0 0 24 24" fill="none"
                   stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
                <circle cx="9" cy="7" r="4" />
                <line x1="17" y1="11" x2="23" y2="11" />
              </svg>
              <button cngxEmptyStateAction class="chip">Add patient</button>
            </cngx-empty-state>
          </td>
        </tr>
      }
    </tbody>
  </table>
</div>`;
  protected showTable = signal(true);
    protected tableItems = signal<string[]>([]);
    protected cardItems = signal<string[]>([]);
    protected listItems = signal<string[]>([]);
}
