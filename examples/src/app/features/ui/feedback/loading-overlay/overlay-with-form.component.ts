// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/ui/feedback/loading-overlay-demo/loading-overlay-demo.story.ts#section[0] "Overlay with Form"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxLoadingOverlay } from '@cngx/ui/feedback';

@Component({
  selector: 'app-loading-overlay-overlay-with-form',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxLoadingOverlay],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <button (click)="handleLoad()" class="chip" style="margin-bottom:16px">
        {{ isLoading() ? 'Saving...' : 'Save (3s)' }}
      </button>

      <cngx-loading-overlay [loading]="isLoading()" label="Saving form">
        <div style="border:1px solid var(--cngx-color-border,#ddd);border-radius:8px;padding:24px;display:flex;flex-direction:column;gap:12px">
          <input placeholder="Name" style="padding:8px 12px;border:1px solid var(--cngx-color-border,#ddd);border-radius:6px" />
          <input placeholder="Email" style="padding:8px 12px;border:1px solid var(--cngx-color-border,#ddd);border-radius:6px" />
          <button type="button" class="chip">A button inside the overlay</button>
        </div>
      </cngx-loading-overlay>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class LoadingOverlayOverlayWithForm {
  protected readonly _exTitle: string = 'Loading Overlay';
  protected readonly _exDescription: string = 'Content container that blocks interaction with inert, renders backdrop + spinner, and manages focus save/restore.';
  protected readonly _exSectionTitle: string = 'Overlay with Form';
  protected readonly _exSubtitle: string = 'Click "Save" to overlay the form. Focus inside is saved and restored after loading completes.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'molecule' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'design' }, { dim: 'audience', value: 'a11y' }, { dim: 'artifact', value: 'standalone' }, { dim: 'focus', value: 'visual-variants' }, { dim: 'focus', value: 'async-state' }, { dim: 'focus', value: 'a11y-pattern' }];
  protected readonly _exTs: string = `import { CngxLoadingOverlay } from '@cngx/ui/feedback';

protected readonly isLoading = signal(false);
protected handleLoad(): void {
  this.isLoading.set(true);
  setTimeout(() => this.isLoading.set(false), 3000);
}`;
  protected readonly _exHtml: string = `<button (click)="handleLoad()" class="chip" style="margin-bottom:16px">
  {{ isLoading() ? 'Saving...' : 'Save (3s)' }}
</button>

<cngx-loading-overlay [loading]="isLoading()" label="Saving form">
  <div style="border:1px solid var(--cngx-color-border,#ddd);border-radius:8px;padding:24px;display:flex;flex-direction:column;gap:12px">
    <input placeholder="Name" style="padding:8px 12px;border:1px solid var(--cngx-color-border,#ddd);border-radius:6px" />
    <input placeholder="Email" style="padding:8px 12px;border:1px solid var(--cngx-color-border,#ddd);border-radius:6px" />
    <button type="button" class="chip">A button inside the overlay</button>
  </div>
</cngx-loading-overlay>`;
  protected readonly isLoading = signal(false);

    protected handleLoad(): void {
      this.isLoading.set(true);
      setTimeout(() => this.isLoading.set(false), 3000);
    }
}
