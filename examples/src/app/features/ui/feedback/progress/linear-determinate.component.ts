// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: examples/stories/ui/feedback/progress-demo/progress-demo.story.ts#section[1] "Linear — Determinate"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxProgress } from '@cngx/ui/feedback';

@Component({
  selector: 'app-progress-linear-determinate',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxProgress],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <button (click)="startDeterminate()" class="chip" style="margin-bottom:16px">
        {{ progress() !== undefined ? progress() + '%' : 'Start Upload' }}
      </button>
      @if (progress() !== undefined) {
        <cngx-progress [progress]="progress()" [showLabel]="true" label="File upload" />
      }
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class ProgressLinearDeterminate {
  protected readonly _exTitle: string = 'Progress';
  protected readonly _exDescription: string = 'Determinate/indeterminate progress indicator. Linear bar or circular variant. CSS transition smoothing for jumpy updates.';
  protected readonly _exSectionTitle: string = 'Linear — Determinate';
  protected readonly _exSubtitle: string = 'Click to simulate upload progress (0-100% in steps of 10).';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'atom' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'design' }, { dim: 'audience', value: 'a11y' }, { dim: 'artifact', value: 'standalone' }, { dim: 'focus', value: 'visual-variants' }, { dim: 'focus', value: 'async-state' }, { dim: 'focus', value: 'a11y-pattern' }];
  protected readonly _exTs: string = `import { CngxProgress } from '@cngx/ui/feedback';

protected readonly progress = signal<number | undefined>(undefined);
private interval: ReturnType<typeof setInterval> | undefined;
protected startDeterminate(): void {
  this.progress.set(0);
  clearInterval(this.interval);
  this.interval = setInterval(() => {
    const p = this.progress() ?? 0;
    if (p >= 100) {
      clearInterval(this.interval);
      this.progress.set(undefined);
      return;
    }
    this.progress.set(p + 10);
  }, 500);
}`;
  protected readonly _exHtml: string = `<button (click)="startDeterminate()" class="chip" style="margin-bottom:16px">
  {{ progress() !== undefined ? progress() + '%' : 'Start Upload' }}
</button>
@if (progress() !== undefined) {
  <cngx-progress [progress]="progress()" [showLabel]="true" label="File upload" />
}`;
  protected readonly progress = signal<number | undefined>(undefined);
    private interval: ReturnType<typeof setInterval> | undefined;

    protected startDeterminate(): void {
      this.progress.set(0);
      clearInterval(this.interval);
      this.interval = setInterval(() => {
        const p = this.progress() ?? 0;
        if (p >= 100) {
          clearInterval(this.interval);
          this.progress.set(undefined);
          return;
        }
        this.progress.set(p + 10);
      }, 500);
    }
}
