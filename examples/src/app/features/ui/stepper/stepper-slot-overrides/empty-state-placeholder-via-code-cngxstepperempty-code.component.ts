// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: examples/stories/ui/stepper/stepper-slot-overrides-demo/stepper-slot-overrides-demo.story.ts#section[5] "Empty-state placeholder via <code>*cngxStepperEmpty</code>"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxStep, CngxStepContent, type CngxStepperCommitAction, CngxStepperEmpty } from '@cngx/common/stepper';
import { CngxStepper } from '@cngx/ui/stepper';

@Component({
  selector: 'app-stepper-slot-overrides-empty-state-placeholder-via-code-cngxstepperempty-code',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxStepper, CngxStep, CngxStepperEmpty, CngxStepContent],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <button
        type="button"
        class="chip"
        style="margin-bottom:var(--demo-grid-gap, 12px)"
        (click)="hasSteps.update((v) => !v)"
      >
        {{ hasSteps() ? 'Unregister all steps' : 'Register steps' }}
      </button>
      <cngx-stepper [(activeStepIndex)]="active" aria-label="Slot-overrides — empty state">
        <ng-template cngxStepperEmpty>
          <div style="padding:24px;text-align:center;color:var(--mat-sys-on-surface-variant, #666)">
            <strong>No steps configured yet</strong>
            <p style="margin:6px 0 0;font-size:0.9em">Steps will appear here once the wizard config loads.</p>
          </div>
        </ng-template>
        @if (hasSteps()) {
          <div cngxStep label="One">
            <ng-template cngxStepContent>
              <p>Step 1.</p>
            </ng-template>
          </div>
          <div cngxStep label="Two">
            <ng-template cngxStepContent>
              <p>Step 2.</p>
            </ng-template>
          </div>
        }
      </cngx-stepper>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class StepperSlotOverridesEmptyStatePlaceholderViaCodeCngxstepperemptyCode {
  protected readonly _exTitle: string = 'Stepper — slot overrides';
  protected readonly _exDescription: string = 'Override every visual region inside <code>&lt;cngx-stepper&gt;</code> via the six new slot directives — <code>*cngxStepIndicator</code>, <code>*cngxStepBadge</code>, <code>*cngxStepBusySpinner</code>, <code>*cngxStepRejection</code>, <code>*cngxStepGroupHeader</code>, <code>*cngxStepperEmpty</code>. Each slot ships a typed context object — destructure via <code>let-status="status"</code> / <code>let-failedIndex="failedIndex"</code> / <code>let-group="group"</code> / etc. The library renders sensible defaults; the slots are purely additive.';
  protected readonly _exSectionTitle: string = 'Empty-state placeholder via <code>*cngxStepperEmpty</code>';
  protected readonly _exSubtitle: string = 'When the registered step list is empty (e.g. async-loaded wizard with no data yet), the slot renders in place of the strip + panels. No context — render static markup or read injected services directly. Toggle the button below to register / unregister all steps and watch the slot swap in.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'organism' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'design' }, { dim: 'artifact', value: 'standalone' }, { dim: 'focus', value: 'composition' }, { dim: 'focus', value: 'visual-variants' }];
  protected readonly _exTs: string = `import { CngxStep, CngxStepContent, type CngxStepperCommitAction, CngxStepperEmpty } from '@cngx/common/stepper';
import { CngxStepper } from '@cngx/ui/stepper';

protected readonly active = signal(0);
protected readonly hasSteps = signal(true);`;
  protected readonly _exHtml: string = `<button
  type="button"
  class="chip"
  style="margin-bottom:var(--demo-grid-gap, 12px)"
  (click)="hasSteps.update((v) => !v)"
>
  {{ hasSteps() ? 'Unregister all steps' : 'Register steps' }}
</button>
<cngx-stepper [(activeStepIndex)]="active" aria-label="Slot-overrides — empty state">
  <ng-template cngxStepperEmpty>
    <div style="padding:24px;text-align:center;color:var(--mat-sys-on-surface-variant, #666)">
      <strong>No steps configured yet</strong>
      <p style="margin:6px 0 0;font-size:0.9em">Steps will appear here once the wizard config loads.</p>
    </div>
  </ng-template>
  @if (hasSteps()) {
    <div cngxStep label="One">
      <ng-template cngxStepContent>
        <p>Step 1.</p>
      </ng-template>
    </div>
    <div cngxStep label="Two">
      <ng-template cngxStepContent>
        <p>Step 2.</p>
      </ng-template>
    </div>
  }
</cngx-stepper>`;
  protected readonly active = signal(0);
    protected readonly busyAttempts = signal(0);
    protected readonly slowAttempts = signal(0);
    protected readonly hasSteps = signal(true);

    // Reject the first commit attempt onto step 2 ('Security'); succeed
    // on retry. Drives the *cngxStepRejection slot — presenter sets
    // lastFailedIndex on the rejection arm and clears it on the next success.
    protected readonly commitAction: CngxStepperCommitAction = (_from, to) => {
      if (to === 2 && this.busyAttempts() === 0) {
        this.busyAttempts.update((n) => n + 1);
        return Promise.reject(new Error('Security check failed — retry'));
      }
      return Promise.resolve(true);
    };

    // Slow commit so the busy-spinner slot stays visible long enough to
    // observe. Resolves after 800ms — drives the *cngxStepBusySpinner slot.
    protected readonly slowCommit: CngxStepperCommitAction = () => {
      this.slowAttempts.update((n) => n + 1);
      return new Promise((resolve) => setTimeout(() => resolve(true), 800));
    };
}
