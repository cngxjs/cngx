// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/ui/stepper/stepper-error-aggregation-demo/stepper-error-aggregation-demo.story.ts#section[0] "Per-step error badges"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxStep, CngxStepContent } from '@cngx/common/stepper';
import { CngxStepper } from '@cngx/ui/stepper';
import { CngxErrorAggregator, CngxErrorSource } from '@cngx/common/interactive';

@Component({
  selector: 'app-stepper-error-aggregation-per-step-error-badges',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxStepper, CngxStep, CngxStepContent, CngxErrorAggregator, CngxErrorSource],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <cngx-stepper [(activeStepIndex)]="active" aria-label="Validated wizard">
        <fieldset cngxErrorAggregator #profileAgg="cngxErrorAggregator" style="display:contents">
          <input cngxErrorSource="profile-name" [when]="profileInvalid()" hidden />
          <div cngxStep label="Profile" [errorAggregator]="profileAgg">
            <ng-template cngxStepContent>
              <p>Profile fields go here. Toggle "profile invalid" to flip the badge.</p>
            </ng-template>
          </div>
        </fieldset>
        <fieldset cngxErrorAggregator #addressAgg="cngxErrorAggregator" style="display:contents">
          <input cngxErrorSource="address-postal" [when]="addressInvalid()" hidden />
          <div cngxStep label="Address" [errorAggregator]="addressAgg">
            <ng-template cngxStepContent>
              <p>Address fields go here.</p>
            </ng-template>
          </div>
        </fieldset>
        <div cngxStep label="Confirm">
          <ng-template cngxStepContent><p>Final review.</p></ng-template>
        </div>
      </cngx-stepper>
      <div class="event-grid" style="margin-top:12px;gap:8px">
        <div class="event-row" style="gap:8px">
          <label><input type="checkbox" [checked]="profileInvalid()" (change)="profileInvalid.set($any($event.target).checked)" /> profile invalid</label>
          <label><input type="checkbox" [checked]="addressInvalid()" (change)="addressInvalid.set($any($event.target).checked)" /> address invalid</label>
        </div>
        <div class="event-row"><span class="event-label">Active step</span><span class="event-value">{{ active() }}</span></div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class StepperErrorAggregationPerStepErrorBadges {
  protected readonly _exTitle: string = 'Stepper — error aggregation';
  protected readonly _exDescription: string = 'Bind <code>[errorAggregator]</code> on a step to surface a badge + SR phrase whenever the aggregator opts to show errors. Compose <code>CngxErrorAggregator</code> on a fieldset; the step reads <code>shouldShow()</code> and <code>announcement()</code> reactively.';
  protected readonly _exSectionTitle: string = 'Per-step error badges';
  protected readonly _exSubtitle: string = 'Toggle the validity flags below — the step badge appears the moment <code>aggregator.shouldShow()</code> turns true. The descriptor span carries the announcement phrase for SR.';
  protected readonly _exTs: string = `import { CngxStep, CngxStepContent } from '@cngx/common/stepper';
import { CngxStepper } from '@cngx/ui/stepper';
import { CngxErrorAggregator, CngxErrorSource } from '@cngx/common/interactive';

protected readonly active = signal(0);
protected readonly profileInvalid = signal(true);
protected readonly addressInvalid = signal(false);`;
  protected readonly _exHtml: string = `<cngx-stepper [(activeStepIndex)]="active" aria-label="Validated wizard">
  <fieldset cngxErrorAggregator #profileAgg="cngxErrorAggregator" style="display:contents">
    <input cngxErrorSource="profile-name" [when]="profileInvalid()" hidden />
    <div cngxStep label="Profile" [errorAggregator]="profileAgg">
      <ng-template cngxStepContent>
        <p>Profile fields go here. Toggle "profile invalid" to flip the badge.</p>
      </ng-template>
    </div>
  </fieldset>
  <fieldset cngxErrorAggregator #addressAgg="cngxErrorAggregator" style="display:contents">
    <input cngxErrorSource="address-postal" [when]="addressInvalid()" hidden />
    <div cngxStep label="Address" [errorAggregator]="addressAgg">
      <ng-template cngxStepContent>
        <p>Address fields go here.</p>
      </ng-template>
    </div>
  </fieldset>
  <div cngxStep label="Confirm">
    <ng-template cngxStepContent><p>Final review.</p></ng-template>
  </div>
</cngx-stepper>`;
  protected readonly active = signal(0);
    protected readonly profileInvalid = signal(true);
    protected readonly addressInvalid = signal(false);
}
