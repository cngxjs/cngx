// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/ui/stepper/stepper-horizontal-demo/stepper-horizontal-demo.story.ts#section[0] "Three-step wizard"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxStep, CngxStepContent } from '@cngx/common/stepper';
import { CngxStepper } from '@cngx/ui/stepper';

@Component({
  selector: 'app-stepper-horizontal-three-step-wizard',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxStepper, CngxStep],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <cngx-stepper [(activeStepIndex)]="active" aria-label="Order wizard">
        <div cngxStep label="Customer">
          <ng-template cngxStepContent>
            <p>Enter customer details — name, email, shipping address.</p>
          </ng-template>
        </div>
        <div cngxStep label="Payment">
          <ng-template cngxStepContent>
            <p>Choose a payment method and confirm billing address.</p>
          </ng-template>
        </div>
        <div cngxStep label="Review">
          <ng-template cngxStepContent>
            <p>Review the order before placing it.</p>
          </ng-template>
        </div>
      </cngx-stepper>
      <div class="event-grid" style="margin-top:12px;gap:8px">
        <div class="event-row">
          <button type="button" class="chip" (click)="handlePrev()">Previous</button>
          <button type="button" class="chip" (click)="handleNext()">Next</button>
        </div>
        <div class="event-row"><span class="event-label">Active step</span><span class="event-value">{{ active() }}</span></div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class StepperHorizontalThreeStepWizard {
  protected readonly _exTitle: string = 'Stepper — horizontal';
  protected readonly _exDescription: string = '<code>&lt;cngx-stepper&gt;</code> in horizontal orientation (default). Strip + panels stacked. Reactive ARIA + roving tabindex via composed host-directives.';
  protected readonly _exSectionTitle: string = 'Three-step wizard';
  protected readonly _exSubtitle: string = 'Bind <code>[(activeStepIndex)]</code> for two-way control. Click any step header or use ArrowLeft/ArrowRight to navigate. Tab leaves the strip.';
  protected readonly _exTs: string = `import { CngxStep, CngxStepContent } from '@cngx/common/stepper';
import { CngxStepper } from '@cngx/ui/stepper';

protected readonly active = signal(0);

protected handleNext(): void {
  this.active.update(i => Math.min(i + 1, 2));
}
protected handlePrev(): void {
  this.active.update(i => Math.max(i - 1, 0));
}`;
  protected readonly _exHtml: string = `<cngx-stepper [(activeStepIndex)]="active" aria-label="Order wizard">
  <div cngxStep label="Customer">
    <ng-template cngxStepContent>
      <p>Enter customer details — name, email, shipping address.</p>
    </ng-template>
  </div>
  <div cngxStep label="Payment">
    <ng-template cngxStepContent>
      <p>Choose a payment method and confirm billing address.</p>
    </ng-template>
  </div>
  <div cngxStep label="Review">
    <ng-template cngxStepContent>
      <p>Review the order before placing it.</p>
    </ng-template>
  </div>
</cngx-stepper>`;
  protected readonly active = signal(0);

    protected handleNext(): void {
      this.active.update(i => Math.min(i + 1, 2));
    }
    protected handlePrev(): void {
      this.active.update(i => Math.max(i - 1, 0));
    }
}
