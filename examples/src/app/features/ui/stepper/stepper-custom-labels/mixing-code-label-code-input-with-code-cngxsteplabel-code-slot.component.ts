// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: examples/stories/ui/stepper/stepper-custom-labels-demo/stepper-custom-labels-demo.story.ts#section[0] "Mixing <code>[label]</code> Input with <code>cngxStepLabel</code> slot"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxStep, CngxStepContent, CngxStepLabel } from '@cngx/common/stepper';
import { CngxStepper } from '@cngx/ui/stepper';

@Component({
  selector: 'app-stepper-custom-labels-mixing-code-label-code-input-with-code-cngxsteplabel-code-slot',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxStepper, CngxStep, CngxStepContent, CngxStepLabel],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0) {
        <ul class="cngx-ex-tags" aria-label="Tags">
          @for (t of _exTags; track t.dim + ':' + t.value) {
            <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
          }
        </ul>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <cngx-stepper [(activeStepIndex)]="active" aria-label="Custom-label wizard">
        <div cngxStep label="Profile">
          <ng-template cngxStepContent>
            <p>This step uses the <code>[label]="Profile"</code> Input — default string rendering.</p>
          </ng-template>
        </div>
        <div cngxStep label="Notifications">
          <ng-template cngxStepLabel>
            <span aria-hidden="true">🔔</span>
            Notifications
            @if (notificationCount() > 0) {
              <span class="chip" style="padding:0 6px;font-size:0.7em">{{ notificationCount() }}</span>
            }
          </ng-template>
          <ng-template cngxStepContent>
            <p>This step's strip-button label is composed via <code>&lt;ng-template cngxStepLabel&gt;</code> — icon glyph + text + reactive counter chip.</p>
            <button type="button" class="chip" (click)="notificationCount.update((n) => n + 1)">+1 notification</button>
            <button type="button" class="chip" (click)="notificationCount.set(0)" style="margin-inline-start:8px">clear</button>
          </ng-template>
        </div>
        <div cngxStep label="Security">
          <ng-template cngxStepLabel>
            <span aria-hidden="true">🔒</span>
            Security
          </ng-template>
          <ng-template cngxStepContent>
            <p>Static icon + text composition. The <code>aria-hidden="true"</code> on the glyph keeps screen readers focused on the text.</p>
          </ng-template>
        </div>
        <div cngxStep label="Done">
          <ng-template cngxStepLabel>
            <span aria-hidden="true">✓</span>
            <strong>Done</strong>
          </ng-template>
          <ng-template cngxStepContent>
            <p>Bold-text override — slot accepts arbitrary inline markup, the strip button keeps its layout.</p>
          </ng-template>
        </div>
      </cngx-stepper>
      <div class="event-grid" style="margin-top:var(--demo-grid-gap, 12px)">
        <div class="event-row"><span class="event-label">Active step</span><span class="event-value">{{ active() }}</span></div>
        <div class="event-row"><span class="event-label">Notification count</span><span class="event-value">{{ notificationCount() }}</span></div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class StepperCustomLabelsMixingCodeLabelCodeInputWithCodeCngxsteplabelCodeSlot {
  protected readonly _exTitle: string = 'Stepper — custom labels';
  protected readonly _exDescription: string = 'Use <code>&lt;ng-template cngxStepLabel&gt;</code> to override the per-step label rendering with arbitrary content — icons, badges, counters, multi-line text. The first step uses the simple <code>[label]="..."</code> Input (default string rendering); the remaining steps use the slot template to demonstrate icon + text + counter composition. Both paths render inside the same strip button — the slot is purely additive.';
  protected readonly _exSectionTitle: string = 'Mixing <code>[label]</code> Input with <code>cngxStepLabel</code> slot';
  protected readonly _exSubtitle: string = 'Step 1 uses the plain <code>[label]</code> Input. Steps 2–4 project a <code>&lt;ng-template cngxStepLabel&gt;</code> with custom content — icon + text + reactive counter. The slot template wins over the Input on a per-step basis.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [{ dim: 'atomic-level', value: 'organism' }, { dim: 'audience', value: 'dev' }, { dim: 'audience', value: 'design' }, { dim: 'artifact', value: 'standalone' }, { dim: 'focus', value: 'composition' }, { dim: 'focus', value: 'visual-variants' }];
  protected readonly _exTs: string = `import { CngxStep, CngxStepContent, CngxStepLabel } from '@cngx/common/stepper';
import { CngxStepper } from '@cngx/ui/stepper';

protected readonly active = signal(0);
protected readonly notificationCount = signal(3);`;
  protected readonly _exHtml: string = `<cngx-stepper [(activeStepIndex)]="active" aria-label="Custom-label wizard">
  <div cngxStep label="Profile">
    <ng-template cngxStepContent>
      <p>This step uses the <code>[label]="Profile"</code> Input — default string rendering.</p>
    </ng-template>
  </div>
  <div cngxStep label="Notifications">
    <ng-template cngxStepLabel>
      <span aria-hidden="true">🔔</span>
      Notifications
      @if (notificationCount() > 0) {
        <span class="chip" style="padding:0 6px;font-size:0.7em">{{ notificationCount() }}</span>
      }
    </ng-template>
    <ng-template cngxStepContent>
      <p>This step's strip-button label is composed via <code>&lt;ng-template cngxStepLabel&gt;</code> — icon glyph + text + reactive counter chip.</p>
      <button type="button" class="chip" (click)="notificationCount.update((n) => n + 1)">+1 notification</button>
      <button type="button" class="chip" (click)="notificationCount.set(0)" style="margin-inline-start:8px">clear</button>
    </ng-template>
  </div>
  <div cngxStep label="Security">
    <ng-template cngxStepLabel>
      <span aria-hidden="true">🔒</span>
      Security
    </ng-template>
    <ng-template cngxStepContent>
      <p>Static icon + text composition. The <code>aria-hidden="true"</code> on the glyph keeps screen readers focused on the text.</p>
    </ng-template>
  </div>
  <div cngxStep label="Done">
    <ng-template cngxStepLabel>
      <span aria-hidden="true">✓</span>
      <strong>Done</strong>
    </ng-template>
    <ng-template cngxStepContent>
      <p>Bold-text override — slot accepts arbitrary inline markup, the strip button keeps its layout.</p>
    </ng-template>
  </div>
</cngx-stepper>`;
  protected readonly active = signal(0);
    protected readonly notificationCount = signal(3);
}
