// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/ui/stepper/stepper-vertical-demo/stepper-vertical-demo.story.ts#section[0] "Vertical sidebar layout"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxStep, CngxStepContent } from '@cngx/common/stepper';
import { CngxStepper } from '@cngx/ui/stepper';

@Component({
  selector: 'app-stepper-vertical-vertical-sidebar-layout',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxStepper, CngxStep, CngxStepContent],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <cngx-stepper [(activeStepIndex)]="active" [orientation]="'vertical'" aria-label="Account setup">
        <div cngxStep label="Profile">
          <ng-template cngxStepContent>
            <p>Set your display name and avatar.</p>
          </ng-template>
        </div>
        <div cngxStep label="Notifications">
          <ng-template cngxStepContent>
            <p>Choose which events should email you.</p>
          </ng-template>
        </div>
        <div cngxStep label="Security">
          <ng-template cngxStepContent>
            <p>Enable two-factor authentication.</p>
          </ng-template>
        </div>
        <div cngxStep label="Done">
          <ng-template cngxStepContent>
            <p>You are ready to go.</p>
          </ng-template>
        </div>
      </cngx-stepper>
      <div class="event-grid" style="margin-top:12px"><div class="event-row"><span class="event-label">Active step</span><span class="event-value">{{ active() }}</span></div></div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class StepperVerticalVerticalSidebarLayout {
  protected readonly _exTitle: string = 'Stepper — vertical';
  protected readonly _exDescription: string = '<code>[orientation]="\'vertical\'"</code> swaps the layout to a 2-column grid: strip in the sidebar, panels on the right. ArrowUp/ArrowDown for keyboard nav.';
  protected readonly _exSectionTitle: string = 'Vertical sidebar layout';
  protected readonly _exSubtitle: string = 'Same component, only <code>[orientation]</code> changes. The presenter forwards the value to <code>CngxRovingTabindex</code> so the keyboard semantics swap with the layout.';
  protected readonly _exTs: string = `import { CngxStep, CngxStepContent } from '@cngx/common/stepper';
import { CngxStepper } from '@cngx/ui/stepper';

protected readonly active = signal(0);`;
  protected readonly _exHtml: string = `<cngx-stepper [(activeStepIndex)]="active" [orientation]="'vertical'" aria-label="Account setup">
  <div cngxStep label="Profile">
    <ng-template cngxStepContent>
      <p>Set your display name and avatar.</p>
    </ng-template>
  </div>
  <div cngxStep label="Notifications">
    <ng-template cngxStepContent>
      <p>Choose which events should email you.</p>
    </ng-template>
  </div>
  <div cngxStep label="Security">
    <ng-template cngxStepContent>
      <p>Enable two-factor authentication.</p>
    </ng-template>
  </div>
  <div cngxStep label="Done">
    <ng-template cngxStepContent>
      <p>You are ready to go.</p>
    </ng-template>
  </div>
</cngx-stepper>`;
  protected readonly active = signal(0);
}
