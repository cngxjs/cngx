// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/ui/tabs/tab-group-vertical-demo/tab-group-vertical-demo.story.ts#section[0] "Vertical sidebar tabs"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxTab, CngxTabLabel, CngxTabContent } from '@cngx/common/tabs';
import { CngxTabGroup } from '@cngx/ui/tabs';

@Component({
  selector: 'app-tab-group-vertical-vertical-sidebar-tabs',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxTabGroup, CngxTab, CngxTabLabel, CngxTabContent],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exTags.length > 0 || _exUses.length > 0) {
        <div class="cngx-ex-meta">
          @if (_exTags.length > 0) {
            <ul class="cngx-ex-tags" aria-label="Tags">
              @for (t of _exTags; track t.dim + ':' + t.value) {
                <li class="cngx-ex-tag" [attr.data-dim]="t.dim" [attr.data-value]="t.value">{{ t.value }}</li>
              }
            </ul>
          }
          @if (_exUses.length > 0) {
            <p class="cngx-ex-uses"><span class="cngx-ex-uses__label">uses</span>
              @for (u of _exUses; track u; let last = $last) {
                <code>{{ u }}</code>@if (!last) {<span class="cngx-ex-uses__sep">, </span>}
              }
            </p>
          }
        </div>
      }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <cngx-tab-group orientation="vertical" [(activeIndex)]="active" aria-label="Vertical tabs">
        <div cngxTab [label]="'Overview'">
          <ng-template cngxTabLabel>Overview</ng-template>
          <ng-template cngxTabContent>
            <p>Overview content — top-level summary.</p>
          </ng-template>
        </div>
        <div cngxTab [label]="'Activity'">
          <ng-template cngxTabLabel>Activity</ng-template>
          <ng-template cngxTabContent>
            <p>Activity content — recent events, audit log.</p>
          </ng-template>
        </div>
        <div cngxTab [label]="'Members'">
          <ng-template cngxTabLabel>Members</ng-template>
          <ng-template cngxTabContent>
            <p>Members content — team roster, roles.</p>
          </ng-template>
        </div>
        <div cngxTab [label]="'Billing'">
          <ng-template cngxTabLabel>Billing</ng-template>
          <ng-template cngxTabContent>
            <p>Billing content — plan, invoices, payment method.</p>
          </ng-template>
        </div>
      </cngx-tab-group>
      <div class="event-grid" style="margin-top:12px;gap:8px">
        <div class="event-row"><span class="event-label">Active tab</span><span class="event-value">{{ active() }}</span></div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class TabGroupVerticalVerticalSidebarTabs {
  protected readonly _exTitle: string = 'Tabs — vertical';
  protected readonly _exDescription: string = 'Sidebar layout — <code>[orientation]="vertical"</code> renders a 2-column grid with a vertical strip and a panel column. Roving tabindex switches to ArrowUp / ArrowDown semantics automatically.';
  protected readonly _exSectionTitle: string = 'Vertical sidebar tabs';
  protected readonly _exSubtitle: string = 'Pass <code>orientation="vertical"</code>. <code>aria-orientation</code> on the tablist plus <code>data-orientation</code> on the wrapper drive the layout split.';
  protected readonly _exTags: readonly { dim: string; value: string }[] = [];
  protected readonly _exUses: readonly string[] = ['CngxTabGroup', 'CngxTab', 'CngxTabLabel', 'CngxTabContent'];
  protected readonly _exTs: string = `import { CngxTab, CngxTabLabel, CngxTabContent } from '@cngx/common/tabs';
import { CngxTabGroup } from '@cngx/ui/tabs';

protected readonly active = signal(0);`;
  protected readonly _exHtml: string = `<cngx-tab-group orientation="vertical" [(activeIndex)]="active" aria-label="Vertical tabs">
  <div cngxTab [label]="'Overview'">
    <ng-template cngxTabLabel>Overview</ng-template>
    <ng-template cngxTabContent>
      <p>Overview content — top-level summary.</p>
    </ng-template>
  </div>
  <div cngxTab [label]="'Activity'">
    <ng-template cngxTabLabel>Activity</ng-template>
    <ng-template cngxTabContent>
      <p>Activity content — recent events, audit log.</p>
    </ng-template>
  </div>
  <div cngxTab [label]="'Members'">
    <ng-template cngxTabLabel>Members</ng-template>
    <ng-template cngxTabContent>
      <p>Members content — team roster, roles.</p>
    </ng-template>
  </div>
  <div cngxTab [label]="'Billing'">
    <ng-template cngxTabLabel>Billing</ng-template>
    <ng-template cngxTabContent>
      <p>Billing content — plan, invoices, payment method.</p>
    </ng-template>
  </div>
</cngx-tab-group>`;
  protected readonly active = signal(0);
}
