// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/forms/mask-demo/mask-demo.story.ts#section[2] "Custom Tokens and Transform"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxInputMask } from '@cngx/forms/input';

@Component({
  selector: 'app-mask-custom-tokens-and-transform',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxInputMask],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <div class="demo-form">
        <div class="demo-field">
          <label class="demo-label">Hex Color (#HHHHHH)</label>
          <input [cngxInputMask]="'\\#HHHHHH'" [customTokens]="hexTokens"
            #hexMask="cngxInputMask" class="demo-input" />
          <div class="status-row">
            <span class="status-badge"
              [style.background-color]="hexMask.isComplete() ? hexMask.maskedValue() : 'transparent'"
              [style.color]="hexMask.isComplete() ? '#fff' : 'inherit'"
              [style.padding]="'2px 8px'"
            >{{ hexMask.maskedValue() }}</span>
          </div>
        </div>
        <div class="demo-field">
          <label class="demo-label">Uppercase Letters Only</label>
          <input cngxInputMask="AAAA-AAAA" [transform]="upper" #upMask="cngxInputMask" class="demo-input" />
          <div class="status-row">
            <span class="status-badge">{{ upMask.rawValue() }}</span>
          </div>
        </div>
      </div>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class MaskCustomTokensAndTransform {
  protected readonly _exTitle: string = 'Input Mask';
  protected readonly _exDescription: string = 'Pattern-based input masking with locale-aware presets, multi-pattern support, custom tokens, and Reactive Forms integration.';
  protected readonly _exSectionTitle: string = 'Custom Tokens and Transform';
  protected readonly _exSubtitle: string = 'Define custom token characters via <code>[customTokens]</code>. Use <code>[transform]</code> for global character transforms.';
  protected readonly _exTs: string = `import { CngxInputMask } from '@cngx/forms/input';

protected readonly maskPhoneRaw = signal('');
protected readonly maskDateRaw = signal('');
protected readonly maskCardRaw = signal('');
protected readonly maskCustomRaw = signal('');



protected readonly hexTokens = { H: { pattern: /[0-9a-fA-F]/, transform: (c: string) => c.toUpperCase() } };
protected readonly upper = (c: string) => c.toUpperCase();`;
  protected readonly _exHtml: string = `<div class="demo-form">
  <div class="demo-field">
    <label class="demo-label">Hex Color (#HHHHHH)</label>
    <input [cngxInputMask]="'\\\\#HHHHHH'" [customTokens]="hexTokens"
      #hexMask="cngxInputMask" class="demo-input" />
    <div class="status-row">
      <span class="status-badge"
        [style.background-color]="hexMask.isComplete() ? hexMask.maskedValue() : 'transparent'"
        [style.color]="hexMask.isComplete() ? '#fff' : 'inherit'"
        [style.padding]="'2px 8px'"
      >{{ hexMask.maskedValue() }}</span>
    </div>
  </div>
  <div class="demo-field">
    <label class="demo-label">Uppercase Letters Only</label>
    <input cngxInputMask="AAAA-AAAA" [transform]="upper" #upMask="cngxInputMask" class="demo-input" />
    <div class="status-row">
      <span class="status-badge">{{ upMask.rawValue() }}</span>
    </div>
  </div>
</div>`;
  protected readonly maskPhoneRaw = signal('');
    protected readonly maskDateRaw = signal('');
    protected readonly maskCardRaw = signal('');
    protected readonly maskCustomRaw = signal('');

  protected readonly hexTokens = { H: { pattern: /[0-9a-fA-F]/, transform: (c: string) => c.toUpperCase() } };
    protected readonly upper = (c: string) => c.toUpperCase();
}
