// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/common/data/data-source-demo/data-source-demo.story.ts#section[1] "Usage Pattern — Consumer Wires It Up"

import { ChangeDetectionStrategy, Component, computed, signal } from '@angular/core';
import { toSignal } from '@angular/core/rxjs-interop';
import { injectDataSource } from '@cngx/common';
import { PEOPLE, type Person } from '../../fixtures';

@Component({
  selector: 'app-data-source-usage-pattern-consumer-wires-it-up',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <pre class="code-block"><code>// In your component:
    private readonly raw   = signal(data);
    private readonly term  = signal('');
    private readonly sort  = signal&lt;SortState | null&gt;(null);

    private readonly processed = computed(() => &#123;
      let list = this.raw();
      const t = this.term();
      const s = this.sort();
      if (t) list = list.filter(p => p.name.includes(t));
      if (s) list = [...list].sort(/* ... */);
      return list;
    &#125;);

    private readonly ds   = injectDataSource(this.processed);
    protected readonly rows = toSignal(this.ds.connect(), &#123; initialValue: [] &#125;);</code></pre>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class DataSourceUsagePatternConsumerWiresItUp {
  protected readonly _exTitle: string = 'DataSource';
  protected readonly _exDescription: string = '';
  protected readonly _exSectionTitle: string = 'Usage Pattern — Consumer Wires It Up';
  protected readonly _exSubtitle: string = 'Pass a <code>computed()</code> as the signal to get sort + filter + search for free. The DataSource itself has zero logic — it just bridges whatever the signal emits.';
  protected readonly _exTs: string = `import { toSignal } from '@angular/core/rxjs-interop';
import { injectDataSource } from '@cngx/common';
import { PEOPLE, type Person } from '../../fixtures';

private readonly items = signal(PEOPLE);
private readonly ds = injectDataSource(this.items);

protected readonly rows = toSignal(this.ds.connect(), { initialValue: [] as Person[] });

// Paginated variant — page state owned by component signals
protected readonly pageIndex = signal(0);
protected readonly pageSize  = signal(3);

protected readonly paginated = computed((): Person[] =>
  PEOPLE.slice(
    this.pageIndex() * this.pageSize(),
    (this.pageIndex() + 1) * this.pageSize(),
  ),
);

protected readonly totalPeople = PEOPLE.length;

protected shuffle(): void {
  this.items.update((list) => [...list].sort(() => Math.random() - 0.5));
}

protected reset(): void {
  this.items.set(PEOPLE);
}

protected setPage(i: number): void {
  const max = Math.max(0, Math.ceil(PEOPLE.length / this.pageSize()) - 1);
  this.pageIndex.set(Math.max(0, Math.min(i, max)));
}

protected setPageSize(s: number): void {
  this.pageSize.set(s);
  this.pageIndex.set(0);
}`;
  protected readonly _exHtml: string = `  <pre class="code-block"><code>// In your component:
private readonly raw   = signal(data);
private readonly term  = signal('');
private readonly sort  = signal&lt;SortState | null&gt;(null);

private readonly processed = computed(() => &#123;
  let list = this.raw();
  const t = this.term();
  const s = this.sort();
  if (t) list = list.filter(p => p.name.includes(t));
  if (s) list = [...list].sort(/* ... */);
  return list;
&#125;);

private readonly ds   = injectDataSource(this.processed);
protected readonly rows = toSignal(this.ds.connect(), &#123; initialValue: [] &#125;);</code></pre>`;
  private readonly items = signal(PEOPLE);
    private readonly ds = injectDataSource(this.items);

    protected readonly rows = toSignal(this.ds.connect(), { initialValue: [] as Person[] });

    // Paginated variant — page state owned by component signals
    protected readonly pageIndex = signal(0);
    protected readonly pageSize  = signal(3);

    protected readonly paginated = computed((): Person[] =>
      PEOPLE.slice(
        this.pageIndex() * this.pageSize(),
        (this.pageIndex() + 1) * this.pageSize(),
      ),
    );

    protected readonly totalPeople = PEOPLE.length;

    protected shuffle(): void {
      this.items.update((list) => [...list].sort(() => Math.random() - 0.5));
    }

    protected reset(): void {
      this.items.set(PEOPLE);
    }

    protected setPage(i: number): void {
      const max = Math.max(0, Math.ceil(PEOPLE.length / this.pageSize()) - 1);
      this.pageIndex.set(Math.max(0, Math.min(i, max)));
    }

    protected setPageSize(s: number): void {
      this.pageSize.set(s);
      this.pageIndex.set(0);
    }
}
