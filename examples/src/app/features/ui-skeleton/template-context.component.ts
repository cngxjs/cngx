// @generated by scripts/generate-examples.mjs — do not edit manually
// Source: dev-app/src/app/demos/ui/skeleton-demo/skeleton-demo.story.ts#section[1] "Template Context"

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { CngxSkeletonContainer, CngxSkeletonPlaceholder } from '@cngx/ui';

@Component({
  selector: 'app-ui-skeleton-template-context',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [CngxSkeletonContainer, CngxSkeletonPlaceholder],
  template: `
    <header class="cngx-ex-intro">
      @if (_exTitle) { <h1>{{ _exTitle }}</h1> }
      @if (_exDescription) { <p [innerHTML]="_exDescription"></p> }
      @if (_exSectionTitle && _exSectionTitle !== _exTitle) { <h2>{{ _exSectionTitle }}</h2> }
      @if (_exSubtitle) { <p class="cngx-ex-hint" [innerHTML]="_exSubtitle"></p> }
    </header>
    <cngx-skeleton [loading]="true" [count]="5">
        <ng-template cngxSkeletonPlaceholder let-i let-first="first" let-last="last" let-count="count">
          <div style="display:flex;align-items:center;gap:8px;padding:8px 0;border-bottom:1px solid var(--cngx-border,#e0e0e0)">
            <span style="font-size:0.75rem;color:var(--text-muted,#999);width:24px">{{ i + 1 }}/{{ count }}</span>
            <div [style.height]="'12px'" [style.width]="first ? '70%' : last ? '40%' : '55%'"
              style="border-radius:4px;background:var(--cngx-surface-alt,#e0e0e0)"></div>
          </div>
        </ng-template>
        <p>Content here after loading</p>
      </cngx-skeleton>
    <details class="cngx-ex-code">
      <summary>TypeScript</summary>
      <pre><code>{{ _exTs }}</code></pre>
    </details>
    <details class="cngx-ex-code">
      <summary>Template</summary>
      <pre><code>{{ _exHtml }}</code></pre>
    </details>
  `,
})
export class UiSkeletonTemplateContext {
  protected readonly _exTitle: string = 'SkeletonContainer';
  protected readonly _exDescription: string = 'Skeleton loading container with placeholder template repetition and automatic loading/content switching.';
  protected readonly _exSectionTitle: string = 'Template Context';
  protected readonly _exSubtitle: string = 'The placeholder template receives <code>$implicit</code> (index), <code>count</code>, <code>first</code>, <code>last</code>. Use them to vary the skeleton appearance.';
  protected readonly _exTs: string = `import { CngxSkeletonContainer, CngxSkeletonPlaceholder } from '@cngx/ui';

protected readonly loading = signal(true);
protected toggleLoading(): void {
  this.loading.update(v => !v);
}`;
  protected readonly _exHtml: string = `<cngx-skeleton [loading]="true" [count]="5">
  <ng-template cngxSkeletonPlaceholder let-i let-first="first" let-last="last" let-count="count">
    <div style="display:flex;align-items:center;gap:8px;padding:8px 0;border-bottom:1px solid var(--cngx-border,#e0e0e0)">
      <span style="font-size:0.75rem;color:var(--text-muted,#999);width:24px">{{ i + 1 }}/{{ count }}</span>
      <div [style.height]="'12px'" [style.width]="first ? '70%' : last ? '40%' : '55%'"
        style="border-radius:4px;background:var(--cngx-surface-alt,#e0e0e0)"></div>
    </div>
  </ng-template>
  <p>Content here after loading</p>
</cngx-skeleton>`;
  protected readonly loading = signal(true);
    protected toggleLoading(): void {
      this.loading.update(v => !v);
    }
}
