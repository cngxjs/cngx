// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/forms/form-primitives-demo/form-primitives-demo.story.ts

import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../shared/example-card.component';
import { DocShellComponent } from '../../../shared/doc-shell.component';
import { form, schema, required } from '@angular/forms/signals';
import { ReactiveFormsModule } from '@angular/forms';
import { JsonPipe } from '@angular/common';
import { DestroyRef } from '@angular/core';
import { CngxFormField, CngxLabel, CngxFieldErrors, adaptFormControl } from '@cngx/forms/field';
import { CngxFormBridge } from '@cngx/forms/controls';
import { CngxToggle, CngxCheckbox, CngxChipInteraction, CngxRadioGroup, CngxRadio, CngxCheckboxGroup, CngxButtonToggleGroup, CngxButtonMultiToggleGroup, CngxButtonToggle, CngxChipGroup, CngxMultiChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';
import { createFormPrimitivesFormGroup } from '../_fixtures/form-primitives-form-group';

@Component({
  selector: 'app-form-primitives-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxFormField,
    CngxLabel,
    CngxFieldErrors,
    CngxToggle,
    CngxCheckbox,
    CngxRadioGroup,
    CngxRadio,
    CngxCheckboxGroup,
    ReactiveFormsModule,
    CngxFormBridge,
    JsonPipe,
    CngxChip,
    CngxChipInteraction,
    CngxButtonToggleGroup,
    CngxButtonMultiToggleGroup,
    CngxButtonToggle,
    CngxChipGroup,
    CngxMultiChipGroup,
    CngxChipInGroup,
  ],
  template: `
    <app-doc-shell title="Form Primitives — write the atom once, pick your forms style"
      description="Nine cngx form controls — toggle, checkbox, radio group, two flavours of checkbox/button-toggle/chip group, plus the standalone chip — bind to whatever forms paradigm your app uses. Drop them into <cngx-form-field [field]> for Signal Forms, or bind [formControl] for Reactive Forms. Same atom, same template. No CVA-per-control boilerplate, and switching paradigms later costs nothing in the atom layer."
      overview="<p><strong>What you get.</strong> One atom layer that fits any forms style. Pick a control (e.g. <code>&lt;cngx-radio-group&gt;</code>) and decide at the template level whether you bind it through Signal Forms (<code>&lt;cngx-form-field [field]=&quot;f.payment&quot;&gt;</code>) or Reactive Forms (<code>[formControl]=&quot;payment&quot;</code>). The atom does not change. ARIA wiring (<code>aria-invalid</code>, <code>aria-describedby</code>, <code>aria-required</code>, <code>aria-busy</code>), focus tracking, and touch-on-blur all light up automatically through the surrounding form-field or bridge.</p><p><strong>Why this matters.</strong> Most component libraries make you choose a single forms paradigm and pay for it: Material ships its own CVA per control, hand-rolled controls require a CVA each, headless atoms (CDK) give you nothing for forms integration. cngx atoms stay forms-agnostic at the brain level. One bridge in <code>@cngx/forms/controls</code> covers Reactive Forms for every value-bearing atom; the same atom drops into <code>&lt;cngx-form-field [field]&gt;</code> for Signal Forms with no extra wiring. If your team migrates RF → Signal Forms (or vice versa) later, the atom code does not move.</p><p><strong>What you do NOT write.</strong> No <code>ControlValueAccessor</code> on the atom. No bridge directive on the consumer side. No per-atom CVA boilerplate. No conditional template logic for &quot;Signal Forms vs RF&quot; — both paradigms target the same atom shape.</p><p><strong>How it works (briefly).</strong> Each atom exposes its value through a small contract token (<code>CNGX_CONTROL_VALUE</code>). The Reactive Forms bridge (<code>CngxFormBridge</code>) and the Signal Forms field-presenter (<code>&lt;cngx-form-field&gt;</code>) both read that token and adapt to their paradigm. Two consumer paths, one atom layer. See the README at <code>projects/forms/controls/README.md</code> for the full architecture.</p><p><strong>Note on <code>CngxChipInput</code>.</strong> The tokenizer chip-input does not fit this bridge — it emits <code>tokenCreated</code> events instead of owning a <code>value</code> model (the consumer holds the chip list). A dedicated bridge ships in a follow-up. Section 3 below stays as a placeholder.</p>"
      [apiComponents]="['CngxFormBridge', 'CngxFormField', 'CngxToggle', 'CngxCheckbox', 'CngxChipInteraction', 'CngxRadioGroup', 'CngxRadio', 'CngxCheckboxGroup', 'CngxButtonToggleGroup', 'CngxButtonMultiToggleGroup', 'CngxChipGroup', 'CngxMultiChipGroup', 'CngxChipInGroup']">
      <app-example-card title="Signal Forms — drop the atom into <cngx-form-field>"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <div style="display:grid;gap:16px;max-width:480px">
    <cngx-form-field [field]="sfForm.notifications">
      <label cngxLabel>Benachrichtigungen</label>
      <cngx-toggle [(value)]="sfForm.notifications().value">
        E-Mails empfangen
      </cngx-toggle>
      <cngx-field-errors />
    </cngx-form-field>

    <cngx-form-field [field]="sfForm.terms">
      <label cngxLabel>Bedingungen</label>
      <cngx-checkbox [(value)]="sfForm.terms().value">
        Ich akzeptiere die Bedingungen
      </cngx-checkbox>
      <cngx-field-errors />
    </cngx-form-field>

    <cngx-form-field [field]="sfForm.payment">
      <label cngxLabel>Zahlungsart</label>
      <cngx-radio-group [(value)]="sfForm.payment().value" name="sf-payment">
        <cngx-radio value="card">Karte</cngx-radio>
        <cngx-radio value="cash">Bar</cngx-radio>
        <cngx-radio value="invoice">Rechnung</cngx-radio>
      </cngx-radio-group>
      <cngx-field-errors />
    </cngx-form-field>

    <cngx-form-field [field]="sfForm.notificationChannels">
      <label cngxLabel>Kanäle</label>
      <cngx-checkbox-group
        [(selectedValues)]="sfForm.notificationChannels().value"
        [allValues]="channelOptions"
      >
        @for (opt of channelOptions; track opt) {
          <cngx-checkbox
            [value]="sfForm.notificationChannels().value().includes(opt)"
            (valueChange)="$event
              ? sfForm.notificationChannels().value.update((v) => [...v, opt])
              : sfForm.notificationChannels().value.update((v) => v.filter((x) => x !== opt))"
          >{{ opt }}</cngx-checkbox>
        }
      </cngx-checkbox-group>
      <cngx-field-errors />
    </cngx-form-field>

    <div class="button-row" style="margin-top:8px;display:flex;gap:8px">
      <button type="button" class="chip" (click)="handleSfValidate()">Validate (mark touched)</button>
      <button type="button" class="chip" (click)="handleSfReset()">Reset</button>
    </div>

    <div class="event-grid" style="margin-top:8px">
      <div class="event-row">
        <span class="event-label">Form valid</span>
        <span class="event-value">{{ sfForm().valid() ? 'yes' : 'no' }}</span>
      </div>
      <div class="event-row">
        <span class="event-label">Errors visible</span>
        <span class="event-value">
          terms: {{ sfForm.terms().touched() && sfForm.terms().invalid() ? 'shown' : '—' }},
          payment: {{ sfForm.payment().touched() && sfForm.payment().invalid() ? 'shown' : '—' }},
          channels: {{ sfForm.notificationChannels().touched() && sfForm.notificationChannels().invalid() ? 'shown' : '—' }}
        </span>
      </div>
    </div>
  </div>
      </app-example-card>
      <app-example-card title="Reactive Forms — same atom, just bind [formControl]"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <form [formGroup]="rfForm" style="display:grid;gap:16px;max-width:560px">
    <!-- Boolean atom (no validator → bare bridge) -->
    <cngx-toggle [formControlName]="'notifications'">
      Benachrichtigungen
    </cngx-toggle>

    <!-- Required: wrap in cngx-form-field via adaptFormControl so per-atom errors render -->
    <cngx-form-field [field]="rfTermsField">
      <label cngxLabel>Bedingungen</label>
      <cngx-checkbox [formControlName]="'terms'">
        Ich akzeptiere die Bedingungen
      </cngx-checkbox>
      <cngx-field-errors />
    </cngx-form-field>

    <!-- Boolean chip (no validator → bare bridge) -->
    <cngx-chip cngxChipInteraction
      [formControlName]="'featured'"
      [value]="'feat'"
    >Featured</cngx-chip>

    <!-- Required scalar group: wrapped for per-atom error rendering -->
    <cngx-form-field [field]="rfPaymentField">
      <label cngxLabel>Zahlungsart</label>
      <cngx-radio-group [formControlName]="'payment'" name="rf-payment">
        @for (opt of paymentOptions; track opt) {
          <cngx-radio [value]="opt">{{ opt }}</cngx-radio>
        }
      </cngx-radio-group>
      <cngx-field-errors />
    </cngx-form-field>

    <!-- Non-required scalar group → bare bridge -->
    <cngx-button-toggle-group [formControlName]="'view'" label="Ansicht">
      @for (opt of viewOptions; track opt) {
        <button cngxButtonToggle [value]="opt">{{ opt }}</button>
      }
    </cngx-button-toggle-group>

    <cngx-chip-group [formControlName]="'size'" label="Größe">
      @for (opt of sizeOptions; track opt) {
        <cngx-chip cngxChipInGroup [value]="opt">{{ opt }}</cngx-chip>
      }
    </cngx-chip-group>

    <!-- Required multi-group: wrapped for per-atom error rendering -->
    <cngx-form-field [field]="rfChannelsField">
      <label cngxLabel>Kanäle</label>
      <cngx-checkbox-group
        [formControlName]="'notificationChannels'"
        [allValues]="channelOptions"
      >
        @for (opt of channelOptions; track opt) {
          <cngx-checkbox [value]="rfForm.controls.notificationChannels.value.includes(opt)">{{ opt }}</cngx-checkbox>
        }
      </cngx-checkbox-group>
      <cngx-field-errors />
    </cngx-form-field>

    <!-- Non-required multi-groups → bare bridge -->
    <cngx-button-multi-toggle-group [formControlName]="'filters'" label="Filter">
      @for (opt of filterOptions; track opt) {
        <button cngxButtonToggle [value]="opt">{{ opt }}</button>
      }
    </cngx-button-multi-toggle-group>

    <cngx-multi-chip-group [formControlName]="'tags'" label="Tags">
      @for (opt of tagOptions; track opt) {
        <cngx-chip cngxChipInGroup [value]="opt">{{ opt }}</cngx-chip>
      }
    </cngx-multi-chip-group>

    <div class="button-row" style="margin-top:8px;display:flex;gap:8px">
      <button type="button" class="chip" (click)="handleRfValidate()">Validate (mark touched)</button>
      <button type="button" class="chip" (click)="handleRfReset()">Reset</button>
    </div>
  </form>

  <div class="event-grid" style="margin-top:16px">
    <div class="event-row">
      <span class="event-label">FormGroup value</span>
      <span class="event-value">{{ rfForm.value | json }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">Valid</span>
      <span class="event-value">{{ rfForm.valid ? 'yes' : 'no' }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">Touched</span>
      <span class="event-value">{{ rfForm.touched ? 'yes' : 'no' }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">Errors visible</span>
      <span class="event-value">
        terms: {{ rfTermsField().touched() && rfTermsField().invalid() ? 'shown' : '—' }},
        payment: {{ rfPaymentField().touched() && rfPaymentField().invalid() ? 'shown' : '—' }},
        channels: {{ rfChannelsField().touched() && rfChannelsField().invalid() ? 'shown' : '—' }}
      </span>
    </div>
  </div>
      </app-example-card>
      <app-example-card title="CngxChipInput — coming in a follow-up"
        [subtitle]="_s2"
        [sourceHtml]="_srcHtml2"
        [sourceTs]="_srcTs2">
        
  <p style="color:var(--cngx-muted, #6b7280);font-style:italic">
    Demo for CngxChipInput Forms integration ships in Phase 7.x.
  </p>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class FormPrimitivesDemoComponent {
  protected readonly _s0 = 'You write <code>&lt;cngx-form-field [field]="f.payment"&gt;&lt;cngx-radio-group ...&gt;</code> and that is it. The form-field finds the atom on its own, projects ARIA onto the host, and shows validator messages through <code>&lt;cngx-field-errors&gt;</code>. Three fields are required (terms, payment, channels). Either tab into and out of a field to surface its error, or click <strong>Validate</strong> below to mark every required field touched at once. Pick a value (or check the box / select a chip) to see the error clear instantly.';
  protected readonly _s1 = 'A full <code>FormGroup</code> wired through <code>[formControlName]</code> on every kind of value-bearing atom: boolean (toggle, checkbox, chip), single-pick group (radio, button-toggle, chip-group), multi-pick group (checkbox-group, button-multi-toggle, multi-chip). Import <code>CngxFormBridge</code> in the component and the binding works. The three required atoms (terms, payment, channels) are wrapped in <code>&lt;cngx-form-field [field]&gt;</code> via <code>adaptFormControl</code> so error messages render per atom — exactly like the Signal Forms section above. Click <strong>Validate</strong> to mark every control touched at once; the readout shows the form-state and the live error visibility per field.';
  protected readonly _s2 = '<code>CngxChipInput</code> (the tokenizer that emits <code>tokenCreated</code> events as the user types) does not fit the same bridge as the nine controls above — it has no single <code>value</code> to bind because the consumer holds the chip list externally. A dedicated bridge ships in a follow-up so consumers can drop <code>&lt;input cngxChipInput formControlName="tags"&gt;</code> next to the existing <code>tokenCreated</code> output. Until then, wire the input manually as you would today.';
  protected readonly _srcHtml0 = `<div style="display:grid;gap:16px;max-width:480px">
    <cngx-form-field [field]="sfForm.notifications">
      <label cngxLabel>Benachrichtigungen</label>
      <cngx-toggle [(value)]="sfForm.notifications().value">
        E-Mails empfangen
      </cngx-toggle>
      <cngx-field-errors />
    </cngx-form-field>

    <cngx-form-field [field]="sfForm.terms">
      <label cngxLabel>Bedingungen</label>
      <cngx-checkbox [(value)]="sfForm.terms().value">
        Ich akzeptiere die Bedingungen
      </cngx-checkbox>
      <cngx-field-errors />
    </cngx-form-field>

    <cngx-form-field [field]="sfForm.payment">
      <label cngxLabel>Zahlungsart</label>
      <cngx-radio-group [(value)]="sfForm.payment().value" name="sf-payment">
        <cngx-radio value="card">Karte</cngx-radio>
        <cngx-radio value="cash">Bar</cngx-radio>
        <cngx-radio value="invoice">Rechnung</cngx-radio>
      </cngx-radio-group>
      <cngx-field-errors />
    </cngx-form-field>

    <cngx-form-field [field]="sfForm.notificationChannels">
      <label cngxLabel>Kanäle</label>
      <cngx-checkbox-group
        [(selectedValues)]="sfForm.notificationChannels().value"
        [allValues]="channelOptions"
      >
        @for (opt of channelOptions; track opt) {
          <cngx-checkbox
            [value]="sfForm.notificationChannels().value().includes(opt)"
            (valueChange)="$event
              ? sfForm.notificationChannels().value.update((v) => [...v, opt])
              : sfForm.notificationChannels().value.update((v) => v.filter((x) => x !== opt))"
          >{{ opt }}</cngx-checkbox>
        }
      </cngx-checkbox-group>
      <cngx-field-errors />
    </cngx-form-field>

    <div class="button-row" style="margin-top:8px;display:flex;gap:8px">
      <button type="button" class="chip" (click)="handleSfValidate()">Validate (mark touched)</button>
      <button type="button" class="chip" (click)="handleSfReset()">Reset</button>
    </div>

    <div class="event-grid" style="margin-top:8px">
      <div class="event-row">
        <span class="event-label">Form valid</span>
        <span class="event-value">{{ sfForm().valid() ? 'yes' : 'no' }}</span>
      </div>
      <div class="event-row">
        <span class="event-label">Errors visible</span>
        <span class="event-value">
          terms: {{ sfForm.terms().touched() && sfForm.terms().invalid() ? 'shown' : '—' }},
          payment: {{ sfForm.payment().touched() && sfForm.payment().invalid() ? 'shown' : '—' }},
          channels: {{ sfForm.notificationChannels().touched() && sfForm.notificationChannels().invalid() ? 'shown' : '—' }}
        </span>
      </div>
    </div>
  </div>`;
  protected readonly _srcTs0 = `import { form, schema, required } from '@angular/forms/signals';
import { ReactiveFormsModule } from '@angular/forms';
import { JsonPipe } from '@angular/common';
import { DestroyRef } from '@angular/core';
import { CngxFormField, CngxLabel, CngxFieldErrors, adaptFormControl } from '@cngx/forms/field';
import { CngxFormBridge } from '@cngx/forms/controls';
import { CngxToggle, CngxCheckbox, CngxChipInteraction, CngxRadioGroup, CngxRadio, CngxCheckboxGroup, CngxButtonToggleGroup, CngxButtonMultiToggleGroup, CngxButtonToggle, CngxChipGroup, CngxMultiChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';
import { createFormPrimitivesFormGroup } from '../_fixtures/form-primitives-form-group';


  private readonly destroyRef = inject(DestroyRef);

  // ── Signal Forms model ─────────────────────────────────────
  protected readonly sfModel = signal<{
    notifications: boolean;
    terms: boolean;
    payment: string;
    notificationChannels: string[];
  }>({
    notifications: false,
    terms: false,
    payment: '',
    notificationChannels: [],
  });

  protected readonly sfForm = form(this.sfModel, schema((root) => {
    required(root.terms, { message: 'Bitte zustimmen' });
    required(root.payment, { message: 'Wahl erforderlich' });
    required(root.notificationChannels, { message: 'Mindestens einen Kanal wählen' });
  }));

  // ── Reactive Forms group (all 9 atoms) ─────────────────────
  protected readonly rfForm = createFormPrimitivesFormGroup();

  // Per-field accessors so the RF section can wrap the required atoms in
  // <cngx-form-field> for symmetric error rendering. The bridge still
  // owns value flow via [formControl]; the form-field only projects ARIA
  // and renders <cngx-field-errors>.
  protected readonly rfTermsField = adaptFormControl(this.rfForm.controls.terms, 'terms', this.destroyRef);
  protected readonly rfPaymentField = adaptFormControl(this.rfForm.controls.payment, 'payment', this.destroyRef);
  protected readonly rfChannelsField = adaptFormControl(this.rfForm.controls.notificationChannels, 'notificationChannels', this.destroyRef);

  // Pool data for selection groups
  protected readonly paymentOptions = ['card', 'cash', 'invoice'];
  protected readonly viewOptions = ['grid', 'list', 'table'];
  protected readonly sizeOptions = ['sm', 'md', 'lg'];
  protected readonly channelOptions = ['email', 'sms', 'push'];
  protected readonly filterOptions = ['open', 'closed', 'archived'];
  protected readonly tagOptions = ['ng', 'rx', 'ts', 'cdk'];

  // ── Triggers ───────────────────────────────────────────────
  protected handleSfValidate(): void {
    // Mark every required SF field touched so cngx-field-errors renders.
    this.sfForm.terms().markAsTouched();
    this.sfForm.payment().markAsTouched();
    this.sfForm.notificationChannels().markAsTouched();
  }

  protected handleSfReset(): void {
    this.sfModel.set({ notifications: false, terms: false, payment: '', notificationChannels: [] });
  }

  protected handleRfValidate(): void {
    // Touching the raw FormControls fires TouchedChangeEvent, which
    // adaptFormControl now subscribes to — adapted accessors update
    // synchronously inside the subscribe callback. No accessor-side touch
    // call needed.
    Object.values(this.rfForm.controls).forEach((c) => c.markAsTouched());
  }

  protected handleRfReset(): void {
    this.rfForm.reset();
  }`;
  protected readonly _srcHtml1 = `<form [formGroup]="rfForm" style="display:grid;gap:16px;max-width:560px">
    <!-- Boolean atom (no validator → bare bridge) -->
    <cngx-toggle [formControlName]="'notifications'">
      Benachrichtigungen
    </cngx-toggle>

    <!-- Required: wrap in cngx-form-field via adaptFormControl so per-atom errors render -->
    <cngx-form-field [field]="rfTermsField">
      <label cngxLabel>Bedingungen</label>
      <cngx-checkbox [formControlName]="'terms'">
        Ich akzeptiere die Bedingungen
      </cngx-checkbox>
      <cngx-field-errors />
    </cngx-form-field>

    <!-- Boolean chip (no validator → bare bridge) -->
    <cngx-chip cngxChipInteraction
      [formControlName]="'featured'"
      [value]="'feat'"
    >Featured</cngx-chip>

    <!-- Required scalar group: wrapped for per-atom error rendering -->
    <cngx-form-field [field]="rfPaymentField">
      <label cngxLabel>Zahlungsart</label>
      <cngx-radio-group [formControlName]="'payment'" name="rf-payment">
        @for (opt of paymentOptions; track opt) {
          <cngx-radio [value]="opt">{{ opt }}</cngx-radio>
        }
      </cngx-radio-group>
      <cngx-field-errors />
    </cngx-form-field>

    <!-- Non-required scalar group → bare bridge -->
    <cngx-button-toggle-group [formControlName]="'view'" label="Ansicht">
      @for (opt of viewOptions; track opt) {
        <button cngxButtonToggle [value]="opt">{{ opt }}</button>
      }
    </cngx-button-toggle-group>

    <cngx-chip-group [formControlName]="'size'" label="Größe">
      @for (opt of sizeOptions; track opt) {
        <cngx-chip cngxChipInGroup [value]="opt">{{ opt }}</cngx-chip>
      }
    </cngx-chip-group>

    <!-- Required multi-group: wrapped for per-atom error rendering -->
    <cngx-form-field [field]="rfChannelsField">
      <label cngxLabel>Kanäle</label>
      <cngx-checkbox-group
        [formControlName]="'notificationChannels'"
        [allValues]="channelOptions"
      >
        @for (opt of channelOptions; track opt) {
          <cngx-checkbox [value]="rfForm.controls.notificationChannels.value.includes(opt)">{{ opt }}</cngx-checkbox>
        }
      </cngx-checkbox-group>
      <cngx-field-errors />
    </cngx-form-field>

    <!-- Non-required multi-groups → bare bridge -->
    <cngx-button-multi-toggle-group [formControlName]="'filters'" label="Filter">
      @for (opt of filterOptions; track opt) {
        <button cngxButtonToggle [value]="opt">{{ opt }}</button>
      }
    </cngx-button-multi-toggle-group>

    <cngx-multi-chip-group [formControlName]="'tags'" label="Tags">
      @for (opt of tagOptions; track opt) {
        <cngx-chip cngxChipInGroup [value]="opt">{{ opt }}</cngx-chip>
      }
    </cngx-multi-chip-group>

    <div class="button-row" style="margin-top:8px;display:flex;gap:8px">
      <button type="button" class="chip" (click)="handleRfValidate()">Validate (mark touched)</button>
      <button type="button" class="chip" (click)="handleRfReset()">Reset</button>
    </div>
  </form>

  <div class="event-grid" style="margin-top:16px">
    <div class="event-row">
      <span class="event-label">FormGroup value</span>
      <span class="event-value">{{ rfForm.value | json }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">Valid</span>
      <span class="event-value">{{ rfForm.valid ? 'yes' : 'no' }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">Touched</span>
      <span class="event-value">{{ rfForm.touched ? 'yes' : 'no' }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">Errors visible</span>
      <span class="event-value">
        terms: {{ rfTermsField().touched() && rfTermsField().invalid() ? 'shown' : '—' }},
        payment: {{ rfPaymentField().touched() && rfPaymentField().invalid() ? 'shown' : '—' }},
        channels: {{ rfChannelsField().touched() && rfChannelsField().invalid() ? 'shown' : '—' }}
      </span>
    </div>
  </div>`;
  protected readonly _srcTs1 = `import { form, schema, required } from '@angular/forms/signals';
import { ReactiveFormsModule } from '@angular/forms';
import { JsonPipe } from '@angular/common';
import { DestroyRef } from '@angular/core';
import { CngxFormField, CngxLabel, CngxFieldErrors, adaptFormControl } from '@cngx/forms/field';
import { CngxFormBridge } from '@cngx/forms/controls';
import { CngxToggle, CngxCheckbox, CngxChipInteraction, CngxRadioGroup, CngxRadio, CngxCheckboxGroup, CngxButtonToggleGroup, CngxButtonMultiToggleGroup, CngxButtonToggle, CngxChipGroup, CngxMultiChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';
import { createFormPrimitivesFormGroup } from '../_fixtures/form-primitives-form-group';


  private readonly destroyRef = inject(DestroyRef);

  // ── Signal Forms model ─────────────────────────────────────
  protected readonly sfModel = signal<{
    notifications: boolean;
    terms: boolean;
    payment: string;
    notificationChannels: string[];
  }>({
    notifications: false,
    terms: false,
    payment: '',
    notificationChannels: [],
  });

  protected readonly sfForm = form(this.sfModel, schema((root) => {
    required(root.terms, { message: 'Bitte zustimmen' });
    required(root.payment, { message: 'Wahl erforderlich' });
    required(root.notificationChannels, { message: 'Mindestens einen Kanal wählen' });
  }));

  // ── Reactive Forms group (all 9 atoms) ─────────────────────
  protected readonly rfForm = createFormPrimitivesFormGroup();

  // Per-field accessors so the RF section can wrap the required atoms in
  // <cngx-form-field> for symmetric error rendering. The bridge still
  // owns value flow via [formControl]; the form-field only projects ARIA
  // and renders <cngx-field-errors>.
  protected readonly rfTermsField = adaptFormControl(this.rfForm.controls.terms, 'terms', this.destroyRef);
  protected readonly rfPaymentField = adaptFormControl(this.rfForm.controls.payment, 'payment', this.destroyRef);
  protected readonly rfChannelsField = adaptFormControl(this.rfForm.controls.notificationChannels, 'notificationChannels', this.destroyRef);

  // Pool data for selection groups
  protected readonly paymentOptions = ['card', 'cash', 'invoice'];
  protected readonly viewOptions = ['grid', 'list', 'table'];
  protected readonly sizeOptions = ['sm', 'md', 'lg'];
  protected readonly channelOptions = ['email', 'sms', 'push'];
  protected readonly filterOptions = ['open', 'closed', 'archived'];
  protected readonly tagOptions = ['ng', 'rx', 'ts', 'cdk'];

  // ── Triggers ───────────────────────────────────────────────
  protected handleSfValidate(): void {
    // Mark every required SF field touched so cngx-field-errors renders.
    this.sfForm.terms().markAsTouched();
    this.sfForm.payment().markAsTouched();
    this.sfForm.notificationChannels().markAsTouched();
  }

  protected handleSfReset(): void {
    this.sfModel.set({ notifications: false, terms: false, payment: '', notificationChannels: [] });
  }

  protected handleRfValidate(): void {
    // Touching the raw FormControls fires TouchedChangeEvent, which
    // adaptFormControl now subscribes to — adapted accessors update
    // synchronously inside the subscribe callback. No accessor-side touch
    // call needed.
    Object.values(this.rfForm.controls).forEach((c) => c.markAsTouched());
  }

  protected handleRfReset(): void {
    this.rfForm.reset();
  }`;
  protected readonly _srcHtml2 = `<p style="color:var(--cngx-muted, #6b7280);font-style:italic">
    Demo for CngxChipInput Forms integration ships in Phase 7.x.
  </p>`;
  protected readonly _srcTs2 = `import { form, schema, required } from '@angular/forms/signals';
import { ReactiveFormsModule } from '@angular/forms';
import { JsonPipe } from '@angular/common';
import { DestroyRef } from '@angular/core';
import { CngxFormField, CngxLabel, CngxFieldErrors, adaptFormControl } from '@cngx/forms/field';
import { CngxFormBridge } from '@cngx/forms/controls';
import { CngxToggle, CngxCheckbox, CngxChipInteraction, CngxRadioGroup, CngxRadio, CngxCheckboxGroup, CngxButtonToggleGroup, CngxButtonMultiToggleGroup, CngxButtonToggle, CngxChipGroup, CngxMultiChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';
import { createFormPrimitivesFormGroup } from '../_fixtures/form-primitives-form-group';


  private readonly destroyRef = inject(DestroyRef);

  // ── Signal Forms model ─────────────────────────────────────
  protected readonly sfModel = signal<{
    notifications: boolean;
    terms: boolean;
    payment: string;
    notificationChannels: string[];
  }>({
    notifications: false,
    terms: false,
    payment: '',
    notificationChannels: [],
  });

  protected readonly sfForm = form(this.sfModel, schema((root) => {
    required(root.terms, { message: 'Bitte zustimmen' });
    required(root.payment, { message: 'Wahl erforderlich' });
    required(root.notificationChannels, { message: 'Mindestens einen Kanal wählen' });
  }));

  // ── Reactive Forms group (all 9 atoms) ─────────────────────
  protected readonly rfForm = createFormPrimitivesFormGroup();

  // Per-field accessors so the RF section can wrap the required atoms in
  // <cngx-form-field> for symmetric error rendering. The bridge still
  // owns value flow via [formControl]; the form-field only projects ARIA
  // and renders <cngx-field-errors>.
  protected readonly rfTermsField = adaptFormControl(this.rfForm.controls.terms, 'terms', this.destroyRef);
  protected readonly rfPaymentField = adaptFormControl(this.rfForm.controls.payment, 'payment', this.destroyRef);
  protected readonly rfChannelsField = adaptFormControl(this.rfForm.controls.notificationChannels, 'notificationChannels', this.destroyRef);

  // Pool data for selection groups
  protected readonly paymentOptions = ['card', 'cash', 'invoice'];
  protected readonly viewOptions = ['grid', 'list', 'table'];
  protected readonly sizeOptions = ['sm', 'md', 'lg'];
  protected readonly channelOptions = ['email', 'sms', 'push'];
  protected readonly filterOptions = ['open', 'closed', 'archived'];
  protected readonly tagOptions = ['ng', 'rx', 'ts', 'cdk'];

  // ── Triggers ───────────────────────────────────────────────
  protected handleSfValidate(): void {
    // Mark every required SF field touched so cngx-field-errors renders.
    this.sfForm.terms().markAsTouched();
    this.sfForm.payment().markAsTouched();
    this.sfForm.notificationChannels().markAsTouched();
  }

  protected handleSfReset(): void {
    this.sfModel.set({ notifications: false, terms: false, payment: '', notificationChannels: [] });
  }

  protected handleRfValidate(): void {
    // Touching the raw FormControls fires TouchedChangeEvent, which
    // adaptFormControl now subscribes to — adapted accessors update
    // synchronously inside the subscribe callback. No accessor-side touch
    // call needed.
    Object.values(this.rfForm.controls).forEach((c) => c.markAsTouched());
  }

  protected handleRfReset(): void {
    this.rfForm.reset();
  }`;

  private readonly destroyRef = inject(DestroyRef);

  // ── Signal Forms model ─────────────────────────────────────
  protected readonly sfModel = signal<{
    notifications: boolean;
    terms: boolean;
    payment: string;
    notificationChannels: string[];
  }>({
    notifications: false,
    terms: false,
    payment: '',
    notificationChannels: [],
  });

  protected readonly sfForm = form(this.sfModel, schema((root) => {
    required(root.terms, { message: 'Bitte zustimmen' });
    required(root.payment, { message: 'Wahl erforderlich' });
    required(root.notificationChannels, { message: 'Mindestens einen Kanal wählen' });
  }));

  // ── Reactive Forms group (all 9 atoms) ─────────────────────
  protected readonly rfForm = createFormPrimitivesFormGroup();

  // Per-field accessors so the RF section can wrap the required atoms in
  // <cngx-form-field> for symmetric error rendering. The bridge still
  // owns value flow via [formControl]; the form-field only projects ARIA
  // and renders <cngx-field-errors>.
  protected readonly rfTermsField = adaptFormControl(this.rfForm.controls.terms, 'terms', this.destroyRef);
  protected readonly rfPaymentField = adaptFormControl(this.rfForm.controls.payment, 'payment', this.destroyRef);
  protected readonly rfChannelsField = adaptFormControl(this.rfForm.controls.notificationChannels, 'notificationChannels', this.destroyRef);

  // Pool data for selection groups
  protected readonly paymentOptions = ['card', 'cash', 'invoice'];
  protected readonly viewOptions = ['grid', 'list', 'table'];
  protected readonly sizeOptions = ['sm', 'md', 'lg'];
  protected readonly channelOptions = ['email', 'sms', 'push'];
  protected readonly filterOptions = ['open', 'closed', 'archived'];
  protected readonly tagOptions = ['ng', 'rx', 'ts', 'cdk'];

  // ── Triggers ───────────────────────────────────────────────
  protected handleSfValidate(): void {
    // Mark every required SF field touched so cngx-field-errors renders.
    this.sfForm.terms().markAsTouched();
    this.sfForm.payment().markAsTouched();
    this.sfForm.notificationChannels().markAsTouched();
  }

  protected handleSfReset(): void {
    this.sfModel.set({ notifications: false, terms: false, payment: '', notificationChannels: [] });
  }

  protected handleRfValidate(): void {
    // Touching the raw FormControls fires TouchedChangeEvent, which
    // adaptFormControl now subscribes to — adapted accessors update
    // synchronously inside the subscribe callback. No accessor-side touch
    // call needed.
    Object.values(this.rfForm.controls).forEach((c) => c.markAsTouched());
  }

  protected handleRfReset(): void {
    this.rfForm.reset();
  }
  
}
