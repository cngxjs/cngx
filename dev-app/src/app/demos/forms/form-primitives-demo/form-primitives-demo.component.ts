// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/forms/form-primitives-demo/form-primitives-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../shared/example-card.component';
import { DocShellComponent } from '../../../shared/doc-shell.component';
import { form, schema, required } from '@angular/forms/signals';
import { ReactiveFormsModule } from '@angular/forms';
import { JsonPipe } from '@angular/common';
import { CngxFormField, CngxLabel, CngxFieldErrors } from '@cngx/forms/field';
import { CngxFormBridge } from '@cngx/forms/controls';
import { CngxToggle, CngxCheckbox, CngxChipInteraction, CngxRadioGroup, CngxRadio, CngxCheckboxGroup, CngxButtonToggleGroup, CngxButtonMultiToggleGroup, CngxButtonToggle, CngxChipGroup, CngxMultiChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';
import { createFormPrimitivesFormGroup } from '../_fixtures/form-primitives-form-group';

@Component({
  selector: 'app-form-primitives-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxFormField,
    CngxLabel,
    CngxFieldErrors,
    CngxToggle,
    CngxCheckbox,
    CngxRadioGroup,
    CngxRadio,
    CngxCheckboxGroup,
    ReactiveFormsModule,
    CngxFormBridge,
    JsonPipe,
    CngxChip,
    CngxChipInteraction,
    CngxButtonToggleGroup,
    CngxButtonMultiToggleGroup,
    CngxButtonToggle,
    CngxChipGroup,
    CngxMultiChipGroup,
    CngxChipInGroup,
  ],
  template: `
    <app-doc-shell title="Form Primitives — Signal Forms + RF"
      description="The nine value-bearing form atoms from @cngx/common/interactive (Toggle, Checkbox, ChipInteraction, RadioGroup, CheckboxGroup, ButtonToggleGroup, ButtonMultiToggleGroup, ChipGroup, MultiChipGroup) work in BOTH Signal Forms and Reactive Forms. Signal Forms via <cngx-form-field [field]=&quot;f.x&quot;> uses the CNGX_FORM_FIELD_CONTROL each atom provides directly; Reactive Forms attaches CngxFormBridge automatically when [formControl] / [formControlName] is bound."
      overview="<p>Phase 7 closure of the form-primitives system. Atoms in <code>@cngx/common/interactive</code> are Forms-agnostic at Level 2 — they expose <code>CNGX_CONTROL_VALUE</code> for value flow and <code>CNGX_FORM_FIELD_CONTROL</code> for ARIA discovery. Two independent paths consume those contracts:</p><ul><li><strong>Signal Forms</strong> — drop the atom inside <code>&lt;cngx-form-field [field]=&quot;f.x&quot;&gt;</code>. The presenter discovers the atom via <code>CNGX_FORM_FIELD_CONTROL</code>; the new <code>CNGX_FORM_FIELD_HOST</code> token (Phase 7 commit 2a) lets the atom react to <code>showError()</code> and call <code>markAsTouched()</code> on focusout — entirely Sheriff-clean across the layer boundary.</li><li><strong>Reactive Forms</strong> — bind <code>[formControl]</code> or <code>[formControlName]</code> on the atom. <code>CngxFormBridge</code> in <code>@cngx/forms/controls</code> auto-attaches via element selector, providing <code>NG_VALUE_ACCESSOR</code> and bidirectionally syncing the <code>FormControl</code> with the atom's <code>value</code> model.</li></ul><p><code>CngxChipInput</code> is deferred to Phase 7.x (its tokenizer shape — emits <code>tokenCreated</code> events, no <code>value</code> model — needs a separate <code>CngxChipInputFieldBridge</code> modelled on <code>CngxListboxFieldBridge</code>).</p>"
      [apiComponents]="['CngxFormBridge', 'CngxFormField', 'CngxToggle', 'CngxCheckbox', 'CngxChipInteraction', 'CngxRadioGroup', 'CngxRadio', 'CngxCheckboxGroup', 'CngxButtonToggleGroup', 'CngxButtonMultiToggleGroup', 'CngxChipGroup', 'CngxMultiChipGroup', 'CngxChipInGroup']">
      <app-example-card title="Signal Forms — boolean + scalar + array shapes"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <div style="display:grid;gap:16px;max-width:480px">
    <cngx-form-field [field]="sfForm.notifications">
      <label cngxLabel>Benachrichtigungen</label>
      <cngx-toggle [(value)]="sfForm.notifications().value">
        E-Mails empfangen
      </cngx-toggle>
      <cngx-field-errors />
    </cngx-form-field>

    <cngx-form-field [field]="sfForm.terms">
      <label cngxLabel>Bedingungen</label>
      <cngx-checkbox [(value)]="sfForm.terms().value">
        Ich akzeptiere die Bedingungen
      </cngx-checkbox>
      <cngx-field-errors />
    </cngx-form-field>

    <cngx-form-field [field]="sfForm.payment">
      <label cngxLabel>Zahlungsart</label>
      <cngx-radio-group [(value)]="sfForm.payment().value" name="sf-payment">
        <cngx-radio value="card">Karte</cngx-radio>
        <cngx-radio value="cash">Bar</cngx-radio>
        <cngx-radio value="invoice">Rechnung</cngx-radio>
      </cngx-radio-group>
      <cngx-field-errors />
    </cngx-form-field>

    <cngx-form-field [field]="sfForm.notificationChannels">
      <label cngxLabel>Kanäle</label>
      <cngx-checkbox-group
        [(selectedValues)]="sfForm.notificationChannels().value"
        [allValues]="channelOptions"
      >
        @for (opt of channelOptions; track opt) {
          <cngx-checkbox
            [value]="sfForm.notificationChannels().value().includes(opt)"
            (valueChange)="$event
              ? sfForm.notificationChannels().value.update((v) => [...v, opt])
              : sfForm.notificationChannels().value.update((v) => v.filter((x) => x !== opt))"
          >{{ opt }}</cngx-checkbox>
        }
      </cngx-checkbox-group>
      <cngx-field-errors />
    </cngx-form-field>
  </div>
      </app-example-card>
      <app-example-card title="Reactive Forms — CngxFormBridge auto-attaches"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <form [formGroup]="rfForm" style="display:grid;gap:16px;max-width:560px">
    <!-- Boolean atoms -->
    <cngx-toggle [formControlName]="'notifications'">
      Benachrichtigungen
    </cngx-toggle>

    <cngx-checkbox [formControlName]="'terms'">
      Ich akzeptiere die Bedingungen
    </cngx-checkbox>

    <cngx-chip cngxChipInteraction
      [formControlName]="'featured'"
      [value]="'feat'"
    >Featured</cngx-chip>

    <!-- Single-value group atoms -->
    <cngx-radio-group [formControlName]="'payment'" name="rf-payment" label="Zahlungsart">
      @for (opt of paymentOptions; track opt) {
        <cngx-radio [value]="opt">{{ opt }}</cngx-radio>
      }
    </cngx-radio-group>

    <cngx-button-toggle-group [formControlName]="'view'" label="Ansicht">
      @for (opt of viewOptions; track opt) {
        <button cngxButtonToggle [value]="opt">{{ opt }}</button>
      }
    </cngx-button-toggle-group>

    <cngx-chip-group [formControlName]="'size'" label="Größe">
      @for (opt of sizeOptions; track opt) {
        <cngx-chip cngxChipInGroup [value]="opt">{{ opt }}</cngx-chip>
      }
    </cngx-chip-group>

    <!-- Multi-value group atoms -->
    <cngx-checkbox-group
      [formControlName]="'notificationChannels'"
      [allValues]="channelOptions"
      label="Kanäle"
    >
      @for (opt of channelOptions; track opt) {
        <cngx-checkbox [value]="rfForm.controls.notificationChannels.value.includes(opt)">{{ opt }}</cngx-checkbox>
      }
    </cngx-checkbox-group>

    <cngx-button-multi-toggle-group [formControlName]="'filters'" label="Filter">
      @for (opt of filterOptions; track opt) {
        <button cngxButtonToggle [value]="opt">{{ opt }}</button>
      }
    </cngx-button-multi-toggle-group>

    <cngx-multi-chip-group [formControlName]="'tags'" label="Tags">
      @for (opt of tagOptions; track opt) {
        <cngx-chip cngxChipInGroup [value]="opt">{{ opt }}</cngx-chip>
      }
    </cngx-multi-chip-group>

    <div class="button-row" style="margin-top:8px">
      <button type="button" class="chip" (click)="handleSubmit()">Mark touched (show errors)</button>
      <button type="button" class="chip" (click)="handleReset()">Reset</button>
    </div>
  </form>

  <div class="event-grid" style="margin-top:16px">
    <div class="event-row">
      <span class="event-label">FormGroup value</span>
      <span class="event-value">{{ rfForm.value | json }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">Valid</span>
      <span class="event-value">{{ rfForm.valid ? 'yes' : 'no' }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">Touched</span>
      <span class="event-value">{{ rfForm.touched ? 'yes' : 'no' }}</span>
    </div>
  </div>
      </app-example-card>
      <app-example-card title="CngxChipInput — deferred to Phase 7.x"
        [subtitle]="_s2"
        [sourceHtml]="_srcHtml2"
        [sourceTs]="_srcTs2">
        
  <p style="color:var(--cngx-muted, #6b7280);font-style:italic">
    Demo for CngxChipInput Forms integration ships in Phase 7.x.
  </p>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class FormPrimitivesDemoComponent {
  protected readonly _s0 = 'Atom drops inside <code>&lt;cngx-form-field [field]="f.x"&gt;</code>. The presenter discovers the atom via <code>CNGX_FORM_FIELD_CONTROL</code>. Required-validator messages auto-render through <code>&lt;cngx-field-errors&gt;</code> once the field is touched.';
  protected readonly _s1 = 'Bind <code>[formControl]</code> on any of the nine atoms; <code>CngxFormBridge</code> from <code>@cngx/forms/controls</code> matches the element selector and provides <code>NG_VALUE_ACCESSOR</code>. No bridge directive on the consumer side.';
  protected readonly _s2 = '<code>CngxChipInput</code> is the tokenizer atom from Phase 5: it emits <code>tokenCreated</code> events and does not own a <code>value</code> model (the consumer holds the chip list). It cannot share the bridge with the nine value-bearing atoms; a dedicated <code>CngxChipInputFieldBridge</code> in <code>@cngx/forms/field</code> (modelled on <code>CngxListboxFieldBridge</code>) will derive <code>id</code> / <code>empty</code> / <code>focused</code> / <code>disabled</code> / <code>errorState</code> from the input element + validation slot. Tracked as Phase 7.x follow-up — not part of this PR.';
  protected readonly _srcHtml0 = `<div style="display:grid;gap:16px;max-width:480px">
    <cngx-form-field [field]="sfForm.notifications">
      <label cngxLabel>Benachrichtigungen</label>
      <cngx-toggle [(value)]="sfForm.notifications().value">
        E-Mails empfangen
      </cngx-toggle>
      <cngx-field-errors />
    </cngx-form-field>

    <cngx-form-field [field]="sfForm.terms">
      <label cngxLabel>Bedingungen</label>
      <cngx-checkbox [(value)]="sfForm.terms().value">
        Ich akzeptiere die Bedingungen
      </cngx-checkbox>
      <cngx-field-errors />
    </cngx-form-field>

    <cngx-form-field [field]="sfForm.payment">
      <label cngxLabel>Zahlungsart</label>
      <cngx-radio-group [(value)]="sfForm.payment().value" name="sf-payment">
        <cngx-radio value="card">Karte</cngx-radio>
        <cngx-radio value="cash">Bar</cngx-radio>
        <cngx-radio value="invoice">Rechnung</cngx-radio>
      </cngx-radio-group>
      <cngx-field-errors />
    </cngx-form-field>

    <cngx-form-field [field]="sfForm.notificationChannels">
      <label cngxLabel>Kanäle</label>
      <cngx-checkbox-group
        [(selectedValues)]="sfForm.notificationChannels().value"
        [allValues]="channelOptions"
      >
        @for (opt of channelOptions; track opt) {
          <cngx-checkbox
            [value]="sfForm.notificationChannels().value().includes(opt)"
            (valueChange)="$event
              ? sfForm.notificationChannels().value.update((v) => [...v, opt])
              : sfForm.notificationChannels().value.update((v) => v.filter((x) => x !== opt))"
          >{{ opt }}</cngx-checkbox>
        }
      </cngx-checkbox-group>
      <cngx-field-errors />
    </cngx-form-field>
  </div>`;
  protected readonly _srcTs0 = `import { form, schema, required } from '@angular/forms/signals';
import { ReactiveFormsModule } from '@angular/forms';
import { JsonPipe } from '@angular/common';
import { CngxFormField, CngxLabel, CngxFieldErrors } from '@cngx/forms/field';
import { CngxFormBridge } from '@cngx/forms/controls';
import { CngxToggle, CngxCheckbox, CngxChipInteraction, CngxRadioGroup, CngxRadio, CngxCheckboxGroup, CngxButtonToggleGroup, CngxButtonMultiToggleGroup, CngxButtonToggle, CngxChipGroup, CngxMultiChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';
import { createFormPrimitivesFormGroup } from '../_fixtures/form-primitives-form-group';


  // ── Signal Forms model ─────────────────────────────────────
  protected readonly sfModel = signal<{
    notifications: boolean;
    terms: boolean;
    payment: string;
    notificationChannels: string[];
  }>({
    notifications: false,
    terms: false,
    payment: '',
    notificationChannels: [],
  });

  protected readonly sfForm = form(this.sfModel, schema((root) => {
    required(root.terms, { message: 'Bitte zustimmen' });
    required(root.payment, { message: 'Wahl erforderlich' });
  }));

  // ── Reactive Forms group (all 9 atoms) ─────────────────────
  protected readonly rfForm = createFormPrimitivesFormGroup();

  // Pool data for selection groups
  protected readonly paymentOptions = ['card', 'cash', 'invoice'];
  protected readonly viewOptions = ['grid', 'list', 'table'];
  protected readonly sizeOptions = ['sm', 'md', 'lg'];
  protected readonly channelOptions = ['email', 'sms', 'push'];
  protected readonly filterOptions = ['open', 'closed', 'archived'];
  protected readonly tagOptions = ['ng', 'rx', 'ts', 'cdk'];

  protected handleSubmit(): void {
    Object.values(this.rfForm.controls).forEach((c) => c.markAsTouched());
  }

  protected handleReset(): void {
    this.rfForm.reset();
  }`;
  protected readonly _srcHtml1 = `<form [formGroup]="rfForm" style="display:grid;gap:16px;max-width:560px">
    <!-- Boolean atoms -->
    <cngx-toggle [formControlName]="'notifications'">
      Benachrichtigungen
    </cngx-toggle>

    <cngx-checkbox [formControlName]="'terms'">
      Ich akzeptiere die Bedingungen
    </cngx-checkbox>

    <cngx-chip cngxChipInteraction
      [formControlName]="'featured'"
      [value]="'feat'"
    >Featured</cngx-chip>

    <!-- Single-value group atoms -->
    <cngx-radio-group [formControlName]="'payment'" name="rf-payment" label="Zahlungsart">
      @for (opt of paymentOptions; track opt) {
        <cngx-radio [value]="opt">{{ opt }}</cngx-radio>
      }
    </cngx-radio-group>

    <cngx-button-toggle-group [formControlName]="'view'" label="Ansicht">
      @for (opt of viewOptions; track opt) {
        <button cngxButtonToggle [value]="opt">{{ opt }}</button>
      }
    </cngx-button-toggle-group>

    <cngx-chip-group [formControlName]="'size'" label="Größe">
      @for (opt of sizeOptions; track opt) {
        <cngx-chip cngxChipInGroup [value]="opt">{{ opt }}</cngx-chip>
      }
    </cngx-chip-group>

    <!-- Multi-value group atoms -->
    <cngx-checkbox-group
      [formControlName]="'notificationChannels'"
      [allValues]="channelOptions"
      label="Kanäle"
    >
      @for (opt of channelOptions; track opt) {
        <cngx-checkbox [value]="rfForm.controls.notificationChannels.value.includes(opt)">{{ opt }}</cngx-checkbox>
      }
    </cngx-checkbox-group>

    <cngx-button-multi-toggle-group [formControlName]="'filters'" label="Filter">
      @for (opt of filterOptions; track opt) {
        <button cngxButtonToggle [value]="opt">{{ opt }}</button>
      }
    </cngx-button-multi-toggle-group>

    <cngx-multi-chip-group [formControlName]="'tags'" label="Tags">
      @for (opt of tagOptions; track opt) {
        <cngx-chip cngxChipInGroup [value]="opt">{{ opt }}</cngx-chip>
      }
    </cngx-multi-chip-group>

    <div class="button-row" style="margin-top:8px">
      <button type="button" class="chip" (click)="handleSubmit()">Mark touched (show errors)</button>
      <button type="button" class="chip" (click)="handleReset()">Reset</button>
    </div>
  </form>

  <div class="event-grid" style="margin-top:16px">
    <div class="event-row">
      <span class="event-label">FormGroup value</span>
      <span class="event-value">{{ rfForm.value | json }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">Valid</span>
      <span class="event-value">{{ rfForm.valid ? 'yes' : 'no' }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">Touched</span>
      <span class="event-value">{{ rfForm.touched ? 'yes' : 'no' }}</span>
    </div>
  </div>`;
  protected readonly _srcTs1 = `import { form, schema, required } from '@angular/forms/signals';
import { ReactiveFormsModule } from '@angular/forms';
import { JsonPipe } from '@angular/common';
import { CngxFormField, CngxLabel, CngxFieldErrors } from '@cngx/forms/field';
import { CngxFormBridge } from '@cngx/forms/controls';
import { CngxToggle, CngxCheckbox, CngxChipInteraction, CngxRadioGroup, CngxRadio, CngxCheckboxGroup, CngxButtonToggleGroup, CngxButtonMultiToggleGroup, CngxButtonToggle, CngxChipGroup, CngxMultiChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';
import { createFormPrimitivesFormGroup } from '../_fixtures/form-primitives-form-group';


  // ── Signal Forms model ─────────────────────────────────────
  protected readonly sfModel = signal<{
    notifications: boolean;
    terms: boolean;
    payment: string;
    notificationChannels: string[];
  }>({
    notifications: false,
    terms: false,
    payment: '',
    notificationChannels: [],
  });

  protected readonly sfForm = form(this.sfModel, schema((root) => {
    required(root.terms, { message: 'Bitte zustimmen' });
    required(root.payment, { message: 'Wahl erforderlich' });
  }));

  // ── Reactive Forms group (all 9 atoms) ─────────────────────
  protected readonly rfForm = createFormPrimitivesFormGroup();

  // Pool data for selection groups
  protected readonly paymentOptions = ['card', 'cash', 'invoice'];
  protected readonly viewOptions = ['grid', 'list', 'table'];
  protected readonly sizeOptions = ['sm', 'md', 'lg'];
  protected readonly channelOptions = ['email', 'sms', 'push'];
  protected readonly filterOptions = ['open', 'closed', 'archived'];
  protected readonly tagOptions = ['ng', 'rx', 'ts', 'cdk'];

  protected handleSubmit(): void {
    Object.values(this.rfForm.controls).forEach((c) => c.markAsTouched());
  }

  protected handleReset(): void {
    this.rfForm.reset();
  }`;
  protected readonly _srcHtml2 = `<p style="color:var(--cngx-muted, #6b7280);font-style:italic">
    Demo for CngxChipInput Forms integration ships in Phase 7.x.
  </p>`;
  protected readonly _srcTs2 = `import { form, schema, required } from '@angular/forms/signals';
import { ReactiveFormsModule } from '@angular/forms';
import { JsonPipe } from '@angular/common';
import { CngxFormField, CngxLabel, CngxFieldErrors } from '@cngx/forms/field';
import { CngxFormBridge } from '@cngx/forms/controls';
import { CngxToggle, CngxCheckbox, CngxChipInteraction, CngxRadioGroup, CngxRadio, CngxCheckboxGroup, CngxButtonToggleGroup, CngxButtonMultiToggleGroup, CngxButtonToggle, CngxChipGroup, CngxMultiChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';
import { createFormPrimitivesFormGroup } from '../_fixtures/form-primitives-form-group';


  // ── Signal Forms model ─────────────────────────────────────
  protected readonly sfModel = signal<{
    notifications: boolean;
    terms: boolean;
    payment: string;
    notificationChannels: string[];
  }>({
    notifications: false,
    terms: false,
    payment: '',
    notificationChannels: [],
  });

  protected readonly sfForm = form(this.sfModel, schema((root) => {
    required(root.terms, { message: 'Bitte zustimmen' });
    required(root.payment, { message: 'Wahl erforderlich' });
  }));

  // ── Reactive Forms group (all 9 atoms) ─────────────────────
  protected readonly rfForm = createFormPrimitivesFormGroup();

  // Pool data for selection groups
  protected readonly paymentOptions = ['card', 'cash', 'invoice'];
  protected readonly viewOptions = ['grid', 'list', 'table'];
  protected readonly sizeOptions = ['sm', 'md', 'lg'];
  protected readonly channelOptions = ['email', 'sms', 'push'];
  protected readonly filterOptions = ['open', 'closed', 'archived'];
  protected readonly tagOptions = ['ng', 'rx', 'ts', 'cdk'];

  protected handleSubmit(): void {
    Object.values(this.rfForm.controls).forEach((c) => c.markAsTouched());
  }

  protected handleReset(): void {
    this.rfForm.reset();
  }`;

  // ── Signal Forms model ─────────────────────────────────────
  protected readonly sfModel = signal<{
    notifications: boolean;
    terms: boolean;
    payment: string;
    notificationChannels: string[];
  }>({
    notifications: false,
    terms: false,
    payment: '',
    notificationChannels: [],
  });

  protected readonly sfForm = form(this.sfModel, schema((root) => {
    required(root.terms, { message: 'Bitte zustimmen' });
    required(root.payment, { message: 'Wahl erforderlich' });
  }));

  // ── Reactive Forms group (all 9 atoms) ─────────────────────
  protected readonly rfForm = createFormPrimitivesFormGroup();

  // Pool data for selection groups
  protected readonly paymentOptions = ['card', 'cash', 'invoice'];
  protected readonly viewOptions = ['grid', 'list', 'table'];
  protected readonly sizeOptions = ['sm', 'md', 'lg'];
  protected readonly channelOptions = ['email', 'sms', 'push'];
  protected readonly filterOptions = ['open', 'closed', 'archived'];
  protected readonly tagOptions = ['ng', 'rx', 'ts', 'cdk'];

  protected handleSubmit(): void {
    Object.values(this.rfForm.controls).forEach((c) => c.markAsTouched());
  }

  protected handleReset(): void {
    this.rfForm.reset();
  }
  
}
