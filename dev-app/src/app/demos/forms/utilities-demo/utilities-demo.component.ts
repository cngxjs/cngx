// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/forms/utilities-demo/utilities-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../shared/example-card.component';
import { DocShellComponent } from '../../../shared/doc-shell.component';
import { CngxInputClear, CngxCopyValue, CngxInputFormat } from '@cngx/forms/input';

@Component({
  selector: 'app-utilities-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxInputClear,
    CngxCopyValue,
    CngxInputFormat,
  ],
  template: `
    <app-doc-shell title="Input Utilities"
      description="Small headless behaviors: clear button, clipboard copy, and display formatting."
      overview="<p><code>CngxInputClear</code> provides a headless clear behavior for any input/textarea.</p><p><code>CngxCopyValue</code> copies text to clipboard with auto-reset feedback.</p><p><code>CngxInputFormat</code> applies formatting on blur and shows the raw value on focus.</p>"
      [apiComponents]="['CngxInputClear', 'CngxCopyValue', 'CngxInputFormat']">
      <app-example-card title="Input Clear"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <div class="demo-form">
    <div class="demo-field">
      <label class="demo-label">Name</label>
      <div style="display:flex;gap:8px;align-items:center">
        <input #nameInput placeholder="Type something..." class="demo-input" style="flex:1" />
        <button [cngxInputClear]="nameInput" #clr="cngxInputClear"
          class="chip" [style.opacity]="clr.hasValue() ? 1 : 0.3">
          Clear
        </button>
      </div>
    </div>
  </div>
      </app-example-card>
      <app-example-card title="Copy to Clipboard"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <div class="demo-form">
    <div class="demo-field">
      <label class="demo-label">API Token</label>
      <div style="display:flex;gap:8px;align-items:center">
        <input #tokenInput readonly [value]="token()" class="demo-input" style="flex:1;font-family:var(--font-mono)" />
        <button [cngxCopyValue] [source]="tokenInput" #cp="cngxCopyValue" class="chip">
          {{ cp.copied() ? 'Copied!' : 'Copy' }}
        </button>
      </div>
    </div>
    <div class="demo-field">
      <label class="demo-label">Copy explicit value</label>
      <button [cngxCopyValue]="'hello@example.com'" #cp2="cngxCopyValue" class="chip">
        {{ cp2.copied() ? 'Copied!' : 'Copy email' }}
      </button>
    </div>
  </div>
      </app-example-card>
      <app-example-card title="Input Format"
        [subtitle]="_s2"
        [sourceHtml]="_srcHtml2"
        [sourceTs]="_srcTs2">
        
  <div class="demo-form">
    <div class="demo-field">
      <label class="demo-label">Amount (formatted as USD on blur)</label>
      <input [cngxInputFormat]="formatCurrency" [parse]="parseCurrency"
        #fmt="cngxInputFormat" placeholder="Enter amount..." class="demo-input" />
      <div class="status-row">
        <span class="status-badge">Raw: {{ fmt.rawValue() }}</span>
        <span class="status-badge">Display: {{ fmt.displayValue() }}</span>
      </div>
    </div>
  </div>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class UtilitiesDemoComponent {
  protected readonly _s0 = '<code>[cngxInputClear]</code> takes a reference to the target input. Exposes <code>hasValue()</code> signal and <code>clear()</code> method.';
  protected readonly _s1 = '<code>[cngxCopyValue]</code> copies text on click. The <code>copied()</code> signal stays <code>true</code> for 2 seconds.';
  protected readonly _s2 = '<code>[cngxInputFormat]</code> formats on blur (display) and parses on focus (edit). Reactive forms receive the raw value.';
  protected readonly _srcHtml0 = `<div class="demo-form">
    <div class="demo-field">
      <label class="demo-label">Name</label>
      <div style="display:flex;gap:8px;align-items:center">
        <input #nameInput placeholder="Type something..." class="demo-input" style="flex:1" />
        <button [cngxInputClear]="nameInput" #clr="cngxInputClear"
          class="chip" [style.opacity]="clr.hasValue() ? 1 : 0.3">
          Clear
        </button>
      </div>
    </div>
  </div>`;
  protected readonly _srcTs0 = `import { CngxInputClear, CngxCopyValue, CngxInputFormat } from '@cngx/forms/input';


  protected readonly formatCurrency = (v: string) => {
    const n = parseFloat(v);
    return isNaN(n) ? v : new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }).format(n);
  };
  protected readonly parseCurrency = (v: string) => v.replace(/[^0-9.\\-]/g, '');
  protected readonly token = signal('sk-proj-abc123def456ghi789');`;
  protected readonly _srcHtml1 = `<div class="demo-form">
    <div class="demo-field">
      <label class="demo-label">API Token</label>
      <div style="display:flex;gap:8px;align-items:center">
        <input #tokenInput readonly [value]="token()" class="demo-input" style="flex:1;font-family:var(--font-mono)" />
        <button [cngxCopyValue] [source]="tokenInput" #cp="cngxCopyValue" class="chip">
          {{ cp.copied() ? 'Copied!' : 'Copy' }}
        </button>
      </div>
    </div>
    <div class="demo-field">
      <label class="demo-label">Copy explicit value</label>
      <button [cngxCopyValue]="'hello@example.com'" #cp2="cngxCopyValue" class="chip">
        {{ cp2.copied() ? 'Copied!' : 'Copy email' }}
      </button>
    </div>
  </div>`;
  protected readonly _srcTs1 = `import { CngxInputClear, CngxCopyValue, CngxInputFormat } from '@cngx/forms/input';


  protected readonly formatCurrency = (v: string) => {
    const n = parseFloat(v);
    return isNaN(n) ? v : new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }).format(n);
  };
  protected readonly parseCurrency = (v: string) => v.replace(/[^0-9.\\-]/g, '');
  protected readonly token = signal('sk-proj-abc123def456ghi789');`;
  protected readonly _srcHtml2 = `<div class="demo-form">
    <div class="demo-field">
      <label class="demo-label">Amount (formatted as USD on blur)</label>
      <input [cngxInputFormat]="formatCurrency" [parse]="parseCurrency"
        #fmt="cngxInputFormat" placeholder="Enter amount..." class="demo-input" />
      <div class="status-row">
        <span class="status-badge">Raw: {{ fmt.rawValue() }}</span>
        <span class="status-badge">Display: {{ fmt.displayValue() }}</span>
      </div>
    </div>
  </div>`;
  protected readonly _srcTs2 = `import { CngxInputClear, CngxCopyValue, CngxInputFormat } from '@cngx/forms/input';


  protected readonly formatCurrency = (v: string) => {
    const n = parseFloat(v);
    return isNaN(n) ? v : new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }).format(n);
  };
  protected readonly parseCurrency = (v: string) => v.replace(/[^0-9.\\-]/g, '');
  protected readonly token = signal('sk-proj-abc123def456ghi789');`;

  protected readonly formatCurrency = (v: string) => {
    const n = parseFloat(v);
    return isNaN(n) ? v : new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }).format(n);
  };
  protected readonly parseCurrency = (v: string) => v.replace(/[^0-9.\-]/g, '');
  protected readonly token = signal('sk-proj-abc123def456ghi789');
  
}
