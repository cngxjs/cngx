// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/ui/skeleton-demo/skeleton-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../shared/example-card.component';
import { DocShellComponent } from '../../../shared/doc-shell.component';
import { CngxSkeletonContainer, CngxSkeletonPlaceholder } from '@cngx/ui';

@Component({
  selector: 'app-skeleton-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxSkeletonContainer,
    CngxSkeletonPlaceholder,
  ],
  template: `
    <app-doc-shell title="SkeletonContainer"
      description="Skeleton loading container with placeholder template repetition and automatic loading/content switching."
      overview="<p><code>CngxSkeletonContainer</code> wraps the headless <code>CngxSkeleton</code> directive into a component with content projection. Project a <code>cngxSkeletonPlaceholder</code> template for the loading state and your real content for the loaded state. No <code>@if</code>/<code>@for</code> needed.</p>"
      [apiComponents]="['CngxSkeletonContainer', 'CngxSkeletonPlaceholder']">
      <app-example-card title="Card Skeleton"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <div class="button-row" style="margin-bottom:16px">
    <button class="chip" [class.chip--active]="loading()" (click)="toggleLoading()">
      {{ loading() ? 'Loading...' : 'Loaded' }}
    </button>
  </div>
  <cngx-skeleton [loading]="loading()" [count]="3">
    <ng-template cngxSkeletonPlaceholder let-i let-last="last">
      <div style="display:flex;gap:12px;padding:16px;border:1px solid var(--cngx-border,#e0e0e0);border-radius:8px;margin-bottom:12px">
        <div style="width:48px;height:48px;border-radius:50%;background:var(--cngx-surface-alt,#e0e0e0);flex-shrink:0"></div>
        <div style="flex:1;display:flex;flex-direction:column;gap:8px">
          <div style="height:14px;width:40%;border-radius:4px;background:var(--cngx-surface-alt,#e0e0e0)"></div>
          <div style="height:12px;border-radius:4px;background:var(--cngx-surface-alt,#e0e0e0)"></div>
          <div [style.height]="'12px'" [style.width]="last ? '60%' : '85%'"
            style="border-radius:4px;background:var(--cngx-surface-alt,#e0e0e0)"></div>
        </div>
      </div>
    </ng-template>
    @for (i of [1, 2, 3]; track i) {
      <div style="padding:16px;border:1px solid var(--cngx-border,#e0e0e0);border-radius:8px;margin-bottom:12px">
        <strong>User {{ i }}</strong>
        <p style="margin:4px 0 0;color:var(--text-muted,#666)">Content loaded successfully.</p>
      </div>
    }
  </cngx-skeleton>
      </app-example-card>
      <app-example-card title="Template Context"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <cngx-skeleton [loading]="true" [count]="5">
    <ng-template cngxSkeletonPlaceholder let-i let-first="first" let-last="last" let-count="count">
      <div style="display:flex;align-items:center;gap:8px;padding:8px 0;border-bottom:1px solid var(--cngx-border,#e0e0e0)">
        <span style="font-size:0.75rem;color:var(--text-muted,#999);width:24px">{{ i + 1 }}/{{ count }}</span>
        <div [style.height]="'12px'" [style.width]="first ? '70%' : last ? '40%' : '55%'"
          style="border-radius:4px;background:var(--cngx-surface-alt,#e0e0e0)"></div>
      </div>
    </ng-template>
    <p>Content here after loading</p>
  </cngx-skeleton>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class SkeletonDemoComponent {
  protected readonly _s0 = 'The placeholder template is repeated <code>[count]</code> times while loading. Toggle the button to switch between skeleton and content.';
  protected readonly _s1 = 'The placeholder template receives <code>$implicit</code> (index), <code>count</code>, <code>first</code>, <code>last</code>. Use them to vary the skeleton appearance.';
  protected readonly _srcHtml0 = `<div class="button-row" style="margin-bottom:16px">
    <button class="chip" [class.chip--active]="loading()" (click)="toggleLoading()">
      {{ loading() ? 'Loading...' : 'Loaded' }}
    </button>
  </div>
  <cngx-skeleton [loading]="loading()" [count]="3">
    <ng-template cngxSkeletonPlaceholder let-i let-last="last">
      <div style="display:flex;gap:12px;padding:16px;border:1px solid var(--cngx-border,#e0e0e0);border-radius:8px;margin-bottom:12px">
        <div style="width:48px;height:48px;border-radius:50%;background:var(--cngx-surface-alt,#e0e0e0);flex-shrink:0"></div>
        <div style="flex:1;display:flex;flex-direction:column;gap:8px">
          <div style="height:14px;width:40%;border-radius:4px;background:var(--cngx-surface-alt,#e0e0e0)"></div>
          <div style="height:12px;border-radius:4px;background:var(--cngx-surface-alt,#e0e0e0)"></div>
          <div [style.height]="'12px'" [style.width]="last ? '60%' : '85%'"
            style="border-radius:4px;background:var(--cngx-surface-alt,#e0e0e0)"></div>
        </div>
      </div>
    </ng-template>
    @for (i of [1, 2, 3]; track i) {
      <div style="padding:16px;border:1px solid var(--cngx-border,#e0e0e0);border-radius:8px;margin-bottom:12px">
        <strong>User {{ i }}</strong>
        <p style="margin:4px 0 0;color:var(--text-muted,#666)">Content loaded successfully.</p>
      </div>
    }
  </cngx-skeleton>`;
  protected readonly _srcTs0 = `import { CngxSkeletonContainer, CngxSkeletonPlaceholder } from '@cngx/ui';


  protected readonly loading = signal(true);
  protected toggleLoading(): void {
    this.loading.update(v => !v);
  }`;
  protected readonly _srcHtml1 = `<cngx-skeleton [loading]="true" [count]="5">
    <ng-template cngxSkeletonPlaceholder let-i let-first="first" let-last="last" let-count="count">
      <div style="display:flex;align-items:center;gap:8px;padding:8px 0;border-bottom:1px solid var(--cngx-border,#e0e0e0)">
        <span style="font-size:0.75rem;color:var(--text-muted,#999);width:24px">{{ i + 1 }}/{{ count }}</span>
        <div [style.height]="'12px'" [style.width]="first ? '70%' : last ? '40%' : '55%'"
          style="border-radius:4px;background:var(--cngx-surface-alt,#e0e0e0)"></div>
      </div>
    </ng-template>
    <p>Content here after loading</p>
  </cngx-skeleton>`;
  protected readonly _srcTs1 = `import { CngxSkeletonContainer, CngxSkeletonPlaceholder } from '@cngx/ui';


  protected readonly loading = signal(true);
  protected toggleLoading(): void {
    this.loading.update(v => !v);
  }`;

  protected readonly loading = signal(true);
  protected toggleLoading(): void {
    this.loading.update(v => !v);
  }
  
}
