// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/ui/stepper/stepper-horizontal-demo/stepper-horizontal-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxStep, CngxStepContent } from '@cngx/common/stepper';
import { CngxStepper } from '@cngx/ui/stepper';

@Component({
  selector: 'app-stepper-horizontal-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxStepper,
    CngxStep,
    CngxStepContent,
  ],
  template: `
    <app-doc-shell title="Stepper — horizontal"
      description="<code>&lt;cngx-stepper&gt;</code> in horizontal orientation (default). Strip + panels stacked. Reactive ARIA + roving tabindex via composed host-directives."
      [apiComponents]="['CngxStepper']">
      <app-example-card title="Three-step wizard"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <cngx-stepper [(activeStepIndex)]="active" aria-label="Order wizard">
    <div cngxStep label="Customer">
      <ng-template cngxStepContent>
        <p>Enter customer details — name, email, shipping address.</p>
      </ng-template>
    </div>
    <div cngxStep label="Payment">
      <ng-template cngxStepContent>
        <p>Choose a payment method and confirm billing address.</p>
      </ng-template>
    </div>
    <div cngxStep label="Review">
      <ng-template cngxStepContent>
        <p>Review the order before placing it.</p>
      </ng-template>
    </div>
  </cngx-stepper>
  <div class="event-grid" style="margin-top:12px;gap:8px">
    <div class="event-row">
      <button type="button" class="chip" (click)="handlePrev()">Previous</button>
      <button type="button" class="chip" (click)="handleNext()">Next</button>
    </div>
    <div class="event-row"><span class="event-label">Active step</span><span class="event-value">{{ active() }}</span></div>
  </div>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class StepperHorizontalDemoComponent {
  protected readonly _s0 = 'Bind <code>[(activeStepIndex)]</code> for two-way control. Click any step header or use ArrowLeft/ArrowRight to navigate. Tab leaves the strip.';
  protected readonly _srcHtml0 = `<cngx-stepper [(activeStepIndex)]="active" aria-label="Order wizard">
    <div cngxStep label="Customer">
      <ng-template cngxStepContent>
        <p>Enter customer details — name, email, shipping address.</p>
      </ng-template>
    </div>
    <div cngxStep label="Payment">
      <ng-template cngxStepContent>
        <p>Choose a payment method and confirm billing address.</p>
      </ng-template>
    </div>
    <div cngxStep label="Review">
      <ng-template cngxStepContent>
        <p>Review the order before placing it.</p>
      </ng-template>
    </div>
  </cngx-stepper>
  <div class="event-grid" style="margin-top:12px;gap:8px">
    <div class="event-row">
      <button type="button" class="chip" (click)="handlePrev()">Previous</button>
      <button type="button" class="chip" (click)="handleNext()">Next</button>
    </div>
    <div class="event-row"><span class="event-label">Active step</span><span class="event-value">{{ active() }}</span></div>
  </div>`;
  protected readonly _srcTs0 = `import { CngxStep, CngxStepContent } from '@cngx/common/stepper';
import { CngxStepper } from '@cngx/ui/stepper';


  protected readonly active = signal(0);

  protected handleNext(): void {
    this.active.update(i => Math.min(i + 1, 2));
  }
  protected handlePrev(): void {
    this.active.update(i => Math.max(i - 1, 0));
  }`;

  protected readonly active = signal(0);

  protected handleNext(): void {
    this.active.update(i => Math.min(i + 1, 2));
  }
  protected handlePrev(): void {
    this.active.update(i => Math.max(i - 1, 0));
  }
  
}
