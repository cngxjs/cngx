// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/ui/feedback/alert-demo/alert-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxAlert, CngxAlertAction } from '@cngx/ui/feedback';
import { createManualState } from '@cngx/common/data';

@Component({
  selector: 'app-alert-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxAlert,
    CngxAlertAction,
  ],
  template: `
    <app-doc-shell title="Alert"
      description="Inline alert atom with enter/exit animations, state-driven visibility, auto-dismiss with pause-on-hover/focus, auto-collapse, and action buttons."
      [apiComponents]="['CngxAlert', 'CngxAlertAction', 'CngxAlertIcon']">
      <app-example-card title="Severities"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <div style="display:flex;flex-direction:column;gap:12px">
    <cngx-alert severity="info" title="Info">This is an informational message.</cngx-alert>
    <cngx-alert severity="success" title="Success">Operation completed successfully.</cngx-alert>
    <cngx-alert severity="warning" title="Warning">Please review before continuing.</cngx-alert>
    <cngx-alert severity="error" title="Error">Something went wrong. Please try again.</cngx-alert>
  </div>
      </app-example-card>
      <app-example-card title="Closable"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  @if (!dismissed()) {
    <cngx-alert severity="warning" title="Unsaved changes" [closable]="true" (dismissed)="dismissed.set(true)">
      Your changes will be lost if you leave this page.
    </cngx-alert>
  } @else {
    <button (click)="dismissed.set(false)" class="chip">Show alert again</button>
  }
      </app-example-card>
      <app-example-card title="Action Buttons"
        [subtitle]="_s2"
        [sourceHtml]="_srcHtml2"
        [sourceTs]="_srcTs2">
        
  <cngx-alert severity="error" title="Save failed" [closable]="true">
    Check your connection and try again.
    <button cngxAlertAction (click)="simulateLoading()" class="chip" style="margin-top:8px">Retry</button>
  </cngx-alert>
      </app-example-card>
      <app-example-card title="State-Driven Visibility"
        [subtitle]="_s3"
        [sourceHtml]="_srcHtml3"
        [sourceTs]="_srcTs3">
        
  <div style="display:flex;flex-wrap:wrap;gap:8px;margin-bottom:12px">
    <button (click)="simulateError()" class="chip">Error</button>
    <button (click)="simulateSuccess()" class="chip">Success (5s auto-dismiss)</button>
    <button (click)="simulateLoading()" class="chip">Loading</button>
    <button (click)="resetState()" class="chip">Reset (idle)</button>
  </div>

  <cngx-alert [state]="saveState" severity="error" title="Operation Status" [closable]="true">
    @switch (saveState.status()) {
      @case ('error') { {{ saveState.error() }} }
      @case ('success') { Saved successfully. This will auto-dismiss in 5s. Hover to pause. }
      @case ('loading') { Loading... please wait. }
    }
  </cngx-alert>

  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">Status</span>
      <span class="event-value">{{ saveState.status() }}</span>
    </div>
  </div>
      </app-example-card>
      <app-example-card title="Boolean Trigger ([when])"
        [subtitle]="_s4"
        [sourceHtml]="_srcHtml4"
        [sourceTs]="_srcTs4">
        
  <div style="display:flex;gap:8px;margin-bottom:12px">
    <button (click)="showAlert.set(true)" class="chip">Show</button>
    <button (click)="showAlert.set(false)" class="chip">Hide</button>
  </div>

  <cngx-alert [when]="showAlert()" severity="info" title="Triggered Alert">
    This alert is controlled by a boolean signal.
  </cngx-alert>
      </app-example-card>
      <app-example-card title="Auto-Collapse"
        [subtitle]="_s5"
        [sourceHtml]="_srcHtml5"
        [sourceTs]="_srcTs5">
        
  <div style="display:flex;gap:8px;margin-bottom:12px">
    <button (click)="triggerCollapseError()" class="chip">Trigger Error</button>
  </div>

  <cngx-alert [state]="collapseState" severity="error" title="Validation Failed"
    [collapsible]="true" [collapseDelay]="3000" [closable]="true">
    3 fields have errors. Please review the form and correct the highlighted fields before submitting again.
    <button cngxAlertAction class="chip" style="margin-top:8px">Show Details</button>
  </cngx-alert>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class AlertDemoComponent {
  protected readonly _s0 = 'Four severity levels with distinct icons. Color is never the only indicator (WCAG 1.4.1). Enter animation plays on first render.';
  protected readonly _s1 = 'Dismiss button via <code>[closable]</code>. SR announces "Alert dismissed".';
  protected readonly _s2 = 'Project <code>button[cngxAlertAction]</code> inside the alert. Sets <code>aria-atomic="false"</code> to prevent full re-announcement on button interaction.';
  protected readonly _s3 = 'Bind <code>[state]</code> to a <code>CngxAsyncState</code>. Shows on error/success/loading, hides on idle. Success auto-dismisses after 5s. Hover/focus pauses the timer (WCAG 2.2.1).';
  protected readonly _s4 = '<code>[when]</code> controls visibility directly. Enter/exit animations play on transitions.';
  protected readonly _s5 = '<code>[collapsible]</code> collapses body after <code>[collapseDelay]</code>. Expands on hover/focus, re-collapses on leave. <code>aria-expanded</code> tracks state. SR still reads full content.';
  protected readonly _srcHtml0 = `<div style="display:flex;flex-direction:column;gap:12px">
    <cngx-alert severity="info" title="Info">This is an informational message.</cngx-alert>
    <cngx-alert severity="success" title="Success">Operation completed successfully.</cngx-alert>
    <cngx-alert severity="warning" title="Warning">Please review before continuing.</cngx-alert>
    <cngx-alert severity="error" title="Error">Something went wrong. Please try again.</cngx-alert>
  </div>`;
  protected readonly _srcTs0 = `import { CngxAlert, CngxAlertAction } from '@cngx/ui/feedback';
import { createManualState } from '@cngx/common/data';


  // ── Static demos ──
  protected readonly dismissed = signal(false);

  // ── State-driven demo ──
  protected readonly saveState = createManualState<string>();

  protected simulateError(): void {
    this.saveState.setError('Network timeout');
  }

  protected simulateSuccess(): void {
    this.saveState.setSuccess('done');
  }

  protected simulateLoading(): void {
    this.saveState.set('loading');
  }

  protected resetState(): void {
    this.saveState.reset();
  }

  // ── [when] demo ──
  protected readonly showAlert = signal(false);

  // ── Collapsible demo ──
  protected readonly collapseState = createManualState<string>();

  protected triggerCollapseError(): void {
    this.collapseState.setError('Validation failed: 3 fields have errors. Please review the form and correct the highlighted fields before submitting again.');
  }`;
  protected readonly _srcHtml1 = `@if (!dismissed()) {
    <cngx-alert severity="warning" title="Unsaved changes" [closable]="true" (dismissed)="dismissed.set(true)">
      Your changes will be lost if you leave this page.
    </cngx-alert>
  } @else {
    <button (click)="dismissed.set(false)" class="chip">Show alert again</button>
  }`;
  protected readonly _srcTs1 = `import { CngxAlert, CngxAlertAction } from '@cngx/ui/feedback';
import { createManualState } from '@cngx/common/data';


  // ── Static demos ──
  protected readonly dismissed = signal(false);

  // ── State-driven demo ──
  protected readonly saveState = createManualState<string>();

  protected simulateError(): void {
    this.saveState.setError('Network timeout');
  }

  protected simulateSuccess(): void {
    this.saveState.setSuccess('done');
  }

  protected simulateLoading(): void {
    this.saveState.set('loading');
  }

  protected resetState(): void {
    this.saveState.reset();
  }

  // ── [when] demo ──
  protected readonly showAlert = signal(false);

  // ── Collapsible demo ──
  protected readonly collapseState = createManualState<string>();

  protected triggerCollapseError(): void {
    this.collapseState.setError('Validation failed: 3 fields have errors. Please review the form and correct the highlighted fields before submitting again.');
  }`;
  protected readonly _srcHtml2 = `<cngx-alert severity="error" title="Save failed" [closable]="true">
    Check your connection and try again.
    <button cngxAlertAction (click)="simulateLoading()" class="chip" style="margin-top:8px">Retry</button>
  </cngx-alert>`;
  protected readonly _srcTs2 = `import { CngxAlert, CngxAlertAction } from '@cngx/ui/feedback';
import { createManualState } from '@cngx/common/data';


  // ── Static demos ──
  protected readonly dismissed = signal(false);

  // ── State-driven demo ──
  protected readonly saveState = createManualState<string>();

  protected simulateError(): void {
    this.saveState.setError('Network timeout');
  }

  protected simulateSuccess(): void {
    this.saveState.setSuccess('done');
  }

  protected simulateLoading(): void {
    this.saveState.set('loading');
  }

  protected resetState(): void {
    this.saveState.reset();
  }

  // ── [when] demo ──
  protected readonly showAlert = signal(false);

  // ── Collapsible demo ──
  protected readonly collapseState = createManualState<string>();

  protected triggerCollapseError(): void {
    this.collapseState.setError('Validation failed: 3 fields have errors. Please review the form and correct the highlighted fields before submitting again.');
  }`;
  protected readonly _srcHtml3 = `<div style="display:flex;flex-wrap:wrap;gap:8px;margin-bottom:12px">
    <button (click)="simulateError()" class="chip">Error</button>
    <button (click)="simulateSuccess()" class="chip">Success (5s auto-dismiss)</button>
    <button (click)="simulateLoading()" class="chip">Loading</button>
    <button (click)="resetState()" class="chip">Reset (idle)</button>
  </div>

  <cngx-alert [state]="saveState" severity="error" title="Operation Status" [closable]="true">
    @switch (saveState.status()) {
      @case ('error') { {{ saveState.error() }} }
      @case ('success') { Saved successfully. This will auto-dismiss in 5s. Hover to pause. }
      @case ('loading') { Loading... please wait. }
    }
  </cngx-alert>

  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">Status</span>
      <span class="event-value">{{ saveState.status() }}</span>
    </div>
  </div>`;
  protected readonly _srcTs3 = `import { CngxAlert, CngxAlertAction } from '@cngx/ui/feedback';
import { createManualState } from '@cngx/common/data';


  // ── Static demos ──
  protected readonly dismissed = signal(false);

  // ── State-driven demo ──
  protected readonly saveState = createManualState<string>();

  protected simulateError(): void {
    this.saveState.setError('Network timeout');
  }

  protected simulateSuccess(): void {
    this.saveState.setSuccess('done');
  }

  protected simulateLoading(): void {
    this.saveState.set('loading');
  }

  protected resetState(): void {
    this.saveState.reset();
  }

  // ── [when] demo ──
  protected readonly showAlert = signal(false);

  // ── Collapsible demo ──
  protected readonly collapseState = createManualState<string>();

  protected triggerCollapseError(): void {
    this.collapseState.setError('Validation failed: 3 fields have errors. Please review the form and correct the highlighted fields before submitting again.');
  }`;
  protected readonly _srcHtml4 = `<div style="display:flex;gap:8px;margin-bottom:12px">
    <button (click)="showAlert.set(true)" class="chip">Show</button>
    <button (click)="showAlert.set(false)" class="chip">Hide</button>
  </div>

  <cngx-alert [when]="showAlert()" severity="info" title="Triggered Alert">
    This alert is controlled by a boolean signal.
  </cngx-alert>`;
  protected readonly _srcTs4 = `import { CngxAlert, CngxAlertAction } from '@cngx/ui/feedback';
import { createManualState } from '@cngx/common/data';


  // ── Static demos ──
  protected readonly dismissed = signal(false);

  // ── State-driven demo ──
  protected readonly saveState = createManualState<string>();

  protected simulateError(): void {
    this.saveState.setError('Network timeout');
  }

  protected simulateSuccess(): void {
    this.saveState.setSuccess('done');
  }

  protected simulateLoading(): void {
    this.saveState.set('loading');
  }

  protected resetState(): void {
    this.saveState.reset();
  }

  // ── [when] demo ──
  protected readonly showAlert = signal(false);

  // ── Collapsible demo ──
  protected readonly collapseState = createManualState<string>();

  protected triggerCollapseError(): void {
    this.collapseState.setError('Validation failed: 3 fields have errors. Please review the form and correct the highlighted fields before submitting again.');
  }`;
  protected readonly _srcHtml5 = `<div style="display:flex;gap:8px;margin-bottom:12px">
    <button (click)="triggerCollapseError()" class="chip">Trigger Error</button>
  </div>

  <cngx-alert [state]="collapseState" severity="error" title="Validation Failed"
    [collapsible]="true" [collapseDelay]="3000" [closable]="true">
    3 fields have errors. Please review the form and correct the highlighted fields before submitting again.
    <button cngxAlertAction class="chip" style="margin-top:8px">Show Details</button>
  </cngx-alert>`;
  protected readonly _srcTs5 = `import { CngxAlert, CngxAlertAction } from '@cngx/ui/feedback';
import { createManualState } from '@cngx/common/data';


  // ── Static demos ──
  protected readonly dismissed = signal(false);

  // ── State-driven demo ──
  protected readonly saveState = createManualState<string>();

  protected simulateError(): void {
    this.saveState.setError('Network timeout');
  }

  protected simulateSuccess(): void {
    this.saveState.setSuccess('done');
  }

  protected simulateLoading(): void {
    this.saveState.set('loading');
  }

  protected resetState(): void {
    this.saveState.reset();
  }

  // ── [when] demo ──
  protected readonly showAlert = signal(false);

  // ── Collapsible demo ──
  protected readonly collapseState = createManualState<string>();

  protected triggerCollapseError(): void {
    this.collapseState.setError('Validation failed: 3 fields have errors. Please review the form and correct the highlighted fields before submitting again.');
  }`;

  // ── Static demos ──
  protected readonly dismissed = signal(false);

  // ── State-driven demo ──
  protected readonly saveState = createManualState<string>();

  protected simulateError(): void {
    this.saveState.setError('Network timeout');
  }

  protected simulateSuccess(): void {
    this.saveState.setSuccess('done');
  }

  protected simulateLoading(): void {
    this.saveState.set('loading');
  }

  protected resetState(): void {
    this.saveState.reset();
  }

  // ── [when] demo ──
  protected readonly showAlert = signal(false);

  // ── Collapsible demo ──
  protected readonly collapseState = createManualState<string>();

  protected triggerCollapseError(): void {
    this.collapseState.setError('Validation failed: 3 fields have errors. Please review the form and correct the highlighted fields before submitting again.');
  }
  
}
