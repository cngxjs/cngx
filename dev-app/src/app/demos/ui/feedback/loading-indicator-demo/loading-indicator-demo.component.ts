// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/ui/feedback/loading-indicator-demo/loading-indicator-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxLoadingIndicator } from '@cngx/ui/feedback';

@Component({
  selector: 'app-loading-indicator-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxLoadingIndicator,
  ],
  template: `
    <app-doc-shell title="Loading Indicator"
      description="Purely visual loading indicator — spinner or bar variant. Delay + minDuration prevent flash."
      [apiComponents]="['CngxLoadingIndicator']">
      <app-example-card title="Spinner Variant"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <button (click)="handleToggle()" class="chip">
    {{ isLoading() ? 'Loading...' : 'Start Loading (2s)' }}
  </button>
  <div style="margin-top:16px;display:flex;align-items:center;gap:12px">
    <cngx-loading-indicator [loading]="isLoading()" variant="spinner" label="Loading data" />
    <span>{{ isLoading() ? 'Fetching data...' : 'Idle' }}</span>
  </div>
      </app-example-card>
      <app-example-card title="Bar Variant"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <div style="position:relative;border:1px solid var(--cngx-border,#ddd);border-radius:8px;padding:24px;min-height:80px">
    <cngx-loading-indicator [loading]="isLoading()" variant="bar" label="Refreshing"
      style="position:absolute;top:0;left:0;right:0" />
    <p>Container content. Click "Start Loading" above to see the bar.</p>
  </div>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class LoadingIndicatorDemoComponent {
  protected readonly _s0 = 'Default variant. Appears after 200ms delay, stays at least 500ms.';
  protected readonly _s1 = 'YouTube-style thin line. Place at the top of a container.';
  protected readonly _srcHtml0 = `<button (click)="handleToggle()" class="chip">
    {{ isLoading() ? 'Loading...' : 'Start Loading (2s)' }}
  </button>
  <div style="margin-top:16px;display:flex;align-items:center;gap:12px">
    <cngx-loading-indicator [loading]="isLoading()" variant="spinner" label="Loading data" />
    <span>{{ isLoading() ? 'Fetching data...' : 'Idle' }}</span>
  </div>`;
  protected readonly _srcTs0 = `import { CngxLoadingIndicator } from '@cngx/ui/feedback';


  protected readonly isLoading = signal(false);

  protected handleToggle(): void {
    this.isLoading.set(true);
    setTimeout(() => this.isLoading.set(false), 2000);
  }`;
  protected readonly _srcHtml1 = `<div style="position:relative;border:1px solid var(--cngx-border,#ddd);border-radius:8px;padding:24px;min-height:80px">
    <cngx-loading-indicator [loading]="isLoading()" variant="bar" label="Refreshing"
      style="position:absolute;top:0;left:0;right:0" />
    <p>Container content. Click "Start Loading" above to see the bar.</p>
  </div>`;
  protected readonly _srcTs1 = `import { CngxLoadingIndicator } from '@cngx/ui/feedback';


  protected readonly isLoading = signal(false);

  protected handleToggle(): void {
    this.isLoading.set(true);
    setTimeout(() => this.isLoading.set(false), 2000);
  }`;

  protected readonly isLoading = signal(false);

  protected handleToggle(): void {
    this.isLoading.set(true);
    setTimeout(() => this.isLoading.set(false), 2000);
  }
  
}
