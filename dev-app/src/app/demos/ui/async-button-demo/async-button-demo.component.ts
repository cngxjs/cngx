// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/ui/async-button-demo/async-button-demo.story.ts

import { ChangeDetectionStrategy, Component } from '@angular/core';
import { ExampleCardComponent } from '../../../shared/example-card.component';
import { DocShellComponent } from '../../../shared/doc-shell.component';
import { CngxActionButton, CngxPending, CngxSucceeded, CngxFailed } from '@cngx/ui';
import { MatProgressSpinnerModule } from '@angular/material/progress-spinner';

@Component({
  selector: 'app-async-button-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxActionButton,
    CngxPending,
    CngxSucceeded,
    CngxFailed,
    MatProgressSpinnerModule,
  ],
  template: `
    <app-doc-shell title="ActionButton"
      description="Action button molecule with built-in status templates. Zero boilerplate alternative to CngxAsyncClick."
      overview="<p><code>CngxActionButton</code> wraps async actions into a button with automatic state management. Use string labels for simple cases or project <code>cngxPending</code>/<code>cngxSucceeded</code>/<code>cngxFailed</code> templates for custom UI per state. No <code>@if</code> boilerplate needed.</p>"
      [apiComponents]="['CngxActionButton', 'CngxPending', 'CngxSucceeded', 'CngxFailed']">
      <app-example-card title="String Labels"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <div class="button-row" style="gap:12px">
    <cngx-action-button [action]="saveAction" pendingLabel="Saving..." succeededLabel="Saved!">
      Save Draft
    </cngx-action-button>
    <cngx-action-button [action]="deleteAction" pendingLabel="Deleting..." failedLabel="Failed!">
      Delete
    </cngx-action-button>
  </div>
      </app-example-card>
      <app-example-card title="Template Slots"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <div class="button-row">
    <cngx-action-button [action]="submitAction">
      Submit Order
      <ng-template cngxPending>
        <span style="display:inline-flex;align-items:center;gap:6px">
          <mat-spinner diameter="16" /> Processing...
        </span>
      </ng-template>
      <ng-template cngxSucceeded>Order placed!</ng-template>
      <ng-template cngxFailed let-err>{{ err }} -- retry?</ng-template>
    </cngx-action-button>
  </div>
      </app-example-card>
      <app-example-card title="Random Outcome"
        [subtitle]="_s2"
        [sourceHtml]="_srcHtml2"
        [sourceTs]="_srcTs2">
        
  <div class="button-row">
    <cngx-action-button [action]="submitAction"
      pendingLabel="Rolling..." succeededLabel="Won!" failedLabel="Lost">
      Roll the Dice
    </cngx-action-button>
  </div>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class AsyncButtonDemoComponent {
  protected readonly _s0 = 'The simplest usage: pass <code>pendingLabel</code>, <code>succeededLabel</code>, <code>failedLabel</code> as strings. Default content is shown when idle.';
  protected readonly _s1 = 'Project <code>cngxPending</code>, <code>cngxSucceeded</code>, <code>cngxFailed</code> templates for full control. The <code>cngxFailed</code> template receives the error as <code>let-err</code>.';
  protected readonly _s2 = 'The action resolves or rejects randomly. Click multiple times to see both states. Feedback auto-resets after 2 seconds. Uses string labels — no templates needed.';
  protected readonly _srcHtml0 = `<div class="button-row" style="gap:12px">
    <cngx-action-button [action]="saveAction" pendingLabel="Saving..." succeededLabel="Saved!">
      Save Draft
    </cngx-action-button>
    <cngx-action-button [action]="deleteAction" pendingLabel="Deleting..." failedLabel="Failed!">
      Delete
    </cngx-action-button>
  </div>`;
  protected readonly _srcTs0 = `import { CngxActionButton, CngxPending, CngxSucceeded, CngxFailed } from '@cngx/ui';
import { MatProgressSpinnerModule } from '@angular/material/progress-spinner';


  protected readonly saveAction = () => new Promise<void>(resolve => setTimeout(resolve, 1500));
  protected readonly deleteAction = () => new Promise<void>((_, reject) =>
    setTimeout(() => reject(new Error('403 Forbidden')), 1000),
  );
  protected readonly submitAction = () => new Promise<void>((resolve, reject) =>
    setTimeout(() => Math.random() > 0.4 ? resolve() : reject(new Error('Random failure')), 1200),
  );`;
  protected readonly _srcHtml1 = `<div class="button-row">
    <cngx-action-button [action]="submitAction">
      Submit Order
      <ng-template cngxPending>
        <span style="display:inline-flex;align-items:center;gap:6px">
          <mat-spinner diameter="16" /> Processing...
        </span>
      </ng-template>
      <ng-template cngxSucceeded>Order placed!</ng-template>
      <ng-template cngxFailed let-err>{{ err }} -- retry?</ng-template>
    </cngx-action-button>
  </div>`;
  protected readonly _srcTs1 = `import { CngxActionButton, CngxPending, CngxSucceeded, CngxFailed } from '@cngx/ui';
import { MatProgressSpinnerModule } from '@angular/material/progress-spinner';


  protected readonly saveAction = () => new Promise<void>(resolve => setTimeout(resolve, 1500));
  protected readonly deleteAction = () => new Promise<void>((_, reject) =>
    setTimeout(() => reject(new Error('403 Forbidden')), 1000),
  );
  protected readonly submitAction = () => new Promise<void>((resolve, reject) =>
    setTimeout(() => Math.random() > 0.4 ? resolve() : reject(new Error('Random failure')), 1200),
  );`;
  protected readonly _srcHtml2 = `<div class="button-row">
    <cngx-action-button [action]="submitAction"
      pendingLabel="Rolling..." succeededLabel="Won!" failedLabel="Lost">
      Roll the Dice
    </cngx-action-button>
  </div>`;
  protected readonly _srcTs2 = `import { CngxActionButton, CngxPending, CngxSucceeded, CngxFailed } from '@cngx/ui';
import { MatProgressSpinnerModule } from '@angular/material/progress-spinner';


  protected readonly saveAction = () => new Promise<void>(resolve => setTimeout(resolve, 1500));
  protected readonly deleteAction = () => new Promise<void>((_, reject) =>
    setTimeout(() => reject(new Error('403 Forbidden')), 1000),
  );
  protected readonly submitAction = () => new Promise<void>((resolve, reject) =>
    setTimeout(() => Math.random() > 0.4 ? resolve() : reject(new Error('Random failure')), 1200),
  );`;

  protected readonly saveAction = () => new Promise<void>(resolve => setTimeout(resolve, 1500));
  protected readonly deleteAction = () => new Promise<void>((_, reject) =>
    setTimeout(() => reject(new Error('403 Forbidden')), 1000),
  );
  protected readonly submitAction = () => new Promise<void>((resolve, reject) =>
    setTimeout(() => Math.random() > 0.4 ? resolve() : reject(new Error('Random failure')), 1200),
  );
  
}
