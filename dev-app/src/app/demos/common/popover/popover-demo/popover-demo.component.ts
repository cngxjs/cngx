// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/popover/popover-demo/popover-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxPopover, CngxPopoverTrigger } from '@cngx/common/popover';
import type { PopoverPlacement } from '@cngx/common/popover';

@Component({
  selector: 'app-popover-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxPopover,
    CngxPopoverTrigger,
  ],
  template: `
    <app-doc-shell title="Popover"
      description="Signal-driven state machine for the native Popover API. CSS Anchor Positioning, transition-aware lifecycle, no CDK Overlay dependency."
      overview="<p><code>[cngxPopover]</code> wraps the browser Popover API with a reactive state machine (<code>closed → opening → open → closing → closed</code>). <code>[cngxPopoverTrigger]</code> provides ARIA wiring (<code>aria-expanded</code>, <code>aria-controls</code>, <code>aria-haspopup</code>). The consumer owns all event handling.</p>"
      [apiComponents]="['CngxPopover', 'CngxPopoverTrigger']">
      <app-example-card title="Click Popover"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <div style="padding-top:20px">
    <button [cngxPopoverTrigger]="pop" (click)="pop.toggle()" class="chip"
            haspopup="menu">
      Actions
    </button>
    <div cngxPopover #pop="cngxPopover" placement="bottom-start"
         style="list-style:none;margin:0">
      <menu style="margin:0;padding:4px 0;min-width:140px">
        @for (item of menuItems(); track item) {
          <li>
            <button (click)="pop.hide()" type="button"
                    style="display:block;width:100%;padding:8px 16px;border:none;background:none;
                           text-align:left;cursor:pointer;font:inherit">
              {{ item }}
            </button>
          </li>
        }
      </menu>
    </div>
  </div>
  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">state</span>
      <span class="event-value">{{ pop.state() }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">isVisible</span>
      <span class="event-value">{{ pop.isVisible() }}</span>
    </div>
  </div>
      </app-example-card>
      <app-example-card title="Placement Variants"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <div style="display:flex;gap:16px;flex-wrap:wrap;padding:40px 0">
    @for (p of placements; track p) {
      <div>
        <button [cngxPopoverTrigger]="plPop" (click)="plPop.toggle()" class="chip">
          {{ p }}
        </button>
        <div cngxPopover #plPop="cngxPopover" [placement]="p"
             style="padding:8px 12px;font-size:0.8125rem">
          Popover at {{ p }}
        </div>
      </div>
    }
  </div>
      </app-example-card>
      <app-example-card title="Controlled Open"
        [subtitle]="_s2"
        [sourceHtml]="_srcHtml2"
        [sourceTs]="_srcTs2">
        
  <div style="display:flex;gap:12px;align-items:center;padding-top:20px">
    <label [cngxPopoverTrigger]="ctrl"
           style="display:flex;align-items:center;gap:6px;font-size:0.875rem;cursor:pointer">
      <input type="checkbox" #chk (change)="0" />
      Show popover
    </label>
    <div cngxPopover #ctrl="cngxPopover" [cngxPopoverOpen]="chk.checked" placement="bottom"
         [exclusive]="false"
         style="padding:8px 12px;font-size:0.8125rem">
      Controlled by checkbox
    </div>
  </div>
  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">state</span>
      <span class="event-value">{{ ctrl.state() }}</span>
    </div>
  </div>
      </app-example-card>
      <app-example-card title="Escape & Mode"
        [subtitle]="_s3"
        [sourceHtml]="_srcHtml3"
        [sourceTs]="_srcTs3">
        
  <div style="display:flex;gap:16px;flex-wrap:wrap;padding-top:20px">
    <div>
      <button [cngxPopoverTrigger]="noEsc" (click)="noEsc.toggle()" class="chip">
        No Escape
      </button>
      <div cngxPopover #noEsc="cngxPopover" [closeOnEscape]="false" placement="bottom"
           style="padding:8px 12px;font-size:0.8125rem">
        Escape won't close this. Click the button again.
      </div>
    </div>
    <div>
      <button [cngxPopoverTrigger]="autoPop" (click)="autoPop.toggle()" class="chip">
        Auto mode
      </button>
      <div cngxPopover #autoPop="cngxPopover" mode="auto" placement="bottom"
           style="padding:8px 12px;font-size:0.8125rem">
        Click outside to dismiss (browser light dismiss).
      </div>
    </div>
  </div>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class PopoverDemoComponent {
  protected readonly _s0 = 'The consumer binds <code>(click)="pop.toggle()"</code> — the trigger directive only handles ARIA.';
  protected readonly _s1 = 'CSS Anchor Positioning handles all placement. No JavaScript repositioning needed.';
  protected readonly _s2 = 'Bind <code>[cngxPopoverOpen]</code> to drive the popover reactively — no <code>show()</code>/<code>hide()</code> calls needed.';
  protected readonly _s3 = 'Set <code>[closeOnEscape]="false"</code> to prevent Escape dismiss. Set <code>mode="auto"</code> for browser-native light dismiss.';
  protected readonly _srcHtml0 = `<div style="padding-top:20px">
    <button [cngxPopoverTrigger]="pop" (click)="pop.toggle()" class="chip"
            haspopup="menu">
      Actions
    </button>
    <div cngxPopover #pop="cngxPopover" placement="bottom-start"
         style="list-style:none;margin:0">
      <menu style="margin:0;padding:4px 0;min-width:140px">
        @for (item of menuItems(); track item) {
          <li>
            <button (click)="pop.hide()" type="button"
                    style="display:block;width:100%;padding:8px 16px;border:none;background:none;
                           text-align:left;cursor:pointer;font:inherit">
              {{ item }}
            </button>
          </li>
        }
      </menu>
    </div>
  </div>
  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">state</span>
      <span class="event-value">{{ pop.state() }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">isVisible</span>
      <span class="event-value">{{ pop.isVisible() }}</span>
    </div>
  </div>`;
  protected readonly _srcTs0 = `import { CngxPopover, CngxPopoverTrigger } from '@cngx/common/popover';
import type { PopoverPlacement } from '@cngx/common/popover';


  protected readonly menuItems = signal(['Edit', 'Duplicate', 'Archive', 'Delete']);
  protected readonly placements: PopoverPlacement[] = ['bottom', 'bottom-start', 'bottom-end', 'top', 'left', 'right'];`;
  protected readonly _srcHtml1 = `<div style="display:flex;gap:16px;flex-wrap:wrap;padding:40px 0">
    @for (p of placements; track p) {
      <div>
        <button [cngxPopoverTrigger]="plPop" (click)="plPop.toggle()" class="chip">
          {{ p }}
        </button>
        <div cngxPopover #plPop="cngxPopover" [placement]="p"
             style="padding:8px 12px;font-size:0.8125rem">
          Popover at {{ p }}
        </div>
      </div>
    }
  </div>`;
  protected readonly _srcTs1 = `import { CngxPopover, CngxPopoverTrigger } from '@cngx/common/popover';
import type { PopoverPlacement } from '@cngx/common/popover';


  protected readonly menuItems = signal(['Edit', 'Duplicate', 'Archive', 'Delete']);
  protected readonly placements: PopoverPlacement[] = ['bottom', 'bottom-start', 'bottom-end', 'top', 'left', 'right'];`;
  protected readonly _srcHtml2 = `<div style="display:flex;gap:12px;align-items:center;padding-top:20px">
    <label [cngxPopoverTrigger]="ctrl"
           style="display:flex;align-items:center;gap:6px;font-size:0.875rem;cursor:pointer">
      <input type="checkbox" #chk (change)="0" />
      Show popover
    </label>
    <div cngxPopover #ctrl="cngxPopover" [cngxPopoverOpen]="chk.checked" placement="bottom"
         [exclusive]="false"
         style="padding:8px 12px;font-size:0.8125rem">
      Controlled by checkbox
    </div>
  </div>
  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">state</span>
      <span class="event-value">{{ ctrl.state() }}</span>
    </div>
  </div>`;
  protected readonly _srcTs2 = `import { CngxPopover, CngxPopoverTrigger } from '@cngx/common/popover';
import type { PopoverPlacement } from '@cngx/common/popover';


  protected readonly menuItems = signal(['Edit', 'Duplicate', 'Archive', 'Delete']);
  protected readonly placements: PopoverPlacement[] = ['bottom', 'bottom-start', 'bottom-end', 'top', 'left', 'right'];`;
  protected readonly _srcHtml3 = `<div style="display:flex;gap:16px;flex-wrap:wrap;padding-top:20px">
    <div>
      <button [cngxPopoverTrigger]="noEsc" (click)="noEsc.toggle()" class="chip">
        No Escape
      </button>
      <div cngxPopover #noEsc="cngxPopover" [closeOnEscape]="false" placement="bottom"
           style="padding:8px 12px;font-size:0.8125rem">
        Escape won't close this. Click the button again.
      </div>
    </div>
    <div>
      <button [cngxPopoverTrigger]="autoPop" (click)="autoPop.toggle()" class="chip">
        Auto mode
      </button>
      <div cngxPopover #autoPop="cngxPopover" mode="auto" placement="bottom"
           style="padding:8px 12px;font-size:0.8125rem">
        Click outside to dismiss (browser light dismiss).
      </div>
    </div>
  </div>`;
  protected readonly _srcTs3 = `import { CngxPopover, CngxPopoverTrigger } from '@cngx/common/popover';
import type { PopoverPlacement } from '@cngx/common/popover';


  protected readonly menuItems = signal(['Edit', 'Duplicate', 'Archive', 'Delete']);
  protected readonly placements: PopoverPlacement[] = ['bottom', 'bottom-start', 'bottom-end', 'top', 'left', 'right'];`;

  protected readonly menuItems = signal(['Edit', 'Duplicate', 'Archive', 'Delete']);
  protected readonly placements: PopoverPlacement[] = ['bottom', 'bottom-start', 'bottom-end', 'top', 'left', 'right'];
  
}
