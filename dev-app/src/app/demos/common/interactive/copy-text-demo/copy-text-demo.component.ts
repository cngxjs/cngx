// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/interactive/copy-text-demo/copy-text-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxCopyText } from '@cngx/common/interactive';

@Component({
  selector: 'app-copy-text-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxCopyText,
  ],
  template: `
    <app-doc-shell title="Copy Text"
      description="Clipboard copy behavior without forms dependency. Clipboard API with execCommand fallback."
      overview="<p><code>[cngxCopyText]</code> copies text to the clipboard on click. Shows a <code>copied()</code> signal for visual feedback that auto-resets after <code>resetDelay</code> ms. Falls back to <code>execCommand(&quot;copy&quot;)</code> when the Clipboard API is unavailable.</p>"
      [apiComponents]="['CngxCopyText']">
      <app-example-card title="Copy Token"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <div style="display:flex;align-items:center;gap:8px">
    <code style="padding:6px 10px;background:var(--code-bg,#f5f5f5);border:1px solid var(--code-border,#ddd);
                 border-radius:4px;font-size:0.8125rem;overflow:hidden;text-overflow:ellipsis;max-width:280px">
      {{ token() }}
    </code>
    <button [cngxCopyText]="token()" #cp="cngxCopyText" class="chip"
            [style.background]="cp.copied() ? 'var(--success-bg, #e8f5e9)' : ''"
            [style.borderColor]="cp.copied() ? 'var(--success-fg, #2e7d32)' : ''">
      {{ cp.copied() ? 'Copied!' : 'Copy' }}
    </button>
  </div>
  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">copied</span>
      <span class="event-value">{{ cp.copied() }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">supported</span>
      <span class="event-value">{{ cp.supported }}</span>
    </div>
  </div>
      </app-example-card>
      <app-example-card title="Copy URL with SR Announcement"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <div style="display:flex;align-items:center;gap:8px">
    <span style="font-size:0.875rem;color:var(--cngx-text-secondary,#666)">{{ shareUrl() }}</span>
    <button [cngxCopyText]="shareUrl()" #cp2="cngxCopyText" class="chip">
      {{ cp2.copied() ? 'Link copied!' : 'Copy Link' }}
    </button>
    <span aria-live="polite" style="position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0,0,0,0)">
      {{ cp2.copied() ? 'Link copied to clipboard' : '' }}
    </span>
  </div>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class CopyTextDemoComponent {
  protected readonly _s0 = 'Click the button to copy the API token. The <code>copied()</code> signal drives "Copied!" feedback.';
  protected readonly _s1 = 'Pair with an <code>aria-live</code> region so screen readers announce the copy.';
  protected readonly _srcHtml0 = `<div style="display:flex;align-items:center;gap:8px">
    <code style="padding:6px 10px;background:var(--code-bg,#f5f5f5);border:1px solid var(--code-border,#ddd);
                 border-radius:4px;font-size:0.8125rem;overflow:hidden;text-overflow:ellipsis;max-width:280px">
      {{ token() }}
    </code>
    <button [cngxCopyText]="token()" #cp="cngxCopyText" class="chip"
            [style.background]="cp.copied() ? 'var(--success-bg, #e8f5e9)' : ''"
            [style.borderColor]="cp.copied() ? 'var(--success-fg, #2e7d32)' : ''">
      {{ cp.copied() ? 'Copied!' : 'Copy' }}
    </button>
  </div>
  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">copied</span>
      <span class="event-value">{{ cp.copied() }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">supported</span>
      <span class="event-value">{{ cp.supported }}</span>
    </div>
  </div>`;
  protected readonly _srcTs0 = `import { CngxCopyText } from '@cngx/common/interactive';


  protected readonly token = signal('eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9');
  protected readonly shareUrl = signal('https://cngx.dev/docs/copy-text');`;
  protected readonly _srcHtml1 = `<div style="display:flex;align-items:center;gap:8px">
    <span style="font-size:0.875rem;color:var(--cngx-text-secondary,#666)">{{ shareUrl() }}</span>
    <button [cngxCopyText]="shareUrl()" #cp2="cngxCopyText" class="chip">
      {{ cp2.copied() ? 'Link copied!' : 'Copy Link' }}
    </button>
    <span aria-live="polite" style="position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0,0,0,0)">
      {{ cp2.copied() ? 'Link copied to clipboard' : '' }}
    </span>
  </div>`;
  protected readonly _srcTs1 = `import { CngxCopyText } from '@cngx/common/interactive';


  protected readonly token = signal('eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9');
  protected readonly shareUrl = signal('https://cngx.dev/docs/copy-text');`;

  protected readonly token = signal('eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9');
  protected readonly shareUrl = signal('https://cngx.dev/docs/copy-text');
  
}
