// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/interactive/button-multi-toggle-group-demo/button-multi-toggle-group-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxButtonMultiToggleGroup, CngxButtonToggle } from '@cngx/common/interactive';

@Component({
  selector: 'app-button-multi-toggle-group-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxButtonMultiToggleGroup,
    CngxButtonToggle,
  ],
  template: `
    <app-doc-shell title="Button toggle group (multi)"
      description="Multi-select button-toggle group. W3C APG toolbar semantics — Tab enters the group, arrow keys MOVE focus only (no auto-select), Space and Enter toggle membership of the focused leaf. Mode is static: this is the multi half of a deliberate split, never a runtime [selectionMode] flag. The leaf <button cngxButtonToggle> binds aria-selected (toolbar APG) instead of aria-checked (radiogroup APG); the choice happens AT INJECTION TIME based on which parent token resolves. Provides CNGX_BUTTON_MULTI_TOGGLE_GROUP for the leaf to inject (never the concrete class) and CNGX_CONTROL_VALUE for forms-bridge integration."
      [apiComponents]="['CngxButtonMultiToggleGroup', 'CngxButtonToggle', 'CNGX_BUTTON_MULTI_TOGGLE_GROUP', 'CNGX_CONTROL_VALUE']">
      <app-example-card title="Basic — multi-filter toolbar"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0"
        [sourceCss]="_srcCss0">
        
  <cngx-button-multi-toggle-group [(selectedValues)]="filters" name="filters">
    <button cngxButtonToggle value="open">Open</button>
    <button cngxButtonToggle value="closed">Closed</button>
    <button cngxButtonToggle value="archived">Archived</button>
  </cngx-button-multi-toggle-group>
  <p class="caption">
    Filters: <code>{{ filters().join(', ') || '(none)' }}</code>
  </p>
      </app-example-card>
      <app-example-card title="Disabled — group cascade vs per-toggle"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1"
        [sourceCss]="_srcCss1">
        
  <button type="button" (click)="groupDisabled.set(!groupDisabled())">
    {{ groupDisabled() ? 'Enable group' : 'Disable group' }}
  </button>
  <cngx-button-multi-toggle-group
    [(selectedValues)]="filters"
    [disabled]="groupDisabled()"
    name="filters-d"
  >
    <button cngxButtonToggle value="open">Open</button>
    <button cngxButtonToggle value="closed">Closed</button>
    <button cngxButtonToggle value="archived" [disabled]="true">Archived (locked)</button>
  </cngx-button-multi-toggle-group>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class ButtonMultiToggleGroupDemoComponent {
  protected readonly _s0 = 'Click any toggle to add or remove it from the selection. Tab into the group and use <strong>ArrowLeft</strong>/<strong>ArrowRight</strong> to move focus (no auto-select); <strong>Space</strong>/<strong>Enter</strong> on the focused toggle flips its membership. aria-selected reflects per-leaf membership reactively via the selection controller.';
  protected readonly _s1 = 'Group <code>[disabled]</code> blocks every leaf\'s <code>toggle()</code> dispatch and reflects <code>aria-disabled="true"</code> on each toggle. Per-toggle <code>[disabled]</code> blocks only that leaf and is skipped by roving navigation. Both also reflect the native <code>disabled</code> attribute so form submission engines see it.';
  protected readonly _srcHtml0 = `<cngx-button-multi-toggle-group [(selectedValues)]="filters" name="filters">
    <button cngxButtonToggle value="open">Open</button>
    <button cngxButtonToggle value="closed">Closed</button>
    <button cngxButtonToggle value="archived">Archived</button>
  </cngx-button-multi-toggle-group>
  <p class="caption">
    Filters: <code>{{ filters().join(', ') || '(none)' }}</code>
  </p>`;
  protected readonly _srcTs0 = `import { CngxButtonMultiToggleGroup, CngxButtonToggle } from '@cngx/common/interactive';


  protected readonly filters = signal<string[]>(['open']);
  protected readonly groupDisabled = signal(false);`;
  protected readonly _srcCss0 = `.caption { font-size: 0.875em; color: var(--cngx-text-muted, #6b7280); margin-top: 12px; }`;
  protected readonly _srcHtml1 = `<button type="button" (click)="groupDisabled.set(!groupDisabled())">
    {{ groupDisabled() ? 'Enable group' : 'Disable group' }}
  </button>
  <cngx-button-multi-toggle-group
    [(selectedValues)]="filters"
    [disabled]="groupDisabled()"
    name="filters-d"
  >
    <button cngxButtonToggle value="open">Open</button>
    <button cngxButtonToggle value="closed">Closed</button>
    <button cngxButtonToggle value="archived" [disabled]="true">Archived (locked)</button>
  </cngx-button-multi-toggle-group>`;
  protected readonly _srcTs1 = `import { CngxButtonMultiToggleGroup, CngxButtonToggle } from '@cngx/common/interactive';


  protected readonly filters = signal<string[]>(['open']);
  protected readonly groupDisabled = signal(false);`;
  protected readonly _srcCss1 = `button:not([cngxButtonToggle]) { margin-bottom: 16px; }`;

  protected readonly filters = signal<string[]>(['open']);
  protected readonly groupDisabled = signal(false);
  
}
