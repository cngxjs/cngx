// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/interactive/button-toggle-group-demo/button-toggle-group-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxButtonToggleGroup, CngxButtonToggle } from '@cngx/common/interactive';

@Component({
  selector: 'app-button-toggle-group-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxButtonToggleGroup,
    CngxButtonToggle,
  ],
  template: `
    <app-doc-shell title="Button toggle group (single)"
      description="Single-select button-toggle group. W3C APG radiogroup semantics — Tab enters the group, arrow keys MOVE focus AND auto-select the next toggle (auto-select-on-arrow), Space and Enter pick the focused toggle. Tab-into-group does not auto-select (no preceding arrow keydown). Mode is static: this is the single half of a deliberate split, never a runtime [selectionMode] flag. The leaf <button cngxButtonToggle> binds aria-checked. Provides CNGX_BUTTON_TOGGLE_GROUP for the leaf to inject (never the concrete class)."
      [apiComponents]="['CngxButtonToggleGroup', 'CngxButtonToggle', 'CNGX_BUTTON_TOGGLE_GROUP', 'CNGX_CONTROL_VALUE']">
      <app-example-card title="Basic — view switcher"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0"
        [sourceCss]="_srcCss0">
        
  <cngx-button-toggle-group label="Layout" [(value)]="view">
    <button cngxButtonToggle value="grid">Grid</button>
    <button cngxButtonToggle value="list">List</button>
    <button cngxButtonToggle value="table">Table</button>
  </cngx-button-toggle-group>
  <p class="caption">View: <code>{{ view() ?? '(none)' }}</code></p>
      </app-example-card>
      <app-example-card title="Vertical orientation"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <cngx-button-toggle-group label="Layout (vertical)" [(value)]="view" orientation="vertical">
    <button cngxButtonToggle value="grid">Grid</button>
    <button cngxButtonToggle value="list">List</button>
    <button cngxButtonToggle value="table">Table</button>
  </cngx-button-toggle-group>
      </app-example-card>
      <app-example-card title="Disabled — group cascade vs per-toggle"
        [subtitle]="_s2"
        [sourceHtml]="_srcHtml2"
        [sourceTs]="_srcTs2"
        [sourceCss]="_srcCss2">
        
  <button type="button" (click)="groupDisabled.set(!groupDisabled())">
    {{ groupDisabled() ? 'Enable group' : 'Disable group' }}
  </button>
  <cngx-button-toggle-group label="Layout (disabled-cascade demo)" [(value)]="view" [disabled]="groupDisabled()">
    <button cngxButtonToggle value="grid">Grid</button>
    <button cngxButtonToggle value="list">List</button>
    <button cngxButtonToggle value="table" [disabled]="true">Table (locked)</button>
  </cngx-button-toggle-group>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class ButtonToggleGroupDemoComponent {
  protected readonly _s0 = 'Click any toggle, or Tab into the group and use ArrowLeft/ArrowRight to move + select. <strong>Space</strong>/<strong>Enter</strong> on the focused toggle pick it idempotently. aria-checked reflects the group value reactively via <code>group.value() === toggle.value()</code>.';
  protected readonly _s1 = '<code>[orientation]="\'vertical\'"</code> stacks the toggles and tells the host roving directive to use ArrowUp/ArrowDown for navigation.';
  protected readonly _s2 = 'Group <code>[disabled]</code> cascades to every toggle via <code>toggleDisabled = computed(() => group.disabled() || disabled())</code>. Per-toggle <code>[disabled]</code> blocks only that leaf and is skipped by roving navigation. Both reflect the native <code>disabled</code> attribute so form submission engines see it.';
  protected readonly _srcHtml0 = `<cngx-button-toggle-group label="Layout" [(value)]="view">
    <button cngxButtonToggle value="grid">Grid</button>
    <button cngxButtonToggle value="list">List</button>
    <button cngxButtonToggle value="table">Table</button>
  </cngx-button-toggle-group>
  <p class="caption">View: <code>{{ view() ?? '(none)' }}</code></p>`;
  protected readonly _srcTs0 = `import { CngxButtonToggleGroup, CngxButtonToggle } from '@cngx/common/interactive';


  protected readonly view = signal<'grid' | 'list' | 'table' | undefined>('grid');
  protected readonly groupDisabled = signal(false);
  protected readonly orientation = signal<'horizontal' | 'vertical'>('horizontal');`;
  protected readonly _srcCss0 = `.caption { font-size: 0.875em; color: var(--cngx-text-muted, #6b7280); margin-top: 12px; }`;
  protected readonly _srcHtml1 = `<cngx-button-toggle-group label="Layout (vertical)" [(value)]="view" orientation="vertical">
    <button cngxButtonToggle value="grid">Grid</button>
    <button cngxButtonToggle value="list">List</button>
    <button cngxButtonToggle value="table">Table</button>
  </cngx-button-toggle-group>`;
  protected readonly _srcTs1 = `import { CngxButtonToggleGroup, CngxButtonToggle } from '@cngx/common/interactive';


  protected readonly view = signal<'grid' | 'list' | 'table' | undefined>('grid');
  protected readonly groupDisabled = signal(false);
  protected readonly orientation = signal<'horizontal' | 'vertical'>('horizontal');`;
  protected readonly _srcHtml2 = `<button type="button" (click)="groupDisabled.set(!groupDisabled())">
    {{ groupDisabled() ? 'Enable group' : 'Disable group' }}
  </button>
  <cngx-button-toggle-group label="Layout (disabled-cascade demo)" [(value)]="view" [disabled]="groupDisabled()">
    <button cngxButtonToggle value="grid">Grid</button>
    <button cngxButtonToggle value="list">List</button>
    <button cngxButtonToggle value="table" [disabled]="true">Table (locked)</button>
  </cngx-button-toggle-group>`;
  protected readonly _srcTs2 = `import { CngxButtonToggleGroup, CngxButtonToggle } from '@cngx/common/interactive';


  protected readonly view = signal<'grid' | 'list' | 'table' | undefined>('grid');
  protected readonly groupDisabled = signal(false);
  protected readonly orientation = signal<'horizontal' | 'vertical'>('horizontal');`;
  protected readonly _srcCss2 = `button:not([cngxButtonToggle]) { margin-bottom: 16px; }`;

  protected readonly view = signal<'grid' | 'list' | 'table' | undefined>('grid');
  protected readonly groupDisabled = signal(false);
  protected readonly orientation = signal<'horizontal' | 'vertical'>('horizontal');
  
}
