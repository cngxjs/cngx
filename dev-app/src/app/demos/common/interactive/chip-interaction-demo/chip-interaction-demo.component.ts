// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/interactive/chip-interaction-demo/chip-interaction-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxChipInteraction } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';

@Component({
  selector: 'app-chip-interaction-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxChipInteraction,
    CngxChip,
  ],
  template: `
    <app-doc-shell title="Chip interaction (standalone)"
      description="Standalone interactive chip atom — applies <code>[cngxChipInteraction]</code> onto the existing <code>&lt;cngx-chip&gt;</code> display pill and adds <code>role=&quot;option&quot;</code> selection semantics with a local-owned <code>selected</code> model. Provides <code>CNGX_CONTROL_VALUE</code> so <code>CngxFormBridge</code> can adapt it to Reactive Forms. Use this when a chip stands alone (filter tag, single suggestion); use <code>[cngxChipInGroup]</code> instead for chips inside a chip-group."
      [apiComponents]="['CngxChipInteraction', 'CngxChip', 'CNGX_CONTROL_VALUE']">
      <app-example-card title="Basic — toggle on click, Space, or Enter"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <cngx-chip cngxChipInteraction [value]="'favourite'" [(selected)]="favourite">Favourite</cngx-chip>
  <cngx-chip cngxChipInteraction [value]="'featured'" [(selected)]="featured">Featured</cngx-chip>
  <p class="caption">favourite: <code>{{ favourite() }}</code> • featured: <code>{{ featured() }}</code></p>
      </app-example-card>
      <app-example-card title="Removable with (removeRequest) on Backspace / Delete"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <cngx-chip
    cngxChipInteraction
    [value]="'tag'"
    [(selected)]="favourite"
    [removable]="true"
    (removeRequest)="tagRemoved.set(tagRemoved() + 1)"
    (remove)="tagRemoved.set(tagRemoved() + 1)"
  >Removable tag</cngx-chip>
  <p class="caption">remove fired: <code>{{ tagRemoved() }}</code></p>
      </app-example-card>
      <app-example-card title="Disabled state"
        [subtitle]="_s2"
        [sourceHtml]="_srcHtml2"
        [sourceTs]="_srcTs2">
        
  <cngx-chip cngxChipInteraction [value]="'locked'" [disabled]="locked()" [(selected)]="favourite">
    Locked tag
  </cngx-chip>
  <button type="button" (click)="locked.set(!locked())">toggle disabled</button>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class ChipInteractionDemoComponent {
  protected readonly _s0 = 'The chip exposes <code>[(selected)]</code> as a two-way model. Click, Space, or Enter flips it; ARIA <code>aria-selected</code> reflects the state reactively.';
  protected readonly _s1 = 'Backspace and Delete fire the <code>(removeRequest)</code> output — the consumer decides what removal means. Click on the chip body still toggles selection; click on the close button fires <code>(remove)</code> from <code>&lt;cngx-chip&gt;</code> (no double-toggle).';
  protected readonly _s2 = 'When <code>[disabled]="true"</code>, click + keyboard + remove are silently short-circuited; <code>aria-disabled="true"</code> and <code>tabindex=-1</code> reflect the state.';
  protected readonly _srcHtml0 = `<cngx-chip cngxChipInteraction [value]="'favourite'" [(selected)]="favourite">Favourite</cngx-chip>
  <cngx-chip cngxChipInteraction [value]="'featured'" [(selected)]="featured">Featured</cngx-chip>
  <p class="caption">favourite: <code>{{ favourite() }}</code> • featured: <code>{{ featured() }}</code></p>`;
  protected readonly _srcTs0 = `import { CngxChipInteraction } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';


  protected readonly favourite = signal(false);
  protected readonly featured = signal(true);
  protected readonly tagRemoved = signal(0);
  protected readonly locked = signal(false);`;
  protected readonly _srcHtml1 = `<cngx-chip
    cngxChipInteraction
    [value]="'tag'"
    [(selected)]="favourite"
    [removable]="true"
    (removeRequest)="tagRemoved.set(tagRemoved() + 1)"
    (remove)="tagRemoved.set(tagRemoved() + 1)"
  >Removable tag</cngx-chip>
  <p class="caption">remove fired: <code>{{ tagRemoved() }}</code></p>`;
  protected readonly _srcTs1 = `import { CngxChipInteraction } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';


  protected readonly favourite = signal(false);
  protected readonly featured = signal(true);
  protected readonly tagRemoved = signal(0);
  protected readonly locked = signal(false);`;
  protected readonly _srcHtml2 = `<cngx-chip cngxChipInteraction [value]="'locked'" [disabled]="locked()" [(selected)]="favourite">
    Locked tag
  </cngx-chip>
  <button type="button" (click)="locked.set(!locked())">toggle disabled</button>`;
  protected readonly _srcTs2 = `import { CngxChipInteraction } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';


  protected readonly favourite = signal(false);
  protected readonly featured = signal(true);
  protected readonly tagRemoved = signal(0);
  protected readonly locked = signal(false);`;

  protected readonly favourite = signal(false);
  protected readonly featured = signal(true);
  protected readonly tagRemoved = signal(0);
  protected readonly locked = signal(false);
  
}
