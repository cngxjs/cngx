// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/interactive/radio-demo/radio-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxRadioGroup, CngxRadio } from '@cngx/common/interactive';

@Component({
  selector: 'app-radio-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxRadioGroup,
    CngxRadio,
  ],
  template: `
    <app-doc-shell title="Radio group"
      description="Single-select radio-group molecule and its CngxRadio leaves. Group provides CNGX_RADIO_GROUP for parent-child contract (never injects the concrete class) and CngxRovingTabindex as host directive for arrow-key focus movement. Auto-select-on-arrow is wired via a transient pendingArrowSelect flag — Tab-into-group does NOT auto-select; only an arrow keydown followed by focus moves the value."
      [apiComponents]="['CngxRadioGroup', 'CngxRadio', 'CNGX_RADIO_GROUP']">
      <app-example-card title="Basic — vertical group"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0"
        [sourceCss]="_srcCss0">
        
  <cngx-radio-group [(value)]="payment" name="payment-method">
    <cngx-radio value="card">Credit card</cngx-radio>
    <cngx-radio value="cash">Cash on delivery</cngx-radio>
    <cngx-radio value="invoice">Invoice</cngx-radio>
  </cngx-radio-group>
  <p class="caption">Bound: <code>{{ payment() ?? '(none)' }}</code></p>
      </app-example-card>
      <app-example-card title="Orientation — horizontal"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <cngx-radio-group [(value)]="payment" orientation="horizontal" name="payment-method-h">
    <cngx-radio value="card">Card</cngx-radio>
    <cngx-radio value="cash">Cash</cngx-radio>
    <cngx-radio value="invoice">Invoice</cngx-radio>
  </cngx-radio-group>
      </app-example-card>
      <app-example-card title="Disabled — group cascades, per-radio overrides"
        [subtitle]="_s2"
        [sourceHtml]="_srcHtml2"
        [sourceTs]="_srcTs2"
        [sourceCss]="_srcCss2">
        
  <button type="button" (click)="groupDisabled.set(!groupDisabled())" class="sort-btn">
    {{ groupDisabled() ? 'Enable group' : 'Disable group' }}
  </button>
  <cngx-radio-group [(value)]="payment" [disabled]="groupDisabled()" name="payment-method-d">
    <cngx-radio value="card">Card</cngx-radio>
    <cngx-radio value="cash">Cash (per-radio disabled)</cngx-radio>
    <cngx-radio value="invoice" [disabled]="true">Invoice (per-radio disabled)</cngx-radio>
  </cngx-radio-group>
      </app-example-card>
      <app-example-card title="Custom dot glyph"
        [subtitle]="_s3"
        [sourceHtml]="_srcHtml3"
        [sourceTs]="_srcTs3">
        
  <ng-template #starGlyph><span aria-hidden="true">★</span></ng-template>
  <ng-template #checkGlyph><span aria-hidden="true">✓</span></ng-template>
  <cngx-radio-group [(value)]="payment" name="payment-method-glyph">
    <cngx-radio value="card" [dotGlyph]="starGlyph">Card</cngx-radio>
    <cngx-radio value="cash" [dotGlyph]="checkGlyph">Cash</cngx-radio>
    <cngx-radio value="invoice">Invoice (default dot)</cngx-radio>
  </cngx-radio-group>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class RadioDemoComponent {
  protected readonly _s0 = 'Tab into the group (lands on the active radio, or the first one if none selected). <strong>ArrowDown</strong>/<strong>ArrowRight</strong> move focus AND select the next radio; <strong>ArrowUp</strong>/<strong>ArrowLeft</strong> the previous. <strong>Space</strong>/<strong>Enter</strong> select the focused radio (idempotent).';
  protected readonly _s1 = '<code>[orientation]="\'horizontal\'"</code> flips the layout AND tells the host roving directive to use ArrowLeft/ArrowRight for navigation.';
  protected readonly _s2 = 'Group <code>[disabled]</code> cascades to every leaf via <code>radioDisabled = computed(() => group.disabled() || disabled())</code>. Per-radio <code>[disabled]</code> blocks only that leaf and is skipped by roving navigation.';
  protected readonly _s3 = 'Each <code>cngx-radio</code> forwards a <code>[dotGlyph]</code> input to the inner <code>cngx-radio-indicator</code>. Project a <code>TemplateRef&lt;void&gt;</code> to replace the default dot — useful for design-system glyphs or branded icons.';
  protected readonly _srcHtml0 = `<cngx-radio-group [(value)]="payment" name="payment-method">
    <cngx-radio value="card">Credit card</cngx-radio>
    <cngx-radio value="cash">Cash on delivery</cngx-radio>
    <cngx-radio value="invoice">Invoice</cngx-radio>
  </cngx-radio-group>
  <p class="caption">Bound: <code>{{ payment() ?? '(none)' }}</code></p>`;
  protected readonly _srcTs0 = `import { CngxRadioGroup, CngxRadio } from '@cngx/common/interactive';


  protected readonly payment = signal<'card' | 'cash' | 'invoice' | undefined>(undefined);
  protected readonly orientation = signal<'horizontal' | 'vertical'>('vertical');
  protected readonly groupDisabled = signal(false);`;
  protected readonly _srcCss0 = `.caption { font-size: 0.875em; color: var(--cngx-text-muted, #6b7280); margin-top: 12px; }`;
  protected readonly _srcHtml1 = `<cngx-radio-group [(value)]="payment" orientation="horizontal" name="payment-method-h">
    <cngx-radio value="card">Card</cngx-radio>
    <cngx-radio value="cash">Cash</cngx-radio>
    <cngx-radio value="invoice">Invoice</cngx-radio>
  </cngx-radio-group>`;
  protected readonly _srcTs1 = `import { CngxRadioGroup, CngxRadio } from '@cngx/common/interactive';


  protected readonly payment = signal<'card' | 'cash' | 'invoice' | undefined>(undefined);
  protected readonly orientation = signal<'horizontal' | 'vertical'>('vertical');
  protected readonly groupDisabled = signal(false);`;
  protected readonly _srcHtml2 = `<button type="button" (click)="groupDisabled.set(!groupDisabled())" class="sort-btn">
    {{ groupDisabled() ? 'Enable group' : 'Disable group' }}
  </button>
  <cngx-radio-group [(value)]="payment" [disabled]="groupDisabled()" name="payment-method-d">
    <cngx-radio value="card">Card</cngx-radio>
    <cngx-radio value="cash">Cash (per-radio disabled)</cngx-radio>
    <cngx-radio value="invoice" [disabled]="true">Invoice (per-radio disabled)</cngx-radio>
  </cngx-radio-group>`;
  protected readonly _srcTs2 = `import { CngxRadioGroup, CngxRadio } from '@cngx/common/interactive';


  protected readonly payment = signal<'card' | 'cash' | 'invoice' | undefined>(undefined);
  protected readonly orientation = signal<'horizontal' | 'vertical'>('vertical');
  protected readonly groupDisabled = signal(false);`;
  protected readonly _srcCss2 = `.sort-btn { margin-bottom: 16px; }`;
  protected readonly _srcHtml3 = `<ng-template #starGlyph><span aria-hidden="true">★</span></ng-template>
  <ng-template #checkGlyph><span aria-hidden="true">✓</span></ng-template>
  <cngx-radio-group [(value)]="payment" name="payment-method-glyph">
    <cngx-radio value="card" [dotGlyph]="starGlyph">Card</cngx-radio>
    <cngx-radio value="cash" [dotGlyph]="checkGlyph">Cash</cngx-radio>
    <cngx-radio value="invoice">Invoice (default dot)</cngx-radio>
  </cngx-radio-group>`;
  protected readonly _srcTs3 = `import { CngxRadioGroup, CngxRadio } from '@cngx/common/interactive';


  protected readonly payment = signal<'card' | 'cash' | 'invoice' | undefined>(undefined);
  protected readonly orientation = signal<'horizontal' | 'vertical'>('vertical');
  protected readonly groupDisabled = signal(false);`;

  protected readonly payment = signal<'card' | 'cash' | 'invoice' | undefined>(undefined);
  protected readonly orientation = signal<'horizontal' | 'vertical'>('vertical');
  protected readonly groupDisabled = signal(false);
  
}
