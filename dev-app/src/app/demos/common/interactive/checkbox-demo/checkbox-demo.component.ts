// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/interactive/checkbox-demo/checkbox-demo.story.ts

import { ChangeDetectionStrategy, Component, computed, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxCheckbox } from '@cngx/common/interactive';

@Component({
  selector: 'app-checkbox-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxCheckbox,
  ],
  template: `
    <app-doc-shell title="Checkbox"
      description="Single-value boolean checkbox atom with WAI-ARIA tristate semantics. Composes <code>cngx-checkbox-indicator</code> from @cngx/common/display for the visual state. Click on an indeterminate checkbox advances to <code>value=true, indeterminate=false</code> in a single step — there is no path that lands the checkbox back in <code>mixed</code> from a user click."
      [apiComponents]="['CngxCheckbox']">
      <app-example-card title="Basic — two-way binding"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0"
        [sourceCss]="_srcCss0">
        
  <cngx-checkbox [(value)]="accept">I accept the terms</cngx-checkbox>
  <p class="caption">Bound: <code>{{ accept() }}</code></p>
      </app-example-card>
      <app-example-card title="Tri-state — Select-all pattern"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1"
        [sourceCss]="_srcCss1">
        
  <cngx-checkbox
    [value]="allChecked()"
    [indeterminate]="groupIndeterminate()"
    (valueChange)="toggleAll($event)"
  >Select all</cngx-checkbox>
  <div class="children">
    <cngx-checkbox [(value)]="itemA">Item A</cngx-checkbox>
    <cngx-checkbox [(value)]="itemB">Item B</cngx-checkbox>
    <cngx-checkbox [(value)]="itemC">Item C</cngx-checkbox>
  </div>
  <p class="caption">aria-checked = <code>{{ groupIndeterminate() ? 'mixed' : allChecked() ? 'true' : 'false' }}</code></p>
      </app-example-card>
      <app-example-card title="Disabled"
        [subtitle]="_s2"
        [sourceHtml]="_srcHtml2"
        [sourceTs]="_srcTs2"
        [sourceCss]="_srcCss2">
        
  <cngx-checkbox [value]="true" [disabled]="true">Locked-on</cngx-checkbox>
  <cngx-checkbox [value]="false" [disabled]="true">Locked-off</cngx-checkbox>
  <cngx-checkbox [value]="false" [indeterminate]="true" [disabled]="true">Locked-mixed</cngx-checkbox>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class CheckboxDemoComponent {
  protected readonly _s0 = 'Click the box or focus + press <strong>Space</strong>/<strong>Enter</strong>.';
  protected readonly _s1 = 'The header checkbox is <code>indeterminate</code> when some-but-not-all items are selected. Clicking it cascades: indeterminate → checked (selects all), checked → unchecked (clears all), unchecked → checked (selects all).';
  protected readonly _s2 = 'Disabled checkboxes ignore click + keydown and reflect <code>aria-disabled="true"</code>.';
  protected readonly _srcHtml0 = `<cngx-checkbox [(value)]="accept">I accept the terms</cngx-checkbox>
  <p class="caption">Bound: <code>{{ accept() }}</code></p>`;
  protected readonly _srcTs0 = `import { CngxCheckbox } from '@cngx/common/interactive';


  protected readonly accept = signal(false);
  protected readonly itemA = signal(true);
  protected readonly itemB = signal(false);
  protected readonly itemC = signal(false);
  protected readonly allChecked = computed(() => this.itemA() && this.itemB() && this.itemC());
  protected readonly someChecked = computed(() => this.itemA() || this.itemB() || this.itemC());
  protected readonly groupIndeterminate = computed(() => this.someChecked() && !this.allChecked());
  protected toggleAll(next: boolean): void {
    this.itemA.set(next);
    this.itemB.set(next);
    this.itemC.set(next);
  }`;
  protected readonly _srcCss0 = `.caption { font-size: 0.875em; color: var(--cngx-text-muted, #6b7280); margin-top: 8px; }`;
  protected readonly _srcHtml1 = `<cngx-checkbox
    [value]="allChecked()"
    [indeterminate]="groupIndeterminate()"
    (valueChange)="toggleAll($event)"
  >Select all</cngx-checkbox>
  <div class="children">
    <cngx-checkbox [(value)]="itemA">Item A</cngx-checkbox>
    <cngx-checkbox [(value)]="itemB">Item B</cngx-checkbox>
    <cngx-checkbox [(value)]="itemC">Item C</cngx-checkbox>
  </div>
  <p class="caption">aria-checked = <code>{{ groupIndeterminate() ? 'mixed' : allChecked() ? 'true' : 'false' }}</code></p>`;
  protected readonly _srcTs1 = `import { CngxCheckbox } from '@cngx/common/interactive';


  protected readonly accept = signal(false);
  protected readonly itemA = signal(true);
  protected readonly itemB = signal(false);
  protected readonly itemC = signal(false);
  protected readonly allChecked = computed(() => this.itemA() && this.itemB() && this.itemC());
  protected readonly someChecked = computed(() => this.itemA() || this.itemB() || this.itemC());
  protected readonly groupIndeterminate = computed(() => this.someChecked() && !this.allChecked());
  protected toggleAll(next: boolean): void {
    this.itemA.set(next);
    this.itemB.set(next);
    this.itemC.set(next);
  }`;
  protected readonly _srcCss1 = `.children { display: flex; flex-direction: column; gap: 8px; padding-inline-start: 24px; margin-top: 8px; }
.caption { font-size: 0.875em; color: var(--cngx-text-muted, #6b7280); margin-top: 12px; }`;
  protected readonly _srcHtml2 = `<cngx-checkbox [value]="true" [disabled]="true">Locked-on</cngx-checkbox>
  <cngx-checkbox [value]="false" [disabled]="true">Locked-off</cngx-checkbox>
  <cngx-checkbox [value]="false" [indeterminate]="true" [disabled]="true">Locked-mixed</cngx-checkbox>`;
  protected readonly _srcTs2 = `import { CngxCheckbox } from '@cngx/common/interactive';


  protected readonly accept = signal(false);
  protected readonly itemA = signal(true);
  protected readonly itemB = signal(false);
  protected readonly itemC = signal(false);
  protected readonly allChecked = computed(() => this.itemA() && this.itemB() && this.itemC());
  protected readonly someChecked = computed(() => this.itemA() || this.itemB() || this.itemC());
  protected readonly groupIndeterminate = computed(() => this.someChecked() && !this.allChecked());
  protected toggleAll(next: boolean): void {
    this.itemA.set(next);
    this.itemB.set(next);
    this.itemC.set(next);
  }`;
  protected readonly _srcCss2 = `cngx-checkbox { display: inline-flex; margin-right: 24px; }`;

  protected readonly accept = signal(false);
  protected readonly itemA = signal(true);
  protected readonly itemB = signal(false);
  protected readonly itemC = signal(false);
  protected readonly allChecked = computed(() => this.itemA() && this.itemB() && this.itemC());
  protected readonly someChecked = computed(() => this.itemA() || this.itemB() || this.itemC());
  protected readonly groupIndeterminate = computed(() => this.someChecked() && !this.allChecked());
  protected toggleAll(next: boolean): void {
    this.itemA.set(next);
    this.itemB.set(next);
    this.itemC.set(next);
  }
  
}
