// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/interactive/chip-group-demo/chip-group-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';

@Component({
  selector: 'app-chip-group-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxChipGroup,
    CngxChipInGroup,
    CngxChip,
  ],
  template: `
    <app-doc-shell title="Chip group (single-select)"
      description="Single-select chip group molecule. Owns <code>selected = model&lt;T | undefined&gt;</code>, provides <code>CNGX_CHIP_GROUP_HOST</code> so projected <code>&lt;cngx-chip cngxChipInGroup&gt;</code> leaves derive their <code>aria-selected</code> from the parent's selection. Renders <code>role=&quot;listbox&quot;</code>; composes <code>CngxRovingTabindex</code> for arrow-key navigation. Single-mode behaviour: re-clicking the active chip clears the selection (toggle off)."
      [apiComponents]="['CngxChipGroup', 'CngxChipInGroup', 'CngxChip', 'CNGX_CHIP_GROUP_HOST']">
      <app-example-card title="Basic — pick exactly one size"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <cngx-chip-group label="T-shirt size" [(selected)]="size" [disabled]="groupDisabled()">
    @for (s of sizes; track s) {
      <cngx-chip cngxChipInGroup [value]="s">{{ s.toUpperCase() }}</cngx-chip>
    }
  </cngx-chip-group>
  <p class="caption">picked: <code>{{ size() ?? '(none)' }}</code></p>
  <button type="button" (click)="groupDisabled.set(!groupDisabled())">toggle group disabled</button>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class ChipGroupDemoComponent {
  protected readonly _s0 = 'Click any chip to select it; click again to deselect. Only one chip is <code>aria-selected</code> at a time.';
  protected readonly _srcHtml0 = `<cngx-chip-group label="T-shirt size" [(selected)]="size" [disabled]="groupDisabled()">
    @for (s of sizes; track s) {
      <cngx-chip cngxChipInGroup [value]="s">{{ s.toUpperCase() }}</cngx-chip>
    }
  </cngx-chip-group>
  <p class="caption">picked: <code>{{ size() ?? '(none)' }}</code></p>
  <button type="button" (click)="groupDisabled.set(!groupDisabled())">toggle group disabled</button>`;
  protected readonly _srcTs0 = `import { CngxChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';


  protected readonly sizes = ['sm', 'md', 'lg'] as const;
  protected readonly size = signal<'sm' | 'md' | 'lg' | undefined>('md');
  protected readonly groupDisabled = signal(false);`;

  protected readonly sizes = ['sm', 'md', 'lg'] as const;
  protected readonly size = signal<'sm' | 'md' | 'lg' | undefined>('md');
  protected readonly groupDisabled = signal(false);
  
}
