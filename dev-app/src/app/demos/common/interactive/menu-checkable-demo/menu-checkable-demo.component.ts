// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/interactive/menu-checkable-demo/menu-checkable-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxMenu, CngxMenuGroup, CngxMenuItemCheckbox, CngxMenuItemRadio } from '@cngx/common/interactive';

@Component({
  selector: 'app-menu-checkable-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxMenu,
    CngxMenuGroup,
    CngxMenuItemCheckbox,
    CngxMenuItemRadio,
  ],
  template: `
    <app-doc-shell title="Checkable Menu Items"
      description="Menu items with their own state: checkboxes for toggles, radios for mutually exclusive choices inside a CngxMenuGroup."
      overview="<p><code>[cngxMenuItemCheckbox]</code> renders <code>role=&quot;menuitemcheckbox&quot;</code> with a two-way <code>[(checked)]</code> model. <code>[cngxMenuItemRadio]</code> renders <code>role=&quot;menuitemradio&quot;</code> and is mutually exclusive inside a <code>[cngxMenuGroup]</code>.</p>"
      [apiComponents]="['CngxMenuItemCheckbox', 'CngxMenuItemRadio', 'CngxMenuGroup']">
      <app-example-card title="Text formatting menu"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0"
        [sourceCss]="_srcCss0">
        
  <ul cngxMenu [label]="'Text formatting'" class="menu" tabindex="0">
    <li cngxMenuItemCheckbox value="bold" [(checked)]="bold">
      Bold
    </li>
    <li cngxMenuItemCheckbox value="italic" [(checked)]="italic">
      Italic
    </li>
    <li cngxMenuItemCheckbox value="underline" [(checked)]="underline">
      Underline
    </li>
    <li role="separator" class="sep"></li>
    <div cngxMenuGroup [label]="'Alignment'" name="alignment">
      <li cngxMenuItemRadio value="left">Left</li>
      <li cngxMenuItemRadio value="center">Center</li>
      <li cngxMenuItemRadio value="right">Right</li>
    </div>
  </ul>
  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">Bold</span>
      <span class="event-value">{{ bold() ? 'on' : 'off' }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">Italic</span>
      <span class="event-value">{{ italic() ? 'on' : 'off' }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">Underline</span>
      <span class="event-value">{{ underline() ? 'on' : 'off' }}</span>
    </div>
  </div>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class MenuCheckableDemoComponent {
  protected readonly _s0 = 'Checkboxes for independent toggles, radios inside a group for exclusive selection.';
  protected readonly _srcHtml0 = `<ul cngxMenu [label]="'Text formatting'" class="menu" tabindex="0">
    <li cngxMenuItemCheckbox value="bold" [(checked)]="bold">
      Bold
    </li>
    <li cngxMenuItemCheckbox value="italic" [(checked)]="italic">
      Italic
    </li>
    <li cngxMenuItemCheckbox value="underline" [(checked)]="underline">
      Underline
    </li>
    <li role="separator" class="sep"></li>
    <div cngxMenuGroup [label]="'Alignment'" name="alignment">
      <li cngxMenuItemRadio value="left">Left</li>
      <li cngxMenuItemRadio value="center">Center</li>
      <li cngxMenuItemRadio value="right">Right</li>
    </div>
  </ul>
  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">Bold</span>
      <span class="event-value">{{ bold() ? 'on' : 'off' }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">Italic</span>
      <span class="event-value">{{ italic() ? 'on' : 'off' }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">Underline</span>
      <span class="event-value">{{ underline() ? 'on' : 'off' }}</span>
    </div>
  </div>`;
  protected readonly _srcTs0 = `import { CngxMenu, CngxMenuGroup, CngxMenuItemCheckbox, CngxMenuItemRadio } from '@cngx/common/interactive';


  protected readonly bold = signal(true);
  protected readonly italic = signal(false);
  protected readonly underline = signal(false);`;
  protected readonly _srcCss0 = `.menu {
  list-style: none;
  margin: 0;
  padding: 4px;
  width: 240px;
  border: 1px solid var(--cngx-surface-border, #d0d5dd);
  border-radius: var(--cngx-radius-md, 8px);
  background: var(--cngx-surface-default, #fff);
  outline: none;
}
.menu:focus-visible {
  outline: 2px solid var(--cngx-focus-ring, #4a8cff);
  outline-offset: 2px;
}
.menu [cngxMenuItemCheckbox], .menu [cngxMenuItemRadio] {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 6px 10px;
  border-radius: 4px;
  cursor: pointer;
  position: relative;
}
.cngx-menu-item--highlighted {
  background: var(--cngx-menu-highlight-bg, rgba(74, 140, 255, 0.15));
}
.cngx-menu-item--checked::before {
  content: '\\2713';
  width: 1em;
  margin-right: 4px;
}
[cngxMenuItemRadio].cngx-menu-item--checked::before {
  content: '\\2022';
}
.sep {
  height: 1px;
  background: var(--cngx-surface-border, #d0d5dd);
  margin: 4px 0;
}`;

  protected readonly bold = signal(true);
  protected readonly italic = signal(false);
  protected readonly underline = signal(false);
  
}
