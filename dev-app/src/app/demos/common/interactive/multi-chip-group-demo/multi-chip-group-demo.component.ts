// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/interactive/multi-chip-group-demo/multi-chip-group-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxMultiChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';

@Component({
  selector: 'app-multi-chip-group-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxMultiChipGroup,
    CngxChipInGroup,
    CngxChip,
  ],
  template: `
    <app-doc-shell title="Multi chip group (multi-select)"
      description="Multi-select chip group molecule. Owns <code>selectedValues = model&lt;T[]&gt;</code>, provides <code>CNGX_CHIP_GROUP_HOST</code> with multi-select semantics, and uses <code>createSelectionController</code> internally for membership tracking. Renders <code>role=&quot;listbox&quot; aria-multiselectable=&quot;true&quot;</code>; chips toggle independently. Use <code>[keyFn]</code> for object-valued options where reference equality is unstable."
      [apiComponents]="['CngxMultiChipGroup', 'CngxChipInGroup', 'CngxChip', 'CNGX_CHIP_GROUP_HOST']">
      <app-example-card title="Multi-select chips with selection count"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <cngx-multi-chip-group #group="cngxMultiChipGroup" label="Tags" [(selectedValues)]="picked">
    @for (tag of tags; track tag) {
      <cngx-chip cngxChipInGroup [value]="tag">{{ tag }}</cngx-chip>
    }
  </cngx-multi-chip-group>
  <p class="caption">
    selected (<code>{{ group.selectedCount() }}</code>):
    <code>{{ picked().join(', ') || '(none)' }}</code>
  </p>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class MultiChipGroupDemoComponent {
  protected readonly _s0 = 'Independent toggle per chip. <code>selectedCount</code> is exposed on the group for label hints. Multi-select uses <code>createSelectionController</code> with structural-equality membership tracking.';
  protected readonly _srcHtml0 = `<cngx-multi-chip-group #group="cngxMultiChipGroup" label="Tags" [(selectedValues)]="picked">
    @for (tag of tags; track tag) {
      <cngx-chip cngxChipInGroup [value]="tag">{{ tag }}</cngx-chip>
    }
  </cngx-multi-chip-group>
  <p class="caption">
    selected (<code>{{ group.selectedCount() }}</code>):
    <code>{{ picked().join(', ') || '(none)' }}</code>
  </p>`;
  protected readonly _srcTs0 = `import { CngxMultiChipGroup, CngxChipInGroup } from '@cngx/common/interactive';
import { CngxChip } from '@cngx/common/display';


  protected readonly tags = ['urgent', 'review', 'blocker', 'follow-up', 'on-hold'] as const;
  protected readonly picked = signal<string[]>(['urgent', 'review']);`;

  protected readonly tags = ['urgent', 'review', 'blocker', 'follow-up', 'on-hold'] as const;
  protected readonly picked = signal<string[]>(['urgent', 'review']);
  
}
