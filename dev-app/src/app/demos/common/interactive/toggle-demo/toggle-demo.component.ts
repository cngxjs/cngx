// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/interactive/toggle-demo/toggle-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxToggle } from '@cngx/common/interactive';

@Component({
  selector: 'app-toggle-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxToggle,
  ],
  template: `
    <app-doc-shell title="Toggle"
      description="Single-value boolean switch atom. role=&quot;switch&quot; with reactive aria-checked, aria-disabled, aria-describedby for the consumer-supplied disabled reason. Click + Space + Enter all flip. Provides CNGX_CONTROL_VALUE so CngxFormBridge (Phase 7) can bind to it without per-atom CVA."
      [apiComponents]="['CngxToggle']">
      <app-example-card title="Basic — two-way binding"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0"
        [sourceCss]="_srcCss0">
        
  <cngx-toggle [(value)]="notifications">Receive e-mail notifications</cngx-toggle>
  <p class="caption">Bound: <code>{{ notifications() }}</code></p>
      </app-example-card>
      <app-example-card title="Disabled with reason"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1"
        [sourceCss]="_srcCss1">
        
  <button type="button" (click)="systemLocked.set(!systemLocked())" class="sort-btn">
    {{ systemLocked() ? 'Unlock OS preference' : 'Lock OS preference' }}
  </button>
  <cngx-toggle
    [(value)]="dark"
    [disabled]="systemLocked()"
    [disabledReason]="systemLocked() ? 'Locked by your OS preference' : ''"
  >Dark mode</cngx-toggle>
      </app-example-card>
      <app-example-card title="Label position"
        [subtitle]="_s2"
        [sourceHtml]="_srcHtml2"
        [sourceTs]="_srcTs2"
        [sourceCss]="_srcCss2">
        
  <cngx-toggle [(value)]="notifications" labelPosition="before">Label before</cngx-toggle>
  <cngx-toggle [(value)]="notifications">Label after (default)</cngx-toggle>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class ToggleDemoComponent {
  protected readonly _s0 = 'Click anywhere on the row, or focus and press <strong>Space</strong>/<strong>Enter</strong>. The host signal updates via <code>[(value)]</code>.';
  protected readonly _s1 = 'When <code>[disabled]</code> is true and <code>disabledReason</code> is non-empty, the host emits <code>aria-describedby</code> pointing to a hidden span — screen-readers announce <em>why</em> the control is disabled.';
  protected readonly _s2 = '<code>[labelPosition]="\'before\'"</code> renders the label to the left of the track via <code>flex-direction: row-reverse</code> on the host class.';
  protected readonly _srcHtml0 = `<cngx-toggle [(value)]="notifications">Receive e-mail notifications</cngx-toggle>
  <p class="caption">Bound: <code>{{ notifications() }}</code></p>`;
  protected readonly _srcTs0 = `import { CngxToggle } from '@cngx/common/interactive';


  protected readonly notifications = signal(false);
  protected readonly dark = signal(true);
  protected readonly systemLocked = signal(false);`;
  protected readonly _srcCss0 = `.caption { font-size: 0.875em; color: var(--cngx-text-muted, #6b7280); margin-top: 8px; }`;
  protected readonly _srcHtml1 = `<button type="button" (click)="systemLocked.set(!systemLocked())" class="sort-btn">
    {{ systemLocked() ? 'Unlock OS preference' : 'Lock OS preference' }}
  </button>
  <cngx-toggle
    [(value)]="dark"
    [disabled]="systemLocked()"
    [disabledReason]="systemLocked() ? 'Locked by your OS preference' : ''"
  >Dark mode</cngx-toggle>`;
  protected readonly _srcTs1 = `import { CngxToggle } from '@cngx/common/interactive';


  protected readonly notifications = signal(false);
  protected readonly dark = signal(true);
  protected readonly systemLocked = signal(false);`;
  protected readonly _srcCss1 = `.sort-btn { margin-bottom: 16px; }`;
  protected readonly _srcHtml2 = `<cngx-toggle [(value)]="notifications" labelPosition="before">Label before</cngx-toggle>
  <cngx-toggle [(value)]="notifications">Label after (default)</cngx-toggle>`;
  protected readonly _srcTs2 = `import { CngxToggle } from '@cngx/common/interactive';


  protected readonly notifications = signal(false);
  protected readonly dark = signal(true);
  protected readonly systemLocked = signal(false);`;
  protected readonly _srcCss2 = `cngx-toggle { display: inline-flex; margin-right: 24px; }`;

  protected readonly notifications = signal(false);
  protected readonly dark = signal(true);
  protected readonly systemLocked = signal(false);
  
}
