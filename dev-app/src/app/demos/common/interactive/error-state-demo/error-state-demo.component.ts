// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/interactive/error-state-demo/error-state-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxErrorState } from '@cngx/common/interactive';

@Component({
  selector: 'app-error-state-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxErrorState,
  ],
  template: `
    <app-doc-shell title="Error State"
      description="<code>[cngxErrorState]</code> is a generic host-element marker. Toggles <code>.cngx-error</code> + reactive <code>aria-invalid</code> / <code>aria-errormessage</code> on any DOM element — cngx, Material, CDK, native, or third-party host. Works without an aggregator; pair with <code>cngxErrorScope</code> when you need reveal-on-submit semantics."
      [apiComponents]="['CngxErrorState']">
      <app-example-card title="Basic — boolean flag flips aria-invalid"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0"
        [sourceCss]="_srcCss0">
        
  <label>
    <span>Email</span>
    <input
      type="email"
      placeholder="user@example.com"
      [cngxErrorState]="emailInvalid()"
      cngxErrorMessageId="email-error"
    />
  </label>
  <p
    id="email-error"
    role="alert"
    [attr.aria-hidden]="emailInvalid() ? null : 'true'"
    class="error-text"
  >Please enter a valid email address.</p>
  <button type="button" (click)="emailInvalid.set(!emailInvalid())">
    Toggle invalid state
  </button>
      </app-example-card>
      <app-example-card title="Without message id — class hook only"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1"
        [sourceCss]="_srcCss1">
        
  <label>
    <span>Password</span>
    <input
      type="password"
      [cngxErrorState]="passwordInvalid()"
    />
  </label>
  <button type="button" (click)="passwordInvalid.set(!passwordInvalid())">
    Toggle invalid state
  </button>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class ErrorStateDemoComponent {
  protected readonly _s0 = 'Toggle the controls below and observe <code>aria-invalid</code> on the input plus the <code>.cngx-error</code> hook.';
  protected readonly _s1 = '<code>cngxErrorMessageId</code> is optional. Skip it when the error message is rendered elsewhere (e.g. summary panel) or when you only need the visual cue.';
  protected readonly _srcHtml0 = `<label>
    <span>Email</span>
    <input
      type="email"
      placeholder="user@example.com"
      [cngxErrorState]="emailInvalid()"
      cngxErrorMessageId="email-error"
    />
  </label>
  <p
    id="email-error"
    role="alert"
    [attr.aria-hidden]="emailInvalid() ? null : 'true'"
    class="error-text"
  >Please enter a valid email address.</p>
  <button type="button" (click)="emailInvalid.set(!emailInvalid())">
    Toggle invalid state
  </button>`;
  protected readonly _srcTs0 = `import { CngxErrorState } from '@cngx/common/interactive';


  protected readonly emailInvalid = signal(false);
  protected readonly passwordInvalid = signal(false);`;
  protected readonly _srcCss0 = `.error-text { color: var(--cngx-error-text, #b00020); margin: 4px 0 12px; font-size: 0.875em; }
.error-text[aria-hidden='true'] { display: none; }
input.cngx-error { border-color: var(--cngx-error-text, #b00020); outline-color: var(--cngx-error-text, #b00020); }
label { display: block; margin-bottom: 4px; }
input { padding: 6px 8px; min-width: 240px; }`;
  protected readonly _srcHtml1 = `<label>
    <span>Password</span>
    <input
      type="password"
      [cngxErrorState]="passwordInvalid()"
    />
  </label>
  <button type="button" (click)="passwordInvalid.set(!passwordInvalid())">
    Toggle invalid state
  </button>`;
  protected readonly _srcTs1 = `import { CngxErrorState } from '@cngx/common/interactive';


  protected readonly emailInvalid = signal(false);
  protected readonly passwordInvalid = signal(false);`;
  protected readonly _srcCss1 = `input.cngx-error { border-color: var(--cngx-error-text, #b00020); outline-color: var(--cngx-error-text, #b00020); }
label { display: block; margin-bottom: 4px; }
input { padding: 6px 8px; min-width: 240px; }`;

  protected readonly emailInvalid = signal(false);
  protected readonly passwordInvalid = signal(false);
  
}
