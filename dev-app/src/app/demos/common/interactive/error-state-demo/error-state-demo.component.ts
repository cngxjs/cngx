// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/interactive/error-state-demo/error-state-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxErrorState } from '@cngx/common/interactive';

@Component({
  selector: 'app-error-state-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxErrorState,
  ],
  template: `
    <app-doc-shell title="Error State"
      description="<code>[cngxErrorState]</code> is a generic host-element marker. Toggles <code>.cngx-error</code> + reactive <code>aria-invalid</code> + <code>aria-errormessage</code> on any DOM element — cngx, Material, CDK, native, or third-party host. Works without an aggregator; pair with <code>cngxErrorScope</code> when you need reveal-on-submit semantics. Both ARIA attributes stay in the DOM with explicit values per cngx convention; visibility of the linked message is the consumer's <code>aria-hidden</code> to wire."
      [apiComponents]="['CngxErrorState']">
      <app-example-card title="Basic — boolean flag flips aria-invalid + aria-errormessage"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <label style="display: block; margin-bottom: 6px; font-weight: 600;">
    <span>Email</span>
    <input
      #emailInput
      type="email"
      placeholder="user@example.com"
      [cngxErrorState]="emailInvalid()"
      cngxErrorMessageId="email-error"
      [style.border]="emailInvalid() ? '1px solid #b00020' : '1px solid #d1d5db'"
      [style.outline-color]="emailInvalid() ? '#b00020' : 'transparent'"
      style="padding: 6px 8px; min-width: 240px; border-radius: 4px; display: block; margin-top: 4px;"
    />
  </label>
  <p
    id="email-error"
    role="alert"
    [attr.aria-hidden]="emailInvalid() ? null : 'true'"
    [style.display]="emailInvalid() ? 'block' : 'none'"
    style="color: #b00020; margin: 4px 0 12px; font-size: 0.875em;"
  >Please enter a valid email address.</p>

  <pre style="margin: 0 0 12px; padding: 8px; background: #f3f4f6; border-radius: 4px; font-size: 0.85em;">aria-invalid      : "{{ emailInput.getAttribute('aria-invalid') }}"
aria-errormessage : "{{ emailInput.getAttribute('aria-errormessage') }}"
class.cngx-error  : {{ emailInput.classList.contains('cngx-error') }}</pre>

  <button type="button" (click)="emailInvalid.set(!emailInvalid())">
    Toggle invalid state
  </button>
      </app-example-card>
      <app-example-card title="Without message id — class hook + aria-invalid only"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <label style="display: block; margin-bottom: 6px; font-weight: 600;">
    <span>Password</span>
    <input
      #passwordInput
      type="password"
      [cngxErrorState]="passwordInvalid()"
      [style.border]="passwordInvalid() ? '1px solid #b00020' : '1px solid #d1d5db'"
      [style.outline-color]="passwordInvalid() ? '#b00020' : 'transparent'"
      style="padding: 6px 8px; min-width: 240px; border-radius: 4px; display: block; margin-top: 4px;"
    />
  </label>

  <pre style="margin: 8px 0 12px; padding: 8px; background: #f3f4f6; border-radius: 4px; font-size: 0.85em;">aria-invalid      : "{{ passwordInput.getAttribute('aria-invalid') }}"
aria-errormessage : {{ passwordInput.getAttribute('aria-errormessage') ?? 'null (no id bound)' }}
class.cngx-error  : {{ passwordInput.classList.contains('cngx-error') }}</pre>

  <button type="button" (click)="passwordInvalid.set(!passwordInvalid())">
    Toggle invalid state
  </button>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class ErrorStateDemoComponent {
  protected readonly _s0 = 'Toggle the button to flip the boolean. Watch the input border + the <code>aria-invalid</code> attribute switch between <code>"false"</code> and <code>"true"</code> on every render. The <code>aria-errormessage</code> ID stays in the DOM regardless — the message element\'s own <code>aria-hidden</code> gates whether the text is announced.';
  protected readonly _s1 = '<code>cngxErrorMessageId</code> is optional. Skip it when the error message is rendered elsewhere (e.g. a summary panel) or when only the visual cue is needed. <code>aria-invalid</code> still flips between <code>"false"</code> and <code>"true"</code>; <code>aria-errormessage</code> simply never appears.';
  protected readonly _srcHtml0 = `<label style="display: block; margin-bottom: 6px; font-weight: 600;">
    <span>Email</span>
    <input
      #emailInput
      type="email"
      placeholder="user@example.com"
      [cngxErrorState]="emailInvalid()"
      cngxErrorMessageId="email-error"
      [style.border]="emailInvalid() ? '1px solid #b00020' : '1px solid #d1d5db'"
      [style.outline-color]="emailInvalid() ? '#b00020' : 'transparent'"
      style="padding: 6px 8px; min-width: 240px; border-radius: 4px; display: block; margin-top: 4px;"
    />
  </label>
  <p
    id="email-error"
    role="alert"
    [attr.aria-hidden]="emailInvalid() ? null : 'true'"
    [style.display]="emailInvalid() ? 'block' : 'none'"
    style="color: #b00020; margin: 4px 0 12px; font-size: 0.875em;"
  >Please enter a valid email address.</p>

  <pre style="margin: 0 0 12px; padding: 8px; background: #f3f4f6; border-radius: 4px; font-size: 0.85em;">aria-invalid      : "{{ emailInput.getAttribute('aria-invalid') }}"
aria-errormessage : "{{ emailInput.getAttribute('aria-errormessage') }}"
class.cngx-error  : {{ emailInput.classList.contains('cngx-error') }}</pre>

  <button type="button" (click)="emailInvalid.set(!emailInvalid())">
    Toggle invalid state
  </button>`;
  protected readonly _srcTs0 = `import { CngxErrorState } from '@cngx/common/interactive';


  protected readonly emailInvalid = signal(false);
  protected readonly passwordInvalid = signal(false);`;
  protected readonly _srcHtml1 = `<label style="display: block; margin-bottom: 6px; font-weight: 600;">
    <span>Password</span>
    <input
      #passwordInput
      type="password"
      [cngxErrorState]="passwordInvalid()"
      [style.border]="passwordInvalid() ? '1px solid #b00020' : '1px solid #d1d5db'"
      [style.outline-color]="passwordInvalid() ? '#b00020' : 'transparent'"
      style="padding: 6px 8px; min-width: 240px; border-radius: 4px; display: block; margin-top: 4px;"
    />
  </label>

  <pre style="margin: 8px 0 12px; padding: 8px; background: #f3f4f6; border-radius: 4px; font-size: 0.85em;">aria-invalid      : "{{ passwordInput.getAttribute('aria-invalid') }}"
aria-errormessage : {{ passwordInput.getAttribute('aria-errormessage') ?? 'null (no id bound)' }}
class.cngx-error  : {{ passwordInput.classList.contains('cngx-error') }}</pre>

  <button type="button" (click)="passwordInvalid.set(!passwordInvalid())">
    Toggle invalid state
  </button>`;
  protected readonly _srcTs1 = `import { CngxErrorState } from '@cngx/common/interactive';


  protected readonly emailInvalid = signal(false);
  protected readonly passwordInvalid = signal(false);`;

  protected readonly emailInvalid = signal(false);
  protected readonly passwordInvalid = signal(false);
  
}
