// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/layout/truncate-demo/truncate-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxTruncate } from '@cngx/common/layout';

@Component({
  selector: 'app-truncate-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxTruncate,
  ],
  template: `
    <app-doc-shell title="Truncate"
      description="Text truncation with expand/collapse and clamped-state detection. Shows a toggle only when content actually overflows."
      overview="<p><code>[cngxTruncate]</code> applies <code>-webkit-line-clamp</code> when collapsed and uses <code>ResizeObserver</code> to detect whether the text actually overflows. The <code>isClamped()</code> signal lets you conditionally show a &quot;Show more&quot; button only when needed.</p>"
      [apiComponents]="['CngxTruncate']">
      <app-example-card title="Truncated Text with Toggle"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <div style="max-width:400px">
    <p [cngxTruncate]="3" [(expanded)]="expanded1" #trunc="cngxTruncate"
       style="margin:0;line-height:1.6;font-size:0.875rem">
      Angular Signals represent a fundamental shift in how we think about reactivity.
      Instead of subscribing to streams and manually managing subscriptions, signals
      provide a synchronous, pull-based model where derived values are automatically
      tracked and updated. This eliminates entire categories of bugs related to
      subscription leaks, stale closures, and timing issues. The computed() function
      creates derived signals that update automatically when their dependencies change,
      making the entire state graph declarative and self-consistent.
    </p>
    @if (trunc.isClamped() || expanded1()) {
      <button (click)="expanded1.set(!expanded1())"
              [attr.aria-expanded]="expanded1()"
              style="margin-top:4px;background:none;border:none;color:var(--interactive,#f5a623);
                     cursor:pointer;padding:0;font-size:0.8125rem">
        {{ expanded1() ? 'Show less' : 'Show more' }}
      </button>
    }
  </div>
  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">isClamped</span>
      <span class="event-value">{{ trunc.isClamped() }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">expanded</span>
      <span class="event-value">{{ expanded1() }}</span>
    </div>
  </div>
      </app-example-card>
      <app-example-card title="Short Text — No Toggle"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <div style="max-width:400px">
    <p [cngxTruncate]="3" [(expanded)]="expanded2" #trunc2="cngxTruncate"
       style="margin:0;line-height:1.6;font-size:0.875rem">
      This text is short enough to fit in 3 lines.
    </p>
    @if (trunc2.isClamped() || expanded2()) {
      <button (click)="expanded2.set(!expanded2())"
              style="margin-top:4px;background:none;border:none;color:var(--interactive,#f5a623);cursor:pointer;padding:0;font-size:0.8125rem">
        {{ expanded2() ? 'Show less' : 'Show more' }}
      </button>
    }
  </div>
  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">isClamped</span>
      <span class="event-value">{{ trunc2.isClamped() }}</span>
    </div>
  </div>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class TruncateDemoComponent {
  protected readonly _s0 = 'Long text clamped to 3 lines. The "Show more" button appears only because <code>isClamped()</code> is <code>true</code>.';
  protected readonly _s1 = 'When content fits within the line limit, <code>isClamped()</code> is <code>false</code> and no toggle appears.';
  protected readonly _srcHtml0 = `<div style="max-width:400px">
    <p [cngxTruncate]="3" [(expanded)]="expanded1" #trunc="cngxTruncate"
       style="margin:0;line-height:1.6;font-size:0.875rem">
      Angular Signals represent a fundamental shift in how we think about reactivity.
      Instead of subscribing to streams and manually managing subscriptions, signals
      provide a synchronous, pull-based model where derived values are automatically
      tracked and updated. This eliminates entire categories of bugs related to
      subscription leaks, stale closures, and timing issues. The computed() function
      creates derived signals that update automatically when their dependencies change,
      making the entire state graph declarative and self-consistent.
    </p>
    @if (trunc.isClamped() || expanded1()) {
      <button (click)="expanded1.set(!expanded1())"
              [attr.aria-expanded]="expanded1()"
              style="margin-top:4px;background:none;border:none;color:var(--interactive,#f5a623);
                     cursor:pointer;padding:0;font-size:0.8125rem">
        {{ expanded1() ? 'Show less' : 'Show more' }}
      </button>
    }
  </div>
  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">isClamped</span>
      <span class="event-value">{{ trunc.isClamped() }}</span>
    </div>
    <div class="event-row">
      <span class="event-label">expanded</span>
      <span class="event-value">{{ expanded1() }}</span>
    </div>
  </div>`;
  protected readonly _srcTs0 = `import { CngxTruncate } from '@cngx/common/layout';


  protected readonly expanded1 = signal(false);
  protected readonly expanded2 = signal(false);`;
  protected readonly _srcHtml1 = `<div style="max-width:400px">
    <p [cngxTruncate]="3" [(expanded)]="expanded2" #trunc2="cngxTruncate"
       style="margin:0;line-height:1.6;font-size:0.875rem">
      This text is short enough to fit in 3 lines.
    </p>
    @if (trunc2.isClamped() || expanded2()) {
      <button (click)="expanded2.set(!expanded2())"
              style="margin-top:4px;background:none;border:none;color:var(--interactive,#f5a623);cursor:pointer;padding:0;font-size:0.8125rem">
        {{ expanded2() ? 'Show less' : 'Show more' }}
      </button>
    }
  </div>
  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">isClamped</span>
      <span class="event-value">{{ trunc2.isClamped() }}</span>
    </div>
  </div>`;
  protected readonly _srcTs1 = `import { CngxTruncate } from '@cngx/common/layout';


  protected readonly expanded1 = signal(false);
  protected readonly expanded2 = signal(false);`;

  protected readonly expanded1 = signal(false);
  protected readonly expanded2 = signal(false);
  
}
