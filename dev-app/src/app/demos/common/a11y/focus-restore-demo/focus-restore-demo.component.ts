// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/a11y/focus-restore-demo/focus-restore-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxFocusRestore } from '@cngx/common/a11y';

@Component({
  selector: 'app-focus-restore-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxFocusRestore,
  ],
  template: `
    <app-doc-shell title="Focus Restore"
      description="Captures the previously focused element and restores focus when the host is destroyed. Prevents focus loss to body."
      overview="<p><code>[cngxFocusRestore]</code> solves the focus-loss problem when dynamic content is removed. Without it, focus falls to <code>&lt;body&gt;</code> and screen reader users lose their place.</p>"
      [apiComponents]="['CngxFocusRestore']">
      <app-example-card title="Panel with Automatic Restore"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <button (click)="panelOpen.set(true)" class="chip">Open Panel</button>

  @if (panelOpen()) {
    <div cngxFocusRestore
         style="margin-top:12px;padding:16px;border:1px solid var(--cngx-border,#ddd);border-radius:8px">
      <p style="margin:0 0 12px">Panel content. Focus will restore on close.</p>
      <button (click)="panelOpen.set(false)" class="chip">Close Panel</button>
    </div>
  }

  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">Panel open</span>
      <span class="event-value">{{ panelOpen() }}</span>
    </div>
  </div>
      </app-example-card>
      <app-example-card title="Inline Details"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <button (click)="detailsOpen.set(!detailsOpen())" class="chip"
          [attr.aria-expanded]="detailsOpen()">
    {{ detailsOpen() ? 'Hide Details' : 'Show Details' }}
  </button>

  @if (detailsOpen()) {
    <div cngxFocusRestore
         style="margin-top:8px;padding:12px;background:var(--cngx-surface-alt,#f9f9f9);border-radius:6px">
      <p style="margin:0">Extra details that appear dynamically. Tab through these fields, then close.</p>
      <input placeholder="Name" style="margin-top:8px;padding:6px 10px;border:1px solid var(--cngx-border,#ddd);border-radius:4px" />
    </div>
  }
      </app-example-card>
    </app-doc-shell>
  `,
})
export class FocusRestoreDemoComponent {
  protected readonly _s0 = 'Click "Open Panel" — focus moves into the panel. Close it — focus returns to the button that opened it.';
  protected readonly _s1 = 'A collapsible detail section. When collapsed, focus returns to the toggle button.';
  protected readonly _srcHtml0 = `<button (click)="panelOpen.set(true)" class="chip">Open Panel</button>

  @if (panelOpen()) {
    <div cngxFocusRestore
         style="margin-top:12px;padding:16px;border:1px solid var(--cngx-border,#ddd);border-radius:8px">
      <p style="margin:0 0 12px">Panel content. Focus will restore on close.</p>
      <button (click)="panelOpen.set(false)" class="chip">Close Panel</button>
    </div>
  }

  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">Panel open</span>
      <span class="event-value">{{ panelOpen() }}</span>
    </div>
  </div>`;
  protected readonly _srcTs0 = `import { CngxFocusRestore } from '@cngx/common/a11y';


  protected readonly panelOpen = signal(false);
  protected readonly detailsOpen = signal(false);`;
  protected readonly _srcHtml1 = `<button (click)="detailsOpen.set(!detailsOpen())" class="chip"
          [attr.aria-expanded]="detailsOpen()">
    {{ detailsOpen() ? 'Hide Details' : 'Show Details' }}
  </button>

  @if (detailsOpen()) {
    <div cngxFocusRestore
         style="margin-top:8px;padding:12px;background:var(--cngx-surface-alt,#f9f9f9);border-radius:6px">
      <p style="margin:0">Extra details that appear dynamically. Tab through these fields, then close.</p>
      <input placeholder="Name" style="margin-top:8px;padding:6px 10px;border:1px solid var(--cngx-border,#ddd);border-radius:4px" />
    </div>
  }`;
  protected readonly _srcTs1 = `import { CngxFocusRestore } from '@cngx/common/a11y';


  protected readonly panelOpen = signal(false);
  protected readonly detailsOpen = signal(false);`;

  protected readonly panelOpen = signal(false);
  protected readonly detailsOpen = signal(false);
  
}
