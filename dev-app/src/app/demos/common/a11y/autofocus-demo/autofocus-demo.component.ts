// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/a11y/autofocus-demo/autofocus-demo.story.ts

import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxAutofocus } from '@cngx/common/a11y';

@Component({
  selector: 'app-autofocus-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxAutofocus,
  ],
  template: `
    <app-doc-shell title="Autofocus"
      description="Reactive autofocus for dynamically inserted elements. Works where native autofocus fails (dialogs, panels, conditional views)."
      overview="<p>The native <code>autofocus</code> attribute only works on initial page load. <code>[cngxAutofocus]</code> handles dynamic content — focusing elements when they appear or when a condition becomes true.</p>"
      [apiComponents]="['CngxAutofocus']">
      <app-example-card title="Focus on Insert"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <button (click)="showSearch.set(!showSearch())" class="chip">
    {{ showSearch() ? 'Hide Search' : 'Show Search' }}
  </button>

  @if (showSearch()) {
    <div style="margin-top:12px">
      <input [cngxAutofocus]="true" placeholder="Search..."
             style="padding:8px 12px;border:1px solid var(--cngx-border,#ddd);border-radius:6px;width:240px" />
    </div>
  }
      </app-example-card>
      <app-example-card title="Conditional Focus"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <button (click)="conditionMet.set(!conditionMet())" class="chip">
    {{ conditionMet() ? 'Deactivate' : 'Activate Field' }}
  </button>
  <div style="margin-top:12px">
    <input [cngxAutofocus]="conditionMet()" placeholder="Focused when active"
           style="padding:8px 12px;border:1px solid var(--cngx-border,#ddd);border-radius:6px;width:240px" />
  </div>
  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">Condition</span>
      <span class="event-value">{{ conditionMet() }}</span>
    </div>
  </div>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class AutofocusDemoComponent {
  protected readonly _s0 = 'Toggle the search bar. The input is automatically focused when it appears.';
  protected readonly _s1 = 'The input is always rendered, but focus is applied only when the condition becomes <code>true</code>.';
  protected readonly _srcHtml0 = `<button (click)="showSearch.set(!showSearch())" class="chip">
    {{ showSearch() ? 'Hide Search' : 'Show Search' }}
  </button>

  @if (showSearch()) {
    <div style="margin-top:12px">
      <input [cngxAutofocus]="true" placeholder="Search..."
             style="padding:8px 12px;border:1px solid var(--cngx-border,#ddd);border-radius:6px;width:240px" />
    </div>
  }`;
  protected readonly _srcTs0 = `import { CngxAutofocus } from '@cngx/common/a11y';


  protected readonly showSearch = signal(false);
  protected readonly conditionMet = signal(false);`;
  protected readonly _srcHtml1 = `<button (click)="conditionMet.set(!conditionMet())" class="chip">
    {{ conditionMet() ? 'Deactivate' : 'Activate Field' }}
  </button>
  <div style="margin-top:12px">
    <input [cngxAutofocus]="conditionMet()" placeholder="Focused when active"
           style="padding:8px 12px;border:1px solid var(--cngx-border,#ddd);border-radius:6px;width:240px" />
  </div>
  <div class="event-grid" style="margin-top:12px">
    <div class="event-row">
      <span class="event-label">Condition</span>
      <span class="event-value">{{ conditionMet() }}</span>
    </div>
  </div>`;
  protected readonly _srcTs1 = `import { CngxAutofocus } from '@cngx/common/a11y';


  protected readonly showSearch = signal(false);
  protected readonly conditionMet = signal(false);`;

  protected readonly showSearch = signal(false);
  protected readonly conditionMet = signal(false);
  
}
