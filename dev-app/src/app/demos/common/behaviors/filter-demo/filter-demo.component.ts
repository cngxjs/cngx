// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/behaviors/filter-demo/filter-demo.story.ts

import { ChangeDetectionStrategy, Component, computed, inject, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxFilter } from '@cngx/common/data';
import { CurrencyPipe } from '@angular/common';
import { PEOPLE, type Person } from '../../../../fixtures';
import { HttpClient } from '@angular/common/http';
import { catchError, finalize, map, of, switchMap } from 'rxjs';
import { toObservable, toSignal } from '@angular/core/rxjs-interop';
import { type DJProduct, type DJCategory } from '../../../../fixtures';

@Component({
  selector: 'app-filter-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxFilter,
    CurrencyPipe,
  ],
  template: `
    <app-doc-shell title="Filter"
      [apiComponents]="['CngxFilter']">
      <app-example-card title="CngxFilter — Controlled"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <div
    [cngxFilter]="filterPredicate()"
    #filterRef="cngxFilter"
    class="filter-container"
  >
    <div class="filter-row">
      <span class="filter-label">Filter by location:</span>
      <button
        type="button"
        class="chip"
        [class.chip--active]="activeLocation() === null"
        (click)="activeLocation.set(null)"
      >All</button>
      @for (loc of locations; track loc) {
        <button
          type="button"
          class="chip"
          [class.chip--active]="activeLocation() === loc"
          (click)="activeLocation.set(loc)"
        >{{ loc }}</button>
      }
    </div>
    <div class="table-wrap">
      <table class="demo-table">
        <thead>
          <tr><th>Name</th><th>Role</th><th>Location</th></tr>
        </thead>
        <tbody>
          @for (row of filteredRows(); track row.name) {
            <tr><td>{{ row.name }}</td><td>{{ row.role }}</td><td>{{ row.location }}</td></tr>
          } @empty {
            <tr><td colspan="3" class="empty-cell">No results.</td></tr>
          }
        </tbody>
      </table>
    </div>
  </div>
  <div class="status-row">
    <span class="status-badge" [class.active]="filterRef.isActive()">
      isActive: {{ filterRef.isActive() }}
    </span>
    <span class="status-badge">{{ filteredRows().length }} / {{ totalPeople }} rows</span>
  </div>
      </app-example-card>
      <app-example-card title="CngxFilter — Uncontrolled"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <div
    [cngxFilter]="null"
    #filterRef2="cngxFilter"
    (filterChange)="onFilterChange($event)"
    class="filter-container"
  >
    <div class="filter-row">
      <span class="filter-label">Filter by role:</span>
      <button
        type="button"
        class="chip"
        [class.chip--active]="activeRole() === null"
        (click)="filterRef2.clear(); activeRole.set(null)"
      >All</button>
      @for (role of roles; track role) {
        <button
          type="button"
          class="chip"
          [class.chip--active]="activeRole() === role"
          (click)="filterRef2.setPredicate(rolePred(role)); activeRole.set(role)"
        >{{ role }}</button>
      }
    </div>
    <div class="table-wrap">
      <table class="demo-table">
        <thead>
          <tr><th>Name</th><th>Role</th><th>Location</th></tr>
        </thead>
        <tbody>
          @for (row of uncontrolledRows(); track row.name) {
            <tr><td>{{ row.name }}</td><td>{{ row.role }}</td><td>{{ row.location }}</td></tr>
          } @empty {
            <tr><td colspan="3" class="empty-cell">No results.</td></tr>
          }
        </tbody>
      </table>
    </div>
  </div>
  <div class="status-row">
    <span class="status-badge" [class.active]="filterRef2.isActive()">
      isActive: {{ filterRef2.isActive() }}
    </span>
    <span class="status-badge">{{ uncontrolledRows().length }} / {{ totalPeople }} rows</span>
  </div>
      </app-example-card>
      <app-example-card title="CngxFilter — Multi (addPredicate / removePredicate)"
        [subtitle]="_s2"
        [sourceHtml]="_srcHtml2"
        [sourceTs]="_srcTs2">
        
  <div [cngxFilter]="null" #filterRef3="cngxFilter" class="filter-container">
    <div class="filter-row">
      <span class="filter-label">Location:</span>
      <button
        type="button"
        class="chip"
        [class.chip--active]="multiActiveLocation() === null"
        (click)="setLocationFilter(filterRef3, null)"
      >All</button>
      @for (loc of locations; track loc) {
        <button
          type="button"
          class="chip"
          [class.chip--active]="multiActiveLocation() === loc"
          (click)="setLocationFilter(filterRef3, loc)"
        >{{ loc }}</button>
      }
    </div>
    <div class="filter-row">
      <span class="filter-label">Role:</span>
      <button
        type="button"
        class="chip"
        [class.chip--active]="multiActiveRole() === null"
        (click)="setRoleFilter(filterRef3, null)"
      >All</button>
      @for (role of roles; track role) {
        <button
          type="button"
          class="chip"
          [class.chip--active]="multiActiveRole() === role"
          (click)="setRoleFilter(filterRef3, role)"
        >{{ role }}</button>
      }
    </div>
    <div class="table-wrap">
      <table class="demo-table">
        <thead>
          <tr><th>Name</th><th>Role</th><th>Location</th></tr>
        </thead>
        <tbody>
          @for (row of multiRows(); track row.name) {
            <tr><td>{{ row.name }}</td><td>{{ row.role }}</td><td>{{ row.location }}</td></tr>
          } @empty {
            <tr><td colspan="3" class="empty-cell">No results.</td></tr>
          }
        </tbody>
      </table>
    </div>
  </div>
  <div class="status-row">
    <span class="status-badge" [class.active]="filterRef3.isActive()">
      isActive: {{ filterRef3.isActive() }}
    </span>
    <span class="status-badge" [class.active]="filterRef3.activeCount() > 0">
      activeCount: {{ filterRef3.activeCount() }}
    </span>
    <span class="status-badge">{{ multiRows().length }} / {{ totalPeople }} rows</span>
  </div>
      </app-example-card>
      <app-example-card title="OR within dimension + AND across dimensions"
        [subtitle]="_s3"
        [sourceHtml]="_srcHtml3"
        [sourceTs]="_srcTs3">
        
  <div [cngxFilter]="null" #filterRef4="cngxFilter" class="filter-container">
    <div class="filter-row">
      <span class="filter-label">Location (OR):</span>
      @for (loc of locations; track loc) {
        <button
          type="button"
          class="chip"
          [class.chip--active]="selectedLocations().has(loc)"
          (click)="toggleLocation(filterRef4, loc)"
        >{{ loc }}</button>
      }
    </div>
    <div class="filter-row">
      <span class="filter-label">Role (OR):</span>
      @for (role of roles; track role) {
        <button
          type="button"
          class="chip"
          [class.chip--active]="selectedRoles().has(role)"
          (click)="toggleRole2(filterRef4, role)"
        >{{ role }}</button>
      }
    </div>
    <div class="button-row" style="margin-bottom:.75rem">
      <button type="button" (click)="clearOrFilters(filterRef4)">Clear all</button>
    </div>
    <div class="table-wrap">
      <table class="demo-table">
        <thead>
          <tr><th>Name</th><th>Role</th><th>Location</th></tr>
        </thead>
        <tbody>
          @for (row of orRows(); track row.name) {
            <tr><td>{{ row.name }}</td><td>{{ row.role }}</td><td>{{ row.location }}</td></tr>
          } @empty {
            <tr><td colspan="3" class="empty-cell">No results.</td></tr>
          }
        </tbody>
      </table>
    </div>
  </div>
  <div class="status-row">
    <span class="status-badge" [class.active]="filterRef4.isActive()">
      isActive: {{ filterRef4.isActive() }}
    </span>
    <span class="status-badge" [class.active]="filterRef4.activeCount() > 0">
      activeCount: {{ filterRef4.activeCount() }}
    </span>
    <span class="status-badge">{{ orRows().length }} / {{ totalPeople }} rows</span>
  </div>
      </app-example-card>
      <app-example-card title="Server-Side Filter by Category (DummyJSON)"
        [subtitle]="_s4"
        [sourceHtml]="_srcHtml4"
        [sourceTs]="_srcTs4">
        
  <div class="filter-row">
    <span class="filter-label">Category:</span>
    <button
      type="button"
      class="chip"
      [class.chip--active]="activeSlug() === null"
      (click)="activeSlug.set(null)"
    >All</button>
    @for (cat of categories().slice(0, 10); track cat.slug) {
      <button
        type="button"
        class="chip"
        [class.chip--active]="activeSlug() === cat.slug"
        (click)="activeSlug.set(cat.slug)"
      >{{ cat.name }}</button>
    }
  </div>
  <div class="table-wrap">
    <table class="demo-table">
      <thead>
        <tr><th>Title</th><th>Brand</th><th>Category</th><th>Price</th><th>Rating</th></tr>
      </thead>
      <tbody>
        @if (apiLoading()) {
          <tr><td colspan="5" class="empty-cell">Loading…</td></tr>
        } @else {
          @for (p of apiProducts(); track p.id) {
            <tr>
              <td>{{ p.title }}</td>
              <td>{{ p.brand }}</td>
              <td>{{ p.category }}</td>
              <td>{{ p.price | currency }}</td>
              <td>{{ p.rating }}</td>
            </tr>
          } @empty {
            <tr><td colspan="5" class="empty-cell">No products.</td></tr>
          }
        }
      </tbody>
    </table>
  </div>
  <div class="status-row">
    <span class="status-badge" [class.active]="activeSlug() !== null">
      filter {{ activeSlug() ?? 'off' }}
    </span>
    <span class="status-badge">{{ apiProducts().length }} results</span>
  </div>
      </app-example-card>
      <app-example-card title="Pattern: Local CngxFilter vs Server-Side"
        [subtitle]="_s5"
        [sourceHtml]="_srcHtml5"
        [sourceTs]="_srcTs5">
        
  <pre class="code-block"><code>// ── Local (CngxFilter) ──────────────────────────────────────
// Good when: data is already fetched, small dataset
protected readonly pred = computed(() =>
  this.activeRole() ? (p: Person) => p.role === this.activeRole() : null
);
// &lt;div [cngxFilter]="pred()"&gt;

// ── Server-side ──────────────────────────────────────────────
// Good when: data lives on the server, large dataset
protected readonly category = signal&lt;string | null&gt;(null);
protected readonly rows = toSignal(
  toObservable(this.category).pipe(
    switchMap((cat) =>
      cat
        ? http.get&lt;&#123; products: Product[] &#125;&gt;(\`/api/products/category/$&#123;cat&#125;\`)
        : http.get&lt;&#123; products: Product[] &#125;&gt;('/api/products'),
    ),
    map((r) => r.products),
  ),
  &#123; initialValue: [] &#125;,
);</code></pre>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class FilterDemoComponent {
  protected readonly _s0 = '<code>[cngxFilter]="pred()"</code> drives the directive from a <code>computed()</code>. When the signal changes, the directive updates automatically. <code>isActive()</code> reflects whether a predicate is set.';
  protected readonly _s1 = '<code>filterRef.setPredicate(fn)</code> mutates the directive\'s internal state imperatively — no bound input needed. <code>(filterChange)</code> emits the new predicate so the consumer can sync local derived state.';
  protected readonly _s2 = 'Each filter chip calls <code>addPredicate(key, fn)</code> or <code>removePredicate(key)</code>. All active predicates are AND-combined automatically. <code>activeCount()</code> shows how many named predicates are stacked.';
  protected readonly _s3 = 'Multiple values per dimension are possible by keeping a <code>Set</code> inside a single named predicate. Click <em>London</em> and <em>Rome</em> to see OR in action. Adding a role filter stacks on top as AND.';
  protected readonly _s4 = 'For server-side filtering, skip <code>CngxFilter</code> — the API does the filtering. A <code>signal</code> holds the active category; <code>toObservable()</code> + <code>switchMap</code> fetches the right endpoint on every change.';
  protected readonly _s5 = 'Use <code>CngxFilter</code> when data is already in memory. Use a signal + API call when the server owns the data. The two patterns are independent — pick the right one per use case.';
  protected readonly _srcHtml0 = `<div
    [cngxFilter]="filterPredicate()"
    #filterRef="cngxFilter"
    class="filter-container"
  >
    <div class="filter-row">
      <span class="filter-label">Filter by location:</span>
      <button
        type="button"
        class="chip"
        [class.chip--active]="activeLocation() === null"
        (click)="activeLocation.set(null)"
      >All</button>
      @for (loc of locations; track loc) {
        <button
          type="button"
          class="chip"
          [class.chip--active]="activeLocation() === loc"
          (click)="activeLocation.set(loc)"
        >{{ loc }}</button>
      }
    </div>
    <div class="table-wrap">
      <table class="demo-table">
        <thead>
          <tr><th>Name</th><th>Role</th><th>Location</th></tr>
        </thead>
        <tbody>
          @for (row of filteredRows(); track row.name) {
            <tr><td>{{ row.name }}</td><td>{{ row.role }}</td><td>{{ row.location }}</td></tr>
          } @empty {
            <tr><td colspan="3" class="empty-cell">No results.</td></tr>
          }
        </tbody>
      </table>
    </div>
  </div>
  <div class="status-row">
    <span class="status-badge" [class.active]="filterRef.isActive()">
      isActive: {{ filterRef.isActive() }}
    </span>
    <span class="status-badge">{{ filteredRows().length }} / {{ totalPeople }} rows</span>
  </div>`;
  protected readonly _srcTs0 = `import { PEOPLE, type Person } from '../../../../fixtures';
import { HttpClient } from '@angular/common/http';
import { catchError, finalize, map, of, switchMap } from 'rxjs';
import { toObservable, toSignal } from '@angular/core/rxjs-interop';
import { type DJProduct, type DJCategory } from '../../../../fixtures';


  protected readonly totalPeople = PEOPLE.length;
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort((a, b) => a.localeCompare(b));
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort((a, b) => a.localeCompare(b));

  // ── Controlled mode ──────────────────────────────────────────────────────
  protected readonly activeLocation = signal<string | null>(null);

  protected readonly filterPredicate = computed(
    (): ((p: Person) => boolean) | null => {
      const loc = this.activeLocation();
      return loc ? (p) => p.location === loc : null;
    },
  );

  protected readonly filteredRows = computed((): Person[] => {
    const pred = this.filterPredicate();
    if (!pred) return PEOPLE;
    return PEOPLE.filter((p) => pred(p));
  });

  // ── Uncontrolled mode ────────────────────────────────────────────────────
  protected readonly activeRole = signal<string | null>(null);
  private readonly uncontrolledPredicate = signal<((p: unknown) => boolean) | null>(null);

  protected readonly uncontrolledRows = computed((): Person[] => {
    const pred = this.uncontrolledPredicate();
    if (!pred) return PEOPLE;
    return PEOPLE.filter((p) => pred(p));
  });

  protected rolePred(role: string): (p: unknown) => boolean {
    return (p) => (p as Person).role === role;
  }

  protected onFilterChange(pred: ((p: unknown) => boolean) | null): void {
    this.uncontrolledPredicate.set(pred);
  }

  // ── Multi-filter: AND across dimensions ──────────────────────────────────
  protected readonly multiActiveLocation = signal<string | null>(null);
  protected readonly multiActiveRole = signal<string | null>(null);
  protected readonly multiRows = signal<Person[]>(PEOPLE);

  protected setLocationFilter(filterRef: CngxFilter, loc: string | null): void {
    this.multiActiveLocation.set(loc);
    if (loc) {
      filterRef.addPredicate('location', (p) => (p as Person).location === loc);
    } else {
      filterRef.removePredicate('location');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.multiRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected setRoleFilter(filterRef: CngxFilter, role: string | null): void {
    this.multiActiveRole.set(role);
    if (role) {
      filterRef.addPredicate('role', (p) => (p as Person).role === role);
    } else {
      filterRef.removePredicate('role');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.multiRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  // ── Multi-filter: OR within dimension + AND across dimensions ────────────
  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles = signal<Set<string>>(new Set());
  protected readonly orRows = signal<Person[]>(PEOPLE);

  protected toggleLocation(filterRef: CngxFilter, loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      filterRef.addPredicate('location', (p) => next.has((p as Person).location));
    } else {
      filterRef.removePredicate('location');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.orRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected toggleRole2(filterRef: CngxFilter, role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      filterRef.addPredicate('role', (p) => next.has((p as Person).role));
    } else {
      filterRef.removePredicate('role');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.orRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected clearOrFilters(filterRef: CngxFilter): void {
    filterRef.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.orRows.set(PEOPLE);
  }

  // ── Backend filter ───────────────────────────────────────────────────────
  private readonly http = inject(HttpClient);

  protected readonly categories = toSignal(
    this.http
      .get<DJCategory[]>('https://dummyjson.com/products/categories')
      .pipe(catchError(() => of([] as DJCategory[]))),
    { initialValue: [] as DJCategory[] },
  );

  protected readonly activeSlug = signal<string | null>(null);
  protected readonly apiLoading = signal(false);

  protected readonly apiProducts = toSignal(
    toObservable(this.activeSlug).pipe(
      switchMap((slug) => {
        this.apiLoading.set(true);
        const url = slug
          ? \`https://dummyjson.com/products/category/\${slug}?limit=8&select=id,title,brand,price,rating,category\`
          : 'https://dummyjson.com/products?limit=8&select=id,title,brand,price,rating,category';
        return this.http.get<{ products: DJProduct[] }>(url).pipe(
          map((r) => r.products),
          catchError(() => of([] as DJProduct[])),
          finalize(() => this.apiLoading.set(false)),
        );
      }),
    ),
    { initialValue: [] as DJProduct[] },
  );`;
  protected readonly _srcHtml1 = `<div
    [cngxFilter]="null"
    #filterRef2="cngxFilter"
    (filterChange)="onFilterChange($event)"
    class="filter-container"
  >
    <div class="filter-row">
      <span class="filter-label">Filter by role:</span>
      <button
        type="button"
        class="chip"
        [class.chip--active]="activeRole() === null"
        (click)="filterRef2.clear(); activeRole.set(null)"
      >All</button>
      @for (role of roles; track role) {
        <button
          type="button"
          class="chip"
          [class.chip--active]="activeRole() === role"
          (click)="filterRef2.setPredicate(rolePred(role)); activeRole.set(role)"
        >{{ role }}</button>
      }
    </div>
    <div class="table-wrap">
      <table class="demo-table">
        <thead>
          <tr><th>Name</th><th>Role</th><th>Location</th></tr>
        </thead>
        <tbody>
          @for (row of uncontrolledRows(); track row.name) {
            <tr><td>{{ row.name }}</td><td>{{ row.role }}</td><td>{{ row.location }}</td></tr>
          } @empty {
            <tr><td colspan="3" class="empty-cell">No results.</td></tr>
          }
        </tbody>
      </table>
    </div>
  </div>
  <div class="status-row">
    <span class="status-badge" [class.active]="filterRef2.isActive()">
      isActive: {{ filterRef2.isActive() }}
    </span>
    <span class="status-badge">{{ uncontrolledRows().length }} / {{ totalPeople }} rows</span>
  </div>`;
  protected readonly _srcTs1 = `import { PEOPLE, type Person } from '../../../../fixtures';
import { HttpClient } from '@angular/common/http';
import { catchError, finalize, map, of, switchMap } from 'rxjs';
import { toObservable, toSignal } from '@angular/core/rxjs-interop';
import { type DJProduct, type DJCategory } from '../../../../fixtures';


  protected readonly totalPeople = PEOPLE.length;
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort((a, b) => a.localeCompare(b));
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort((a, b) => a.localeCompare(b));

  // ── Controlled mode ──────────────────────────────────────────────────────
  protected readonly activeLocation = signal<string | null>(null);

  protected readonly filterPredicate = computed(
    (): ((p: Person) => boolean) | null => {
      const loc = this.activeLocation();
      return loc ? (p) => p.location === loc : null;
    },
  );

  protected readonly filteredRows = computed((): Person[] => {
    const pred = this.filterPredicate();
    if (!pred) return PEOPLE;
    return PEOPLE.filter((p) => pred(p));
  });

  // ── Uncontrolled mode ────────────────────────────────────────────────────
  protected readonly activeRole = signal<string | null>(null);
  private readonly uncontrolledPredicate = signal<((p: unknown) => boolean) | null>(null);

  protected readonly uncontrolledRows = computed((): Person[] => {
    const pred = this.uncontrolledPredicate();
    if (!pred) return PEOPLE;
    return PEOPLE.filter((p) => pred(p));
  });

  protected rolePred(role: string): (p: unknown) => boolean {
    return (p) => (p as Person).role === role;
  }

  protected onFilterChange(pred: ((p: unknown) => boolean) | null): void {
    this.uncontrolledPredicate.set(pred);
  }

  // ── Multi-filter: AND across dimensions ──────────────────────────────────
  protected readonly multiActiveLocation = signal<string | null>(null);
  protected readonly multiActiveRole = signal<string | null>(null);
  protected readonly multiRows = signal<Person[]>(PEOPLE);

  protected setLocationFilter(filterRef: CngxFilter, loc: string | null): void {
    this.multiActiveLocation.set(loc);
    if (loc) {
      filterRef.addPredicate('location', (p) => (p as Person).location === loc);
    } else {
      filterRef.removePredicate('location');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.multiRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected setRoleFilter(filterRef: CngxFilter, role: string | null): void {
    this.multiActiveRole.set(role);
    if (role) {
      filterRef.addPredicate('role', (p) => (p as Person).role === role);
    } else {
      filterRef.removePredicate('role');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.multiRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  // ── Multi-filter: OR within dimension + AND across dimensions ────────────
  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles = signal<Set<string>>(new Set());
  protected readonly orRows = signal<Person[]>(PEOPLE);

  protected toggleLocation(filterRef: CngxFilter, loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      filterRef.addPredicate('location', (p) => next.has((p as Person).location));
    } else {
      filterRef.removePredicate('location');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.orRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected toggleRole2(filterRef: CngxFilter, role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      filterRef.addPredicate('role', (p) => next.has((p as Person).role));
    } else {
      filterRef.removePredicate('role');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.orRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected clearOrFilters(filterRef: CngxFilter): void {
    filterRef.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.orRows.set(PEOPLE);
  }

  // ── Backend filter ───────────────────────────────────────────────────────
  private readonly http = inject(HttpClient);

  protected readonly categories = toSignal(
    this.http
      .get<DJCategory[]>('https://dummyjson.com/products/categories')
      .pipe(catchError(() => of([] as DJCategory[]))),
    { initialValue: [] as DJCategory[] },
  );

  protected readonly activeSlug = signal<string | null>(null);
  protected readonly apiLoading = signal(false);

  protected readonly apiProducts = toSignal(
    toObservable(this.activeSlug).pipe(
      switchMap((slug) => {
        this.apiLoading.set(true);
        const url = slug
          ? \`https://dummyjson.com/products/category/\${slug}?limit=8&select=id,title,brand,price,rating,category\`
          : 'https://dummyjson.com/products?limit=8&select=id,title,brand,price,rating,category';
        return this.http.get<{ products: DJProduct[] }>(url).pipe(
          map((r) => r.products),
          catchError(() => of([] as DJProduct[])),
          finalize(() => this.apiLoading.set(false)),
        );
      }),
    ),
    { initialValue: [] as DJProduct[] },
  );`;
  protected readonly _srcHtml2 = `<div [cngxFilter]="null" #filterRef3="cngxFilter" class="filter-container">
    <div class="filter-row">
      <span class="filter-label">Location:</span>
      <button
        type="button"
        class="chip"
        [class.chip--active]="multiActiveLocation() === null"
        (click)="setLocationFilter(filterRef3, null)"
      >All</button>
      @for (loc of locations; track loc) {
        <button
          type="button"
          class="chip"
          [class.chip--active]="multiActiveLocation() === loc"
          (click)="setLocationFilter(filterRef3, loc)"
        >{{ loc }}</button>
      }
    </div>
    <div class="filter-row">
      <span class="filter-label">Role:</span>
      <button
        type="button"
        class="chip"
        [class.chip--active]="multiActiveRole() === null"
        (click)="setRoleFilter(filterRef3, null)"
      >All</button>
      @for (role of roles; track role) {
        <button
          type="button"
          class="chip"
          [class.chip--active]="multiActiveRole() === role"
          (click)="setRoleFilter(filterRef3, role)"
        >{{ role }}</button>
      }
    </div>
    <div class="table-wrap">
      <table class="demo-table">
        <thead>
          <tr><th>Name</th><th>Role</th><th>Location</th></tr>
        </thead>
        <tbody>
          @for (row of multiRows(); track row.name) {
            <tr><td>{{ row.name }}</td><td>{{ row.role }}</td><td>{{ row.location }}</td></tr>
          } @empty {
            <tr><td colspan="3" class="empty-cell">No results.</td></tr>
          }
        </tbody>
      </table>
    </div>
  </div>
  <div class="status-row">
    <span class="status-badge" [class.active]="filterRef3.isActive()">
      isActive: {{ filterRef3.isActive() }}
    </span>
    <span class="status-badge" [class.active]="filterRef3.activeCount() > 0">
      activeCount: {{ filterRef3.activeCount() }}
    </span>
    <span class="status-badge">{{ multiRows().length }} / {{ totalPeople }} rows</span>
  </div>`;
  protected readonly _srcTs2 = `import { PEOPLE, type Person } from '../../../../fixtures';
import { HttpClient } from '@angular/common/http';
import { catchError, finalize, map, of, switchMap } from 'rxjs';
import { toObservable, toSignal } from '@angular/core/rxjs-interop';
import { type DJProduct, type DJCategory } from '../../../../fixtures';


  protected readonly totalPeople = PEOPLE.length;
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort((a, b) => a.localeCompare(b));
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort((a, b) => a.localeCompare(b));

  // ── Controlled mode ──────────────────────────────────────────────────────
  protected readonly activeLocation = signal<string | null>(null);

  protected readonly filterPredicate = computed(
    (): ((p: Person) => boolean) | null => {
      const loc = this.activeLocation();
      return loc ? (p) => p.location === loc : null;
    },
  );

  protected readonly filteredRows = computed((): Person[] => {
    const pred = this.filterPredicate();
    if (!pred) return PEOPLE;
    return PEOPLE.filter((p) => pred(p));
  });

  // ── Uncontrolled mode ────────────────────────────────────────────────────
  protected readonly activeRole = signal<string | null>(null);
  private readonly uncontrolledPredicate = signal<((p: unknown) => boolean) | null>(null);

  protected readonly uncontrolledRows = computed((): Person[] => {
    const pred = this.uncontrolledPredicate();
    if (!pred) return PEOPLE;
    return PEOPLE.filter((p) => pred(p));
  });

  protected rolePred(role: string): (p: unknown) => boolean {
    return (p) => (p as Person).role === role;
  }

  protected onFilterChange(pred: ((p: unknown) => boolean) | null): void {
    this.uncontrolledPredicate.set(pred);
  }

  // ── Multi-filter: AND across dimensions ──────────────────────────────────
  protected readonly multiActiveLocation = signal<string | null>(null);
  protected readonly multiActiveRole = signal<string | null>(null);
  protected readonly multiRows = signal<Person[]>(PEOPLE);

  protected setLocationFilter(filterRef: CngxFilter, loc: string | null): void {
    this.multiActiveLocation.set(loc);
    if (loc) {
      filterRef.addPredicate('location', (p) => (p as Person).location === loc);
    } else {
      filterRef.removePredicate('location');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.multiRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected setRoleFilter(filterRef: CngxFilter, role: string | null): void {
    this.multiActiveRole.set(role);
    if (role) {
      filterRef.addPredicate('role', (p) => (p as Person).role === role);
    } else {
      filterRef.removePredicate('role');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.multiRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  // ── Multi-filter: OR within dimension + AND across dimensions ────────────
  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles = signal<Set<string>>(new Set());
  protected readonly orRows = signal<Person[]>(PEOPLE);

  protected toggleLocation(filterRef: CngxFilter, loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      filterRef.addPredicate('location', (p) => next.has((p as Person).location));
    } else {
      filterRef.removePredicate('location');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.orRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected toggleRole2(filterRef: CngxFilter, role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      filterRef.addPredicate('role', (p) => next.has((p as Person).role));
    } else {
      filterRef.removePredicate('role');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.orRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected clearOrFilters(filterRef: CngxFilter): void {
    filterRef.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.orRows.set(PEOPLE);
  }

  // ── Backend filter ───────────────────────────────────────────────────────
  private readonly http = inject(HttpClient);

  protected readonly categories = toSignal(
    this.http
      .get<DJCategory[]>('https://dummyjson.com/products/categories')
      .pipe(catchError(() => of([] as DJCategory[]))),
    { initialValue: [] as DJCategory[] },
  );

  protected readonly activeSlug = signal<string | null>(null);
  protected readonly apiLoading = signal(false);

  protected readonly apiProducts = toSignal(
    toObservable(this.activeSlug).pipe(
      switchMap((slug) => {
        this.apiLoading.set(true);
        const url = slug
          ? \`https://dummyjson.com/products/category/\${slug}?limit=8&select=id,title,brand,price,rating,category\`
          : 'https://dummyjson.com/products?limit=8&select=id,title,brand,price,rating,category';
        return this.http.get<{ products: DJProduct[] }>(url).pipe(
          map((r) => r.products),
          catchError(() => of([] as DJProduct[])),
          finalize(() => this.apiLoading.set(false)),
        );
      }),
    ),
    { initialValue: [] as DJProduct[] },
  );`;
  protected readonly _srcHtml3 = `<div [cngxFilter]="null" #filterRef4="cngxFilter" class="filter-container">
    <div class="filter-row">
      <span class="filter-label">Location (OR):</span>
      @for (loc of locations; track loc) {
        <button
          type="button"
          class="chip"
          [class.chip--active]="selectedLocations().has(loc)"
          (click)="toggleLocation(filterRef4, loc)"
        >{{ loc }}</button>
      }
    </div>
    <div class="filter-row">
      <span class="filter-label">Role (OR):</span>
      @for (role of roles; track role) {
        <button
          type="button"
          class="chip"
          [class.chip--active]="selectedRoles().has(role)"
          (click)="toggleRole2(filterRef4, role)"
        >{{ role }}</button>
      }
    </div>
    <div class="button-row" style="margin-bottom:.75rem">
      <button type="button" (click)="clearOrFilters(filterRef4)">Clear all</button>
    </div>
    <div class="table-wrap">
      <table class="demo-table">
        <thead>
          <tr><th>Name</th><th>Role</th><th>Location</th></tr>
        </thead>
        <tbody>
          @for (row of orRows(); track row.name) {
            <tr><td>{{ row.name }}</td><td>{{ row.role }}</td><td>{{ row.location }}</td></tr>
          } @empty {
            <tr><td colspan="3" class="empty-cell">No results.</td></tr>
          }
        </tbody>
      </table>
    </div>
  </div>
  <div class="status-row">
    <span class="status-badge" [class.active]="filterRef4.isActive()">
      isActive: {{ filterRef4.isActive() }}
    </span>
    <span class="status-badge" [class.active]="filterRef4.activeCount() > 0">
      activeCount: {{ filterRef4.activeCount() }}
    </span>
    <span class="status-badge">{{ orRows().length }} / {{ totalPeople }} rows</span>
  </div>`;
  protected readonly _srcTs3 = `import { PEOPLE, type Person } from '../../../../fixtures';
import { HttpClient } from '@angular/common/http';
import { catchError, finalize, map, of, switchMap } from 'rxjs';
import { toObservable, toSignal } from '@angular/core/rxjs-interop';
import { type DJProduct, type DJCategory } from '../../../../fixtures';


  protected readonly totalPeople = PEOPLE.length;
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort((a, b) => a.localeCompare(b));
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort((a, b) => a.localeCompare(b));

  // ── Controlled mode ──────────────────────────────────────────────────────
  protected readonly activeLocation = signal<string | null>(null);

  protected readonly filterPredicate = computed(
    (): ((p: Person) => boolean) | null => {
      const loc = this.activeLocation();
      return loc ? (p) => p.location === loc : null;
    },
  );

  protected readonly filteredRows = computed((): Person[] => {
    const pred = this.filterPredicate();
    if (!pred) return PEOPLE;
    return PEOPLE.filter((p) => pred(p));
  });

  // ── Uncontrolled mode ────────────────────────────────────────────────────
  protected readonly activeRole = signal<string | null>(null);
  private readonly uncontrolledPredicate = signal<((p: unknown) => boolean) | null>(null);

  protected readonly uncontrolledRows = computed((): Person[] => {
    const pred = this.uncontrolledPredicate();
    if (!pred) return PEOPLE;
    return PEOPLE.filter((p) => pred(p));
  });

  protected rolePred(role: string): (p: unknown) => boolean {
    return (p) => (p as Person).role === role;
  }

  protected onFilterChange(pred: ((p: unknown) => boolean) | null): void {
    this.uncontrolledPredicate.set(pred);
  }

  // ── Multi-filter: AND across dimensions ──────────────────────────────────
  protected readonly multiActiveLocation = signal<string | null>(null);
  protected readonly multiActiveRole = signal<string | null>(null);
  protected readonly multiRows = signal<Person[]>(PEOPLE);

  protected setLocationFilter(filterRef: CngxFilter, loc: string | null): void {
    this.multiActiveLocation.set(loc);
    if (loc) {
      filterRef.addPredicate('location', (p) => (p as Person).location === loc);
    } else {
      filterRef.removePredicate('location');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.multiRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected setRoleFilter(filterRef: CngxFilter, role: string | null): void {
    this.multiActiveRole.set(role);
    if (role) {
      filterRef.addPredicate('role', (p) => (p as Person).role === role);
    } else {
      filterRef.removePredicate('role');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.multiRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  // ── Multi-filter: OR within dimension + AND across dimensions ────────────
  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles = signal<Set<string>>(new Set());
  protected readonly orRows = signal<Person[]>(PEOPLE);

  protected toggleLocation(filterRef: CngxFilter, loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      filterRef.addPredicate('location', (p) => next.has((p as Person).location));
    } else {
      filterRef.removePredicate('location');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.orRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected toggleRole2(filterRef: CngxFilter, role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      filterRef.addPredicate('role', (p) => next.has((p as Person).role));
    } else {
      filterRef.removePredicate('role');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.orRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected clearOrFilters(filterRef: CngxFilter): void {
    filterRef.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.orRows.set(PEOPLE);
  }

  // ── Backend filter ───────────────────────────────────────────────────────
  private readonly http = inject(HttpClient);

  protected readonly categories = toSignal(
    this.http
      .get<DJCategory[]>('https://dummyjson.com/products/categories')
      .pipe(catchError(() => of([] as DJCategory[]))),
    { initialValue: [] as DJCategory[] },
  );

  protected readonly activeSlug = signal<string | null>(null);
  protected readonly apiLoading = signal(false);

  protected readonly apiProducts = toSignal(
    toObservable(this.activeSlug).pipe(
      switchMap((slug) => {
        this.apiLoading.set(true);
        const url = slug
          ? \`https://dummyjson.com/products/category/\${slug}?limit=8&select=id,title,brand,price,rating,category\`
          : 'https://dummyjson.com/products?limit=8&select=id,title,brand,price,rating,category';
        return this.http.get<{ products: DJProduct[] }>(url).pipe(
          map((r) => r.products),
          catchError(() => of([] as DJProduct[])),
          finalize(() => this.apiLoading.set(false)),
        );
      }),
    ),
    { initialValue: [] as DJProduct[] },
  );`;
  protected readonly _srcHtml4 = `<div class="filter-row">
    <span class="filter-label">Category:</span>
    <button
      type="button"
      class="chip"
      [class.chip--active]="activeSlug() === null"
      (click)="activeSlug.set(null)"
    >All</button>
    @for (cat of categories().slice(0, 10); track cat.slug) {
      <button
        type="button"
        class="chip"
        [class.chip--active]="activeSlug() === cat.slug"
        (click)="activeSlug.set(cat.slug)"
      >{{ cat.name }}</button>
    }
  </div>
  <div class="table-wrap">
    <table class="demo-table">
      <thead>
        <tr><th>Title</th><th>Brand</th><th>Category</th><th>Price</th><th>Rating</th></tr>
      </thead>
      <tbody>
        @if (apiLoading()) {
          <tr><td colspan="5" class="empty-cell">Loading…</td></tr>
        } @else {
          @for (p of apiProducts(); track p.id) {
            <tr>
              <td>{{ p.title }}</td>
              <td>{{ p.brand }}</td>
              <td>{{ p.category }}</td>
              <td>{{ p.price | currency }}</td>
              <td>{{ p.rating }}</td>
            </tr>
          } @empty {
            <tr><td colspan="5" class="empty-cell">No products.</td></tr>
          }
        }
      </tbody>
    </table>
  </div>
  <div class="status-row">
    <span class="status-badge" [class.active]="activeSlug() !== null">
      filter {{ activeSlug() ?? 'off' }}
    </span>
    <span class="status-badge">{{ apiProducts().length }} results</span>
  </div>`;
  protected readonly _srcTs4 = `import { PEOPLE, type Person } from '../../../../fixtures';
import { HttpClient } from '@angular/common/http';
import { catchError, finalize, map, of, switchMap } from 'rxjs';
import { toObservable, toSignal } from '@angular/core/rxjs-interop';
import { type DJProduct, type DJCategory } from '../../../../fixtures';


  protected readonly totalPeople = PEOPLE.length;
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort((a, b) => a.localeCompare(b));
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort((a, b) => a.localeCompare(b));

  // ── Controlled mode ──────────────────────────────────────────────────────
  protected readonly activeLocation = signal<string | null>(null);

  protected readonly filterPredicate = computed(
    (): ((p: Person) => boolean) | null => {
      const loc = this.activeLocation();
      return loc ? (p) => p.location === loc : null;
    },
  );

  protected readonly filteredRows = computed((): Person[] => {
    const pred = this.filterPredicate();
    if (!pred) return PEOPLE;
    return PEOPLE.filter((p) => pred(p));
  });

  // ── Uncontrolled mode ────────────────────────────────────────────────────
  protected readonly activeRole = signal<string | null>(null);
  private readonly uncontrolledPredicate = signal<((p: unknown) => boolean) | null>(null);

  protected readonly uncontrolledRows = computed((): Person[] => {
    const pred = this.uncontrolledPredicate();
    if (!pred) return PEOPLE;
    return PEOPLE.filter((p) => pred(p));
  });

  protected rolePred(role: string): (p: unknown) => boolean {
    return (p) => (p as Person).role === role;
  }

  protected onFilterChange(pred: ((p: unknown) => boolean) | null): void {
    this.uncontrolledPredicate.set(pred);
  }

  // ── Multi-filter: AND across dimensions ──────────────────────────────────
  protected readonly multiActiveLocation = signal<string | null>(null);
  protected readonly multiActiveRole = signal<string | null>(null);
  protected readonly multiRows = signal<Person[]>(PEOPLE);

  protected setLocationFilter(filterRef: CngxFilter, loc: string | null): void {
    this.multiActiveLocation.set(loc);
    if (loc) {
      filterRef.addPredicate('location', (p) => (p as Person).location === loc);
    } else {
      filterRef.removePredicate('location');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.multiRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected setRoleFilter(filterRef: CngxFilter, role: string | null): void {
    this.multiActiveRole.set(role);
    if (role) {
      filterRef.addPredicate('role', (p) => (p as Person).role === role);
    } else {
      filterRef.removePredicate('role');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.multiRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  // ── Multi-filter: OR within dimension + AND across dimensions ────────────
  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles = signal<Set<string>>(new Set());
  protected readonly orRows = signal<Person[]>(PEOPLE);

  protected toggleLocation(filterRef: CngxFilter, loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      filterRef.addPredicate('location', (p) => next.has((p as Person).location));
    } else {
      filterRef.removePredicate('location');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.orRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected toggleRole2(filterRef: CngxFilter, role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      filterRef.addPredicate('role', (p) => next.has((p as Person).role));
    } else {
      filterRef.removePredicate('role');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.orRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected clearOrFilters(filterRef: CngxFilter): void {
    filterRef.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.orRows.set(PEOPLE);
  }

  // ── Backend filter ───────────────────────────────────────────────────────
  private readonly http = inject(HttpClient);

  protected readonly categories = toSignal(
    this.http
      .get<DJCategory[]>('https://dummyjson.com/products/categories')
      .pipe(catchError(() => of([] as DJCategory[]))),
    { initialValue: [] as DJCategory[] },
  );

  protected readonly activeSlug = signal<string | null>(null);
  protected readonly apiLoading = signal(false);

  protected readonly apiProducts = toSignal(
    toObservable(this.activeSlug).pipe(
      switchMap((slug) => {
        this.apiLoading.set(true);
        const url = slug
          ? \`https://dummyjson.com/products/category/\${slug}?limit=8&select=id,title,brand,price,rating,category\`
          : 'https://dummyjson.com/products?limit=8&select=id,title,brand,price,rating,category';
        return this.http.get<{ products: DJProduct[] }>(url).pipe(
          map((r) => r.products),
          catchError(() => of([] as DJProduct[])),
          finalize(() => this.apiLoading.set(false)),
        );
      }),
    ),
    { initialValue: [] as DJProduct[] },
  );`;
  protected readonly _srcHtml5 = `<pre class="code-block"><code>// ── Local (CngxFilter) ──────────────────────────────────────
// Good when: data is already fetched, small dataset
protected readonly pred = computed(() =>
  this.activeRole() ? (p: Person) => p.role === this.activeRole() : null
);
// &lt;div [cngxFilter]="pred()"&gt;

// ── Server-side ──────────────────────────────────────────────
// Good when: data lives on the server, large dataset
protected readonly category = signal&lt;string | null&gt;(null);
protected readonly rows = toSignal(
  toObservable(this.category).pipe(
    switchMap((cat) =>
      cat
        ? http.get&lt;&#123; products: Product[] &#125;&gt;(\`/api/products/category/$&#123;cat&#125;\`)
        : http.get&lt;&#123; products: Product[] &#125;&gt;('/api/products'),
    ),
    map((r) => r.products),
  ),
  &#123; initialValue: [] &#125;,
);</code></pre>`;
  protected readonly _srcTs5 = `import { PEOPLE, type Person } from '../../../../fixtures';
import { HttpClient } from '@angular/common/http';
import { catchError, finalize, map, of, switchMap } from 'rxjs';
import { toObservable, toSignal } from '@angular/core/rxjs-interop';
import { type DJProduct, type DJCategory } from '../../../../fixtures';


  protected readonly totalPeople = PEOPLE.length;
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort((a, b) => a.localeCompare(b));
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort((a, b) => a.localeCompare(b));

  // ── Controlled mode ──────────────────────────────────────────────────────
  protected readonly activeLocation = signal<string | null>(null);

  protected readonly filterPredicate = computed(
    (): ((p: Person) => boolean) | null => {
      const loc = this.activeLocation();
      return loc ? (p) => p.location === loc : null;
    },
  );

  protected readonly filteredRows = computed((): Person[] => {
    const pred = this.filterPredicate();
    if (!pred) return PEOPLE;
    return PEOPLE.filter((p) => pred(p));
  });

  // ── Uncontrolled mode ────────────────────────────────────────────────────
  protected readonly activeRole = signal<string | null>(null);
  private readonly uncontrolledPredicate = signal<((p: unknown) => boolean) | null>(null);

  protected readonly uncontrolledRows = computed((): Person[] => {
    const pred = this.uncontrolledPredicate();
    if (!pred) return PEOPLE;
    return PEOPLE.filter((p) => pred(p));
  });

  protected rolePred(role: string): (p: unknown) => boolean {
    return (p) => (p as Person).role === role;
  }

  protected onFilterChange(pred: ((p: unknown) => boolean) | null): void {
    this.uncontrolledPredicate.set(pred);
  }

  // ── Multi-filter: AND across dimensions ──────────────────────────────────
  protected readonly multiActiveLocation = signal<string | null>(null);
  protected readonly multiActiveRole = signal<string | null>(null);
  protected readonly multiRows = signal<Person[]>(PEOPLE);

  protected setLocationFilter(filterRef: CngxFilter, loc: string | null): void {
    this.multiActiveLocation.set(loc);
    if (loc) {
      filterRef.addPredicate('location', (p) => (p as Person).location === loc);
    } else {
      filterRef.removePredicate('location');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.multiRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected setRoleFilter(filterRef: CngxFilter, role: string | null): void {
    this.multiActiveRole.set(role);
    if (role) {
      filterRef.addPredicate('role', (p) => (p as Person).role === role);
    } else {
      filterRef.removePredicate('role');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.multiRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  // ── Multi-filter: OR within dimension + AND across dimensions ────────────
  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles = signal<Set<string>>(new Set());
  protected readonly orRows = signal<Person[]>(PEOPLE);

  protected toggleLocation(filterRef: CngxFilter, loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      filterRef.addPredicate('location', (p) => next.has((p as Person).location));
    } else {
      filterRef.removePredicate('location');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.orRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected toggleRole2(filterRef: CngxFilter, role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      filterRef.addPredicate('role', (p) => next.has((p as Person).role));
    } else {
      filterRef.removePredicate('role');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.orRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected clearOrFilters(filterRef: CngxFilter): void {
    filterRef.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.orRows.set(PEOPLE);
  }

  // ── Backend filter ───────────────────────────────────────────────────────
  private readonly http = inject(HttpClient);

  protected readonly categories = toSignal(
    this.http
      .get<DJCategory[]>('https://dummyjson.com/products/categories')
      .pipe(catchError(() => of([] as DJCategory[]))),
    { initialValue: [] as DJCategory[] },
  );

  protected readonly activeSlug = signal<string | null>(null);
  protected readonly apiLoading = signal(false);

  protected readonly apiProducts = toSignal(
    toObservable(this.activeSlug).pipe(
      switchMap((slug) => {
        this.apiLoading.set(true);
        const url = slug
          ? \`https://dummyjson.com/products/category/\${slug}?limit=8&select=id,title,brand,price,rating,category\`
          : 'https://dummyjson.com/products?limit=8&select=id,title,brand,price,rating,category';
        return this.http.get<{ products: DJProduct[] }>(url).pipe(
          map((r) => r.products),
          catchError(() => of([] as DJProduct[])),
          finalize(() => this.apiLoading.set(false)),
        );
      }),
    ),
    { initialValue: [] as DJProduct[] },
  );`;

  protected readonly totalPeople = PEOPLE.length;
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort((a, b) => a.localeCompare(b));
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort((a, b) => a.localeCompare(b));

  // ── Controlled mode ──────────────────────────────────────────────────────
  protected readonly activeLocation = signal<string | null>(null);

  protected readonly filterPredicate = computed(
    (): ((p: Person) => boolean) | null => {
      const loc = this.activeLocation();
      return loc ? (p) => p.location === loc : null;
    },
  );

  protected readonly filteredRows = computed((): Person[] => {
    const pred = this.filterPredicate();
    if (!pred) return PEOPLE;
    return PEOPLE.filter((p) => pred(p));
  });

  // ── Uncontrolled mode ────────────────────────────────────────────────────
  protected readonly activeRole = signal<string | null>(null);
  private readonly uncontrolledPredicate = signal<((p: unknown) => boolean) | null>(null);

  protected readonly uncontrolledRows = computed((): Person[] => {
    const pred = this.uncontrolledPredicate();
    if (!pred) return PEOPLE;
    return PEOPLE.filter((p) => pred(p));
  });

  protected rolePred(role: string): (p: unknown) => boolean {
    return (p) => (p as Person).role === role;
  }

  protected onFilterChange(pred: ((p: unknown) => boolean) | null): void {
    this.uncontrolledPredicate.set(pred);
  }

  // ── Multi-filter: AND across dimensions ──────────────────────────────────
  protected readonly multiActiveLocation = signal<string | null>(null);
  protected readonly multiActiveRole = signal<string | null>(null);
  protected readonly multiRows = signal<Person[]>(PEOPLE);

  protected setLocationFilter(filterRef: CngxFilter, loc: string | null): void {
    this.multiActiveLocation.set(loc);
    if (loc) {
      filterRef.addPredicate('location', (p) => (p as Person).location === loc);
    } else {
      filterRef.removePredicate('location');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.multiRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected setRoleFilter(filterRef: CngxFilter, role: string | null): void {
    this.multiActiveRole.set(role);
    if (role) {
      filterRef.addPredicate('role', (p) => (p as Person).role === role);
    } else {
      filterRef.removePredicate('role');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.multiRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  // ── Multi-filter: OR within dimension + AND across dimensions ────────────
  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles = signal<Set<string>>(new Set());
  protected readonly orRows = signal<Person[]>(PEOPLE);

  protected toggleLocation(filterRef: CngxFilter, loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      filterRef.addPredicate('location', (p) => next.has((p as Person).location));
    } else {
      filterRef.removePredicate('location');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.orRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected toggleRole2(filterRef: CngxFilter, role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      filterRef.addPredicate('role', (p) => next.has((p as Person).role));
    } else {
      filterRef.removePredicate('role');
    }
    const pred = filterRef.predicate() as ((p: Person) => boolean) | null;
    this.orRows.set(pred ? PEOPLE.filter(pred) : PEOPLE);
  }

  protected clearOrFilters(filterRef: CngxFilter): void {
    filterRef.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.orRows.set(PEOPLE);
  }

  // ── Backend filter ───────────────────────────────────────────────────────
  private readonly http = inject(HttpClient);

  protected readonly categories = toSignal(
    this.http
      .get<DJCategory[]>('https://dummyjson.com/products/categories')
      .pipe(catchError(() => of([] as DJCategory[]))),
    { initialValue: [] as DJCategory[] },
  );

  protected readonly activeSlug = signal<string | null>(null);
  protected readonly apiLoading = signal(false);

  protected readonly apiProducts = toSignal(
    toObservable(this.activeSlug).pipe(
      switchMap((slug) => {
        this.apiLoading.set(true);
        const url = slug
          ? `https://dummyjson.com/products/category/${slug}?limit=8&select=id,title,brand,price,rating,category`
          : 'https://dummyjson.com/products?limit=8&select=id,title,brand,price,rating,category';
        return this.http.get<{ products: DJProduct[] }>(url).pipe(
          map((r) => r.products),
          catchError(() => of([] as DJProduct[])),
          finalize(() => this.apiLoading.set(false)),
        );
      }),
    ),
    { initialValue: [] as DJProduct[] },
  );
  
}
