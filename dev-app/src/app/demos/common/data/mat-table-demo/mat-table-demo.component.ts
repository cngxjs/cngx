// @generated by scripts/generate-demos.mjs — do not edit manually
// Source: demos/common/data/mat-table-demo/mat-table-demo.story.ts

import { ChangeDetectionStrategy, Component, computed, inject, signal } from '@angular/core';
import { ExampleCardComponent } from '../../../../shared/example-card.component';
import { DocShellComponent } from '../../../../shared/doc-shell.component';
import { CngxSortHeader, CngxPaginate } from '@cngx/common/data';
import { MatTableModule } from '@angular/material/table';
import { CngxMatPaginator } from '@cngx/ui/material';
import { CngxFilter, CngxSort, injectDataSource, injectSmartDataSource } from '@cngx/common';
import { PEOPLE, type Person } from '../../../../fixtures';

@Component({
  selector: 'app-mat-table-demo',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  hostDirectives: [
    { directive: CngxSort },
    { directive: CngxFilter },
  ],
  imports: [
    ExampleCardComponent,
    DocShellComponent,
    CngxSortHeader,
    MatTableModule,
    CngxPaginate,
    CngxMatPaginator,
  ],
  template: `
    <app-doc-shell title="mat-table"
      [apiComponents]="['CngxSort', 'CngxSortHeader', 'CngxFilter', 'CngxPaginate', 'CngxMatPaginator']">
      <app-example-card title="DataSource + mat-table — Manual Pipeline with Multi-Sort & Multi-Filter"
        [subtitle]="_s0"
        [sourceHtml]="_srcHtml0"
        [sourceTs]="_srcTs0">
        
  <div class="filter-row">
    <span class="filter-label">Location (OR):</span>
    @for (loc of locations; track loc) {
      <button type="button" class="chip"
        [class.chip--active]="selectedLocations().has(loc)"
        (click)="toggleLocation(loc)">{{ loc }}</button>
    }
  </div>
  <div class="filter-row">
    <span class="filter-label">Role (OR):</span>
    @for (role of roles; track role) {
      <button type="button" class="chip"
        [class.chip--active]="selectedRoles().has(role)"
        (click)="toggleRole(role)">{{ role }}</button>
    }
  </div>
  <div class="search-row">
    <input
      type="text"
      class="search-input"
      placeholder="Search…"
      [value]="searchTerm()"
      (input)="searchTerm.set($any($event.target).value)"
    />
    <button type="button" (click)="clearAll()">Clear all</button>
  </div>

  <mat-table [dataSource]="ds" class="table-wrap">
    <ng-container matColumnDef="name">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="name" [cngxSortRef]="sort" #nH="cngxSortHeader" class="sort-btn">
          Name @if (nH.isActive()) {<span class="sort-arrow">{{ nH.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.name }}</mat-cell>
    </ng-container>

    <ng-container matColumnDef="role">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="role" [cngxSortRef]="sort" #rH="cngxSortHeader" class="sort-btn">
          Role @if (rH.isActive()) {<span class="sort-arrow">{{ rH.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.role }}</mat-cell>
    </ng-container>

    <ng-container matColumnDef="location">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="location" [cngxSortRef]="sort" #lH="cngxSortHeader" class="sort-btn">
          Location @if (lH.isActive()) {<span class="sort-arrow">{{ lH.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.location }}</mat-cell>
    </ng-container>

    <mat-header-row *matHeaderRowDef="displayedColumns"></mat-header-row>
    <mat-row *matRowDef="let row; columns: displayedColumns"></mat-row>
    <div *matNoDataRow class="empty-cell">No results.</div>
  </mat-table>

  <div class="status-row">
    <span class="status-badge" [class.active]="filter.isActive()">
      filters: {{ filter.activeCount() }}
    </span>
    <span class="status-badge" [class.active]="!!searchTerm()">
      search: {{ searchTerm() || 'off' }}
    </span>
    <span class="status-badge" [class.active]="sort.isActive()">
      sort: {{ sort.isActive() ? sort.active() + ' ' + sort.direction() : 'off' }}
    </span>
    <span class="status-badge">{{ processed().length }} / {{ total }} rows</span>
  </div>
      </app-example-card>
      <app-example-card title="Pipeline — How the pieces connect"
        [subtitle]="_s1"
        [sourceHtml]="_srcHtml1"
        [sourceTs]="_srcTs1">
        
  <pre class="code-block"><code>// 1. Directives auto-discovered via hostDirectives + inject()
protected readonly sort   = inject(CngxSort,   &#123; host: true &#125;);
protected readonly filter = inject(CngxFilter, &#123; host: true &#125;);
protected readonly searchTerm = signal('');  // plain signal — no directive needed

// 2. One computed() owns the full pipeline
private readonly processed = computed(() => &#123;
  let list = PEOPLE;

  // filter first — cheapest, reduces the set early
  const pred = this.filter.predicate();
  if (pred) list = list.filter(pred);

  // text search — runs on already-filtered set
  const term = this.searchTerm().toLowerCase();
  if (term) list = list.filter(p => matchesSearch(p, term));

  // sort last — operates on smallest possible set
  const s = this.sort.sort();        // primary SortEntry | null
  if (s) list = [...list].sort((a, b) => compare(a, b, s));

  return list;
&#125;);

// 3. Bridge signal → CDK DataSource → mat-table
protected readonly ds = injectDataSource(this.processed);

// 4. mat-table
// &lt;mat-table [dataSource]="ds"&gt; — same as any CDK DataSource</code></pre>
      </app-example-card>
      <app-example-card title="SmartDataSource + mat-table — auto-wired Sort & Filter"
        [subtitle]="_s2"
        [sourceHtml]="_srcHtml2"
        [sourceTs]="_srcTs2">
        
  <div class="filter-row">
    <span class="filter-label">Location:</span>
    <button type="button" class="chip" [class.chip--active]="smartActiveLocation() === null" (click)="smartFilterBy(null)">All</button>
    @for (loc of locations; track loc) {
      <button type="button" class="chip" [class.chip--active]="smartActiveLocation() === loc" (click)="smartFilterBy(loc)">{{ loc }}</button>
    }
  </div>

  <mat-table [dataSource]="smartDs" class="table-wrap">
    <ng-container matColumnDef="name">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="name" [cngxSortRef]="sort" #nH2="cngxSortHeader" class="sort-btn">
          Name @if (nH2.isActive()) {<span class="sort-arrow">{{ nH2.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.name }}</mat-cell>
    </ng-container>

    <ng-container matColumnDef="role">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="role" [cngxSortRef]="sort" #rH2="cngxSortHeader" class="sort-btn">
          Role @if (rH2.isActive()) {<span class="sort-arrow">{{ rH2.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.role }}</mat-cell>
    </ng-container>

    <ng-container matColumnDef="location">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="location" [cngxSortRef]="sort" #lH2="cngxSortHeader" class="sort-btn">
          Location @if (lH2.isActive()) {<span class="sort-arrow">{{ lH2.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.location }}</mat-cell>
    </ng-container>

    <mat-header-row *matHeaderRowDef="displayedColumns"></mat-header-row>
    <mat-row *matRowDef="let row; columns: displayedColumns"></mat-row>
    <div *matNoDataRow class="empty-cell">No results.</div>
  </mat-table>

  <div class="status-row">
    <span class="status-badge" [class.active]="filter.isActive()">
      filter: {{ filter.isActive() ? 'active' : 'off' }}
    </span>
    <span class="status-badge" [class.active]="sort.isActive()">
      sort: {{ sort.isActive() ? sort.active() + ' ' + sort.direction() : 'off' }}
    </span>
    <span class="status-badge">{{ total }} rows total</span>
  </div>
      </app-example-card>
      <app-example-card title="Why CngxSearch does not auto-wire"
        [subtitle]="_s3"
        [sourceHtml]="_srcHtml3"
        [sourceTs]="_srcTs3">
        
  <pre class="code-block"><code>// Works: CngxSort + CngxFilter as hostDirectives
// inject() finds them in the same element injector
@Component(&#123;
  hostDirectives: [
    &#123; directive: CngxSort   &#125;,
    &#123; directive: CngxFilter &#125;,
  ],
&#125;)
class MyTable &#123;
  private readonly ds = injectSmartDataSource(this.items);
  //  ↑ auto-discovers CngxSort + CngxFilter
&#125;

// Does not work: CngxSearch is on an &lt;input&gt; inside the template
// That is a child element — inject() does not traverse downward
&lt;input cngxSearch #s="cngxSearch"&gt;  ← below the component injector

// Solution: CngxDataSource + manual computed() — see "mat-table — DataSource (Manual Pipeline)"</code></pre>
      </app-example-card>
      <app-example-card title="mat-table + CngxPaginate + CngxMatPaginator"
        [subtitle]="_s4"
        [sourceHtml]="_srcHtml4"
        [sourceTs]="_srcTs4">
        
  <div class="filter-row">
    <span class="filter-label">Location:</span>
    <button type="button" class="chip" [class.chip--active]="pgActiveLocation() === null" (click)="pgFilterBy(null)">All</button>
    @for (loc of locations; track loc) {
      <button type="button" class="chip" [class.chip--active]="pgActiveLocation() === loc" (click)="pgFilterBy(loc)">{{ loc }}</button>
    }
  </div>

  <mat-table [dataSource]="pgDs" class="table-wrap">
    <ng-container matColumnDef="name">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="name" [cngxSortRef]="sort" #nH3="cngxSortHeader" class="sort-btn">
          Name @if (nH3.isActive()) {<span class="sort-arrow">{{ nH3.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.name }}</mat-cell>
    </ng-container>

    <ng-container matColumnDef="role">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="role" [cngxSortRef]="sort" #rH3="cngxSortHeader" class="sort-btn">
          Role @if (rH3.isActive()) {<span class="sort-arrow">{{ rH3.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.role }}</mat-cell>
    </ng-container>

    <ng-container matColumnDef="location">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="location" [cngxSortRef]="sort" #lH3="cngxSortHeader" class="sort-btn">
          Location @if (lH3.isActive()) {<span class="sort-arrow">{{ lH3.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.location }}</mat-cell>
    </ng-container>

    <mat-header-row *matHeaderRowDef="displayedColumns"></mat-header-row>
    <mat-row *matRowDef="let row; columns: displayedColumns"></mat-row>
    <div *matNoDataRow class="empty-cell">No results.</div>
  </mat-table>

  <!-- CngxPaginate in controlled mode — component signals drive the state -->
  <div cngxPaginate #pg="cngxPaginate"
    [cngxPageIndex]="pgPageIndex()"
    [cngxPageSize]="pgPageSize()"
    [total]="pgFilteredCount()"
    (pageChange)="pgSetPage($event)"
    (pageSizeChange)="pgSetPageSize($event)"
    style="display:contents">
  </div>
  <cngx-mat-paginator [cngxPaginateRef]="pg" [pageSizeOptions]="[3, 5, 8]" />

  <div class="status-row">
    <span class="status-badge" [class.active]="filter.isActive()">filter: {{ filter.isActive() ? 'active' : 'off' }}</span>
    <span class="status-badge" [class.active]="sort.isActive()">sort: {{ sort.isActive() ? sort.active() + ' ' + sort.direction() : 'off' }}</span>
    <span class="status-badge">{{ pgFilteredCount() }} rows · page {{ pgPageIndex() + 1 }} / {{ pg.totalPages() }}</span>
  </div>
      </app-example-card>
      <app-example-card title="SmartDataSource + CngxPaginate (hostDirective)"
        [subtitle]="_s5"
        [sourceHtml]="_srcHtml5"
        [sourceTs]="_srcTs5">
        
  <pre class="code-block"><code>// Component with CngxSort, CngxFilter, CngxPaginate as hostDirectives
@Component(&#123;
  hostDirectives: [
    &#123; directive: CngxSort &#125;,
    &#123; directive: CngxFilter &#125;,
    &#123; directive: CngxPaginate, inputs: ['total'] &#125;,
  ],
&#125;)
class MyTable &#123;
  private readonly ds = injectSmartDataSource(this.items);
  // ↑ auto-discovers CngxSort, CngxFilter, AND CngxPaginate

  // Expose filteredCount so the parent can bind it as [total]
  protected readonly filteredCount = this.ds.filteredCount;
&#125;

// Parent template:
// &lt;my-table [total]="filteredCount()"&gt;
//   &lt;cngx-mat-paginator [cngxPaginateRef]="..." /&gt;
// &lt;/my-table&gt;</code></pre>
      </app-example-card>
      <app-example-card title="CngxPaginate — Headless with Custom Nav"
        [subtitle]="_s6"
        [sourceHtml]="_srcHtml6"
        [sourceTs]="_srcTs6">
        
  <div cngxPaginate #pg2="cngxPaginate" [total]="people().length" [cngxPageSize]="3" style="display:contents">
    <div class="table-wrap">
      <table class="demo-table">
        <thead><tr><th>Name</th><th>Role</th><th>Location</th></tr></thead>
        <tbody>
          @for (p of people().slice(pg2.range()[0], pg2.range()[1]); track p.name) {
            <tr><td>{{ p.name }}</td><td>{{ p.role }}</td><td>{{ p.location }}</td></tr>
          }
        </tbody>
      </table>
    </div>

    <!-- Custom accessible pagination nav — no Material dependency -->
    <nav role="navigation" aria-label="Pagination" class="button-row">
      <button type="button" [disabled]="pg2.isFirst()" (click)="pg2.first()" aria-label="First page">&laquo;</button>
      <button type="button" [disabled]="pg2.isFirst()" (click)="pg2.previous()" aria-label="Previous page">&lsaquo;</button>

      @for (i of pageIndices(pg2.totalPages()); track i) {
        <button type="button"
          (click)="pg2.setPage(i)"
          [attr.aria-current]="pg2.pageIndex() === i ? 'page' : null"
          [attr.aria-label]="'Page ' + (i + 1)"
          [class.chip--active]="pg2.pageIndex() === i"
          class="chip">{{ i + 1 }}</button>
      }

      <button type="button" [disabled]="pg2.isLast()" (click)="pg2.next()" aria-label="Next page">&rsaquo;</button>
      <button type="button" [disabled]="pg2.isLast()" (click)="pg2.last()" aria-label="Last page">&raquo;</button>
    </nav>

    <!-- Live region — screen readers announce page changes -->
    <span aria-live="polite" aria-atomic="true" class="visually-hidden">
      Page {{ pg2.pageIndex() + 1 }} of {{ pg2.totalPages() }}
    </span>

    <div class="status-row">
      <span class="status-badge">page {{ pg2.pageIndex() + 1 }} of {{ pg2.totalPages() }}</span>
      <span class="status-badge">showing {{ pg2.range()[0] + 1 }}–{{ pg2.range()[1] > pg2.total() ? pg2.total() : pg2.range()[1] }} of {{ pg2.total() }}</span>
    </div>
  </div>
      </app-example-card>
      <app-example-card title="ARIA Pattern Reference"
        [subtitle]="_s7"
        [sourceHtml]="_srcHtml7"
        [sourceTs]="_srcTs7">
        
  <pre class="code-block"><code>&lt;div cngxPaginate #pg="cngxPaginate" [total]="items.length"&gt;
  &lt;!-- list or table reads pg.range() --&gt;
  &#64;for (item of items.slice(pg.range()[0], pg.range()[1]); track item.id) &#123; ... &#125;

  &lt;nav role="navigation" aria-label="Pagination"&gt;
    &lt;button [disabled]="pg.isFirst()" (click)="pg.previous()" aria-label="Previous page"&gt;&lt;/button&gt;

    &#64;for (i of pageIndices(pg.totalPages()); track i) &#123;
      &lt;button
        (click)="pg.setPage(i)"
        [attr.aria-current]="pg.pageIndex() === i ? 'page' : null"
        [attr.aria-label]="'Page ' + (i + 1)"&gt;
        &#123;<!-- -->&#123; i + 1 &#125;<!-- -->&#125;
      &lt;/button&gt;
    &#125;

    &lt;button [disabled]="pg.isLast()" (click)="pg.next()" aria-label="Next page"&gt;&lt;/button&gt;
  &lt;/nav&gt;

  &lt;!-- Live region — screen readers announce page changes --&gt;
  &lt;span aria-live="polite" aria-atomic="true" class="visually-hidden"&gt;
    Page &#123;<!-- -->&#123; pg.pageIndex() + 1 &#125;<!-- -->&#125; of &#123;<!-- -->&#123; pg.totalPages() &#125;<!-- -->&#125;
  &lt;/span&gt;
&lt;/div&gt;</code></pre>
      </app-example-card>
    </app-doc-shell>
  `,
})
export class MatTableDemoComponent {
  protected readonly _s0 = 'A manual <code>computed()</code> owns the full pipeline: multi-filter (AND across dimensions, OR within), text search, and multi-sort (Shift+click). <code>injectDataSource(computed)</code> bridges the signal to <code>mat-table</code>. Pipeline order — filter → search → sort — keeps the sort operating on the smallest possible set.';
  protected readonly _s1 = 'Everything flows through one <code>computed()</code>. Angular\'s reactivity reruns it automatically whenever any signal changes — no subscriptions, no manual triggering. Shift+click a column header to stack secondary sort keys.';
  protected readonly _s2 = '<code>CngxSort</code> and <code>CngxFilter</code> are declared as <code>hostDirectives</code> on the component. <code>injectSmartDataSource()</code> discovers them automatically via <code>inject()</code> — no explicit wiring needed. <code>CngxSmartDataSource</code> extends CDK\'s <code>DataSource&lt;T&gt;</code>, so <code>mat-table</code> accepts it directly as <code>[dataSource]</code>. Multi-filter is fully supported — all named predicates are AND-combined automatically. Multi-sort is also supported — add <code>[multiSort]="true"</code> to the <code>[cngxSort]</code> element and Shift+click column headers.';
  protected readonly _s3 = '<code>inject()</code> traverses the injector tree <em>upward</em> — from the DataSource context to the component and its ancestors. <code>CngxSearch</code> lives on an <code>&lt;input&gt;</code> element inside the template (a child element), which is <em>below</em> the component injector. For search integration use <code>CngxDataSource</code> with a manual <code>computed()</code> — shown in the next demo.';
  protected readonly _s4 = '<code>CngxPaginate</code> manages pagination state in the template via <code>#pg</code>. <code>CngxMatPaginator</code> reads and writes the same state through the explicit <code>[cngxPaginateRef]="pg"</code> binding. The component\'s <code>pageIndex</code> and <code>pageSize</code> signals drive a manual pipeline — filter → sort → paginate — and the paginate directive is wired in <em>controlled mode</em> via <code>(pageChange)</code> / <code>(pageSizeChange)</code> outputs. Filtering resets to page 0 automatically.';
  protected readonly _s5 = 'When <code>CngxPaginate</code> is added as a <code>hostDirective</code> on the same element as <code>injectSmartDataSource()</code>, the data source auto-discovers it and applies the page slice <em>after</em> sort. The consumer binds <code>[total]</code> on the host element from the parent template. Use <code>ds.filteredCount()</code> as the source of truth for the paginator length — it reflects the count <em>after</em> filter/search but <em>before</em> pagination.';
  protected readonly _s6 = '<code>CngxPaginate</code> is purely headless — it has no template of its own. Consumers read <code>isFirst()</code>, <code>isLast()</code>, <code>pageIndex()</code>, and <code>totalPages()</code> to build any pagination UI they need. Here the directive lives on a <code>&lt;div&gt;</code> wrapper via template ref <code>#pg</code>. The <code>@for</code> loop slices the array using <code>pg.range()</code> directly in the template — no DataSource, no Material dependency.';
  protected readonly _s7 = 'Required ARIA attributes for accessible custom paginators. <code>CngxPaginate</code> exposes exactly the signals needed: <code>isFirst()</code>, <code>isLast()</code>, <code>pageIndex()</code>, <code>totalPages()</code>. The directive itself does <em>not</em> manage ARIA — that is the rendering layer\'s responsibility (headless principle).';
  protected readonly _srcHtml0 = `<div class="filter-row">
    <span class="filter-label">Location (OR):</span>
    @for (loc of locations; track loc) {
      <button type="button" class="chip"
        [class.chip--active]="selectedLocations().has(loc)"
        (click)="toggleLocation(loc)">{{ loc }}</button>
    }
  </div>
  <div class="filter-row">
    <span class="filter-label">Role (OR):</span>
    @for (role of roles; track role) {
      <button type="button" class="chip"
        [class.chip--active]="selectedRoles().has(role)"
        (click)="toggleRole(role)">{{ role }}</button>
    }
  </div>
  <div class="search-row">
    <input
      type="text"
      class="search-input"
      placeholder="Search…"
      [value]="searchTerm()"
      (input)="searchTerm.set($any($event.target).value)"
    />
    <button type="button" (click)="clearAll()">Clear all</button>
  </div>

  <mat-table [dataSource]="ds" class="table-wrap">
    <ng-container matColumnDef="name">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="name" [cngxSortRef]="sort" #nH="cngxSortHeader" class="sort-btn">
          Name @if (nH.isActive()) {<span class="sort-arrow">{{ nH.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.name }}</mat-cell>
    </ng-container>

    <ng-container matColumnDef="role">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="role" [cngxSortRef]="sort" #rH="cngxSortHeader" class="sort-btn">
          Role @if (rH.isActive()) {<span class="sort-arrow">{{ rH.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.role }}</mat-cell>
    </ng-container>

    <ng-container matColumnDef="location">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="location" [cngxSortRef]="sort" #lH="cngxSortHeader" class="sort-btn">
          Location @if (lH.isActive()) {<span class="sort-arrow">{{ lH.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.location }}</mat-cell>
    </ng-container>

    <mat-header-row *matHeaderRowDef="displayedColumns"></mat-header-row>
    <mat-row *matRowDef="let row; columns: displayedColumns"></mat-row>
    <div *matNoDataRow class="empty-cell">No results.</div>
  </mat-table>

  <div class="status-row">
    <span class="status-badge" [class.active]="filter.isActive()">
      filters: {{ filter.activeCount() }}
    </span>
    <span class="status-badge" [class.active]="!!searchTerm()">
      search: {{ searchTerm() || 'off' }}
    </span>
    <span class="status-badge" [class.active]="sort.isActive()">
      sort: {{ sort.isActive() ? sort.active() + ' ' + sort.direction() : 'off' }}
    </span>
    <span class="status-badge">{{ processed().length }} / {{ total }} rows</span>
  </div>`;
  protected readonly _srcTs0 = `import { CngxFilter, CngxSort, injectDataSource, injectSmartDataSource } from '@cngx/common';
import { PEOPLE, type Person } from '../../../../fixtures';


  // ── Shared: host directive references ──────────────────────────────────
  protected readonly sort   = inject(CngxSort,           { host: true });
  protected readonly filter = inject(CngxFilter<Person>, { host: true });

  // ══════════════════════════════════════════════════════════════════════════
  // Section 1+2: Manual pipeline
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly total = PEOPLE.length;
  protected readonly displayedColumns = ['name', 'role', 'location'];

  protected readonly searchTerm = signal('');

  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles     = signal<Set<string>>(new Set());

  protected readonly processed = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const term = this.searchTerm().trim().toLowerCase();
    const s = this.sort.sort();

    let list = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (term) {
      list = list.filter((p) =>
        [p.name, p.role, p.location].some((v) => v.toLowerCase().includes(term)),
      );
    }
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly ds = injectDataSource(this.processed);

  protected toggleLocation(loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('location', (p) => next.has(p.location));
    } else {
      this.filter.removePredicate('location');
    }
  }

  protected toggleRole(role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('role', (p) => next.has(p.role));
    } else {
      this.filter.removePredicate('role');
    }
  }

  protected clearAll(): void {
    this.filter.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.searchTerm.set('');
    this.sort.clear();
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 3+4: SmartDataSource
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly smartActiveLocation = signal<string | null>(null);
  private readonly smartItems = signal(PEOPLE);
  protected readonly smartDs = injectSmartDataSource(this.smartItems);

  protected smartFilterBy(loc: string | null): void {
    this.smartActiveLocation.set(loc);
    if (loc === null) {
      this.filter.clear();
    } else {
      this.filter.setPredicate((p) => p.location === loc);
    }
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 5+6: Paginate (Material)
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly pgActiveLocation = signal<string | null>(null);
  protected readonly pgPageIndex = signal(0);
  protected readonly pgPageSize  = signal(3);

  private readonly pgFiltered = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const s    = this.sort.sort();
    let list   = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly pgFilteredCount = computed(() => this.pgFiltered().length);

  private readonly pgPaginated = computed(() =>
    this.pgFiltered().slice(
      this.pgPageIndex() * this.pgPageSize(),
      (this.pgPageIndex() + 1) * this.pgPageSize(),
    ),
  );

  protected readonly pgDs = injectDataSource(this.pgPaginated);

  protected pgFilterBy(loc: string | null): void {
    this.pgActiveLocation.set(loc);
    this.pgPageIndex.set(0);
    if (loc === null) { this.filter.clear(); }
    else { this.filter.setPredicate((p: Person) => p.location === loc); }
  }

  protected pgSetPage(i: number): void {
    const max = Math.max(0, Math.ceil(this.pgFilteredCount() / this.pgPageSize()) - 1);
    this.pgPageIndex.set(Math.max(0, Math.min(i, max)));
  }

  protected pgSetPageSize(s: number): void {
    this.pgPageSize.set(s);
    this.pgPageIndex.set(0);
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 7+8: Custom Paginate
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly people = signal(PEOPLE);

  protected pageIndices(total: number): number[] {
    return Array.from({ length: total }, (_, i) => i);
  }`;
  protected readonly _srcHtml1 = `<pre class="code-block"><code>// 1. Directives auto-discovered via hostDirectives + inject()
protected readonly sort   = inject(CngxSort,   &#123; host: true &#125;);
protected readonly filter = inject(CngxFilter, &#123; host: true &#125;);
protected readonly searchTerm = signal('');  // plain signal — no directive needed

// 2. One computed() owns the full pipeline
private readonly processed = computed(() => &#123;
  let list = PEOPLE;

  // filter first — cheapest, reduces the set early
  const pred = this.filter.predicate();
  if (pred) list = list.filter(pred);

  // text search — runs on already-filtered set
  const term = this.searchTerm().toLowerCase();
  if (term) list = list.filter(p => matchesSearch(p, term));

  // sort last — operates on smallest possible set
  const s = this.sort.sort();        // primary SortEntry | null
  if (s) list = [...list].sort((a, b) => compare(a, b, s));

  return list;
&#125;);

// 3. Bridge signal → CDK DataSource → mat-table
protected readonly ds = injectDataSource(this.processed);

// 4. mat-table
// &lt;mat-table [dataSource]="ds"&gt; — same as any CDK DataSource</code></pre>`;
  protected readonly _srcTs1 = `import { CngxFilter, CngxSort, injectDataSource, injectSmartDataSource } from '@cngx/common';
import { PEOPLE, type Person } from '../../../../fixtures';


  // ── Shared: host directive references ──────────────────────────────────
  protected readonly sort   = inject(CngxSort,           { host: true });
  protected readonly filter = inject(CngxFilter<Person>, { host: true });

  // ══════════════════════════════════════════════════════════════════════════
  // Section 1+2: Manual pipeline
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly total = PEOPLE.length;
  protected readonly displayedColumns = ['name', 'role', 'location'];

  protected readonly searchTerm = signal('');

  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles     = signal<Set<string>>(new Set());

  protected readonly processed = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const term = this.searchTerm().trim().toLowerCase();
    const s = this.sort.sort();

    let list = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (term) {
      list = list.filter((p) =>
        [p.name, p.role, p.location].some((v) => v.toLowerCase().includes(term)),
      );
    }
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly ds = injectDataSource(this.processed);

  protected toggleLocation(loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('location', (p) => next.has(p.location));
    } else {
      this.filter.removePredicate('location');
    }
  }

  protected toggleRole(role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('role', (p) => next.has(p.role));
    } else {
      this.filter.removePredicate('role');
    }
  }

  protected clearAll(): void {
    this.filter.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.searchTerm.set('');
    this.sort.clear();
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 3+4: SmartDataSource
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly smartActiveLocation = signal<string | null>(null);
  private readonly smartItems = signal(PEOPLE);
  protected readonly smartDs = injectSmartDataSource(this.smartItems);

  protected smartFilterBy(loc: string | null): void {
    this.smartActiveLocation.set(loc);
    if (loc === null) {
      this.filter.clear();
    } else {
      this.filter.setPredicate((p) => p.location === loc);
    }
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 5+6: Paginate (Material)
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly pgActiveLocation = signal<string | null>(null);
  protected readonly pgPageIndex = signal(0);
  protected readonly pgPageSize  = signal(3);

  private readonly pgFiltered = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const s    = this.sort.sort();
    let list   = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly pgFilteredCount = computed(() => this.pgFiltered().length);

  private readonly pgPaginated = computed(() =>
    this.pgFiltered().slice(
      this.pgPageIndex() * this.pgPageSize(),
      (this.pgPageIndex() + 1) * this.pgPageSize(),
    ),
  );

  protected readonly pgDs = injectDataSource(this.pgPaginated);

  protected pgFilterBy(loc: string | null): void {
    this.pgActiveLocation.set(loc);
    this.pgPageIndex.set(0);
    if (loc === null) { this.filter.clear(); }
    else { this.filter.setPredicate((p: Person) => p.location === loc); }
  }

  protected pgSetPage(i: number): void {
    const max = Math.max(0, Math.ceil(this.pgFilteredCount() / this.pgPageSize()) - 1);
    this.pgPageIndex.set(Math.max(0, Math.min(i, max)));
  }

  protected pgSetPageSize(s: number): void {
    this.pgPageSize.set(s);
    this.pgPageIndex.set(0);
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 7+8: Custom Paginate
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly people = signal(PEOPLE);

  protected pageIndices(total: number): number[] {
    return Array.from({ length: total }, (_, i) => i);
  }`;
  protected readonly _srcHtml2 = `<div class="filter-row">
    <span class="filter-label">Location:</span>
    <button type="button" class="chip" [class.chip--active]="smartActiveLocation() === null" (click)="smartFilterBy(null)">All</button>
    @for (loc of locations; track loc) {
      <button type="button" class="chip" [class.chip--active]="smartActiveLocation() === loc" (click)="smartFilterBy(loc)">{{ loc }}</button>
    }
  </div>

  <mat-table [dataSource]="smartDs" class="table-wrap">
    <ng-container matColumnDef="name">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="name" [cngxSortRef]="sort" #nH2="cngxSortHeader" class="sort-btn">
          Name @if (nH2.isActive()) {<span class="sort-arrow">{{ nH2.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.name }}</mat-cell>
    </ng-container>

    <ng-container matColumnDef="role">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="role" [cngxSortRef]="sort" #rH2="cngxSortHeader" class="sort-btn">
          Role @if (rH2.isActive()) {<span class="sort-arrow">{{ rH2.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.role }}</mat-cell>
    </ng-container>

    <ng-container matColumnDef="location">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="location" [cngxSortRef]="sort" #lH2="cngxSortHeader" class="sort-btn">
          Location @if (lH2.isActive()) {<span class="sort-arrow">{{ lH2.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.location }}</mat-cell>
    </ng-container>

    <mat-header-row *matHeaderRowDef="displayedColumns"></mat-header-row>
    <mat-row *matRowDef="let row; columns: displayedColumns"></mat-row>
    <div *matNoDataRow class="empty-cell">No results.</div>
  </mat-table>

  <div class="status-row">
    <span class="status-badge" [class.active]="filter.isActive()">
      filter: {{ filter.isActive() ? 'active' : 'off' }}
    </span>
    <span class="status-badge" [class.active]="sort.isActive()">
      sort: {{ sort.isActive() ? sort.active() + ' ' + sort.direction() : 'off' }}
    </span>
    <span class="status-badge">{{ total }} rows total</span>
  </div>`;
  protected readonly _srcTs2 = `import { CngxFilter, CngxSort, injectDataSource, injectSmartDataSource } from '@cngx/common';
import { PEOPLE, type Person } from '../../../../fixtures';


  // ── Shared: host directive references ──────────────────────────────────
  protected readonly sort   = inject(CngxSort,           { host: true });
  protected readonly filter = inject(CngxFilter<Person>, { host: true });

  // ══════════════════════════════════════════════════════════════════════════
  // Section 1+2: Manual pipeline
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly total = PEOPLE.length;
  protected readonly displayedColumns = ['name', 'role', 'location'];

  protected readonly searchTerm = signal('');

  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles     = signal<Set<string>>(new Set());

  protected readonly processed = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const term = this.searchTerm().trim().toLowerCase();
    const s = this.sort.sort();

    let list = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (term) {
      list = list.filter((p) =>
        [p.name, p.role, p.location].some((v) => v.toLowerCase().includes(term)),
      );
    }
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly ds = injectDataSource(this.processed);

  protected toggleLocation(loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('location', (p) => next.has(p.location));
    } else {
      this.filter.removePredicate('location');
    }
  }

  protected toggleRole(role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('role', (p) => next.has(p.role));
    } else {
      this.filter.removePredicate('role');
    }
  }

  protected clearAll(): void {
    this.filter.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.searchTerm.set('');
    this.sort.clear();
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 3+4: SmartDataSource
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly smartActiveLocation = signal<string | null>(null);
  private readonly smartItems = signal(PEOPLE);
  protected readonly smartDs = injectSmartDataSource(this.smartItems);

  protected smartFilterBy(loc: string | null): void {
    this.smartActiveLocation.set(loc);
    if (loc === null) {
      this.filter.clear();
    } else {
      this.filter.setPredicate((p) => p.location === loc);
    }
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 5+6: Paginate (Material)
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly pgActiveLocation = signal<string | null>(null);
  protected readonly pgPageIndex = signal(0);
  protected readonly pgPageSize  = signal(3);

  private readonly pgFiltered = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const s    = this.sort.sort();
    let list   = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly pgFilteredCount = computed(() => this.pgFiltered().length);

  private readonly pgPaginated = computed(() =>
    this.pgFiltered().slice(
      this.pgPageIndex() * this.pgPageSize(),
      (this.pgPageIndex() + 1) * this.pgPageSize(),
    ),
  );

  protected readonly pgDs = injectDataSource(this.pgPaginated);

  protected pgFilterBy(loc: string | null): void {
    this.pgActiveLocation.set(loc);
    this.pgPageIndex.set(0);
    if (loc === null) { this.filter.clear(); }
    else { this.filter.setPredicate((p: Person) => p.location === loc); }
  }

  protected pgSetPage(i: number): void {
    const max = Math.max(0, Math.ceil(this.pgFilteredCount() / this.pgPageSize()) - 1);
    this.pgPageIndex.set(Math.max(0, Math.min(i, max)));
  }

  protected pgSetPageSize(s: number): void {
    this.pgPageSize.set(s);
    this.pgPageIndex.set(0);
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 7+8: Custom Paginate
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly people = signal(PEOPLE);

  protected pageIndices(total: number): number[] {
    return Array.from({ length: total }, (_, i) => i);
  }`;
  protected readonly _srcHtml3 = `<pre class="code-block"><code>// Works: CngxSort + CngxFilter as hostDirectives
// inject() finds them in the same element injector
@Component(&#123;
  hostDirectives: [
    &#123; directive: CngxSort   &#125;,
    &#123; directive: CngxFilter &#125;,
  ],
&#125;)
class MyTable &#123;
  private readonly ds = injectSmartDataSource(this.items);
  //  ↑ auto-discovers CngxSort + CngxFilter
&#125;

// Does not work: CngxSearch is on an &lt;input&gt; inside the template
// That is a child element — inject() does not traverse downward
&lt;input cngxSearch #s="cngxSearch"&gt;  ← below the component injector

// Solution: CngxDataSource + manual computed() — see "mat-table — DataSource (Manual Pipeline)"</code></pre>`;
  protected readonly _srcTs3 = `import { CngxFilter, CngxSort, injectDataSource, injectSmartDataSource } from '@cngx/common';
import { PEOPLE, type Person } from '../../../../fixtures';


  // ── Shared: host directive references ──────────────────────────────────
  protected readonly sort   = inject(CngxSort,           { host: true });
  protected readonly filter = inject(CngxFilter<Person>, { host: true });

  // ══════════════════════════════════════════════════════════════════════════
  // Section 1+2: Manual pipeline
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly total = PEOPLE.length;
  protected readonly displayedColumns = ['name', 'role', 'location'];

  protected readonly searchTerm = signal('');

  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles     = signal<Set<string>>(new Set());

  protected readonly processed = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const term = this.searchTerm().trim().toLowerCase();
    const s = this.sort.sort();

    let list = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (term) {
      list = list.filter((p) =>
        [p.name, p.role, p.location].some((v) => v.toLowerCase().includes(term)),
      );
    }
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly ds = injectDataSource(this.processed);

  protected toggleLocation(loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('location', (p) => next.has(p.location));
    } else {
      this.filter.removePredicate('location');
    }
  }

  protected toggleRole(role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('role', (p) => next.has(p.role));
    } else {
      this.filter.removePredicate('role');
    }
  }

  protected clearAll(): void {
    this.filter.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.searchTerm.set('');
    this.sort.clear();
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 3+4: SmartDataSource
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly smartActiveLocation = signal<string | null>(null);
  private readonly smartItems = signal(PEOPLE);
  protected readonly smartDs = injectSmartDataSource(this.smartItems);

  protected smartFilterBy(loc: string | null): void {
    this.smartActiveLocation.set(loc);
    if (loc === null) {
      this.filter.clear();
    } else {
      this.filter.setPredicate((p) => p.location === loc);
    }
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 5+6: Paginate (Material)
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly pgActiveLocation = signal<string | null>(null);
  protected readonly pgPageIndex = signal(0);
  protected readonly pgPageSize  = signal(3);

  private readonly pgFiltered = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const s    = this.sort.sort();
    let list   = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly pgFilteredCount = computed(() => this.pgFiltered().length);

  private readonly pgPaginated = computed(() =>
    this.pgFiltered().slice(
      this.pgPageIndex() * this.pgPageSize(),
      (this.pgPageIndex() + 1) * this.pgPageSize(),
    ),
  );

  protected readonly pgDs = injectDataSource(this.pgPaginated);

  protected pgFilterBy(loc: string | null): void {
    this.pgActiveLocation.set(loc);
    this.pgPageIndex.set(0);
    if (loc === null) { this.filter.clear(); }
    else { this.filter.setPredicate((p: Person) => p.location === loc); }
  }

  protected pgSetPage(i: number): void {
    const max = Math.max(0, Math.ceil(this.pgFilteredCount() / this.pgPageSize()) - 1);
    this.pgPageIndex.set(Math.max(0, Math.min(i, max)));
  }

  protected pgSetPageSize(s: number): void {
    this.pgPageSize.set(s);
    this.pgPageIndex.set(0);
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 7+8: Custom Paginate
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly people = signal(PEOPLE);

  protected pageIndices(total: number): number[] {
    return Array.from({ length: total }, (_, i) => i);
  }`;
  protected readonly _srcHtml4 = `<div class="filter-row">
    <span class="filter-label">Location:</span>
    <button type="button" class="chip" [class.chip--active]="pgActiveLocation() === null" (click)="pgFilterBy(null)">All</button>
    @for (loc of locations; track loc) {
      <button type="button" class="chip" [class.chip--active]="pgActiveLocation() === loc" (click)="pgFilterBy(loc)">{{ loc }}</button>
    }
  </div>

  <mat-table [dataSource]="pgDs" class="table-wrap">
    <ng-container matColumnDef="name">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="name" [cngxSortRef]="sort" #nH3="cngxSortHeader" class="sort-btn">
          Name @if (nH3.isActive()) {<span class="sort-arrow">{{ nH3.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.name }}</mat-cell>
    </ng-container>

    <ng-container matColumnDef="role">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="role" [cngxSortRef]="sort" #rH3="cngxSortHeader" class="sort-btn">
          Role @if (rH3.isActive()) {<span class="sort-arrow">{{ rH3.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.role }}</mat-cell>
    </ng-container>

    <ng-container matColumnDef="location">
      <mat-header-cell *matHeaderCellDef>
        <button cngxSortHeader="location" [cngxSortRef]="sort" #lH3="cngxSortHeader" class="sort-btn">
          Location @if (lH3.isActive()) {<span class="sort-arrow">{{ lH3.isAsc() ? '↑' : '↓' }}</span>}
        </button>
      </mat-header-cell>
      <mat-cell *matCellDef="let row">{{ row.location }}</mat-cell>
    </ng-container>

    <mat-header-row *matHeaderRowDef="displayedColumns"></mat-header-row>
    <mat-row *matRowDef="let row; columns: displayedColumns"></mat-row>
    <div *matNoDataRow class="empty-cell">No results.</div>
  </mat-table>

  <!-- CngxPaginate in controlled mode — component signals drive the state -->
  <div cngxPaginate #pg="cngxPaginate"
    [cngxPageIndex]="pgPageIndex()"
    [cngxPageSize]="pgPageSize()"
    [total]="pgFilteredCount()"
    (pageChange)="pgSetPage($event)"
    (pageSizeChange)="pgSetPageSize($event)"
    style="display:contents">
  </div>
  <cngx-mat-paginator [cngxPaginateRef]="pg" [pageSizeOptions]="[3, 5, 8]" />

  <div class="status-row">
    <span class="status-badge" [class.active]="filter.isActive()">filter: {{ filter.isActive() ? 'active' : 'off' }}</span>
    <span class="status-badge" [class.active]="sort.isActive()">sort: {{ sort.isActive() ? sort.active() + ' ' + sort.direction() : 'off' }}</span>
    <span class="status-badge">{{ pgFilteredCount() }} rows · page {{ pgPageIndex() + 1 }} / {{ pg.totalPages() }}</span>
  </div>`;
  protected readonly _srcTs4 = `import { CngxFilter, CngxSort, injectDataSource, injectSmartDataSource } from '@cngx/common';
import { PEOPLE, type Person } from '../../../../fixtures';


  // ── Shared: host directive references ──────────────────────────────────
  protected readonly sort   = inject(CngxSort,           { host: true });
  protected readonly filter = inject(CngxFilter<Person>, { host: true });

  // ══════════════════════════════════════════════════════════════════════════
  // Section 1+2: Manual pipeline
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly total = PEOPLE.length;
  protected readonly displayedColumns = ['name', 'role', 'location'];

  protected readonly searchTerm = signal('');

  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles     = signal<Set<string>>(new Set());

  protected readonly processed = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const term = this.searchTerm().trim().toLowerCase();
    const s = this.sort.sort();

    let list = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (term) {
      list = list.filter((p) =>
        [p.name, p.role, p.location].some((v) => v.toLowerCase().includes(term)),
      );
    }
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly ds = injectDataSource(this.processed);

  protected toggleLocation(loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('location', (p) => next.has(p.location));
    } else {
      this.filter.removePredicate('location');
    }
  }

  protected toggleRole(role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('role', (p) => next.has(p.role));
    } else {
      this.filter.removePredicate('role');
    }
  }

  protected clearAll(): void {
    this.filter.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.searchTerm.set('');
    this.sort.clear();
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 3+4: SmartDataSource
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly smartActiveLocation = signal<string | null>(null);
  private readonly smartItems = signal(PEOPLE);
  protected readonly smartDs = injectSmartDataSource(this.smartItems);

  protected smartFilterBy(loc: string | null): void {
    this.smartActiveLocation.set(loc);
    if (loc === null) {
      this.filter.clear();
    } else {
      this.filter.setPredicate((p) => p.location === loc);
    }
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 5+6: Paginate (Material)
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly pgActiveLocation = signal<string | null>(null);
  protected readonly pgPageIndex = signal(0);
  protected readonly pgPageSize  = signal(3);

  private readonly pgFiltered = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const s    = this.sort.sort();
    let list   = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly pgFilteredCount = computed(() => this.pgFiltered().length);

  private readonly pgPaginated = computed(() =>
    this.pgFiltered().slice(
      this.pgPageIndex() * this.pgPageSize(),
      (this.pgPageIndex() + 1) * this.pgPageSize(),
    ),
  );

  protected readonly pgDs = injectDataSource(this.pgPaginated);

  protected pgFilterBy(loc: string | null): void {
    this.pgActiveLocation.set(loc);
    this.pgPageIndex.set(0);
    if (loc === null) { this.filter.clear(); }
    else { this.filter.setPredicate((p: Person) => p.location === loc); }
  }

  protected pgSetPage(i: number): void {
    const max = Math.max(0, Math.ceil(this.pgFilteredCount() / this.pgPageSize()) - 1);
    this.pgPageIndex.set(Math.max(0, Math.min(i, max)));
  }

  protected pgSetPageSize(s: number): void {
    this.pgPageSize.set(s);
    this.pgPageIndex.set(0);
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 7+8: Custom Paginate
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly people = signal(PEOPLE);

  protected pageIndices(total: number): number[] {
    return Array.from({ length: total }, (_, i) => i);
  }`;
  protected readonly _srcHtml5 = `<pre class="code-block"><code>// Component with CngxSort, CngxFilter, CngxPaginate as hostDirectives
@Component(&#123;
  hostDirectives: [
    &#123; directive: CngxSort &#125;,
    &#123; directive: CngxFilter &#125;,
    &#123; directive: CngxPaginate, inputs: ['total'] &#125;,
  ],
&#125;)
class MyTable &#123;
  private readonly ds = injectSmartDataSource(this.items);
  // ↑ auto-discovers CngxSort, CngxFilter, AND CngxPaginate

  // Expose filteredCount so the parent can bind it as [total]
  protected readonly filteredCount = this.ds.filteredCount;
&#125;

// Parent template:
// &lt;my-table [total]="filteredCount()"&gt;
//   &lt;cngx-mat-paginator [cngxPaginateRef]="..." /&gt;
// &lt;/my-table&gt;</code></pre>`;
  protected readonly _srcTs5 = `import { CngxFilter, CngxSort, injectDataSource, injectSmartDataSource } from '@cngx/common';
import { PEOPLE, type Person } from '../../../../fixtures';


  // ── Shared: host directive references ──────────────────────────────────
  protected readonly sort   = inject(CngxSort,           { host: true });
  protected readonly filter = inject(CngxFilter<Person>, { host: true });

  // ══════════════════════════════════════════════════════════════════════════
  // Section 1+2: Manual pipeline
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly total = PEOPLE.length;
  protected readonly displayedColumns = ['name', 'role', 'location'];

  protected readonly searchTerm = signal('');

  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles     = signal<Set<string>>(new Set());

  protected readonly processed = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const term = this.searchTerm().trim().toLowerCase();
    const s = this.sort.sort();

    let list = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (term) {
      list = list.filter((p) =>
        [p.name, p.role, p.location].some((v) => v.toLowerCase().includes(term)),
      );
    }
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly ds = injectDataSource(this.processed);

  protected toggleLocation(loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('location', (p) => next.has(p.location));
    } else {
      this.filter.removePredicate('location');
    }
  }

  protected toggleRole(role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('role', (p) => next.has(p.role));
    } else {
      this.filter.removePredicate('role');
    }
  }

  protected clearAll(): void {
    this.filter.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.searchTerm.set('');
    this.sort.clear();
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 3+4: SmartDataSource
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly smartActiveLocation = signal<string | null>(null);
  private readonly smartItems = signal(PEOPLE);
  protected readonly smartDs = injectSmartDataSource(this.smartItems);

  protected smartFilterBy(loc: string | null): void {
    this.smartActiveLocation.set(loc);
    if (loc === null) {
      this.filter.clear();
    } else {
      this.filter.setPredicate((p) => p.location === loc);
    }
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 5+6: Paginate (Material)
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly pgActiveLocation = signal<string | null>(null);
  protected readonly pgPageIndex = signal(0);
  protected readonly pgPageSize  = signal(3);

  private readonly pgFiltered = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const s    = this.sort.sort();
    let list   = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly pgFilteredCount = computed(() => this.pgFiltered().length);

  private readonly pgPaginated = computed(() =>
    this.pgFiltered().slice(
      this.pgPageIndex() * this.pgPageSize(),
      (this.pgPageIndex() + 1) * this.pgPageSize(),
    ),
  );

  protected readonly pgDs = injectDataSource(this.pgPaginated);

  protected pgFilterBy(loc: string | null): void {
    this.pgActiveLocation.set(loc);
    this.pgPageIndex.set(0);
    if (loc === null) { this.filter.clear(); }
    else { this.filter.setPredicate((p: Person) => p.location === loc); }
  }

  protected pgSetPage(i: number): void {
    const max = Math.max(0, Math.ceil(this.pgFilteredCount() / this.pgPageSize()) - 1);
    this.pgPageIndex.set(Math.max(0, Math.min(i, max)));
  }

  protected pgSetPageSize(s: number): void {
    this.pgPageSize.set(s);
    this.pgPageIndex.set(0);
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 7+8: Custom Paginate
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly people = signal(PEOPLE);

  protected pageIndices(total: number): number[] {
    return Array.from({ length: total }, (_, i) => i);
  }`;
  protected readonly _srcHtml6 = `<div cngxPaginate #pg2="cngxPaginate" [total]="people().length" [cngxPageSize]="3" style="display:contents">
    <div class="table-wrap">
      <table class="demo-table">
        <thead><tr><th>Name</th><th>Role</th><th>Location</th></tr></thead>
        <tbody>
          @for (p of people().slice(pg2.range()[0], pg2.range()[1]); track p.name) {
            <tr><td>{{ p.name }}</td><td>{{ p.role }}</td><td>{{ p.location }}</td></tr>
          }
        </tbody>
      </table>
    </div>

    <!-- Custom accessible pagination nav — no Material dependency -->
    <nav role="navigation" aria-label="Pagination" class="button-row">
      <button type="button" [disabled]="pg2.isFirst()" (click)="pg2.first()" aria-label="First page">&laquo;</button>
      <button type="button" [disabled]="pg2.isFirst()" (click)="pg2.previous()" aria-label="Previous page">&lsaquo;</button>

      @for (i of pageIndices(pg2.totalPages()); track i) {
        <button type="button"
          (click)="pg2.setPage(i)"
          [attr.aria-current]="pg2.pageIndex() === i ? 'page' : null"
          [attr.aria-label]="'Page ' + (i + 1)"
          [class.chip--active]="pg2.pageIndex() === i"
          class="chip">{{ i + 1 }}</button>
      }

      <button type="button" [disabled]="pg2.isLast()" (click)="pg2.next()" aria-label="Next page">&rsaquo;</button>
      <button type="button" [disabled]="pg2.isLast()" (click)="pg2.last()" aria-label="Last page">&raquo;</button>
    </nav>

    <!-- Live region — screen readers announce page changes -->
    <span aria-live="polite" aria-atomic="true" class="visually-hidden">
      Page {{ pg2.pageIndex() + 1 }} of {{ pg2.totalPages() }}
    </span>

    <div class="status-row">
      <span class="status-badge">page {{ pg2.pageIndex() + 1 }} of {{ pg2.totalPages() }}</span>
      <span class="status-badge">showing {{ pg2.range()[0] + 1 }}–{{ pg2.range()[1] > pg2.total() ? pg2.total() : pg2.range()[1] }} of {{ pg2.total() }}</span>
    </div>
  </div>`;
  protected readonly _srcTs6 = `import { CngxFilter, CngxSort, injectDataSource, injectSmartDataSource } from '@cngx/common';
import { PEOPLE, type Person } from '../../../../fixtures';


  // ── Shared: host directive references ──────────────────────────────────
  protected readonly sort   = inject(CngxSort,           { host: true });
  protected readonly filter = inject(CngxFilter<Person>, { host: true });

  // ══════════════════════════════════════════════════════════════════════════
  // Section 1+2: Manual pipeline
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly total = PEOPLE.length;
  protected readonly displayedColumns = ['name', 'role', 'location'];

  protected readonly searchTerm = signal('');

  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles     = signal<Set<string>>(new Set());

  protected readonly processed = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const term = this.searchTerm().trim().toLowerCase();
    const s = this.sort.sort();

    let list = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (term) {
      list = list.filter((p) =>
        [p.name, p.role, p.location].some((v) => v.toLowerCase().includes(term)),
      );
    }
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly ds = injectDataSource(this.processed);

  protected toggleLocation(loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('location', (p) => next.has(p.location));
    } else {
      this.filter.removePredicate('location');
    }
  }

  protected toggleRole(role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('role', (p) => next.has(p.role));
    } else {
      this.filter.removePredicate('role');
    }
  }

  protected clearAll(): void {
    this.filter.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.searchTerm.set('');
    this.sort.clear();
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 3+4: SmartDataSource
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly smartActiveLocation = signal<string | null>(null);
  private readonly smartItems = signal(PEOPLE);
  protected readonly smartDs = injectSmartDataSource(this.smartItems);

  protected smartFilterBy(loc: string | null): void {
    this.smartActiveLocation.set(loc);
    if (loc === null) {
      this.filter.clear();
    } else {
      this.filter.setPredicate((p) => p.location === loc);
    }
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 5+6: Paginate (Material)
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly pgActiveLocation = signal<string | null>(null);
  protected readonly pgPageIndex = signal(0);
  protected readonly pgPageSize  = signal(3);

  private readonly pgFiltered = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const s    = this.sort.sort();
    let list   = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly pgFilteredCount = computed(() => this.pgFiltered().length);

  private readonly pgPaginated = computed(() =>
    this.pgFiltered().slice(
      this.pgPageIndex() * this.pgPageSize(),
      (this.pgPageIndex() + 1) * this.pgPageSize(),
    ),
  );

  protected readonly pgDs = injectDataSource(this.pgPaginated);

  protected pgFilterBy(loc: string | null): void {
    this.pgActiveLocation.set(loc);
    this.pgPageIndex.set(0);
    if (loc === null) { this.filter.clear(); }
    else { this.filter.setPredicate((p: Person) => p.location === loc); }
  }

  protected pgSetPage(i: number): void {
    const max = Math.max(0, Math.ceil(this.pgFilteredCount() / this.pgPageSize()) - 1);
    this.pgPageIndex.set(Math.max(0, Math.min(i, max)));
  }

  protected pgSetPageSize(s: number): void {
    this.pgPageSize.set(s);
    this.pgPageIndex.set(0);
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 7+8: Custom Paginate
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly people = signal(PEOPLE);

  protected pageIndices(total: number): number[] {
    return Array.from({ length: total }, (_, i) => i);
  }`;
  protected readonly _srcHtml7 = `<pre class="code-block"><code>&lt;div cngxPaginate #pg="cngxPaginate" [total]="items.length"&gt;
  &lt;!-- list or table reads pg.range() --&gt;
  &#64;for (item of items.slice(pg.range()[0], pg.range()[1]); track item.id) &#123; ... &#125;

  &lt;nav role="navigation" aria-label="Pagination"&gt;
    &lt;button [disabled]="pg.isFirst()" (click)="pg.previous()" aria-label="Previous page"&gt;&lt;/button&gt;

    &#64;for (i of pageIndices(pg.totalPages()); track i) &#123;
      &lt;button
        (click)="pg.setPage(i)"
        [attr.aria-current]="pg.pageIndex() === i ? 'page' : null"
        [attr.aria-label]="'Page ' + (i + 1)"&gt;
        &#123;<!-- -->&#123; i + 1 &#125;<!-- -->&#125;
      &lt;/button&gt;
    &#125;

    &lt;button [disabled]="pg.isLast()" (click)="pg.next()" aria-label="Next page"&gt;&lt;/button&gt;
  &lt;/nav&gt;

  &lt;!-- Live region — screen readers announce page changes --&gt;
  &lt;span aria-live="polite" aria-atomic="true" class="visually-hidden"&gt;
    Page &#123;<!-- -->&#123; pg.pageIndex() + 1 &#125;<!-- -->&#125; of &#123;<!-- -->&#123; pg.totalPages() &#125;<!-- -->&#125;
  &lt;/span&gt;
&lt;/div&gt;</code></pre>`;
  protected readonly _srcTs7 = `import { CngxFilter, CngxSort, injectDataSource, injectSmartDataSource } from '@cngx/common';
import { PEOPLE, type Person } from '../../../../fixtures';


  // ── Shared: host directive references ──────────────────────────────────
  protected readonly sort   = inject(CngxSort,           { host: true });
  protected readonly filter = inject(CngxFilter<Person>, { host: true });

  // ══════════════════════════════════════════════════════════════════════════
  // Section 1+2: Manual pipeline
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly total = PEOPLE.length;
  protected readonly displayedColumns = ['name', 'role', 'location'];

  protected readonly searchTerm = signal('');

  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles     = signal<Set<string>>(new Set());

  protected readonly processed = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const term = this.searchTerm().trim().toLowerCase();
    const s = this.sort.sort();

    let list = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (term) {
      list = list.filter((p) =>
        [p.name, p.role, p.location].some((v) => v.toLowerCase().includes(term)),
      );
    }
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly ds = injectDataSource(this.processed);

  protected toggleLocation(loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('location', (p) => next.has(p.location));
    } else {
      this.filter.removePredicate('location');
    }
  }

  protected toggleRole(role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('role', (p) => next.has(p.role));
    } else {
      this.filter.removePredicate('role');
    }
  }

  protected clearAll(): void {
    this.filter.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.searchTerm.set('');
    this.sort.clear();
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 3+4: SmartDataSource
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly smartActiveLocation = signal<string | null>(null);
  private readonly smartItems = signal(PEOPLE);
  protected readonly smartDs = injectSmartDataSource(this.smartItems);

  protected smartFilterBy(loc: string | null): void {
    this.smartActiveLocation.set(loc);
    if (loc === null) {
      this.filter.clear();
    } else {
      this.filter.setPredicate((p) => p.location === loc);
    }
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 5+6: Paginate (Material)
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly pgActiveLocation = signal<string | null>(null);
  protected readonly pgPageIndex = signal(0);
  protected readonly pgPageSize  = signal(3);

  private readonly pgFiltered = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const s    = this.sort.sort();
    let list   = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly pgFilteredCount = computed(() => this.pgFiltered().length);

  private readonly pgPaginated = computed(() =>
    this.pgFiltered().slice(
      this.pgPageIndex() * this.pgPageSize(),
      (this.pgPageIndex() + 1) * this.pgPageSize(),
    ),
  );

  protected readonly pgDs = injectDataSource(this.pgPaginated);

  protected pgFilterBy(loc: string | null): void {
    this.pgActiveLocation.set(loc);
    this.pgPageIndex.set(0);
    if (loc === null) { this.filter.clear(); }
    else { this.filter.setPredicate((p: Person) => p.location === loc); }
  }

  protected pgSetPage(i: number): void {
    const max = Math.max(0, Math.ceil(this.pgFilteredCount() / this.pgPageSize()) - 1);
    this.pgPageIndex.set(Math.max(0, Math.min(i, max)));
  }

  protected pgSetPageSize(s: number): void {
    this.pgPageSize.set(s);
    this.pgPageIndex.set(0);
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 7+8: Custom Paginate
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly people = signal(PEOPLE);

  protected pageIndices(total: number): number[] {
    return Array.from({ length: total }, (_, i) => i);
  }`;

  // ── Shared: host directive references ──────────────────────────────────
  protected readonly sort   = inject(CngxSort,           { host: true });
  protected readonly filter = inject(CngxFilter<Person>, { host: true });

  // ══════════════════════════════════════════════════════════════════════════
  // Section 1+2: Manual pipeline
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly locations = [...new Set(PEOPLE.map((p: Person) => p.location))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly roles = [...new Set(PEOPLE.map((p: Person) => p.role))].sort(
    (a: string, b: string) => a.localeCompare(b),
  );
  protected readonly total = PEOPLE.length;
  protected readonly displayedColumns = ['name', 'role', 'location'];

  protected readonly searchTerm = signal('');

  protected readonly selectedLocations = signal<Set<string>>(new Set());
  protected readonly selectedRoles     = signal<Set<string>>(new Set());

  protected readonly processed = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const term = this.searchTerm().trim().toLowerCase();
    const s = this.sort.sort();

    let list = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (term) {
      list = list.filter((p) =>
        [p.name, p.role, p.location].some((v) => v.toLowerCase().includes(term)),
      );
    }
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly ds = injectDataSource(this.processed);

  protected toggleLocation(loc: string): void {
    const next = new Set(this.selectedLocations());
    if (next.has(loc)) { next.delete(loc); } else { next.add(loc); }
    this.selectedLocations.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('location', (p) => next.has(p.location));
    } else {
      this.filter.removePredicate('location');
    }
  }

  protected toggleRole(role: string): void {
    const next = new Set(this.selectedRoles());
    if (next.has(role)) { next.delete(role); } else { next.add(role); }
    this.selectedRoles.set(next);
    if (next.size > 0) {
      this.filter.addPredicate('role', (p) => next.has(p.role));
    } else {
      this.filter.removePredicate('role');
    }
  }

  protected clearAll(): void {
    this.filter.clear();
    this.selectedLocations.set(new Set());
    this.selectedRoles.set(new Set());
    this.searchTerm.set('');
    this.sort.clear();
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 3+4: SmartDataSource
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly smartActiveLocation = signal<string | null>(null);
  private readonly smartItems = signal(PEOPLE);
  protected readonly smartDs = injectSmartDataSource(this.smartItems);

  protected smartFilterBy(loc: string | null): void {
    this.smartActiveLocation.set(loc);
    if (loc === null) {
      this.filter.clear();
    } else {
      this.filter.setPredicate((p) => p.location === loc);
    }
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 5+6: Paginate (Material)
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly pgActiveLocation = signal<string | null>(null);
  protected readonly pgPageIndex = signal(0);
  protected readonly pgPageSize  = signal(3);

  private readonly pgFiltered = computed((): Person[] => {
    const pred = this.filter.predicate() as ((p: Person) => boolean) | null;
    const s    = this.sort.sort();
    let list   = PEOPLE as Person[];
    if (pred) list = list.filter(pred);
    if (s) {
      const { active, direction } = s;
      list = [...list].sort((a, b) => {
        const av = String((a as unknown as Record<string, unknown>)[active] ?? '');
        const bv = String((b as unknown as Record<string, unknown>)[active] ?? '');
        const cmp = av.localeCompare(bv, undefined, { numeric: true, sensitivity: 'base' });
        return direction === 'asc' ? cmp : -cmp;
      });
    }
    return list;
  });

  protected readonly pgFilteredCount = computed(() => this.pgFiltered().length);

  private readonly pgPaginated = computed(() =>
    this.pgFiltered().slice(
      this.pgPageIndex() * this.pgPageSize(),
      (this.pgPageIndex() + 1) * this.pgPageSize(),
    ),
  );

  protected readonly pgDs = injectDataSource(this.pgPaginated);

  protected pgFilterBy(loc: string | null): void {
    this.pgActiveLocation.set(loc);
    this.pgPageIndex.set(0);
    if (loc === null) { this.filter.clear(); }
    else { this.filter.setPredicate((p: Person) => p.location === loc); }
  }

  protected pgSetPage(i: number): void {
    const max = Math.max(0, Math.ceil(this.pgFilteredCount() / this.pgPageSize()) - 1);
    this.pgPageIndex.set(Math.max(0, Math.min(i, max)));
  }

  protected pgSetPageSize(s: number): void {
    this.pgPageSize.set(s);
    this.pgPageIndex.set(0);
  }

  // ══════════════════════════════════════════════════════════════════════════
  // Section 7+8: Custom Paginate
  // ══════════════════════════════════════════════════════════════════════════
  protected readonly people = signal(PEOPLE);

  protected pageIndices(total: number): number[] {
    return Array.from({ length: total }, (_, i) => i);
  }
  
}
